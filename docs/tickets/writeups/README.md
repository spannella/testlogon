# Ticket Investigation Write-ups

One ~5-page deep-dive per ticket in `docs/tickets/`, following `_TEMPLATE.md`.
Generated by reading the real codebase and citing `file:line`. Tracks toward all 327 tickets.

Progress is tracked in `_PROGRESS.md`.
