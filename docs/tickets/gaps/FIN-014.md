- [HIGH] `check_and_alert` never called — `app/services/payment_provider_health.py:569` implements threshold-based alerting but is only reachable via direct call; there is no background periodic task, no startup hook in `app/main.py`, and no API endpoint that invokes it; configured alert thresholds are silently ignored and admins receive no proactive alerts — Fix: wire a background task (e.g. every 5 min per provider) in `app/main.py` startup that calls `check_and_alert(provider)` for each of `["stripe", "paypal", "ccbill"]` — Effort: S
- [HIGH] `check_and_alert` returns alert details but never sends email or notification — `app/services/payment_provider_health.py:586–590` returns a dict with `alert_email` but does not invoke any email/notification service; the alert is computed but never delivered to the configured recipient — Fix: call the platform email or in-app notification service from `check_and_alert` (or from the background task caller) when `breaches` is non-empty — Effort: S
- [HIGH] `toggle_provider` does not enforce payment initiation gate — `app/services/payment_provider_health.py:399` sets `enabled=false` in the `provider_health` table, but `app/routers/billing.py` and `app/routers/billing_ccbill.py` never call `is_provider_enabled()` before processing a payment; disabling a provider has no effect on in-flight or new transactions — Fix: add `is_provider_enabled(provider)` guard at the start of each billing router's charge path (cross-ref SEC-004) — Effort: M
- [MED] Alert threshold units are bps (int) in service but the ticket spec documents them as percent (float 1.0–100.0) — `app/models.py:9874` `ProviderConfigUpdate.alert_error_rate_threshold` accepts `int ge=1 le=10000` (bps); the ticket spec table shows percent values (default 5%); admin UI users configuring "5% error rate" must enter "500" which is non-obvious — Fix: document the bps convention in the API response and UI, or convert to percent at the API boundary — Effort: S
- [MED] No `webhook_stats.py` integration — the ticket states `app/services/webhook_stats.py` should be updated to also write per-provider stats to `provider_health` table; the only integration is via `app/services/payment_incident_metrics.py:176` which calls `record_provider_event` indirectly; direct webhook delivery outcomes never populate health snapshots — Fix: call `record_provider_event` from `webhook_stats.record_delivery_stat` for the three payment providers — Effort: S
- [LOW] E2E test file renamed — the ticket specifies `frontend/e2e/admin-provider-health.spec.ts` but the actual file is `frontend/e2e/payment-provider-health.spec.ts` (419 lines); not a functional gap — Fix: treat as documentation inconsistency; verify coverage matches ticket sections 527–531 — Effort: S
