- [HIGH] Comments are stored in the VideoViews table using a `VCOMMENT#` prefix instead of the newsfeed `app_single_table` — `app/services/video_comments.py:49` — the ticket design (Section 3.5) requires either a shadow post record in `app_single_table` or separate video comment endpoints; using the VideoViews table means comment data is co-mingled with view records and TTL-expired (VideoViews items use 90-day TTL) losing comments after 90 days — Fix: store comments in a dedicated table or in `app_single_table` with the `POST#{video_id}` shadow-record pattern described in the ticket — Effort: M
- [MED] `ByCategory` GSI sort key is `trending_score_sort` (not `published_at`) — `scripts/local-ddb-init.py:907-910` — `browse_gallery()` queries `ByCategory` expecting `published_at` descending order but the actual sort key is `trending_score_sort`; `newest` sort will silently use trending order instead — Fix: add a second GSI `ByCategoryPublishedAt` with `sort_key=published_at`, or query in-memory and sort after fetching — Effort: M
- [MED] `unpublish_from_gallery` uses `REMOVE gallery_status` but never sets `gallery_published=False` correctly — `app/services/video_gallery.py:143` — the UpdateExpression sets `gallery_published = :gp` (False) but also `REMOVE gallery_status` in the same expression without separating SET and REMOVE clauses; DDB requires `SET ... REMOVE ...` in the same expression which is valid, but omitting `gallery_published_flag` removal means the `ByGalleryPublished` GSI entry may linger until next update — Fix: also `REMOVE gallery_status` from the GSI and test that un-published videos no longer appear in gallery queries — Effort: S
- [MED] Trending score is only updated via explicit `update_trending_score()` call; no background job wires this — `app/services/video_gallery.py:381` — the ticket specifies a background job to periodically recompute trending scores; without it `trending_score` on all records stays 0 and `sort=trending` always returns unranked results — Fix: register a periodic background task (e.g., cron or startup loop) that calls `update_trending_score` for recently-active videos — Effort: M
- [LOW] `browse_gallery` in-memory duration filter after DDB query means a page of 24 may return fewer than 24 items when many are filtered, and cursor-based pagination will skip hidden items — `app/services/video_gallery.py:596-600` — Fix: add `duration_seconds` as a numeric attribute and use DDB FilterExpression on a scan/GSI, or document the limitation — Effort: M
