- [HIGH] Poll vote does not publish SSE events — `app/routers/newsfeed.py:6659` `vote_on_poll` — the endpoint casts the vote and returns updated counts but never calls `sse_hub.publish()`; the real-time "live vote count" feature described in the ticket is completely absent — viewers must manually reload to see updated tallies — Fix: after cast_vote, build a `poll:vote` payload and call `asyncio.get_event_loop().create_task(sse_hub.publish(post_owner_id, payload))` (or publish to all subscribers of the post's feed); alternatively push via the author's SSE stream and fan-out via existing notify path — Effort: M
- [HIGH] Poll vote endpoint does not enforce post visibility/subscription gate — `app/routers/newsfeed.py:6659` — `vote_on_poll` fetches the post and checks it is a poll, but never checks `post["visibility"]` (followers-only) or subscription entitlement; any authenticated user can vote on a followers-only or subscription-gated poll — Fix: reuse the existing visibility/entitlement check from `_post_to_dict` or extract a shared `_check_post_access(post, user_id)` guard and call it before `cast_vote` — Effort: S
- [MED] `poll_votes` map stored inline on post item with unbounded growth — `app/services/newsfeed_polls.py:135` `cast_vote` — every unique voter adds `poll_votes.{question_id}.{option_id}.{user_sub} = True` to the post item; a viral poll with millions of voters can exceed DynamoDB's 400 KB per-item limit, causing silent write failures — Fix: migrate per-voter tracking to a separate `poll_vote_records` table (PK=`post_id`, SK=`{user_sub}#{question_id}`); keep only denormalized counts on the post item — Effort: L
- [MED] `cast_vote` vote-change is non-atomic — `app/services/newsfeed_polls.py:161` — `current_vote` is read from the stale `post` dict passed in from the router (not from DDB); the old vote removal and new vote add are two separate `update_item` calls with no transaction; concurrent votes from the same user can double-count — Fix: use `TransactWriteItems` to atomically remove old vote and add new vote, or use a ConditionExpression on the existing voter map — Effort: M
- [LOW] `remove_poll_vote` DELETE endpoint accepts `user_id: UserIdDep = None` — `app/routers/newsfeed.py:6706` — `user_id` is typed as `Optional` with `= None`; if the dependency injection somehow fails, `None` is passed as `user_sub` to `remove_vote`, which removes the `None` key from DDB without error — Fix: change signature to `user_id: UserIdDep` (no default) matching `vote_on_poll` — Effort: S
- [LOW] `close_poll` always overwrites `closes_at` to now even if it was already future-dated — `app/services/newsfeed_polls.py:346` — `UpdateExpression` sets both `poll_data.closed = True` and `poll_data.closes_at = now`; a poll configured to close at a future time gets its closes_at timestamp rewritten to early-close time, altering the historical record — Fix: only update `closes_at` if the new timestamp is earlier than the existing value, or omit it and rely solely on `closed = True` flag — Effort: S
