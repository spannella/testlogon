- [MED] Discount code create uses unconditional put — `app/routers/subscription_server.py:1607` — `ddb_put_item` silently overwrites an existing code, resetting `created_at`, `percent_off`, and all fields with no 409 response — Fix: frontend should pre-check `listDiscounts()` and warn; backend should add `ConditionExpression=attribute_not_exists(sk)` — Effort: S
- [LOW] Price changes on existing plans not communicated to subscribers — `app/routers/subscription_server.py:756` — `update_plan` allows changing `price_cents` but does not push changes to Stripe or notify existing subscribers; only new subscribers are affected — Fix: add a UI note "Price changes apply to new subscribers only" in `PlanEditor.tsx` — Effort: S
- [LOW] Archive confirmation does not explain subscriber impact — `frontend/src/pages/subscriptions/TierManager.tsx:308` — archiving a plan stops new subscriptions but existing subscribers remain active; this is not communicated in the `AlertDialog` confirmation — Fix: add explanatory text to the archive confirmation dialog — Effort: S
- [LOW] Tier Manager sidebar link shown to all users, not just creators — `frontend/src/components/layout/Sidebar.tsx:126` — non-creator users see "Tier Manager" link which shows an empty state; minor UX noise — Fix: conditionally show link based on whether user has any active plan (or accept empty-state as v1) — Effort: S
- [LOW] Missing Playwright E2E spec for UI layer — no `frontend/e2e/subscription-tier-editor.spec.ts` — full TierManager UI flow (create, edit, archive, discount, preview) is untested at browser level — Fix: add spec covering scenarios in section 5.2 — Effort: S
