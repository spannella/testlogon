- [HIGH] `emit_social_alert()` never called from social event triggers — `app/routers/newsfeed.py:4696-4712,4589-4653,5324-5427,5583-5640`, `app/services/social.py:32-103`, `app/routers/subscription_server.py:950-959` — the `social_alerts.py` module and `emit_social_alert()` are fully implemented but zero callers exist outside the module itself; reactions, comments, tips, reposts, and follows only use `put_notification()` (newsfeed-internal GSI3 system), so no social alerts ever land in the `alerts` table and the bell badge stays silent for all social events — Fix: add `emit_social_alert()` calls after each event: reaction handler (post_reaction/post_liked), comment handler (post_comment/comment_reply), tip_post handler (post_tip), message tip handler (message_tip), `social.follow_user()` (new_follower), subscription create (subscription_started) — Effort: M
- [HIGH] `emit_mention_alerts()` never called from post/comment creation — `app/routers/newsfeed.py` (create_post ~line 3618, create_comment ~line 5324) — mention parsing (`extract_mentions`, `emit_mention_alerts`) exists in `social_alerts.py:667-701` but is never invoked from any post or comment handler; @mentions never generate notifications — Fix: call `emit_mention_alerts(text=body, author_user_id=user_id, ...)` at end of create_post and create_comment handlers — Effort: S
- [MED] `_resolve_username_to_user_sub()` uses a full table scan — `app/services/social_alerts.py:654` — `T.profile.scan(FilterExpression=Attr("alias").eq(username), Limit=1)` scans the entire profiles table to resolve @username → user_sub; at 10K+ users this reads up to 1MB per lookup and slows mention resolution proportionally — Fix: add a GSI on the profiles table keyed on `alias` (or reuse the alias-based lookup cache in `profile.py:_resolve_profile_identifier_to_user_sub()` which already has an in-memory cache) — Effort: S
- [MED] `_batch_alert()` issues 3 serial DDB round-trips per batched alert — `app/services/social_alerts.py:324-400` — the batch write performs: (1) `update_item` append+increment, (2) conditional `update_item` to trim actors list when >10, (3) `update_item` to set the formatted title; all three are sequential; under high-concurrency (e.g., viral post receiving 100 reactions/second) this adds latency and unnecessary write load — Fix: combine the title computation into the first `update_item` using a conditional expression, and do the actor trim only when the returned list length exceeds cap — Effort: S
- [LOW] No E2E tests for social alert delivery — `frontend/e2e/` — no spec file exists for SOC-004; the `emit_social_alert` codepath is untested end-to-end; once the event hook gaps above are fixed there will be no automated coverage of the batching, unread badge, type preferences, or SSE delivery paths — Fix: add `frontend/e2e/social-alerts.spec.ts` covering: unread badge increments on reaction, mark-all-read resets badge, type-preference toggle silences alert, batched title shows "Alice and N others" — Effort: M
