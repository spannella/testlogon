- [MED] VNC `_resolve_target()` not extended for user-defined hosts — `app/services/vnc_sessions.py:237` — clicking "Connect" on a VNC host in the inventory always returns 404 (no `user:{host_id}` prefix handling) — Fix: extend `_resolve_target()` to query `T.host_inventory` for `user:`-prefixed target_ids, passing `user_sub` from the VNC session; requires passing `user_sub` through `create_session` → `_resolve_target` — Effort: S

- [MED] SSH terminal `record_connection()` never called — `app/routers/browser_ssh_terminal.py:~868` — `last_connected_at` and `connection_count` on host inventory records are never updated when an SSH session connects successfully; "Recent connections" sort is always stale — Fix: call `host_inventory.record_connection(user_sub, host_id)` on successful SSH connect (after `session_connected = True`) when `host_id` query param is present — Effort: S

- [LOW] No per-user host count limit enforced — `app/services/host_inventory.py:184` — ticket spec calls for a max of 500 hosts per user; `create_host()` does no count check; a user can create unlimited hosts bloating DDB and list queries — Fix: add `MAX_HOSTS_PER_USER = 500` constant and a count check (query table, `len >= max` → raise `HostLimitExceeded`) in `create_host()` — Effort: S

- [LOW] Missing audit events for `update_host` and `delete_host` on CSV import path — `app/services/host_inventory.py:607` — `import_hosts_csv` only emits a single aggregate `remote_host.csv_import` event; individual per-host creates within the import do each emit `remote_host.created` (via `create_host`); `update_host` and `delete_host` emit events; acceptable but CSV row errors produce no per-row audit trail — Fix: acceptable as-is; document that import uses aggregate audit — Effort: S

- [LOW] Background scan in `check_idle_instances` (SECOPS-007 / INFRA-003 cross-concern) — `app/services/ec2_launcher.py:394` — idle checker uses a full table scan; on a busy DDB the 1MB scan page may miss items if `LastEvaluatedKey` is returned; no pagination loop implemented — Fix: add a `while True: ... if not resp.get("LastEvaluatedKey"): break` loop around the scan — Effort: S
