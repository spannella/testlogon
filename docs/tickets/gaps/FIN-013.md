- [HIGH] Full billing table scan on every rollup — `app/services/platform_financial_dashboard.py:136` (`_scan_ledger_entries`) performs an unbounded `T.billing.scan()` over all rows in the `billing` table (which contains sessions, payment methods, and all user billing rows, not just ledger entries) with in-memory filtering; at production scale this exhausts read capacity and memory — Fix: add the `GSI_LEDGER_DATE` GSI to the `billing` table as specified in the ticket (`scripts/local-ddb-init.py:59`) and replace the scan with a GSI query keyed by `ledger_date` — Effort: M
- [HIGH] `provider` field unreliable on ledger entries — `app/services/platform_financial_dashboard.py:110` (`_provider_for`) falls back to `"unknown"` when `provider` is absent from the ledger item; `app/services/billing_shared.py:217` (`new_ledger_entry`) writes `ledger_date` but not `provider`, so all entries routed through the billing shared layer show `provider="unknown"` in the breakdown — Fix: pass `provider` through `new_ledger_entry`'s `extra` dict at all call sites (Stripe, PayPal, CCBill paths) (cross-ref SEC-004) — Effort: M
- [MED] PDF export absent ��� the ticket spec defines `GET /v1/admin/financials/export/pdf` and `export_summary_pdf()` in `app/services/financial_dashboard.py`, but no PDF export endpoint or function exists in `app/routers/platform_financial_dashboard.py` or `app/services/platform_financial_dashboard.py` — Fix: implement PDF export using `reportlab` or equivalent and add the router endpoint — Effort: M
- [MED] Route prefix diverges from ticket spec — actual prefix is `/ui/admin/financial-dashboard` (`app/routers/platform_financial_dashboard.py:34`) vs `/v1/admin/financials` in the ticket design; the frontend API client at `frontend/src/api/endpoints/platformFinancialDashboard.ts` must be kept consistent with whichever prefix is canonical — Fix: decide and document the canonical prefix; ensure frontend client and E2E tests use it — Effort: S
- [LOW] No rate-limit on manual rollup trigger — `app/routers/platform_financial_dashboard.py:163` (`trigger_rollup`) has no per-admin rate limit; the ticket spec says "limit to 1 manual trigger per hour"; a ROOT user can spam rollup scans and exhaust DDB read capacity — Fix: add a DDB TTL sentinel or in-memory cooldown keyed by admin sub — Effort: S
- [LOW] E2E test file renamed — the ticket specifies `frontend/e2e/admin-financials.spec.ts` but the actual file is `frontend/e2e/platform-financial-dashboard.spec.ts` (446 lines); not a functional gap but may cause confusion — Fix: treat as documentation inconsistency; verify test coverage matches ticket section 523–527 — Effort: S
