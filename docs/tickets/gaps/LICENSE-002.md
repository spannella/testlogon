- [HIGH] No content ownership enforcement on issue_license — `app/services/issued_licenses.py:25` — Any authenticated user can issue a license claiming to be the licensor of any content_id without verifying ownership; a malicious user can grant blanket licenses on content they don't own, polluting the Licensed Content Library and triggering fraudulent revenue splits — Fix: add `_validate_content_ownership(licensor_sub, content_id, content_type)` against the relevant content table before writing any license record — Effort: M
- [MED] `list_issued_licenses` returns only licensor-index stub fields — `app/routers/issued_licenses.py:118` — The `GET /ui/licenses/issued` endpoint uses `_license_out_from_index` which returns 6 fields (no terms, no title, no status detail) instead of the full `IssuedLicenseOut`; frontend IssuedLicensesPage cannot render terms or status badges correctly — Fix: fetch the primary record for each index item or project needed fields into the licensor index at write time — Effort: S
- [MED] Status filter applied post-fetch on licensor and licensee list queries — `app/services/issued_licenses.py:344,373` — `status_filter` is applied in Python after DynamoDB returns `Limit` items; with active licenses beyond the first page, filtered results silently under-count — Fix: use GSI2 (`CONTENT_STATUS#...`) for status-filtered queries, or set DynamoDB `FilterExpression` and loop `LastEvaluatedKey` — Effort: M
- [MED] `update_license_terms` only syncs `profit_share_pct` and `fixed_cost_cents` to library index, not `revenue_share_pct` — `app/services/issued_licenses.py:190` — Blanket library browse returns stale `revenue_share_pct` after terms update; ticket states all three term fields must be updated — Fix: include `revenue_share_pct` in `lib_update_parts` list — Effort: S
- [LOW] No expiry enforcement on active licenses — `app/services/issued_licenses.py:438` — `check_license_for_use` returns licenses regardless of `expires_at`; an expired per-user license remains valid indefinitely without a background expiry task — Fix: add `expires_at` null/future check in `check_license_for_use`; add background task to set `status=expired` when `expires_at < now_ts()` — Effort: M
- [LOW] `_validate_content_ownership` stub referenced in ticket design but not implemented — `app/services/issued_licenses.py` — Function appears in the ticket's pseudocode as a placeholder `# TODO` but is absent entirely; no guard against cross-user license issuance at service layer — Fix: implement after content ownership lookup is confirmed (depends on content table PK convention per content_type) — Effort: M
