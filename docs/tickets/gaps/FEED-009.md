- [MED] `is_bookmarked` field not injected into feed/post responses — `app/routers/newsfeed.py` — `_post_to_dict` (line ~2180) does not include `is_bookmarked`; the feed endpoint does not fetch bookmarked IDs and annotate posts; PostCard derives `isBookmarked` from optimistic local state only, losing the indicator on hard refresh — Fix: in `GET /feed`, query `BOOKMARK_LOOKUP#{user_id}` with `begins_with("post#")`, build a set, and pass into `_post_to_dict` so posts carry `is_bookmarked: bool` — Effort: S
- [MED] move bookmark between collections not implemented — `app/routers/newsfeed.py` — the `PATCH /ui/bookmarks/{content_type}/{content_id}/move` (or equivalent) endpoint specified in the ticket design does not exist; users cannot reassign a bookmark to a different collection — Fix: add `PATCH /ui/bookmarks/{content_type}/{content_id}` accepting `{collection_id}`; update both the main bookmark item and the lookup item; adjust old/new collection bookmark_count counters — Effort: S
- [LOW] collection sidebar missing from SavedPage — `frontend/src/pages/saved/SavedPage.tsx` — the ticket specifies a left-side collection list that filters bookmarks by collection; the current SavedPage has only a content-type tab (`post`/`video`) but no collection selector; collection-scoped GET `/ui/bookmarks?collection_id=…` is never called — Fix: fetch `GET /ui/bookmark-collections`, render a sidebar with counts, and pass `collection_id` as a query param to `getBookmarks` — Effort: M
- [LOW] bookmark_count counter on collection items not decremented on bookmark delete — `app/routers/newsfeed.py:6355` — `delete_bookmark` removes the bookmark/lookup DDB items but does not update the `COLL#{collection_id}` item's `bookmark_count` field — Fix: after delete, get the existing bookmark to retrieve its `collection_id`, then `ADD bookmark_count :-1` on the collection item — Effort: S
