- [MED] SSE delegate stream not implemented — `app/routers/messaging.py` (no GET /delegate/{creator_id}/sse endpoint) — delegates do not receive real-time message events; acceptance criterion "delegates receive real-time SSE events" not met — Fix: add SSE endpoint reusing messaging SSE infrastructure keyed on creator_id; add 30s heartbeat revocation check — Effort: M
- [MED] delegate msg rate limiting absent — `app/services/delegate_chat.py` (no per-delegate rate bucket) — delegates have no 60-msg/min limit as specified; a misbehaving delegate can flood creator's conversations — Fix: add _delegate_msg_rate_buckets dict keyed by (delegate_id, creator_id) matching broadcast_chat_store pattern — Effort: S
- [LOW] CreatorSelector component missing — `frontend/src/pages/messages/` (no CreatorSelector.tsx) — delegates managing multiple creators have no UI to switch between them without going to /delegates — Fix: add CreatorSelector dropdown inside DelegateBanner calling listManagedCreators — Effort: S
- [LOW] DelegateAuditView component missing — `frontend/src/pages/messages/` (no DelegateAuditView.tsx) — backend audit endpoint and listChatDelegateAudit API wrapper exist but no React component surfaces the data — Fix: add DelegateAuditView tab visible when managingCreatorId is set — Effort: S
