# UX-003 gaps — Drag-and-Drop Reorder
# Note: overlaps PLATFORM-015 (see docs/tickets/gaps/PLATFORM-015.md for additional gaps)

- [MED] No dedicated E2E test for drag-drop UI interactions — `frontend/e2e/drag-drop.spec.ts` covers the catalog reorder API (section 73) and shelf drag handle render (section 75) but has no keyboard-reorder test (Space/ArrowDown/Space) and no actual mouse-drag simulation test for questionnaire sections; ticket §8.2 specifies section 7 "Questionnaire section reorder via keyboard" — Fix: add a keyboard-drag test using `page.focus(dragHandle); page.keyboard.press("Space"); page.keyboard.press("ArrowDown"); page.keyboard.press("Space")` and assert the section moved — Effort: S
- [LOW] DragOverlay absent from ProductShelfManager — `frontend/src/pages/broadcast/ProductShelfManager.tsx:186` uses the generic `SortableList` which provides a default semi-transparent clone overlay; no `renderOverlay` prop is passed, so the ghost shows the full item card with all interactive controls visible and clickable during drag — Fix: pass a `renderOverlay` prop that renders a simplified non-interactive card (name + price only) — Effort: S
- [LOW] Gallery playlist reorder deferred with no tracking issue — ticket §4.6 explicitly defers gallery playlist reorder to VOD-017, but VOD-017's gap file does not mention this dependency; `SortableList` is ready but the integration is untracked — Fix: add a note to `docs/tickets/gaps/VOD-017.md` that playlist reorder depends on `SortableList` from UX-003 — Effort: S
- [LOW] Catalog reorder endpoint performs N sequential GetItem + UpdateItem calls — `app/routers/catalog.py:398-437` loops over `item_ids` with one GetItem and one UpdateItem per item; for 100 items this is 200 sequential DDB round-trips (~1-2s); DynamoDB BatchWriteItem cannot do UpdateItem, but the GetItem verification loop is unnecessary if ownership is already enforced by the category-owner check — Fix: do a single query for all items in the category, verify ownership in-memory, then batch the position writes using `TransactWriteItems` (up to 25 per call) — Effort: M
