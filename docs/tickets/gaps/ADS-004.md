- [CRIT] no daily spent_today_cents reset task — `app/services/ad_serving.py:_has_budget` — daily-budget campaigns exhaust first-day cap and never resume serving — Fix: add startup background task that resets spent_today_cents=0 at midnight UTC for budget_type=daily campaigns — Effort: M
- [HIGH] list_campaigns_by_status does not use ByStatusCreatedAt GSI — `app/services/ad_campaigns.py:list_campaigns_by_status` — implicit table scan at scale; slow and expensive for every serve request — Fix: use Query(IndexName="ByStatusCreatedAt", KeyConditionExpression=Key("status").eq(status)) — Effort: S
- [HIGH] bid_cpm_cents not a campaign field — `app/models.py:CampaignCreateIn` — all campaigns default to 500 CPM; auction cannot rank by advertiser bid — Fix: add bid_cpm_cents to CampaignCreateIn/CampaignOut (coordinate with ADS-001 model) — Effort: S
- [MED] no rate limiting on POST /ui/ads/serve — `app/routers/ads.py:~331` — unlimited serve requests per user; risk of DDB hot-partition abuse — Fix: apply check_rate_limit with limit=60/min from app/services/rate_limiter.py — Effort: S
- [MED] _is_frequency_capped makes 3 sequential GetItem calls per campaign — `app/services/ad_serving.py:_is_frequency_capped` — up to 150 DDB reads per serve with 50 active campaigns — Fix: batch all cap keys into a single BatchGetItem call — Effort: M
- [MED] advertiser balance not debited on impression — `app/services/ad_serving.py:track_ad_event` — impression tracking records events but does not charge advertiser or settle billing — Fix: wire to ad_billing charge functions in ADS-007 — Effort: M
- [LOW] record_ad_impression SK uses VIDEO# prefix for non-video surfaces — `app/services/ad_serving.py:track_ad_event` — newsfeed/broadcast impression SKs read as VIDEO#{content_id} in ad_impressions table — Fix: use CONTENT# prefix or surface-specific SK for non-video impressions in ADS-008 analytics — Effort: S
