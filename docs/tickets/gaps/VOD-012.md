- [HIGH] download rate limiting not enforced — `app/routers/video_listing.py:772-824` — settings.py defines video_download_rate_limit_per_10m=5 but the download endpoint never reads this setting or checks a rate-limit counter; spec §1.4 requires 5 requests per 10 minutes per user per video to prevent URL farming — Fix: add a Redis/DynamoDB rate-limit check keyed by user_sub+video_id before minting the presigned URL; return 429 if limit exceeded — Effort: M
- [MED] download audit trail is counter-only (no user_sub/ip/timestamp per request) — `app/routers/video_listing.py:813-822` and `app/services/video_metadata_store.py:626-630` — spec §1.4 requires audit logging with user_sub, video_id, ip, and timestamp for creator analytics and abuse detection; implementation only increments download_count; individual request details are not persisted — Fix: write a DownloadAudit DynamoDB item (or append to an audit log) with user_sub, video_id, ip (from Request.client.host), and timestamp on each download URL issuance — Effort: M
- [LOW] deferred MP4 cleanup when allow_download is toggled off not implemented — `app/routers/video_listing.py:753-757` — spec §1.2 states the MP4 file should be marked for deferred cleanup when allow_download is disabled; the PATCH handler only clears the flag (allow_download=False) but never marks download_mp4_key for deferred deletion or schedules S3 object removal; stale MP4 files accumulate in S3 — Fix: on allow_download toggled False, set download_mp4_status="pending_delete" and add a background cleanup job or TTL-triggered S3 lifecycle rule — Effort: M
- [LOW] download_bytes_total admin analytics field missing — `app/models_video.py` and `app/routers/video_listing.py` — spec §1.5 (Admin user story) requires download_count and download_bytes_total in admin video detail; download_count is tracked but download_bytes_total is never accumulated (mp4 size is available in download_mp4_size_bytes per download but never summed) — Fix: add download_bytes_total to VideoMetadataModel and increment by download_mp4_size_bytes on each download URL mint — Effort: S
