- [CRIT] SSRF via SEC-020 — no RFC-1918/metadata IP denylist in bastion hop validation — `app/services/ssh_bastion.py:65-83` — `validate_host()` normalises IPs via `ipaddress.ip_address()` but never calls `.is_private()`, `.is_loopback()`, or `.is_link_local()`; a user can store a bastion path with hops pointing at `169.254.169.254`, `127.0.0.1`, or any RFC-1918 address; the path resolves successfully and produces a `ProxyJump` config that, when used with a real SSH client, would reach internal services — Fix: after normalising the IP, call `addr.is_private() or addr.is_loopback() or addr.is_link_local()` and raise `InvalidHop` if true; also reject hostnames that resolve to these ranges (DNS rebinding guard) — Effort: S
- [HIGH] `MultiHopSshBridge` class not implemented — `app/routers/browser_ssh_terminal.py:452-488` — the ticket design requires a `MultiHopSshBridge` class using Paramiko `direct-tcpip` channel forwarding; no such class exists in the terminal router; `SshBastionPage.tsx` can configure chains but the WebSocket handler ignores `host_id` and the `resolve_connection_chain` path; multi-hop connections cannot actually be established — Fix: implement `MultiHopSshBridge` as designed in the ticket (section 3.3) and wire it into the WebSocket `connect` handler — Effort: L
- [HIGH] `resolve_connection_chain` function missing — `app/services/ssh_bastion.py:335-410` — the service provides `resolve_bastion_path()` which produces a ProxyJump/ssh_config representation, but the actual chain resolution function that returns a list of hop dicts with credentials (for programmatic connection) described in ticket section 3.2 is absent; there is no `get_connection_profile()` call or SSH-key lookup per hop — Fix: add `resolve_connection_chain(user_sub, path_id)` that fetches each hop's SSH key via `ssh_key_manager.get_decrypted_private_key()` and returns a ready-to-connect chain — Effort: M
- [MED] `connection_profiles.py` does not expose per-hop credentials — `app/services/connection_profiles.py:1` — the ticket requires `get_connection_profile(user_sub, host_id)` to return `ssh_key_id` and `default_username` per hop; the file exists but its integration with bastion hop resolution is incomplete; hops in `ssh_bastion.py` store an `ssh_key_id` field but there is no validation that the key ID corresponds to a real key owned by the user — Fix: add key-existence validation in `_normalize_hop()` by calling `ssh_key_manager.get_key(user_sub, ssh_key_id)` and raising `InvalidHop` if not found — Effort: S
- [MED] bastion `is_bastion` / `bastion_host_id` fields not on `remote_hosts` — `app/services/ssh_bastion.py:1` — the ticket design requires these fields on host inventory records (INFRA-001); the bastion implementation is a separate `ssh_bastion_paths` table with no link back to `remote_hosts`; users cannot set `is_bastion: true` on a host and have it auto-appear in the bastion selector — Fix: either add `is_bastion` and `bastion_host_id` fields to `remote_hosts` (as designed) or document the separate `ssh_bastion_paths` table as the canonical approach and update the UI accordingly — Effort: M
- [MED] per-hop audit events not emitted — `app/routers/ssh_bastion.py:1` — the ticket requires `ssh.connect_hop` audit events with hop number and hostname for each hop in a multi-hop session; `audit_event()` is not called from anywhere in the bastion router or service; only the path CRUD operations are audited �� Fix: emit `audit_event("ssh.connect_hop", ...)` for each hop in `MultiHopSshBridge.connect()` — Effort: S
- [LOW] connection chain preview UI not wired — `frontend/src/pages/remote/SshBastionPage.tsx:1` — the ticket specifies a `ConnectionChainPreview` component and bastion selector in `AddHostDialog`; `SshBastionPage.tsx` exists but the design-specified `BastionSelector` dropdown in `AddHostDialog` / `HostInventoryPage` is not present — Fix: add `BastionSelectorDropdown` and `ConnectionChainPreview` to the host edit dialog as described in ticket section 3.6 — Effort: M
