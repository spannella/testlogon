- [HIGH] no scheduled-publish background loop — `app/services/agent_marketing.py:507` — schedule_content sets status=scheduled and GSI3SK but no background task promotes due content to published; scheduled items never auto-publish — Fix: add asyncio background task in app/main.py calling publish_due_scheduled_content every 30s — Effort: S
- [MED] feature-completion trigger not wired — `app/services/tickets.py` — ticket spec describes auto-trigger on ticket status → done but no hook exists in tickets.py status-transition code — Fix: emit event at status=done transition, check S.marketing_agent_enabled and trigger_on_feature_completion config — Effort: S
- [MED] get_engagement_summary is O(N content × 1 DDB query) — `app/services/agent_marketing.py:720` — scans all content then issues one engagement query per item; no pre-aggregated summary row — Fix: add SUMMARY row per user updated atomically on each record_engagement call — Effort: M
- [LOW] generate_content_for_feature silently skips missing tickets — `app/services/agent_marketing.py:942` — get_ticket may return None for IDs not in-process (prod); missing_ticket_ids reported but generation silently skips — Fix: none material (observable via missing_ticket_ids field); document the limitation — Effort: S
- [LOW] Prometheus metrics not wired — `app/metrics.py` — marketing_agent_content_generated_total and marketing_agent_llm_cost_cents counters from spec not registered in app/metrics.py — Fix: add counter definitions and hook into create_content and generate_content_for_feature — Effort: S
