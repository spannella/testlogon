- [MED] edit_post ignores req.video_id — `app/routers/newsfeed.py:3762` (edit_post) — video_id is accepted on EditPostRequest (line 1686) but never written into the DDB update expression; a creator cannot change or remove a video after post creation — Fix: add video_id handling to edit_post's update logic analogous to image_urls (SET or REMOVE based on req.model_fields_set) — Effort: S
- [MED] N+1 DDB GetItem for video metadata on feed page load — `app/routers/newsfeed.py:2141` (_post_to_dict) — each video post triggers a separate `get_video` call at read time; for a 20-post feed with 5 video posts this adds 5 serial DDB reads — Fix: collect all video_ids in the feed results then use BatchGetItem before serialisation — Effort: M
- [LOW] CountdownCard CTA button non-functional for event-linked video posts — `frontend/src/components/shared/CountdownCard.tsx:140` — CTA Button has no onClick/href; clicking "Watch Live" / "Join Call" on a countdown post does nothing — Fix: pass an onCtaClick callback or href derived from associated_event_id into CountdownCard — Effort: S
