- [CRIT] No `POST /{packet_id}/signers` API endpoint — `app/routers/signature_packets.py` exposes no add/remove signer endpoints; `app/services/signature_packet_store.py` has no `add_packet_signer()` / `remove_packet_signer()` helpers; signer records must be injected directly into DynamoDB (confirmed in `frontend/e2e/signing.spec.ts`); a normal user literally cannot send a document for signature end-to-end — Fix: add `POST /{packet_id}/signers` and `DELETE /{packet_id}/signers/{signer_id}` per ticket §2.1 — Effort: M
- [HIGH] No visual PDF viewer — `frontend/src/pages/files/SignaturePacketComposer.tsx:609-629` is a dashed placeholder `<div>`; the source document is never rendered; react-pdf/pdf.js not installed; fields are placed blind onto an empty canvas — Fix: install react-pdf, render source PDF pages, overlay field boxes per ticket §1b — Effort: L
- [HIGH] No signature drawing canvas — `SignaturePacketComposer.tsx:565` uses a plain text `<input>` with placeholder "Drawn JSON points, e.g. [[0.1,0.2],[0.2,0.3]]"; users must type raw JSON coordinates; no HTML5 canvas stroke capture — Fix: add an HTML5 canvas component for freehand drawing per ticket §1c — Effort: M
- [MED] No `GET /{packet_id}/source-pdf` stream endpoint — the composer cannot fetch the source PDF bytes for display in the viewer; `app/routers/signature_packets.py` has no source-pdf endpoint — Fix: add `GET /{packet_id}/source-pdf` streaming S3 bytes per ticket §2.1 — Effort: S
- [MED] No email/link invite on send — `POST /{packet_id}/send` (`signature_packets.py:384`) does not mint per-signer signing links or email invites; `SIGNATURE_INVITE_LINKS_ENABLED` feature flag does not exist; signers have no way to be notified — Fix: generate per-signer token, send via notifications service, expose in send response per ticket §2b — Effort: M
- [MED] Final-PDF download allows non-completed signers — `app/routers/signature_packets.py:858` checks `if not is_owner and not signer_record` but does NOT verify `signer_record["status"] == "completed"`; any invited-but-unsigned signer can download the completed PDF (SEC-005 cross-ref) — Fix: add `if signer_record and signer_record.get("status") != "completed": raise 403` — Effort: S
- [MED] No rate limit on final-PDF download — `signature_packets.py:850` has no per-user download throttle; a signer could enumerate/download large PDFs at full speed (SEC-005 cross-ref) — Fix: apply existing sliding-window rate-limit helper — Effort: S
- [LOW] Page hardcoded to `1` — `SignaturePacketComposer.tsx:239` always sends `page: 1` when creating fields; multi-page documents cannot have fields placed beyond page 1 — Fix: expose a page selector once the PDF viewer (P1b) is implemented — Effort: S
- [LOW] No guided signer experience — signer sees a flat field list with no on-PDF highlighting or "next field" navigation; ticket §P2a is fully unimplemented — Effort: L
