- [HIGH] FTC labeling limited to post content_type only — `app/services/sponsorship_deals.py:_add_ftc_label:272` — video and broadcast sponsored content silently skipped; FTC disclosure not applied — Fix: extend _add_ftc_label to update T.video_metadata and T.broadcasts for respective content types — Effort: S
- [HIGH] brand name fallback exposes advertiser user sub as PII — `app/services/sponsorship_deals.py:submit_content:521` — when account lookup fails, ftc_disclosure contains "Paid partnership with alice@company.com" — Fix: fall back to "a verified brand partner" rather than the user sub string — Effort: S
- [MED] cross-layer import — service imports router internals — `app/services/sponsorship_deals.py:274` — from app.routers.newsfeed import tbl, pk_post, sk_post; breaks on any newsfeed router refactor — Fix: use T.newsfeed directly and construct PK/SK inline in the service — Effort: S
- [MED] DM fallback creates phantom conversation IDs — `app/services/sponsorship_deals.py:_create_deal_dm:329` — on messaging failure returns "spdeal_{deal_id}" which is not a real conversation; frontend navigation to DM returns 404 — Fix: store None for dm_conversation_id on failure and surface retry action in the UI — Effort: S
- [MED] escrow ConditionalCheckFailedException detected via fragile string matching — `app/services/sponsorship_deals.py:143–147` — type(exc).__name__ match may miss exception on some boto3 versions — Fix: use botocore.exceptions.ClientError with e.response["Error"]["Code"] check — Effort: S
- [LOW] list_deals_by_advertiser_sub scans entire GSI3 and Python-filters — `app/services/sponsorship_deals.py:GSI3` — DEAL#ALL partition grows unboundedly; full scan required for every advertiser listing — Fix: add GSI4 keyed on ADV_SUB#{advertiser_sub} for direct partition-level query — Effort: M
