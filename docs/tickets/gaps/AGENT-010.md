- [HIGH] health check URL shell injection latent — `app/services/agent_devops.py:1217` — run_health_checks builds "curl -sf {h['url']}" f-string; URL with shell metacharacters (;, &&) executes when DEVOPS_AGENT_EXECUTE_COMMANDS=1 — Fix: use subprocess.run(["curl","--silent","--fail",url], shell=False) — Effort: S
- [MED] deploy_commands free-form strings allow admin privilege escalation — `app/services/agent_devops.py:640` — deploy_commands stored as free strings validated only for non-empty; admin could set commands with shell metacharacters — Fix: shlex.split() parse step and/or allowlist of safe command prefixes (terraform, kubectl, aws, docker, helm) — Effort: M
- [MED] secret scrubbing regex incomplete — `app/services/agent_devops.py:549` — _SECRET_KEYS tuple misses access_key, private_key, bearer, credential, cert patterns — Fix: extend tuple to include additional common secret key names — Effort: S
- [MED] approval expiry race condition — `app/services/agent_devops.py:540` — process_approval checks expiry in Python; two concurrent requests at boundary can both pass — Fix: DDB conditional update with TTL-based expiry on RUNNING_LOCK item — Effort: S
- [HIGH] auto_deploy_on_qa_approved trigger not wired — `app/services/agent_devops.py` — config field auto_deploy_on_qa_approved is stored but AGENT-003 polling loop never acts on qa_approved status — Fix: add qa_approved filter in AGENT-003 worker loop when this flag is set — Effort: M
