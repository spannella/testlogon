- [HIGH] /delegates route missing from App.tsx — `frontend/src/App.tsx:~346` — DelegatesPage exists and is functional but cannot be navigated to; all E2E tests that navigate to the page will fail — Fix: add <Route path="delegates" element={<DelegatesPage />} /> alongside delegation-api route — Effort: S
- [MED] Sidebar and AppShell have no Delegates entry — `frontend/src/components/layout/Sidebar.tsx` and AppShell.tsx — feature is unreachable from navigation even after route is added — Fix: add "Delegates" nav entry with UserCog icon in settings/account group — Effort: S
- [LOW] no CHAT_DELEGATION_ENABLED or BROADCAST_DELEGATION_ENABLED feature flags — `app/core/settings.py:~2110` — only delegate_feed_enabled and delegation_api_enabled exist; chat and broadcast delegation have no individual kill switches — Fix: add chat_delegation_enabled and broadcast_delegation_enabled to settings.py — Effort: S
- [LOW] ban check absent from delegate cookie-auth path for downstream services — `app/services/delegates.py:256` (require_delegate_permission) — does not verify delegate's own account ban status; only checks delegation record active — Fix: add is_user_currently_banned(delegate_id) check in require_delegate_permission (also blocks 002-004 access) — Effort: S
- [LOW] SSE not terminated on revocation — `app/services/delegates.py:196` (revoke_delegate) — revoked delegate's in-flight SSE connections stay open until next heartbeat; no active close mechanism — Fix: add revokedAt watermark and validate on each SSE heartbeat (tracked as SEC-018 follow-on) — Effort: M
- [LOW] sub-component files inlined rather than split — `frontend/src/pages/delegates/DelegatesPage.tsx` — AddDelegateDialog, EditPermissionsDialog, DelegateSettingsCard etc. are inlined rather than separate files as specified — Fix: extract as standalone component files (code-organization gap, not functional) — Effort: M
