- [HIGH] trigger_review in-memory lock not distributed — `app/services/agent_pm.py:714` — _RUNNING_REVIEWS is a module-level set; with multiple uvicorn workers two concurrent requests both pass and double-execute — Fix: replace with DDB conditional put_item on LOCK# item with TTL — Effort: S
- [MED] screenshot/trace S3 storage not implemented — `app/services/agent_pm.py:796` — store_review_artifacts from spec is absent; get_review_screenshots only returns text descriptions, not real S3 presigned URLs — Fix: implement store_review_artifacts uploading Playwright screenshots to S3 (moto in dev, real S3 in prod) — Effort: M
- [MED] SSRF guard incomplete for competitor URLs — `app/services/agent_pm.py:263` — validate_pm_config blocks localhost and ://10. prefix but not 169.254.x.x link-local, 172.16-31.x.x, 192.168.x.x, or DNS rebinding — Fix: extend blocklist to full RFC-1918 + link-local ranges — Effort: S
- [LOW] app_auth_credentials_secret not wired — `app/services/agent_pm.py` — ticket spec requires credentials for PM Agent to browse live app but no credentials field exists in config schema or trigger_review path — Fix: add app_auth_credentials_secret_name field; resolve via KMS/Secrets Manager at execution time — Effort: M
- [LOW] Prometheus metrics not wired — `app/metrics.py` — seven pm_* metrics from ticket spec (pm_ideas_created_total, pm_approval_rate, etc.) not registered in app/metrics.py — Fix: add counter/gauge definitions and hook into create_feature_idea, approve_idea, reject_idea — Effort: S
