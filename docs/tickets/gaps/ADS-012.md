- [CRIT] elevate_feed_items() not called from newsfeed router — `app/routers/newsfeed.py` — creators are charged for boosts that have no effect on reach; every boost is a misleading charge — Fix: call elevate_feed_items(posts, viewer_id) in GET /feed handler after organic post fetch — Effort: S
- [HIGH] no content ownership validation at boost creation — `app/services/content_boost.py:create_boost` — any user can pay to boost content they don't own, skewing feed ranking for others' content — Fix: call _verify_content_ownership(owner_sub, content_type, content_id) before wallet charge — Effort: S
- [HIGH] no duplicate-active-boost guard — `app/services/content_boost.py:create_boost` — user can create multiple simultaneous boosts for same content, paying budget multiple times — Fix: query GSI2 for active boosts before write; raise ValueError if one exists — Effort: S
- [MED] no stats endpoint for boost effectiveness — `app/routers/content_boost.py` — creators have no visibility into impressions/reach/engagement from their boost — Fix: add get_boost_stats(boost_id) service function and GET /ui/ads/boost/{id}/stats endpoint — Effort: M
- [MED] broad except Exception in _wallet_charge could mask unrelated DDB errors — `app/services/content_boost.py:~188` — ConditionalCheckFailedException caught alongside all other exceptions; real errors silently swallowed — Fix: catch ConditionalCheckFailedException specifically via botocore.exceptions.ClientError — Effort: S
- [MED] BoostDialog.tsx and PostCard Boost button integration absent — `frontend/src/pages/feed/PostCard.tsx` — no in-feed boost entry point; creators must navigate to boost management page separately — Fix: create BoostDialog.tsx component and add Boost button to PostCard for post owners — Effort: M
- [LOW] E2E spec only 7 tests vs 22 planned — `frontend/e2e/content-boost.spec.ts` — cancellation, UI, concurrent/content-type sections are absent — Fix: expand spec to cover sections 394-396 — Effort: M
- [LOW] unit tests absent — `tests/test_content_boost.py` — no offline pytest coverage for wallet deduction, status transitions, or elevation logic — Fix: create test_content_boost.py with moto mock — Effort: S
