# BCAST-010 Gap List

- [MED] Feature flag `broadcast_newsfeed_promotion_enabled` not enforced — `broadcast.py:2348`, `broadcast_orchestrator.py:84`, `broadcast_recording_worker.py:235` — lifecycle hooks create posts unconditionally; setting `BROADCAST_NEWSFEED_PROMOTION_ENABLED=0` has no effect — Fix: wrap all three call sites with `if S.broadcast_newsfeed_promotion_enabled:` guard — Effort: S
- [MED] Peak viewer count not tracked — `broadcast_viewers.py` / `broadcast_newsfeed.py` — VOD post's `peak_viewer_count` uses `get_viewer_count()` (post-stop value, likely near 0) not the lifetime peak — Fix: add `record_viewer_count()` in `broadcast_viewers.py` that conditionally updates `peak_viewer_count` attribute on session — Effort: S
- [LOW] Two parallel writer modules with different item schemas — `broadcast_newsfeed.py` vs `broadcast_newsfeed_promo.py` — `broadcast_newsfeed.py` uses `bcast_<hex>` IDs and its own `_write_broadcast_post`; `broadcast_newsfeed_promo.py` uses `nf.new_id`; schema drift risk if newsfeed SK format changes — Fix: consolidate to a single shared writer using `nf.new_id("post")` — Effort: M
- [LOW] `fan_out_delete_post` vs `fan_out_unpublish_post` semantic difference — `broadcast_newsfeed.py:143` vs `broadcast_newsfeed_promo.py:191` — inconsistent deletion semantics between lifecycle-created and manually-promoted post cancellations — Fix: verify both functions produce desired FEEDREF cleanup; align to one function — Effort: S
- [LOW] Broadcast spam from short sessions — no minimum session duration filter before creating VOD posts — any brief start/stop creates feed entries for all followers — Fix: add minimum session `duration_seconds` threshold before `create_vod_post` fires — Effort: S
