# BCAST-015 Gap List

- [HIGH] Billing atomicity gap in `_write_chat_billing()` — `broadcast_chat_rich.py:247` — debit and credit `put_item` calls in separate `try/except`; credit failure after debit = viewer charged, broadcaster not paid (fourth occurrence of same pattern across BCAST-011/012/014) — Fix: use `TransactWriteItems`; consolidate into shared `write_billing_pair()` utility in `app/services/billing_utils.py` — Effort: S
- [HIGH] `_find_sort_key()` is O(n) scan for reactions/unlocks — `broadcast_chat_rich.py:229` — forward query scanning up to all messages in session to find one item by `message_id`; unacceptable latency at >5k messages — Fix: add `MessageIdIndex` GSI to `BroadcastChatMessages` (pk=`message_id`) or accept `sort_key` from caller — Effort: S
- [MED] Expired message text not nulled in `_chat_msg_out()` — `broadcast_chat_store.py` — when `expires_at < now_ts()`, `expired=True` is set but `text` field is still returned in clear — Fix: set `out["text"] = None` when `expires_at` is past in `_chat_msg_out()` — Effort: S
- [MED] Phase E (view-once) not implemented — `broadcast.py` / `broadcast_chat_store.py` — `view_once` field absent from `BroadcastChatSendIn`; no `view_once_seen` attribute; no `POST /chat/{msg_id}/view` endpoint — Fix: add `view_once` to send model, `view_once_seen` DDB set attribute, and mark-viewed endpoint — Effort: M
- [MED] SSE stream delivers `is_unlocked=None` for locked messages — `broadcast_chat_store.py` — `_store_fetch_after()` calls `_chat_msg_out()` without `viewer_user_id`; SSE-polled messages always show `is_unlocked=None` — Fix: pass `viewer_user_id` through `_store_fetch_after()` to `_chat_msg_out()` — Effort: S
- [LOW] Reaction map initialization race (two-step write) — `broadcast_chat_rich.py:65` — fallback `SET reactions = :empty` then `ADD` between two concurrent reactions can cause duplicate initialization; DDB `ADD` on nested map path cannot auto-create the map — Fix: always initialize `reactions={}` at message creation time in `send_chat_message()` — Effort: S
