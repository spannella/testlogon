# TEST-001 gaps — Residual E2E Shard-Interference Failures

- [MED] 12 residual failing tests under restart-per-shard runs — `group-feed.spec.ts` (pin badge :359, pin >3 → 409 :377) are deterministic failures rooted in `_count_pinned` undercounting under moto BatchGet throttling + pinned-index derivation in `app/services/group_feed.py`; `video-upload.spec.ts:495` and `vod-broadcast-pricing.spec.ts:277,:292` are newly surfaced and undiagnosed as of ticket filing — Fix: (1) finish pinned-index robustness under BatchGet throttling in `app/services/group_feed.py`; (2) run stability re-run to classify deterministic vs flaky for the remaining 8 tests; (3) apply scope-to-unique-marker / self-find-by-id patterns for the interference class (billing-wallet:418, delegates-newsfeed:228, kyc-address-verification:360, bug-fixes:814+:1050, agent-product:340) — Effort: M
- [LOW] No sharding configuration in `frontend/playwright.config.ts` — config uses `workers: 1`, `retries: 1` with no shard support; the restart-per-shard runs described in the ticket require an external wrapper script rather than built-in Playwright sharding — Fix: add `--shard N/M` capability documentation or a `justfile` target `just e2e-shard N` using `npx playwright test --shard=N/M`; consider reducing shard size to 10 specs to cut accumulation — Effort: S
- [LOW] Dev-only rate-limit cap inflation is undocumented — inflated rate limits (applied to prevent 429 shard interference) are not mentioned in `CLAUDE.md` common gotchas or `docs/local-dev-stack.md`; a future engineer may unknowingly rely on inflated limits — Fix: add a comment in the relevant rate-limit service file and a note in `CLAUDE.md` explaining the dev-only inflation — Effort: S
