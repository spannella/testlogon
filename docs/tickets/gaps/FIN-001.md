# FIN-001 Gaps

- [MED] Date-range filter applied in-memory after DDB page fetch — `app/services/invoices.py:404-436` — when `date_from`/`date_to` is combined with `type` filter the service fetches up to `limit` raw DDB items first and then discards out-of-range rows; on a high-volume account the first page may be entirely out of range causing silently truncated results — Fix: use GSI1SK (numeric `created_at`) in `KeyConditionExpression` `between` to push date range into the DDB query rather than Python post-filter — Effort: M

- [MED] Subscription-renewal flows do not create invoices — `app/routers/subscription_server.py:912,1296,1436` calls `emit_subscription_cycle_order_and_reconcile` which writes a billing ledger entry but never calls `create_invoice_safe`; `app/services/subscription_cycle_orders.py` has no reference to invoices — Fix: add `create_invoice_safe(invoice_type="subscription", ...)` call inside `emit_subscription_cycle_order_and_reconcile` after the ledger write, or inside the three `emit_subscription_cycle_order_and_reconcile` callsites — Effort: S

- [LOW] `InvoiceEmailOut` from router omits `emailed_to` when email address is empty — `app/routers/invoices.py:89` returns hardcoded `ok=True` but `emailed_to` can be `""` if `buyer_email` and profile lookup both fail; no caller-visible error — Fix: propagate `result["ok"]` truthiness check and surface the email address used — Effort: S

- [LOW] `admin_list_invoices` with `target_user_sub=None` applies `invoice_type` filter in-memory after GSI2 page — `app/services/invoices.py:519-521` fetches `limit` items by `ADMIN_ALL` then filters by type; type-specific admin listing silently truncates beyond first page — Fix: add type-specific GSI or accept the limitation and document it — Effort: M
