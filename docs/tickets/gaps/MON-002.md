# MON-002 gaps

- [MED] `write_tip_ledger` performs two sequential non-atomic `put_item` calls with independent try/except blocks — `app/services/tip_ledger.py:133-172` (cross-ref SEC-004 atomicity at tip_ledger.py:146-172) — debit can succeed while credit silently fails leaving an orphaned charge with no corresponding creator revenue; no detection or reconciliation path exists — Fix: log a structured warning with both `debit_id` and `credit_id` when one write succeeds and the other fails, store `debit_id` on the credit item and vice-versa, and add a reconciliation scan job keyed on `meta.tip_payment_id` — Effort: M

- [LOW] `TipLedgerEntry.__init__` does not enforce `tipper_user_id != recipient_user_id` — `app/services/tip_ledger.py:48` — a code path that bypasses endpoint-level self-tip guards would silently write self-debit + self-credit (net zero, but pollutes the ledger) — Fix: add assertion `if tipper_user_id == recipient_user_id: raise ValueError("tipper and recipient must differ")` — Effort: S

- [LOW] `write_tip_ledger` silently drops collaboration-split errors and falls back to single credit — `app/services/tip_ledger.py:121-122` — if `maybe_split_content_revenue` raises, the full amount is credited to one creator even when a split agreement exists; the collaboration partner sees no revenue — Fix: log the exception with content_id at WARNING level and include a `split_attempted=True` flag in the returned dict so downstream callers can alert — Effort: S
