- [HIGH] VNC password / SSH password not stored (SEC-022 surface) — `app/services/connection_profiles.py:50,171-252`, `app/models.py:10812-10844` — `auth_method="password"` is accepted but no password field exists on create/update; callers set auth_method=password with no way to store or retrieve the credential, making auto-connect for password-auth impossible — Fix: add optional `vnc_password` / `ssh_password` fields with KMS-encrypt on write (using `app/core/crypto.kms_encrypt`) and `has_password: bool` on read; never return plaintext (aligns with SEC-022 pattern) — Effort: M
- [MED] Recent-connections singleton list not implemented — ticket spec: `RECENT#{user_sub}` DDB item capped at 10; actual impl only uses `last_used_at` ordering in profile list — connection-specific recency (hostname/connected_at) for non-profile ad-hoc connects is not tracked — Fix: implement `record_recent_connection` + `get_recent_connections` + `clear_recent_connections` on a `RECENT#{user_sub}` singleton item; expose `GET /recent` and `DELETE /recent` endpoints — Effort: M
- [LOW] No per-user profile count enforcement at router level — `app/services/connection_profiles.py:247-252` — limit is checked in the service but service raises `ProfileLimitExceeded(409)` while router maps it; consistent but error message does not include the current count — Fix: include `current_count` and `max_allowed` in the 409 response body — Effort: S
