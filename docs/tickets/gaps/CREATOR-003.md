- [HIGH] dashboard_sse_publish has no call sites — `app/services/dashboard_sse.py:39` (no callers outside own module) — SSE publish infrastructure exists but real-time earnings/milestone events are never pushed — Fix: call dashboard_sse_publish from write_tip_ledger (earnings:update) and check_milestone (milestone:reached) — Effort: S
- [HIGH] check_milestone has no call sites — `app/services/milestones.py:49` (no callers outside own module) — milestones can only be created by manually seeding DynamoDB; no automatic detection — Fix: call check_milestone from tip_ledger write, subscription signup, and analytics rollup completion — Effort: S
- [MED] SSE not connected in frontend — `frontend/src/pages/dashboard/CreatorDashboard.tsx` (no EventSource, no useSSE hook) — GET /ui/dashboard/stream exists but frontend polls every 60s only; real-time revenue ticker user story not met — Fix: create frontend/src/hooks/useSSE.ts and wire earnings:update + milestone:reached handlers in CreatorDashboard — Effort: S
- [MED] affiliate earnings category missing — `app/services/creator_earnings.py:36` (no "affiliate" branch in classify_entry) — affiliate commissions fall into "other" in dashboard breakdown — Fix: add "if 'affiliate' in reason: return 'affiliate'" before return "other" — Effort: S
- [LOW] AnalyticsSparkline component absent — `frontend/src/pages/dashboard/CreatorDashboard.tsx` (not imported) — no 7-day earnings/views chart; sparkline_data not included in summary response — Fix: add sparkline data to GET /ui/dashboard/summary from existing rollup fetch; add AnalyticsSparkline component — Effort: M
- [LOW] MilestoneToast absent — `frontend/src/pages/dashboard/CreatorDashboard.tsx` (no toast on SSE event) — milestone achievements only visible via polling list; no proactive toast notification mid-session — Fix: SSE milestone:reached handler calls toast.success + invalidateQueries(["milestones"]) — Effort: S
- [LOW] PullToRefresh gesture missing — `frontend/src/pages/dashboard/CreatorDashboard.tsx` (no gesture handler) — mobile acceptance criterion "pull to refresh" not met — Fix: add PullToRefresh component with touchstart/touchmove/touchend handling — Effort: M
- [LOW] ActiveBroadcastCard not extracted — `frontend/src/pages/dashboard/CreatorDashboard.tsx:123-138` (inline ul) — active broadcasts rendered inline rather than as reusable component — Fix: extract ActiveBroadcastCard.tsx (code-organization gap, not functional) — Effort: S
