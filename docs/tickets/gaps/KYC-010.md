- [CRIT] `ByCase` GSI fallback performs full table scan — `app/services/kyc_id_scanner.py` (`list_scans_for_case()`, lines 615–627) — on GSI query failure the `except` block falls through to scanning the entire `kyc_id_scans` table, returning all users' scans to any caller — Fix: replace fallback with `logger.exception(...); return []` to return an empty list safely — Effort: S
- [HIGH] Profile field name misalignment causes zero match score — `app/services/kyc_id_scanner.py` (`_load_crosscheck_profile()`, lines 433–454) — reads `profile.get("first_name")` / `profile.get("last_name")`; if the profile stores only `display_name`, both fields are empty, name comparison scores 0, scan is flagged for all users — Fix: add fallback to split `display_name` when `first_name`/`last_name` are absent — Effort: S
- [HIGH] Default passport mock MRZ is expired — `app/services/kyc_id_scanner.py` (`_MOCK_MRZ`, lines 386–407) — mock passport expiry `120415` parses to April 15, 2012; any E2E test calling `POST scan-document` without supplying `mrz_lines` gets `status="rejected"` instead of `"matched"` — Fix: update `_MOCK_MRZ` passport expiry to a future date (e.g., `361231` → 2036-12-31) with recomputed check digits; or document required `mrz_lines` test fixture — Effort: S
- [MED] `production` image URL not implemented — `app/services/kyc_id_scanner.py` (lines 545–548) — `image_url` is only populated when `S.dev_mode=True` (returns `/mock/s3/...`); production deployments have no pre-signed S3 URL for the scanned image — Fix: generate pre-signed S3 URL via `s3_client().generate_presigned_url()` for both dev and prod paths — Effort: S
- [MED] Risk scoring fires synchronously on every scan — `app/services/kyc_id_scanner.py` (`_feed_risk_scoring()`, lines 601–609) — `KycRiskScoringService().compute_score()` called synchronously adds ~500ms to every scan response; fires twice for a two-sided national ID — Fix: run in a daemon thread (`threading.Thread(target=_bg, daemon=True).start()`) or asyncio task to make it fire-and-forget — Effort: S
- [MED] `KycIdScannerStateError("kyc_case_finalized")` translation to HTTP 400 unverified — `app/routers/kyc_id_scanner.py` — ticket spec requires 400 for finalized-case scans; whether the router's exception handler translates `KycIdScannerStateError` to 400 vs 500 must be confirmed — Fix: verify exception-to-HTTP mapping in the router; add explicit `@kyc_id_scanner_router.exception_handler` if needed — Effort: S
- [LOW] TD1 composite check digit coverage lacks canonical test — `app/services/kyc_id_scanner.py` (`parse_td1_mrz()`, lines 231–274) — composite check covers `line1[5:30]+line2[0:7]+line2[8:15]+line2[18:29]`; no unit test against the canonical ICAO 9303 TD1 example confirms correctness — Fix: add `test_parse_td1_mrz_valid` with canonical ICAO example to `tests/test_kyc_scanner.py` — Effort: S
