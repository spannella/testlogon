# UX-002 gaps — Keyboard Shortcuts / Command Palette
# Note: overlaps PLATFORM-014 (see docs/tickets/gaps/PLATFORM-014.md for additional gaps)

- [HIGH] Ctrl+Enter send not wired in ComposeBar — `frontend/src/pages/messages/ComposeBar.tsx:690-694` only handles `Enter && !shiftKey`; no `ctrlKey || metaKey` branch exists; the global shortcut at `Header.tsx:238-243` lists "Send message" with an empty action ("handled locally in ComposeBar") but ComposeBar does not implement it; e2e test `keyboard-shortcuts.spec.ts:216` (section 76) tests this and will fail — Fix: add `if (e.key === "Enter" && (e.ctrlKey || e.metaKey)) { e.preventDefault(); void handleSubmit(); }` to `handleKeyDown` in `ComposeBar.tsx:694` — Effort: S
- [MED] ? key registered as `shift+?` not `?` — `frontend/src/components/layout/Header.tsx:214` registers the shortcut help key as `key: "shift+?"` which requires Shift to be held; on most keyboard layouts `?` is already `Shift+/`, making the effective combo `Shift+Shift+/` (double-modifier); on some layouts it may never fire — Fix: change the key string to `"?"` (unmodified) and ensure `normalizeKeyEvent` correctly emits `"?"` for `e.key === "?"` without adding `shift` — Effort: S
- [MED] Ctrl+1-9 section navigation not implemented — ticket §2.1 US-5 and §4.5 table list Ctrl+1-9 as a desired shortcut; `Header.tsx:205-244` shortcuts memo does not include any `ctrl+[1-9]` entries; the ticket's own open-question §14.1 recommends dropping them in favour of chords, but the chord-based navigation (g+m etc.) does not cover all 9 sidebar items — Fix: either add 9 chord shortcuts for missing sections or explicitly document the decision to drop Ctrl+1-9 in CLAUDE.md — Effort: S
- [LOW] ShortcutHelpDialog does not render chord shortcuts — `frontend/src/components/shared/ShortcutHelpDialog.tsx` receives only the `Shortcut[]` array from `Header.tsx:854`; chord shortcuts (`ChordMapping[]` at `Header.tsx:249+`) are never passed; the "Navigation" group in the overlay is empty — Fix: extend `ShortcutHelpDialog` props to accept `chords: ChordMapping[]` and render them in the Navigation group — Effort: S
