- [MED] no maximum future limit on target_datetime — `app/routers/newsfeed.py:3521` (create_post countdown validation) — only `<= now` is rejected; a creator can set a countdown 100 years in the future, creating feed clutter that can never expire — Fix: enforce an upper bound (e.g. 2 years: `target_datetime > now_ts + 63072000`) and return 400 with detail "target_datetime cannot be more than 2 years in the future" — Effort: S
- [MED] CountdownCard CTA button has no navigation action — `frontend/src/components/shared/CountdownCard.tsx:139` — the "Watch Live" / "Join Call" / "View Event" Button renders with no onClick handler or href; clicking it does nothing, breaking the expected deep-link to the associated broadcast/call/calendar event — Fix: accept an optional onCtaClick callback prop in CountdownCard and wire PostCard to pass a navigation function using associated_event_type and associated_event_id — Effort: S
- [LOW] associated_event_id is not validated against the referenced entity — `app/routers/newsfeed.py:3523` (create_post countdown validation) — the check only verifies presence when event_type is non-custom; no lookup of the actual broadcast/call/calendar record is performed, so a countdown can be linked to a non-existent event — Fix: optionally fetch the referenced record by event_type (broadcast table, call table, calendar table) and return 404 if not found — Effort: M
- [LOW] countdown_posts_enabled feature flag uses getattr fallback True instead of S attribute — `app/routers/newsfeed.py:3517` — `getattr(S, "countdown_posts_enabled", True)` will silently default to True even if the settings attribute is removed or misspelled; should use `S.countdown_posts_enabled` directly to get a hard AttributeError on misconfiguration — Fix: replace `getattr(S, "countdown_posts_enabled", True)` with `S.countdown_posts_enabled` — Effort: S
