- [HIGH] ROAS calculation service not implemented — `app/services/` — no ad_roas.py, no GET /ui/ads/campaigns/{id}/roas endpoint; advertisers cannot measure campaign effectiveness via conversion attribution — Fix: create app/services/ad_roas.py with calculate_campaign_roas and wire GET endpoint in ad_creative_affiliate.py router — Effort: M
- [HIGH] E2E spec absent — `frontend/e2e/` — no ad-affiliate-promo.spec.ts; redirect URL, cookie setting, and attribution recording are untested — Fix: create spec with 18 tests covering sections 405-408 — Effort: M
- [MED] affiliate_user_id ownership field may not match actual affiliate_links schema — `app/services/ad_creative_affiliate.py:88` — link.get("affiliate_user_id") check could silently pass or reject all codes if field name differs — Fix: cross-check against affiliate_links.create_affiliate_link() to confirm actual field name — Effort: S
- [MED] promo code validated at config-save time but cookie set at click time — `app/services/ad_creative_affiliate.py:handle_click` — expired promo cookie still set for codes that expire after creative config was saved — Fix: re-validate promo expiry at click time in handle_click; omit cookie if expired — Effort: S
- [LOW] click_through_url redirect injection risk — `app/models.py:AdAffiliateConfigCreate` — URL scheme not validated at model level; javascript: or data: URI could be stored as redirect target — Fix: add Field(pattern=r"^https?://") or use Pydantic HttpUrl type on click_through_url — Effort: S
