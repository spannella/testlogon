- [HIGH] `stored_key` auth type NOT wired into SSH terminal — `app/routers/browser_ssh_terminal.py:666` — `authType` validation only accepts `"password"` or `"private_key"`; the `"stored_key"` + `keyId` flow specified by INFRA-002 is absent; `get_decrypted_private_key()` in `app/services/ssh_key_manager.py:271` is never called from the SSH bridge — the entire "private key never reaches the browser" security guarantee is therefore not delivered — Fix: add `"stored_key"` to the `authType` allowlist; in the connect handler, if `authType == "stored_key"` fetch `keyId` from payload, call `get_decrypted_private_key(user_sub, key_id)`, pass result as `private_key` to `ParamikoSshBridge`, and call `record_connection()` on the associated host — SEC-022 cross-ref: key plaintext must never appear in WS messages or logs — Effort: M

- [HIGH] No audit events emitted by SSH key manager service — `app/services/ssh_key_manager.py` (entire file, 0 `audit_event` calls) — ticket spec requires audit events for `ssh_key.generate`, `ssh_key.upload`, `ssh_key.delete`, `ssh_key.decrypt`, `ssh_key.associate/disassociate`; only `logger.info` structured logs are emitted; no entries reach the audit trail or SECOPS-001 telemetry — Fix: import `audit_event` from `app.services.alerts` and call it in `generate_key`, `upload_key`, `delete_key`, `get_decrypted_private_key`, `associate_key_with_host`, `disassociate_key_from_host` — Effort: S

- [MED] SSH terminal destination policy allows RFC-1918 / metadata targets (SEC-020) — `app/routers/browser_ssh_terminal.py:452-488` — `_enforce_destination_policy` only checks configured `BROWSER_SSH_DENIED_HOSTS` / `BROWSER_SSH_ALLOWED_HOSTS`; both default to empty (allow-all); no hardcoded denylist blocks `169.254.169.254`, `127.0.0.1`, RFC-1918 ranges, or `::1`; storing a host record with `hostname=169.254.169.254` and using it for quick-connect is unrestricted — This is the SEC-020 SSRF issue; INFRA-002 keys make it more exploitable because quick-connect can use a stored key automatically — Fix: add a hardcoded RFC-1918 + metadata denylist evaluated **before** the configurable policy; resolve hostname and re-check resolved IP (DNS rebinding) — Effort: M

- [MED] `delete_key` does not unlink associated hosts — `app/services/ssh_key_manager.py:242` — `delete_key()` deletes the DDB item but does not iterate `associated_hosts` and clear `ssh_key_id` on each linked host record; host records continue to show a dangling `ssh_key_id` referencing a deleted key; auto-connect via `stored_key` would then receive `None` from `get_decrypted_private_key` and fail silently — Fix: in `delete_key()`, call `disassociate_key_from_host()` for each entry in `associated_hosts` before deleting the key item — Effort: S

- [LOW] No rate limiting on key generation or upload endpoints — `app/routers/ssh_key_manager.py` (POST `/` and POST `/generate`) — INFRA-002 spec calls for 5 RSA-key-generates/min and 10 uploads/min per user; RSA-4096 generation is CPU-intensive; no rate-limit decorator or check exists — Fix: add `require_rate_limit(user_sub, "ssh_key_generate", limit=5, window=60)` (reuse platform rate-limiter) to `generate_ssh_key` endpoint — Effort: S

- [LOW] Credential exposure risk: `UploadSshKeyIn.private_key_pem` logged via default FastAPI error handlers — `app/models.py:~SshKeyOut` / `app/routers/ssh_key_manager.py:47` — if Pydantic validation raises `RequestValidationError`, FastAPI default error handler may serialize the body including `private_key_pem` to the error response; redaction middleware (if any) may not cover this field — SEC-022 cross-ref — Fix: add a custom 422 handler or ensure `private_key_pem` is marked as `Secret` / excluded from error serialization — Effort: S
