- [HIGH] `_count_user_clips_for_session` uses `FilterExpression` with `Select=COUNT` but no pagination — `app/services/broadcast_clip.py:335` — DDB applies `FilterExpression` after reading up to 1 MB of items; for a busy session with many clips, a single `query()` call with `Select=COUNT` and a `FilterExpression` may silently under-count, allowing a user to exceed `_MAX_CLIPS_PER_BROADCAST=10` — Fix: loop on `LastEvaluatedKey` to aggregate the full count across all pages — Effort: S
- [HIGH] `delete_clip` does not check admin/moderator role — `app/services/broadcast_clip.py:281` — authorization only checks `actor == creator_user_id or actor == broadcaster_user_id`; admin users (role >= ADMIN) cannot moderate or remove clips from any broadcast even if the feature is described as "admin can also delete" in the docstring — Fix: pass `role` into `delete_clip` and add `or role in (Role.ADMIN, Role.ROOT)` to the authorization check — Effort: S
- [MED] Gallery sort key (`GSI3SK`) overflows at 100 million views — `app/services/broadcast_clip.py:307` — `str(new_count).zfill(8)` produces an 8-char string for counts up to 99,999,999; at 100,000,000 views `zfill(8)` returns a 9-char string (`"100000000"`), breaking lexicographic sort order so extremely popular clips sort behind less-viewed ones — Fix: increase padding to `zfill(12)` to support up to 999 billion views — Effort: S
- [MED] `creator_display_name` always falls back to `user_sub` in clip output — `app/routers/broadcast_clips.py:46` — `ctx.get("display_name") or ctx.get("user_sub", "Viewer")` always evaluates `display_name` as None (not in session context), so every clip record stores a raw UUID sub as creator display name — Fix: look up the display name from the user's profile using `ctx["user_sub"]` before calling `create_broadcast_clip` — Effort: S
- [MED] No "share clip to newsfeed" endpoint — `app/routers/broadcast_clips.py` — the ticket user story "As a viewer, I want to post my clip to the newsfeed" (`POST /broadcast/clips/{clip_id}/share-to-feed`) is absent; `record_share` only increments a counter and returns a URL — Fix: add a `POST /broadcast/clips/{clip_id}/share-to-feed` endpoint that creates a newsfeed post with the clip embedded (reuse the `create_post` service with `post_type="clip"` or video_id pointing to the clip's video_id) — Effort: M
- [MED] No frontend clip UI — `frontend/src/pages/` — no `ClipPage`, `ClipGallery`, or clip creation dialog exists; the broadcaster dashboard and viewer player lack a "Clip" button — Fix: implement `ClipGalleryPage.tsx`, `MyClipsPage.tsx`, `ClipPage.tsx` (public share page at `/clips/:clipId`), and a `ClipCreatorDialog.tsx` triggered from the broadcast viewer — Effort: L
- [LOW] `list_clips_for_session` and `list_my_clips` use `FilterExpression=Attr("status").ne("deleted")` with a hard `Limit` — `app/services/broadcast_clip.py:216,227` — same DDB FilterExpression-after-Limit problem as ENGAGE-002; deleted clips count against the 50-item read window; if many clips are soft-deleted the visible result set is smaller than expected — Fix: loop on `LastEvaluatedKey` until `limit` non-deleted items are collected — Effort: S
- [LOW] `record_view` and `record_share` public endpoints have no authentication or rate limiting — `app/routers/broadcast_clips.py:142,150` — any unauthenticated client can call `POST /broadcast/public/clips/{clip_id}/view` or `share` in a tight loop, inflating view/share counts arbitrarily — Fix: add IP-based rate limiting (reuse `_enforce_clip_rate_limit` pattern) or require a signed token — Effort: S
