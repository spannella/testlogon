- [HIGH] Discovery index not populated on profile update — `app/services/profile.py:apply_profile_update()` saves the profile and audits changes but never calls `index_user_for_discovery()`; a user who changes their display name remains findable only by their old name until the periodic re-index runs; new users are never indexed at all unless they explicitly hit `POST /ui/discover/reindex` — Fix: add `index_user_for_discovery(user_sub)` (non-fatal try/except) at end of `apply_profile_update()` per ticket §4.6 — Effort: S
- [HIGH] `POST /ui/discover/reindex` is self-only — `app/routers/discovery.py:70-75` only indexes the authenticated user; no admin/root bulk re-index endpoint exists; initial seeding of existing users must happen via the `reindex_all_users()` function manually — Fix: add an admin/root-gated `POST /ui/discover/admin/reindex-all` endpoint backed by `reindex_all_users()` — Effort: S
- [MED] Discovery index not updated on follower count change — `app/services/social.py:follow_user/unfollow_user` update `follower_count` on the profiles table but `DiscoveryIndex` entries retain stale `follower_count`/`GSI1SK` values; trending order is incorrect until the periodic full re-index runs — Fix: call `index_user_for_discovery(followed_id)` in follow/unfollow (see also SOC-001 gap) — Effort: S
- [MED] No periodic background re-index job registered — `app/services/discovery.py:reindex_all_users()` exists but is never called on a schedule; `app/main.py` has no startup task for it — Fix: register as a daily background task in `app/main.py` startup (consistent with SOC-001/SOC-003 ticket specs) — Effort: S
- [LOW] `remove_user_from_discovery()` uses a full table scan — `app/services/discovery.py:507` calls `tbl_discovery.scan()` with a FilterExpression on `sk`; for large indexes this reads every item; for account deletion/hide this is the only path — Fix: add a GSI with `sk` (USER#{user_id}) as partition key to the DiscoveryIndex table definition so deletion can use a query — Effort: M
- [LOW] `TRENDING` GSI partition holds all visible users — `app/services/discovery.py:492` uses a constant `GSI1PK="TRENDING"` for every user token; at >100K users this partition exceeds DynamoDB's 10GB item-collection limit and hot-partition rate limits — Fix: shard by `TRENDING#{user_id_hash % 10}` and query all shards — Effort: M
