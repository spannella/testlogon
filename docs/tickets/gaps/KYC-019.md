- [HIGH] `_scan_availability_items` uses a full-table Scan with a `begins_with` filter on PK — `app/services/kyc_case_assignment.py:161-176` — DynamoDB `begins_with` is a `FilterExpression`, not a `KeyConditionExpression`; the scan fetches ALL items from the `kyc_cases` table and discards non-`ADMIN#` rows; the `kyc_cases` table is the primary application table containing thousands of case items; on a busy table this scan is slow, expensive, and will return incomplete results when more than one 1 MB page of non-ADMIN items exists before all ADMIN items are scanned — Fix: move admin availability records to a GSI-friendly PK/SK layout (e.g., query `entity_type = "kyc_admin_availability"`) or add a dedicated `admin_availability` table; as a minimal fix, loop `LastEvaluatedKey` until all pages are consumed and document the O(N) cost — Effort: M
- [HIGH] `_active_case_counts` calls `_scan_active_cases` which runs three separate GSI queries (one per status) and returns all active cases to count assignments in Python — `app/services/kyc_case_assignment.py:238-243` — every call to `auto_assign`, `list_admin_workloads`, `workload_dashboard`, or `get_admin_availability` triggers this multi-query/scan; in a production system with thousands of under-review cases this is an O(N) operation on the critical path of assignment scoring; the ticket suggested using a dedicated `assigned-admin-index` GSI on the `kyc_cases` table — Fix: add an `assigned_admin_sub` GSI to `kyc_cases` and count per-admin via direct GSI queries instead of loading all active cases into memory — Effort: L
- [MED] `escalate_case` router endpoint (line 193) reads `escalation_level` directly from the case item before calling the service instead of letting the service own that logic — `app/routers/kyc_case_assignment.py:193-203` — the router calls `ASSIGNMENT_SERVICE._get_case(case_id)` and extracts `escalation_level` from the `review` sub-object to pass back to `escalate_case()`; if the case does not exist the code dereferences `None` with `(case.get("review") or {}).get(...)` silently defaulting to `0`, masking the 404 — Fix: move the case-existence check and `escalation_level` resolution inside `KycCaseAssignmentService.escalate_case()` and raise `KycCaseAssignmentError("case_not_found", status_code=404)` when the case is missing — Effort: S
- [LOW] Auto-assign-on-submit is not triggered from `kyc_cases.py` when a case transitions to `submitted` — `app/services/kyc_cases.py` (no call to `ASSIGNMENT_SERVICE.auto_assign`) — the ticket (§4.5, `KYC_AUTO_ASSIGN_ON_SUBMIT`) describes hooking auto-assignment into the submission flow; the flag `kyc_auto_assign_on_submit` exists in settings (not verified) but no call to the assignment service is made from `submit_case`; newly submitted cases remain unassigned until an admin manually triggers assignment — Fix: in `STORE.submit_case()`, if `S.kyc_auto_assign_on_submit` is True, call `ASSIGNMENT_SERVICE.auto_assign(case_id=..., actor_sub="SYSTEM")` after a successful DDB status update — Effort: S
