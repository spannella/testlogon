- [HIGH] Readiness gate not extended for enhanced/high_risk profiles — `app/routers/kyc_cases.py:244` (`_readiness_for_case`) — users with `intake_profile=enhanced` or `high_risk` can submit without any residency document; violates AML/CDD enhanced due diligence requirements — Fix: add residency check block using `RESIDENCY_STORE.get_verified_docs_for_case(case_id)` gated on `intake_profile in ("enhanced","high_risk")` — Effort: S
- [HIGH] `get_verified_docs_for_case` helper missing — `app/services/kyc_residency.py` — `_readiness_for_case` extension has no method to query verified residency docs for a case — Fix: add `get_verified_docs_for_case(case_id)` querying the ByStatus GSI with `status=verified` and `FilterExpression: case_id` — Effort: S
- [HIGH] ResidencyVerificationTab missing from admin case detail — `frontend/src/pages/admin/KycCaseDetailPage.tsx` (depends on KYC-001) — reviewer cannot see address comparison or match status per document in the case view — Fix: add "Residency" tab with two-column address comparison and per-field match badges — Effort: M
- [MED] Future `document_date` accepted without validation — `app/models.py` (KycResidencyUploadRequest) — a user can submit a document with a future date; `_check_recency` computes negative `recency_days` and sets `recency_valid=True` (not expired) — Fix: add `@field_validator("document_date")` enforcing `parsed <= date.today()` — Effort: S
- [MED] Document deletable after submission — `app/routers/kyc_residency.py` (DELETE endpoint) — a user can delete their only verified residency document after the case is submitted; no retroactive readiness re-check occurs — Fix: allow deletion only when case status is `draft` or `needs_more_info`; reject with 409 if case is `submitted` or later — Effort: S
- [MED] Applicant KYC wizard missing residency step — `frontend/src/pages/kyc/KycWizardPage.tsx` — enhanced/high_risk users see no upload step for proof of residency in the wizard, leaving them unable to discover the requirement — Fix: conditionally render "Proof of Residency" step when `intake_profile in ("enhanced","high_risk")` — Effort: S
- [LOW] International address normalization incomplete — `app/services/kyc_residency.py` (`_US_STATES` map, lines 98–113) — only US state abbreviations are covered; Canadian provinces, UK counties produce `mismatch` for correct addresses — Fix: add `_CA_PROVINCES` and `_UK_COUNTIES` maps as follow-up; document as known limitation for MVP — Effort: M
- [LOW] Extracted address PII stored in plaintext — `kyc_residency_documents` DDB table — address fields are PII not encrypted at rest; KYC-023 field-level encryption is not yet extended to this table — Fix: extend KYC-023 encryption to `kyc_residency_documents` as follow-up — Effort: M
