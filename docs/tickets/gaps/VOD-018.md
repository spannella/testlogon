- [HIGH] `static/ads/` directory and placeholder creative files do not exist — `app/services/ad_placement.py:30,37,44` — dev-mode ad slot URLs reference `/static/ads/placeholder_preroll.mp4`, `placeholder_midroll.mp4`, and `placeholder_overlay.png` but the directory `app/static/ads/` is absent; player requests for these assets return 404, breaking the full ad flow in dev/test — Fix: create `app/static/ads/` and add placeholder files (e.g., generated with ffmpeg for the MP4s and a solid-color PNG) — Effort: S
- [HIGH] Ad impression recording has no deduplication — `app/services/ad_placement.py:253-271` — the ticket security section (6.2) calls out impression deduplication as required; a single authenticated user can call `POST /{video_id}/ad-impression` with `event_type=complete` unlimited times per video per day, inflating creator ad revenue without bound — Fix: add a DDB conditional write using key `AD_IMP#{date}#USER#{user_id}#VIDEO#{video_id}#SLOT#{slot_index}` with `attribute_not_exists(pk)` to cap one complete event per slot per user per day — Effort: M
- [MED] `VideoDetailPage` (`/gallery/:videoId`) does not integrate ad playback — `frontend/src/pages/gallery/VideoDetailPage.tsx` — the `ads_enabled` field returned by the access check endpoint is not consumed; `AdOverlay` and `VodAdSupportedPage` exist but are on a separate route (`/vod/:videoId/free-with-ads`) unreachable from the gallery detail page — Fix: read `access.ads_enabled` on `VideoDetailPage`, fetch ad placement slots via `getAdPlacements`, and render `AdOverlay` inline with the player — Effort: M
- [MED] `ad_impression_count` is incremented for all event types (impression, complete, skip) not just impressions — `app/services/ad_placement.py:274-281` — the counter is mis-named; it should track completed views or be split into separate counters — Fix: only increment `ad_impression_count` on `event_type == "impression"` and track `ad_complete_count` / `ad_skip_count` separately, or rename the field to `ad_event_count` — Effort: S
- [LOW] ADS-014 creative validation is not applied to the dev placeholder creatives or to any future creative upload path — `app/services/ad_placement.py` — the ticket cross-references ADS-014 for MIME type checks and size limits; no `validate_creative_upload()` function exists in the service — Fix: add the `validate_creative_upload()` stub from the ticket design and wire it to any future creative ingest endpoint — Effort: M
