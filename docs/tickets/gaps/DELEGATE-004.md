- [HIGH] banned viewer can still send chat messages — `app/services/broadcast_chat_store.py:158` — send_chat_message calls _enforce_chat_mute but never calls is_viewer_banned; ban_viewer writes BAN# item that is never consulted on message send — Fix: add _enforce_chat_ban(session_id, user_id) querying T.broadcast_moderation BAN# item before _enforce_chat_mute — Effort: S
- [MED] ModeratorPanel not mounted in BroadcastPage — `frontend/src/pages/broadcast/BroadcastPage.tsx` (no import of ModeratorPanel) — moderator panel component exists but is never rendered; all moderation UI actions require direct API calls — Fix: import and render ModeratorPanel when managingCreatorId is set and user has broadcast_moderate delegation — Effort: M
- [MED] mod:* SSE events not handled in frontend — `frontend/src/pages/broadcast/BroadcastChat.tsx` (no mod:pin/delete/mute/ban/announcement handlers) — pin indicators, "message removed" notices, and mute notifications never shown to viewers or moderator — Fix: add mod:* SSE event handlers in broadcast SSE consumer to update local state — Effort: M
- [LOW] BroadcastControlPanel missing — `frontend/src/pages/broadcast/` (no BroadcastControlPanel.tsx) — delegates with broadcast_control cannot start/stop/schedule broadcasts from the UI — Fix: implement BroadcastControlPanel, ModeratorActionBar, ModeratorUserMenu components — Effort: L
- [LOW] no broadcast_delegation_enabled feature flag — `app/core/settings.py:~2113` — broadcast delegation router is always registered with no kill switch — Fix: add broadcast_delegation_enabled flag and gate delegate_broadcast_router registration in main.py — Effort: S
