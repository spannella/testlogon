- [HIGH] No `post_shared` notification emitted to original author on repost — `app/routers/newsfeed.py:6108-6164` — the repost handler writes the repost entity, increments `repost_count`, fans out to followers, but never calls `put_notification()` or `emit_social_alert()` to notify the original post author that their content was reposted; the `post_shared` alert type is registered in `ALERT_EVENT_TYPES` (alerts.py:143) but never triggered — Fix: add `put_notification(recipient_user_id=author_id, notif_type="post_shared", payload={"post_id": post_id, "repost_id": repost_id, "from_user_id": user_id})` after the repost entity is written — Effort: S
- [MED] Repost fan-out silently discards followers beyond `MAX_SYNC_FOLLOWERS=500` — `app/routers/newsfeed.py:6140-6162` uses `_get_all_follower_ids(user_id)` from `app/services/newsfeed_fanout.py:21` which is hard-capped at 500 followers; a creator with 10K followers reposts content and only 500 of their followers see the repost in their feeds; no async overflow queue is implemented — Fix: same async queue solution as SOC-002 ticket §3.3 and SOC-002 in `newsfeed_fanout.py` — Effort: M
- [MED] Repost attribution skipped when `author_filter` is active — `app/routers/newsfeed.py:5109` — `repost_meta = _repost_meta_by_post.get(post_id) if not author_filter else None` disables repost attribution entirely when browsing a user's posts by author (e.g., `GET /feed?author={id}`); reposts shown on a creator's profile page (`StorefrontPostsFeed`) have no "X reposted" label, giving the impression the creator was the original author — Fix: remove the `if not author_filter` guard; `_repost_meta_by_post` is populated per-batch regardless of filter — Effort: S
- [LOW] Undo-repost fan-out cleanup queries with `FilterExpression` on unbounded partition — `app/routers/newsfeed.py:6196-6218` — undoing a repost runs `query(pk=f"POST#{post_id}", FilterExpression="repost_id = :rid")` with `Limit=1000`; for a viral post this partition may hold thousands of FEEDREF items; DDB reads up to 1MB per page before applying the filter, so cleanup may silently miss fan-out items beyond the first page's raw results — Fix: store the fan-out FEEDREFs under a dedicated PK keyed by `REPOST#{repost_id}` so cleanup can be a targeted `query` with no filter — Effort: M
