- [HIGH] `display_name` always falls back to `user_sub` for question submitters — `app/routers/live_qa.py:73` — `ctx.get("display_name")` is always `None` because `require_ui_session` returns only `{user_sub, session_id, role, ip}`; every submitted question shows a raw UUID-style sub as the submitter name in the host queue — Fix: fetch the user's profile display name from the profiles/account service using `ctx["user_sub"]` before calling `submit_question`, or add `display_name` to the session context — Effort: S
- [MED] `get_stats` does a full unbounded table query without pagination — `app/services/live_qa.py:402` — the query uses `KeyConditionExpression=Key("session_id").eq(session_id)` with no `Limit` and no `LastEvaluatedKey` loop; a busy session with thousands of questions and a config row will read up to DDB's 1 MB per call and silently under-count — Fix: paginate the query using a `LastEvaluatedKey` loop to accumulate all items — Effort: S
- [MED] `list_questions` applies `FilterExpression=Attr("deleted").ne(True)` after a hard `Limit` — `app/services/live_qa.py:376` — DDB evaluates `Limit` before `FilterExpression`; if many deleted questions exist for a status, the returned page may have fewer than `limit` items even when more non-deleted questions exist, and `has_more` in the router response will incorrectly report `False` — Fix: loop on `LastEvaluatedKey` until `limit` non-deleted items have been collected — Effort: S
- [MED] No frontend components or UI integration for Live Q&A — `frontend/src/pages/` — no `LiveQA*` or `BroadcastQA*` page/component exists in the frontend tree; the broadcaster dashboard and viewer player have no Q&A panel — Fix: implement `LiveQAPanel`, `QuestionQueue`, `FeaturedQuestionOverlay` components as described in ticket section 4 — Effort: L
- [LOW] Rate-limit buckets (`_LIVE_QA_RATE_BUCKETS`) are in-process memory and not reset between tests or deploys — `app/services/live_qa.py:45` — in multi-worker deployments (even though dev uses 1 worker) each worker has an independent bucket; in tests, stale rate state from one test can bleed into the next — Fix: prefix the bucket key with process/worker ID in tests, or use DDB-backed atomic counters for prod — Effort: S
- [LOW] `upvote_question` uses `ConditionExpression="NOT contains(voters, :uid_str)"` with a StringSet but checks a String placeholder — `app/services/live_qa.py:318` — `voters` is a DynamoDB StringSet (SS); `contains(voters, :uid_str)` requires `:uid_str` to be a plain String value, which it is (`:uid_str: user_id`); however if `voters` attribute does not yet exist the condition `NOT contains(...)` evaluates to `True` which is correct, but the pattern is fragile and undocumented — Fix: add comment explaining the SS contains check; no code change needed but should be tested explicitly — Effort: S
