- [MED] No `GET /mock/ups/track/{tracking_number}` endpoint — `app/routers/ups.py` has mock label/quote/validate but no mock tracking-status lookup; mock lives at `/mock/carrier-tracking/{carrier}/{tracking_number}` (carrier_tracking_mock.py:86) instead of the UPS-specific path specified in the ticket — Effort: S
- [HIGH] UPS webhook has no timestamp-tolerance or replay-protection check — `app/routers/ups.py:91` processes any validly-signed request without a nonce/timestamp window, enabling replay attacks that could mark orders delivered multiple times (SEC-002 cross-ref) — Fix: store processed event IDs in DDB and reject duplicates; add X-UPS-Timestamp window check — Effort: M
- [MED] `verify_tracking_webhook_signature` silently bypasses HMAC when `UPS_WEBHOOK_SECRET` is empty — `app/services/ups.py:96-97` returns `True` if secret unset; in prod with a missing env var any caller can forge delivery webhooks — Fix: in non-dev mode raise `HTTPException(500, "ups_webhook_secret not configured")` on empty secret (aligns with SEC-002 fail-closed requirement) — Effort: S
- [MED] `app/services/ups.py` has no `track_package()` function — background poller (`carrier_tracking_poller.py`) reads from the in-memory mock state, not a real UPS Tracking API call; production tracking polling will silently return nothing for non-seeded tracking numbers — Fix: implement `track_package(tracking_number)` calling `_ups_post("api/track/v1/details/{tn}", {})` as specified in ticket §4.2 — Effort: M
- [LOW] Fan-out fanout uses individual `put_item` per follower rather than `batch_writer` — `app/services/newsfeed_fanout.py:71` makes N sequential puts; at >25 followers DynamoDB `batch_write_item` would be ~25x faster — Fix: replace the loop with a `tbl.batch_writer()` context manager — Effort: S
- [LOW] Async fan-out queue for >500-follower posts not implemented — `newsfeed_fanout.py` fan_out_post_to_followers cap at 500 followers with no background dispatch for the remainder; silent data loss for popular authors at scale — Effort: M
