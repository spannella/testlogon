- [HIGH] top_content view attribution bug — `app/services/creator_analytics.py:432` — get_top_content attributes entire day's total_views to every content ID in top_content_ids list; a creator with 500 views across 3 videos shows 500 attributed to each video; metric is incorrect and misleading — Fix: store per-content view counts as a DDB map in rollup rows, or use _resolve_content_details live view counts from T.video_metadata (Option B already available via ANALYTICS-002) — Effort: M
- [MED] app_single_table accessed via env var workaround — `app/services/creator_analytics.py:400` — ddb.Table(os.environ.get("APP_TABLE", "app_single_table")) bypasses the standard T.* handle pattern; if APP_TABLE env var is unset in prod the fallback string must match the real table name — Fix: wire T.app_single as a named handle in app/core/tables.py and replace the env var call — Effort: S
- [MED] no backfill mechanism for historical data — `app/services/creator_analytics.py:1` — rollup job processes only analytics_rollup_lookback_days (default 3) per run; data older than 3 days is never populated; new creators see empty analytics until window catches up — Fix: add ?lookback_days=N parameter to POST /ui/analytics/refresh capped at 90 days with separate rate limit — Effort: S
- [LOW] audience_countries / audience_devices write path unverified — `app/services/creator_analytics.py:478` — get_audience merges these fields from rollup rows but the source write path (which service stores GeoIP / device data and in which table) is undocumented; fields may always be empty in practice — Fix: trace and document or implement the write path before relying on audience analytics in prod — Effort: S
