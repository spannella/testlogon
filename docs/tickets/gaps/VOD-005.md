- [CRIT] VideoMetadata.hls_manifest_url never populated after transcoding — `app/services/transcode_worker.py:195` — `complete_job_with_outputs` stores playback URL only on the TranscodeJobs record; `VideoMetadataModel.hls_manifest_url` stays None; `_try_issue_playback_token` guards on this field and returns (None, None); published videos have no watchable URL — Fix: after `complete_job_with_outputs`, fetch the VideoMetadata record and update `hls_manifest_url`, `thumbnail_url`, and renditions via `put_item` — Effort: S
- [MED] `upload_transcode_outputs` is synchronous blocking the asyncio event loop — `app/services/vod_s3_uploader.py:100` — ~800 synchronous boto3 S3 upload calls for a 1080p/4-rendition video block the uvicorn event loop; API cannot serve requests during upload phase — Fix: wrap the call in `await asyncio.get_event_loop().run_in_executor(None, lambda: upload_transcode_outputs(...))` — Effort: S
- [MED] Thumbnail extraction silently skipped if source file not downloaded — `app/services/transcode_worker.py:145` — `source_candidates = list(scratch_dir.glob("source*"))` returns empty when the VOD-004 s3:// gap means no local file exists; zero thumbnails produced — Fix: prerequisite on VOD-004 source download fix — Effort: S
- [LOW] Default CloudFront signing secret is hardcoded — `app/core/settings.py:1417` — `vod_cloudfront_signing_secret` defaults to `"dev-vod-cf-secret"`; prod deployments that don't override this value have trivially forgeable playback tokens — Fix: require non-default value in prod startup check; alert if secret equals default — Effort: S
