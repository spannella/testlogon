- [HIGH] Production comparison raises `KycFacialComparisonError("comparison_service_error")` unconditionally — `app/services/kyc_facial_comparison.py:231-232` — Any deployment with `dev_mode=False` will return 500 on every comparison attempt; no AWS Rekognition or alternative provider is wired — Fix: implement `_production_compare()` to call `boto3.client("rekognition").compare_faces()` with the S3 object references, or gate with a feature flag that routes to mock until production provider is configured — Effort: L
- [MED] Admin facial comparison view (`/admin/cases/{case_id}/face-comparison`) has no dedicated frontend route in `App.tsx` — `frontend/src/App.tsx` — The `KycFacialComparisonReview.tsx` component exists but there is no `<Route>` for it; the component is only surfaced if the admin case detail page embeds it — Fix: add `<Route path="admin/kyc/cases/:caseId/face-comparison" element={<KycFacialComparisonReview />} />` in App.tsx — Effort: S
- [MED] `_node_metadata` silently falls back to empty `file_size=0` / empty `mime_type` when the file manager node is not found — `app/services/kyc_facial_comparison.py:91-127` — Anti-spoof `file_size` check always fails for E2E-seeded cases (file_size=0 < 50 000), making every comparison result `fail` unless filename contains "match"; masked by `dev_mode` but will affect integration tests that seed real files — Fix: propagate `file_size` and `mime_type` from the case file reference dict when the file manager lookup misses, and document the minimum required fields on the file reference — Effort: S
- [LOW] `FACE_MATCH` SK uses `{ts:013d}#{comparison_id}` but `_get_comparisons` queries only `begins_with("FACE_MATCH#")` without a dedicated sort; sort is reapplied client-side — `app/services/kyc_facial_comparison.py:249-259` — Redundant in-memory sort after a DynamoDB query that already returns items in SK order; not incorrect but wasteful — Fix: remove the redundant `items.sort(...)` since `ScanIndexForward=False` already returns newest-SK-first — Effort: S
- [LOW] `attempt_number > MAX_SELFIE_ATTEMPTS` check counts stored items but does not handle concurrent submissions — `app/services/kyc_facial_comparison.py:328-331` — Two simultaneous compare-face calls for the same case could both pass the attempt check and both write, producing 4 records for a 3-attempt limit — Fix: use a DynamoDB conditional expression (`ConditionExpression=Attr("sk").not_exists()` or a counter attribute) to enforce the cap atomically — Effort: M
