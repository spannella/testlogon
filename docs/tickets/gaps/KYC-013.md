- [MED] ID-upload step fires face comparison inside the selfie step only; no `POST /{case_id}/scan-document` call is made after ID upload — `frontend/src/pages/kyc/KycWizardPage.tsx` (IdUploadStep) — Ticket spec requires triggering KYC-010 document scan immediately after `id_front` attach; the wizard uploads and attaches but never calls the scan endpoint, so MRZ extraction is skipped — Fix: after successful `attachKycFile` for `id_front`, call `client.post(\`/v1/kyc/cases/${caseId}/scan-document\`, {file_node_id})` and display extraction results — Effort: S
- [MED] `GET /v1/kyc/cases/estimated-wait` exposed only to authenticated sessions but the status page fetches it unconditionally before any case exists — `frontend/src/pages/kyc/KycStatusPage.tsx:14` — If the user is logged in but has no approved case and no queue position, the backend still returns a response, but there is no user-facing message distinguishing "you are not in the queue" from a genuine wait-time estimate; users with no submission see misleading wait text — Fix: return `null` or `status: "not_submitted"` from the backend when the user has no submitted case, and hide the wait card in the frontend — Effort: S
- [LOW] `KycHelp` FAQ is defined as a local inline component in `KycWizardPage.tsx`; ticket spec called for a standalone `KycHelp.tsx` reusable from both the wizard and other pages — `frontend/src/pages/kyc/KycWizardPage.tsx:714-759` — Minor: the component cannot be imported by other KYC pages without duplicating code — Fix: extract to `frontend/src/pages/kyc/KycHelp.tsx` and re-export — Effort: S
- [LOW] No "draft" case is auto-created on first wizard visit; user must click "Start New Application" button before any step is enabled — `frontend/src/pages/kyc/KycWizardPage.tsx:108-118` — The ticket spec says the wizard creates a draft automatically on mount if none exists. Currently the create mutation fires on button click, leaving step 1 unusable until clicked — Fix: fire `createKycCase` mutation in a `useEffect` when `!caseId && !createMut.isPending` — Effort: S
