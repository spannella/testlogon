- [UNBUILT] feature not implemented — see writeups/KYC-007.md
- [HIGH] `app/services/kyc_signature_templates.py` does not exist — five hard-coded KYC consent/declaration templates (terms_of_service, aml_declaration, pep_declaration, tax_compliance, data_consent) with auto-populate and version tracking are absent — Fix: create service with `KYC_TEMPLATE_TYPES` dict, `create_packets_for_case()`, `check_version_migration()`, and `auto_populate_fields()` calling `signature_packet_store.upsert_packet_field()` at line 133 — Effort: L
- [HIGH] Five template/witness router endpoints missing — `app/routers/kyc_cases.py` — `GET /templates`, `POST /{case_id}/signature-templates/create-packets`, `GET /{case_id}/signature-templates/status`, `POST /admin/cases/{case_id}/add-witness`, `GET /{case_id}/signature-templates/version-check` do not exist — Fix: add all five endpoints after creating the service — Effort: M
- [HIGH] `notary_stamp` field type not valid — `app/services/signature_packet_store.py` — only `text`, `signature`, and `date` field types are accepted; `notary_stamp` with `stamp_image_ref`, `stamp_number`, `stamp_expiry` is needed for high-risk cases — Fix: extend valid field-type set in `signature_packet_store.py` to include `notary_stamp`; add S3-backed stamp image ref (moto in dev) — Effort: S
- [HIGH] `case["signature"]` stores single packet; template packets require a list — `app/routers/kyc_cases.py:1206` (`create_or_link_signature_packet`) — existing structure holds one `packet_id`; template flow needs `signature.template_packets` list to avoid breaking existing single-packet check at `_signature_status_for_case()` line 184 — Fix: extend case META to store `signature.template_packets = [{template_type, packet_id, version}]` using `list_append` — Effort: S
- [MED] Frontend ConsentDeclarationsSection missing — `frontend/src/pages/kyc/KycCaseForm.tsx` — no user-facing component to initiate template packet creation or track per-template signature status — Fix: add `ConsentDeclarationsSection` to KycCaseForm, conditional on `KYC_TEMPLATE_SIGNING_ENABLED` flag — Effort: M
- [MED] Admin SignaturesDeclarationsTab missing — `frontend/src/pages/admin/KycCaseDetailPage.tsx` (depends on KYC-001) — reviewer cannot see template packet status, version, or witness info in the case detail — Fix: add `SignaturesDeclarationsTab` to KycCaseDetailPage — Effort: S
- [LOW] Stale auto-populated profile values — `app/services/kyc_signature_templates.py` (to be created) — profile data is captured at packet creation; address updated after creation is stale in the packet; documented as acceptable — Fix: note in UI that pre-filled values reflect profile at packet creation time — Effort: S
