- [HIGH] global ad kill switch absent — `app/services/admin_ad_platform.py` — no emergency path to stop all ad serving platform-wide; admins must reject campaigns one by one — Fix: implement toggle_kill_switch service + POST /ui/admin/ad-platform/kill-switch endpoint + serve_ad check at startup — Effort: S
- [HIGH] account suspension does not cascade to pause active campaigns — `app/services/admin_ad_platform.py:moderate_account` — suspended advertiser's active campaigns continue serving ads — Fix: in moderate_account for action=suspend, update all active campaigns for the account to status=paused — Effort: S
- [HIGH] frontend AdminAdDashboard absent — `frontend/src/pages/admin/ads/` — no AdminAdDashboard.tsx or adminAds.ts API endpoints; admins have no UI access to the ad management backend — Fix: create AdminAdDashboard.tsx and frontend/src/api/endpoints/adminAds.ts — Effort: L
- [MED] creative suspend conflicts with rejected status — `app/services/admin_ad_platform.py:moderate_creative:244` — moderate_creative(action=suspend) writes status=rejected; suspension (temporary) conflated with rejection (final) — Fix: add "suspended" as a valid creative status and write it on suspend action — Effort: S
- [MED] house ads not manageable — `app/services/ad_serving.py:HOUSE_AD:30` — house creative is a hardcoded static dict; platform operators cannot update it or add rotation — Fix: implement house_ads.py service + ad_house_ads DDB table; fall back to static dict when table is empty — Effort: M
- [MED] AdminScope.AD_MANAGEMENT not added to CANONICAL_ADMIN_SCOPES — `app/auth/roles.py` — all admin-role users can access ad-platform endpoints rather than only those with ad_management scope — Fix: add AD_MANAGEMENT scope and gate non-destructive endpoints on require_admin_scope(AD_MANAGEMENT) — Effort: S
- [LOW] cross-user listing functions use full table scans — `app/services/admin_ad_platform.py:77–136` — list_all_advertiser_accounts/campaigns/creatives issue full DDB scans; expensive at scale — Fix: add GSI-based pagination for production-scale listing before GA — Effort: M
