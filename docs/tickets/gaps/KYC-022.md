- [MED] No rate limiting on `POST /v1/kyc/cases/{case_id}/eid/start` — `app/routers/kyc_eidv.py:79` — the ticket (§7.3) specifies 5 sessions per user per 15 minutes to prevent session flooding; the router calls `create_verification_session` with no preceding `rate_limit_or_429` check; the existing rate-limit infrastructure (`app/services/rate_limit.py`) is used throughout the platform but absent here — Fix: add `rate_limit_or_429(user_sub=user.sub, factor="kyc_eid:start")` at the top of `start_eid_verification` — Effort: S
- [MED] Admin case detail page (`KycCaseDetailPage.tsx`) does not display eID verification results — `frontend/src/pages/admin/KycCaseDetailPage.tsx` (685 lines, 0 hits for "eid") — the ticket (§3.11) requires admins to see eID scheme, assertion ID, verified fields, and discrepancies on the case detail view; `EidResultCard` exists at `frontend/src/components/shared/EidVerificationPanel.tsx:131` but is never imported/rendered in the admin page — Fix: import `EidResultCard` and conditionally render it in `KycCaseDetailPage` when `case.eid_verification` is set (ticket §3.11 component tree) — Effort: S
- [LOW] `EidResultCard` and `EidSchemeSelector` are unexported colocated functions inside `EidVerificationPanel.tsx` rather than standalone components — `frontend/src/components/shared/EidVerificationPanel.tsx:74,131` — the ticket (§3.11) specifies them as separate named components; colocation means the admin page cannot import only `EidResultCard` without pulling in the full panel logic — Fix: either export both functions from the same file (already partially done — `EidResultCard` is exported) or extract to dedicated component files as the ticket specifies — Effort: S
