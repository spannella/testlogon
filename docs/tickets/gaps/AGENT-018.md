- [HIGH] no automatic cost collection schedule — `app/main.py` — POST /collect triggers one manual collection but no asyncio background task is registered in app/main.py even when accountant_agent_execute_commands=True — Fix: add hourly asyncio background task calling run_cost_collection_if_enabled gated by S.accountant_agent_execute_commands — Effort: S
- [MED] auto-pause worker integration incomplete — `app/services/agent_accountant.py` — check_budgets sets auto_action_taken field but no call to a worker-pause API exists; auto_pause_on_exceed flag is a no-op — Fix: wire pause_worker(worker_id=scope_ref) once AGENT-002 pause function is available; guard with silent no-op if function absent — Effort: S
- [MED] get_cost_trends N+1 DDB queries — `app/services/agent_accountant.py` — trends function queries each date individually over a 90-day window; up to 90 DDB calls per request — Fix: replace with single GSI1 scan across date range — Effort: M
- [LOW] OptimizationRecommendationOut and AccountantConfigOut models completeness unverified — `app/models.py` — writeup flags these two response models for completeness review before merge — Fix: verify all fields serialised by get_optimization_recommendations and get_accountant_config are present in respective Pydantic models — Effort: S
