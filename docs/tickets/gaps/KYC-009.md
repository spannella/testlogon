- [CRIT] Users table key schema mismatch — `app/services/kyc_tiers.py` (`get_user_kyc_tier()`, `upgrade_tier()`) — service accesses `T.users` with `Key={"user_sub": user_sub}` while `kyc_risk_scoring._compute_country_risk()` uses `Key={"pk": f"USER#{user_sub}", "sk": "PROFILE"}`; if the table uses composite PK+SK, tier service reads silently return empty items and every user defaults to Tier 0 — Fix: audit `scripts/local-ddb-init.py` for the users table key schema; correct all `GetItem`/`UpdateItem` calls in `kyc_tiers.py` to use the canonical key format — Effort: S
- [HIGH] `ByKycTier` GSI not declared in DDB init — `scripts/local-ddb-init.py` — `list_users_by_tier()` queries `IndexName="ByKycTier"` on `T.users`; without the GSI definition including `attr_types={"kyc_tier":"N","kyc_tier_updated_at":"N"}`, the query fails with `ValidationException` at runtime — Fix: add the GSI and `attr_types` to the users `TableDef` — Effort: S
- [HIGH] Feature gating not applied to endpoints — multiple routers (messaging, billing, newsfeed, etc.) — `require_kyc_tier` dependency exists at `app/auth/deps.py:311` but is not wired to the Tier 1+ messaging, Tier 2+ billing, Tier 3+ broadcast, or Tier 4 API key endpoints called out in the ticket spec — Fix: apply `Depends(require_kyc_tier(N))` to each endpoint in priority order per Phase 2–4 rollout plan — Effort: M
- [HIGH] Auto-upgrade triggers not wired — `app/routers/register.py`, `app/routers/mfa_devices.py`, `app/services/kyc_cases.py:534` — `auto_evaluate_tier()` exists but is not called after email verification, phone MFA setup, or KYC case approval — Fix: add `auto_evaluate_tier(user_sub)` in try/except at each of the three trigger points — Effort: S
- [MED] Tier 4 requirements not data-backed — `app/services/kyc_tiers.py` (`check_tier_requirements()`) — `business_kyc_approved` and `api_access_approved` conditions are never added to `met` set; Tier 4 is admin-override-only with no automated path — Fix: implement `business_kyc_approved` source when KYC-015 merges; document as blocked on KYC-015 — Effort: L
- [MED] Automatic tier downgrade absent — `app/services/kyc_tiers.py` (`auto_evaluate_tier()`) — tier downgrades on KYC case expiry are not implemented; only admin override can lower a tier; expired verifications leave users at elevated tiers indefinitely — Fix: hook `auto_evaluate_tier()` into the KYC case expiry path (KYC-016) to re-evaluate downward — Effort: M
- [MED] Existing E2E tests break when gating enabled — `frontend/e2e/*.spec.ts` (all messaging tests) — enabling `KYC_TIER_GATING_ENABLED=true` with Alice/Bob at Tier 0 blocks all messaging E2E tests; existing sessions have no tier set — Fix: set `KYC_TIER_GATING_ENABLED=false` in `.env.local` for test environments, or pre-set Alice/Bob to Tier 1+ in `e2e_session_setup.py` — Effort: S
- [LOW] Race condition in `auto_evaluate_tier()` — `app/services/kyc_tiers.py:217` — two concurrent evaluation calls for the same user both read `current_tier=0`, both call `upgrade_tier()`, writing a duplicate history entry; tier value is correct, history is polluted — Fix: add a conditional `ConditionExpression` on `upgrade_tier` UpdateItem to reject if tier already changed — Effort: S
