- [HIGH] repo_branch shell injection latent — `app/services/agent_architect.py:1153` — git clone command f-strings branch from config; validate_architect_config does not validate repo_branch at all — Fix: add _sanitize_branch() in _normalize_config and validate repo_branch in validate_architect_config — Effort: S
- [HIGH] analysis prompt command injection latent — `app/services/agent_architect.py:1144` — analysis_prompt[:160] f-stringed into double-quoted claude invocation; user-controlled ticket subject with " or $() executes when ARCHITECT_EXECUTE_COMMANDS=1 — Fix: store as argv field, exec via subprocess(argv, shell=False) — Effort: M
- [HIGH] absolute path not rejected in scan_paths — `app/services/agent_architect.py:204` — validate_architect_config rejects ".." but not absolute paths like /etc/passwd; scan_paths=["/etc/passwd"] passes validation — Fix: add str(path).startswith("/") check to reject absolute paths — Effort: S
- [MED] git+ transport not blocked in repo_url — `app/services/agent_architect.py` — validate_architect_config checks https:// or git@ prefix only; git+https://evil.com passes and enables advanced git transport attacks — Fix: explicitly reject git+, ext::, file:// prefixes — Effort: S
- [LOW] float complexity estimation causes DynamoDB Decimal noise — `app/services/agent_architect.py:156` — _DEFAULT_COMPLEXITY_ESTIMATION uses Python floats; Decimal("4.1") may show as Decimal('4.100000000000001') — Fix: replace floats with integers or Decimal literals — Effort: S
- [MED] dependency cycle detection not always enforced — `app/services/agent_architect.py:950` — validate_dependency_graph uses DFS to find cycles but if mock output bypasses validation the Coder Agent queue loops indefinitely — Fix: always call validate_dependency_graph in build_dependency_graph regardless of input source — Effort: S
