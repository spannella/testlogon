- [MED] GifPicker and StickerPicker not integrated into CreatePost — `frontend/src/pages/feed/CreatePost.tsx` — `GifPicker.tsx` and `StickerPicker.tsx` exist under `frontend/src/components/shared/` but are never imported or rendered in CreatePost; no GIF or Sticker toolbar buttons appear — Fix: import both pickers, add "GIF" and sticker toolbar buttons, wire `handleGifSelect`/`handleStickerSelect` handlers that set `imageUrls` and track content type — Effort: S
- [MED] character count with limit indicator missing — `frontend/src/pages/feed/CreatePost.tsx` — there is no per-character counter UI and no client-side 5 000-character cap enforcement; the Publish button is not disabled when body exceeds the limit — Fix: add `MAX_CHARS=5000` constant, render `{body.length} / {MAX_CHARS}` counter below the textarea with yellow/red colour thresholds, and disable Publish when over limit — Effort: S
- [MED] preview mode missing — `frontend/src/pages/feed/CreatePost.tsx` — the ticket requires a "Preview" toggle that renders a read-only `PostCard` preview of the draft before publishing; no `previewMode` state or preview rendering path exists — Fix: add `previewMode` boolean state, a Preview/Edit toggle button, and render `<PostCard post={…} isPreview={true} />` (with a new `isPreview` prop on PostCard that disables all interactive controls) — Effort: M
- [LOW] localStorage draft auto-save uses server-side draft API instead of simple localStorage key — `frontend/src/pages/feed/CreatePost.tsx:43` — the current draft mechanism migrates legacy `localStorage` keys to a server-side draft API (create/list/update/publish draft endpoints). This is more complex than the ticket spec (simple `localStorage` save). The migration path is functional but stale legacy key names are still referenced; future readers may be confused about dual mechanisms — Fix: document in code that legacy local drafts are one-time migrated to server drafts; remove LEGACY_DRAFT_KEYS list after migration window closes — Effort: S
