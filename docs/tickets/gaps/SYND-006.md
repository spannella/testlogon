# SYND-006 gaps — Syndicate Advertising

- [LOW] CampaignAnalyticsChart not extracted as standalone component — analytics rendering is inlined in `frontend/src/pages/syndicates/SyndicateAdvertisingDetailPage.tsx:37-120` rather than the planned `CampaignAnalyticsChart.tsx`; chart is a raw inline table/list with no bar/line chart — Fix: extract to `CampaignAnalyticsChart.tsx` with a recharts bar chart as the ticket design specifies — Effort: S
- [LOW] No rate limiting on campaign mutation endpoints — `app/routers/syndicate_advertising.py` create/status/add-budget endpoints have no rate_limit decorator; ticket §6.4 requires max 5 campaigns/day, 20 status changes/day, 10 budget top-ups/day — Fix: add `rate_limit_check` calls to each mutation endpoint matching the per-endpoint caps — Effort: S
- [LOW] CTA URL not validated as platform-internal URL — `_validate_creative` in `app/services/syndicate_advertising.py` performs only length-cap validation on `cta_url`; ticket §6.3 notes external URLs should be restricted in future — Fix: add domain-allowlist check or at minimum a URL format validator using `urllib.parse` — Effort: S
- [LOW] Missing unit test file — `tests/test_syndicate_ads.py` does not exist; ticket specifies 8 pytest unit tests — Fix: create the file with moto-mocked DynamoDB and 8 tests covering create, treasury debit, impression recording, lifecycle, non-admin 403, and stats aggregation — Effort: M
