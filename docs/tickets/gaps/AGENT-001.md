- [MED] provider error body leakage — `app/services/llm_provider_keys.py:234` — first 200 bytes of provider error response returned verbatim to client, leaking account metadata — Fix: return generic "Authentication failed" for 401/403, "Provider returned HTTP N" for 5xx — Effort: S
- [HIGH] budget bypass via race condition — `app/services/llm_provider_keys.py:310` — two-step ADD + conditional SET allows concurrent call to read status="active" between writes — Fix: replace with single atomic UpdateItem or DDB transaction — Effort: M
- [HIGH] monthly usage reset not automated — `app/services/llm_provider_keys.py:310` — usage_reset_at stored but no background task zeros current_month_usage_cents at month boundary — Fix: add daily startup task scanning for expired usage_reset_at and resetting — Effort: S
- [CRIT] custom base_url SSRF — `app/services/llm_provider_keys.py:173` — provider="custom" allows user-supplied base_url; test_key issues GET to that URL with no SSRF guard — Fix: validate https:// scheme only, reject RFC-1918/link-local/loopback ranges in field_validator — Effort: S
- [MED] key assignment worker ownership not verified — `app/services/llm_provider_keys.py:360` — assign_key_to_worker appends worker_id without verifying the worker belongs to the same user — Fix: call get_worker(user_id, worker_id) and 404 if not found — Effort: S
- [LOW] ciphertext field not excluded at model level — `app/services/llm_provider_keys.py:408` — encrypted_api_key stripped by _safe_out only; future code path omitting _safe_out would leak ciphertext — Fix: add encrypted_api_key to LlmKeyOut.model_config excludes — Effort: S
