- [HIGH] `thumbnail_url` / `hls_manifest_url` not populated in dev — `app/routers/admin_video_review.py:155-156` — fields are passed through from `VideoMetadataModel` but the ABR pipeline only writes these when a real transcode completes; in the local dev environment (moto S3, no real FFmpeg transcode), both fields are `null` for all pending-review videos, making the review UI a blank preview pane — Fix: in dev mode, synthesize a mock `hls_manifest_url` from S3 mock path (same pattern used in `_message_out_from_item` for file messages) — Effort: S
- [MED] SEC-025 cross-ref: batch-review endpoint unguarded against admin flood — `app/routers/admin_video_review.py:315` — `POST /batch-review` accepts up to 25 decisions per call with no per-admin rate limit; a compromised content_moderation admin account could approve thousands of videos per minute by scripting repeated calls — Fix: add per-admin rate limit (e.g., 100 decisions/minute) via `app/core/rate_limit.py` on the batch endpoint — Effort: S
- [MED] batch-review executes serially — `app/routers/admin_video_review.py:325` — the `for decision in body.decisions` loop calls `approve_video`/`reject_video` sequentially; for 25 decisions that is 25 × ~50ms = ~1.25s p50 latency; ticket section 5.4 noted `asyncio.gather()` as a mitigation — Fix: wrap individual decisions in `asyncio.gather()` with exception catching per item — Effort: S
- [MED] `feature_flag` / `VIDEO_REVIEW_QUEUE_ENABLED` setting absent — `app/core/settings.py:1` — ticket section 7.2 specifies a feature flag for phased rollout; the router is live for all admins immediately with no dark-launch path; the flag is also referenced in section 7.3 rollback steps — Fix: add `video_review_queue_enabled` to Settings; gate router endpoints; gate the sidebar nav link — Effort: S
- [LOW] owner profile enrichment uses N separate `get_item` calls — `app/routers/admin_video_review.py:1` — queue list enriches `owner_display_name` / `owner_profile_photo_url` with one `T.profile.get_item()` per video; for 25-item page that is 25 round-trips; ticket section 5.3 calls for `BatchGetItem` — Fix: batch profile lookups with `T.profile.meta.client.batch_get_item` — Effort: S
- [LOW] `count_pending_review` called on every queue list request — `app/services/video_review.py:96` — total count query runs on every paginated GET even when the caller only needs the current page items; adds a full-table COUNT scan on each request — Fix: make count optional via a `include_total=false` query param or cache count for 30s — Effort: S
