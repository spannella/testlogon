- [LOW] Version selector not rendered when only one published version exists — `frontend/src/pages/questionnaires/QuestionnaireAnalyticsTab.tsx` — Spec requires a version selector for multi-version questionnaires; the component conditionally hides the selector when `versions.length <= 1`, which is correct per spec, but requires manual verification that the "Totals" default view still renders all sections — Fix: Add a unit test asserting single-version analytics renders all chart sections without the selector — Effort: S
- [LOW] `humanize()` key map is sparse — `frontend/src/pages/questionnaires/QuestionnaireAnalyticsTab.tsx` — Only `form:submit` and `form:validate` are mapped to human-readable labels; all other hotspot keys (e.g. `q_slider`, `q_email`, `q_text`) fall through to a partial replace that produces `q slider` rather than `Question: slider` (the `replace` uses `/^q /` which only strips a leading `q ` with space, not `q_`) — Fix: Fix regex to `/^q_/` → `"Question: "` — Effort: S
