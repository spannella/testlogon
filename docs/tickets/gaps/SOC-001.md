- [MED] Follow rate limiting documented but not implemented — `app/services/social.py:36` docstring says raises `rate_limited` but there is no rate-limit check in `follow_user()`; the router handler at `app/routers/social.py:171` catches `rate_limited` but it can never be raised — Fix: add sliding-window counter check (30 follows/min) per ticket §3.7 — Effort: S
- [LOW] Discovery index not updated on follow/unfollow — `app/services/social.py:follow_user/unfollow_user` increment `follower_count` on the profiles table but never call `index_user_for_discovery()` to propagate the updated count into the DiscoveryIndex; trending/search results show stale follower counts until the periodic re-index runs — Fix: call `index_user_for_discovery(followed_id)` (non-fatal, async-safe) at the end of follow/unfollow per SOC-003 ticket §3.4 and ticket §4.7 — Effort: S
- [LOW] Reconciliation/drift-correction job not scheduled — `app/services/social.py` has `reconcile_follow_counts()` defined but it is never called on a schedule; the ticket specifies a daily background job to correct counter drift — Fix: register as a daily startup task in `app/main.py` (or cron job) — Effort: S
