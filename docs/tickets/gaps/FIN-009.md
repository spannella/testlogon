- [HIGH] Payout methods CRUD not implemented — `app/services/creator_payouts.py` (no `add_payout_method`/`list_payout_methods`/`delete_payout_method`), `app/routers/creator_payouts.py` (no `/ui/payouts/methods` endpoints), `frontend/src/pages/payouts/PayoutDashboard.tsx` (no methods tab) — Creators cannot configure bank/PayPal destinations; payout request hardcodes `"bank_transfer"` string with no real routing info — Fix: add payout method service functions, router endpoints, `PayoutMethodIn`/Out models, and "Payout Methods" tab in dashboard — Effort: M
- [HIGH] Admin queue tab absent from PayoutDashboard — `frontend/src/pages/payouts/PayoutDashboard.tsx` (no admin tab) — Admin endpoints exist (`app/routers/admin_payouts.py`) but are inaccessible from the UI; admins must process payouts via raw API calls — Fix: add "Admin Queue" tab visible to role >= ADMIN, with approve/reject actions and reject-reason dialog — Effort: M
- [MED] Batch-complete endpoint missing — `app/routers/admin_payouts.py` (no `POST /batch-complete`), `app/services/creator_payouts.py` (no `batch_complete_payouts`) — Admin cannot bulk-process all approved payouts from UI — Fix: add `batch_complete_payouts()` service function, `POST /v1/admin/payouts/batch-complete` endpoint, `BatchCompleteOut` model, and "Process All Approved" button in admin tab — Effort: S
- [MED] Payout schedule endpoint and display absent — `app/services/creator_payouts.py` (no `get_payout_schedule`), `app/routers/creator_payouts.py` (no `GET /ui/payouts/schedule`) — Creators cannot see next automatic payout date or frequency; schedule card specified in ticket does not exist — Fix: add `PayoutScheduleOut` model, schedule service function, endpoint, and schedule card in dashboard — Effort: S
- [LOW] Payout detail view not implemented — `app/routers/creator_payouts.py` (no `GET /ui/payouts/{id}`) — Clicking a payout row shows nothing; status timeline described in ticket is absent — Fix: add `get_payout_detail` service function and `PayoutDetailOut` model — Effort: S
