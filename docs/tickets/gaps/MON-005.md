# MON-005 gaps

- [MED] `_record_subscription_access` writes a `source="subscription"` entitlement to `vod_entitlements` with `ConditionExpression="attribute_not_exists(pk)"` to avoid overwriting paid purchases — `app/services/vod_purchase.py:253-269` — but if a user first gets a subscription record written, then purchases the video outright, `purchase_video` calls `put_item` unconditionally (after the idempotency pre-check which returns early on any existing record including subscription-sourced ones) — `app/services/vod_purchase.py:428-452` — a subscriber who also wants to permanently own a video is blocked from purchasing because the subscription entitlement record triggers the "already owned" early return path — Fix: in `purchase_video`, treat `source="subscription"` existing records as non-blocking (allow the paid purchase to overwrite them) — Effort: S

- [MED] `check_vod_access` calls `has_active_subscription` on every single-video detail request, performing a full DDB query of all viewer subscriptions with in-memory filtering — `app/services/vod_purchase.py:182` → `app/services/subscription_access.py:55-73` — for a viewer with many subscriptions, every video detail page load executes an unbounded subscription table scan; no caching layer exists — Fix: add a per-request (or short TTL in-process) cache keyed by `(subscriber_id, creator_id)` since the entitlement decision only changes when subscriptions change — Effort: S

- [LOW] `VideoPlayerPage.tsx` renders the subscription CTA using `video.access_mode === "subscriber_only"` check but the backend response includes `subscription_available: bool` specifically for this purpose — `frontend/src/pages/videos/VideoPlayerPage.tsx:224` — if the backend ever changes how it expresses subscriber-only gating, the frontend check may diverge from the authoritative server-side flag — Fix: prefer `video.subscription_available` (from `access_reason/subscription_available` backend fields) over re-deriving from `access_mode` on the client — Effort: S

- [LOW] `_batch_check_entitlements` used by `list_creator_videos_with_access` does not filter out expired rental or consumed view-once entitlements — `app/services/vod_purchase.py:271-311` — a video list page shows `entitled=true` badges for rentals that have since expired or view-once purchases that were consumed, misleading the viewer until they click through to the detail page — Fix: filter out items where `expires_at > 0 AND expires_at < now_ts()` or `views_remaining == 0` in the batch result processing loop — Effort: S
