- [HIGH] SNS notification endpoint unauthenticated and reachable without network restriction — `app/routers/ses_notifications.py` (pattern shared with `app/routers/webhooks.py` for inbound SNS) — `POST /internal/ses/notifications` must accept unauthenticated SNS calls; without a security group or WAF restricting to AWS SNS IP ranges, an attacker can POST crafted bounce notifications to suppress any email address — Fix: restrict endpoint to VPC-only traffic via security group; document required network controls in ops runbook — Effort: M
- [MED] ByStatus GSI hot partition at high delivery volume — `scripts/local-ddb-init.py` (`webhook_deliveries` table) — all pending deliveries share `status="pending"` as GSI partition key; at 10,000+ active deliveries this creates a DDB hot partition — Fix: shard status key (e.g., `pending#0`…`pending#9`); fan-out dispatcher query across shards — Effort: M
- [LOW] Webhook test endpoint subject to SSRF guard but dev localhost receivers blocked — `app/services/webhook_ssrf.py` — SSRF guard rejects RFC 1918 addresses including `127.0.0.1`; developers cannot register `http://localhost:9999` as a local test receiver even in dev mode — Fix: in dev mode (`S.dev_mode=True`) allow localhost URLs through the SSRF guard — Effort: S
- [LOW] Retry schedule sends up to 5 deliveries over 2.5 hours then dead-letters — `app/services/webhook_retry.py` — no configurable max_attempts per endpoint; hard-coded 5 attempts may be too few for intermittent receiver downtime spanning hours — Fix: add `max_attempts` field to the endpoint config with a default of 5; allow per-endpoint override — Effort: S
