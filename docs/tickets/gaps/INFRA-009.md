- [HIGH] Dangerous-rule block covers only SSH port 22; VNC (5900-5999), RDP (3389), and IPv6 (::/0) are not blocked — `app/services/security_groups.py:145-157` — `is_dangerous_rule()` checks `port_from <= 22 <= port_to` and `source == "0.0.0.0/0"` only; a user can create an inbound rule opening VNC port 5900 or RDP 3389 to all of the internet, or open SSH to `::/0` (IPv6 any), bypassing the protection entirely — Fix: extend `is_dangerous_rule` to also flag port ranges covering 3389, 5900-5999, and source `::/0`; or generalise to a configurable blocklist of (port-range, source) pairs — Effort: S
- [MED] `associated_instances` list is never updated when an instance is launched with a security group — `app/services/security_groups.py` has no `add_instance_association` call; `app/services/ec2_launcher.py` / `app/services/k8s_launcher.py` do not call `ensure_default_sg` or update `associated_instances` — SG delete guard (`SecurityGroupInUse`) therefore never triggers, allowing deletion of in-use SGs — Fix: call `add_instance_association(user_sub, sg_id, instance_id)` in the EC2/K8s launch path; call `remove_instance_association` on terminate — Effort: M
- [LOW] `get_effective_rules` not exposed via API — `app/services/security_groups.py:resolve_platform_cidrs`, `get_effective_rules` are implemented but no endpoint calls them — clients cannot see what IP ranges `platform_only` resolves to — Fix: add `GET /ui/remote/security-groups/{sg_id}/effective-rules` that returns the fully expanded rule list — Effort: S
