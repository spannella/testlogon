- [HIGH] Real K8s launch path raises `NotImplementedError` — `app/services/k8s_launcher.py:176` — when `S.k8s_mock_enabled = False` (production mode), `launch_pod()` hits `raise NotImplementedError("Real K8s launch not implemented yet")`; `get_pod_logs()` also raises at line 257; no real Kubernetes Python client is integrated — dev/prod parity (SECOPS-007) is broken: mock mode works; prod mode is a 500 crash — Fix: add `kubernetes` client to dependencies; implement `_real_k8s_launch()` using `kubernetes.client.CoreV1Api().create_namespaced_pod()`; implement real log fetch via `kubernetes.client.CoreV1Api().read_namespaced_pod_log()` — Effort: L

- [HIGH] Auto-registration in host inventory never happens — `app/services/k8s_launcher.py:196-197` — `launch_pod()` stores `host_id: ""` and never calls `create_host()` from `app/services/host_inventory`; the `host_id` is always empty; acceptance criterion #3 ("Pods auto-register in host inventory with `source: k8s_auto`") is unmet — Fix: after storing the pod record, call `host_inventory.create_host(user_sub, label=f"{label} (K8s)", hostname=result["service_hostname"], port=22, protocol="ssh", source="k8s_auto", group="K8s Containers")` and write back `host_id` ��� Effort: S

- [HIGH] Host inventory not cleaned up on pod termination — `app/services/k8s_launcher.py:260-286` — `terminate_pod()` updates DDB status to `"terminated"` but never removes the linked host record; acceptance criterion on inventory cleanup is unmet — Fix: call `host_inventory.delete_host(user_sub, item["host_id"])` inside `terminate_pod()` if `host_id` is non-empty — Effort: S

- [MED] SSH key injection is a no-op in mock mode and unimplemented in real mode — `app/services/k8s_launcher.py:165-176` — `launch_pod()` accepts `ssh_key_id` and stores it in DDB, but the mock `_MockK8sStore.create_pod()` receives `ssh_pub_key=None` (hardcoded); in real mode the path raises `NotImplementedError`; the "mount public key as `authorized_keys` ConfigMap" feature is fully absent — SEC-022 cross-ref: only the public key must be mounted; the encrypted private key blob must never appear in K8s manifests or ConfigMaps — Fix: (mock) pass `ssh_pub_key` through to mock store and add it to mock log; (real) create a K8s `Secret` with `authorized_keys` content and mount at `/home/{username}/.ssh/authorized_keys` via `volumeMounts` — Effort: M

- [MED] No audit events emitted — `app/services/k8s_launcher.py` (0 `audit_event` calls) — ticket spec requires `k8s.pod_launched`, `k8s.pod_terminated`, `k8s.ttl_expired` audit events; only `logger.info` is used; no entries reach the audit trail or SECOPS-001 telemetry — Fix: import `audit_event` from `app.services.alerts` and call in `launch_pod`, `terminate_pod`, `check_expired_pods` — Effort: S

- [MED] TTL background scanner does not paginate — `app/services/k8s_launcher.py:298-303` — `check_expired_pods()` calls `T.k8s_pods.scan()` with no `LastEvaluatedKey` loop; pods beyond the first 1MB DDB page are silently skipped and not terminated on schedule (per CLAUDE.md "DDB FilterExpression doesn't reduce page size" gotcha) — Fix: add pagination loop around the scan, same pattern as INFRA-003 idle checker fix — Effort: S

- [MED] Namespace collision: `_sanitize_namespace()` truncates `user_sub` to 12 chars — `app/services/k8s_launcher.py:102-105` — two users whose `user_sub` values share the same first 12 characters (possible with UUID-style subs that start with common prefixes) would map to the same K8s namespace and share pod resources; cross-user isolation guarantee is broken — Fix: use a longer unique prefix or a hash (e.g., `hashlib.sha256(user_sub.encode()).hexdigest()[:16]`) — Effort: S

- [LOW] `env_vars` not persisted to DDB item — `app/services/k8s_launcher.py:179-205` — `launch_pod()` accepts `env_vars` and passes it to `_mock_store.create_pod()`, but the DDB item dict does not include an `env_vars` field; reloading the pod via `get_pod()` loses env var information — Fix: add `"env_vars": env_vars or {}` to the DDB item dict — Effort: S
