- [HIGH] No client-side forensic watermark overlay — `frontend/src/pages/videos/VideoPlayerPage.tsx:479-504` — Spec requires a `WatermarkOverlay` component with semi-transparent session ID text rendered over the player surface as a secondary deterrent against screen recording; the player renders `<MediaPlayer>` with no overlay div — Fix: Add `<WatermarkOverlay sessionId={video.playback_token?.slice(0,12)} tenantId={video.owner_user_id} />` absolutely positioned over the player container — Effort: S
- [HIGH] No automatic playback token refresh — `frontend/src/pages/videos/VideoPlayerPage.tsx:375-378` — The player uses a static `playback_token` field from the video detail response (`video?.playback_token`); if the token expires during a long viewing session HLS.js will receive 401/403 on manifest/segment fetches causing fatal playback errors with no recovery path — Fix: Add `usePlaybackEntitlement` hook with `refetchInterval: 90_000` to rotate the token before expiry and update the `playbackUrl` ref without tearing down the HLS instance — Effort: M
- [MED] Entitlement token is not injected into HLS.js segment requests — `frontend/src/components/shared/MediaPlayer.tsx:311` — `MediaPlayer` initialises HLS.js with no `xhrSetup` callback; segment and manifest requests carry no `Authorization` header, so any backend enforcement of the playback token on individual segment fetches is bypassed — Fix: Add `xhrSetup: (xhr) => { if (tokenRef.current) xhr.setRequestHeader("Authorization", \`Bearer \${tokenRef.current}\`) }` to the HLS config; thread an `authToken` prop through `MediaPlayerProps` — Effort: M
- [LOW] No standalone `WatermarkOverlay` component file — `frontend/src/pages/videos/` — Spec calls for `WatermarkOverlay.tsx` as a named file; watermark logic must be added inline or as a new component; currently absent — Fix: Create `frontend/src/pages/videos/WatermarkOverlay.tsx` per spec design and import in `VideoPlayerPage` — Effort: S
