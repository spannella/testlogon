- [HIGH] Submission hook wiring unverified — `app/routers/kyc_cases.py:830` (`submit_kyc_case`) — the architectural intent is for `SCREENING_STORE.screen_case()` to fire on every submission; whether the call is actually stitched at line 830 is noted as unconfirmed in the write-up — Fix: verify the hook exists; if absent, add `SCREENING_STORE.screen_case(case_id, user_sub, trigger="submission")` wrapped in try/except so screening failure does not block submission — Effort: S
- [HIGH] Profile-change re-screening not wired — `app/routers/settings.py` (or equivalent profile update endpoint) — changing `display_name`, `date_of_birth`, or `nationality` after case approval does not trigger re-screening; AML continuous monitoring requirement violated — Fix: add hook in profile update endpoint to call `SCREENING_STORE.rescreen_user()` when sensitive fields change — Effort: S
- [HIGH] `attr_types={"created_at":"N"}` must be verified on `kyc_screening_results` table — `scripts/local-ddb-init.py` — omitting `attr_types` causes DynamoDB to store `created_at` as String, breaking `ScanIndexForward` queries with numeric predicates on both `ByUserSub` and `ByStatus` GSIs — Fix: confirm `attr_types={"created_at": "N"}` is present in the `TableDef`; add if missing — Effort: S
- [MED] Adjudication vocabulary mismatch between spec and implementation — `app/models.py` (KycScreeningReviewRequest) — ticket spec says `false_positive`/`true_match`; service constants at `kyc_sanctions_screening.py:89-92` use `clear`/`confirm`/`escalate`; if models use spec vocabulary the router will reject valid decisions — Fix: align `app/models.py` models to use `clear`/`confirm`/`escalate` and update UI labels accordingly — Effort: S
- [MED] `ByStatus` GSI hot partition risk — `app/services/kyc_sanctions_screening.py` (`ByStatus` index on `result`) — with many users, `result="potential_match"` becomes a hot partition; write throughput is capped — Fix: document as known limitation for current scale; plan partition relief (e.g., random suffix sharding) before exceeding ~100 active matches — Effort: M
- [MED] Async screening latency in production — `app/services/kyc_sanctions_screening.py` (`screen_case`, sequential loop over 5 screen types) — five sequential real-provider HTTP calls could add 25+ seconds to the submission response in prod — Fix: parallelize with `asyncio.gather()` for real-provider path; keep mock sequential — Effort: M
- [LOW] Empty-name profile produces silent clear — `app/services/kyc_sanctions_screening.py:231` (`_subject_matches_watchlist`) — user with no profile name returns `clear` for all screen types without any flag; undetectable if the watchlist contains nameless entity records — Fix: log a warning when `name=None` at screening time so reviewers can manually check — Effort: S
