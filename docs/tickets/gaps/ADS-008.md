- [HIGH] compute_hourly_rollup never called by anything — `app/services/ad_analytics.py:compute_hourly_rollup` — AdAnalyticsRollups table always empty; all dashboard endpoints return zeros — Fix: register hourly background task in app/main.py startup that calls compute_hourly_rollup for all active campaigns — Effort: M
- [HIGH] by_creative, by_surface, by_targeting maps written as empty dicts — `app/services/ad_analytics.py:compute_hourly_rollup:201–203` — get_breakdown always returns empty arrays for all dimension breakdowns — Fix: compute breakdown maps from billing ledger entries grouped by creative_id and surface — Effort: M
- [MED] rollup aggregates all historical spend, not just the target hour — `app/services/ad_analytics.py:compute_hourly_rollup:176` — hourly rollups double-count prior hours' impressions on each run — Fix: filter billing entries by created_at within [hour_start, hour_start+3600) before aggregating — Effort: S
- [MED] get_campaign_spending called with hard-coded limit=1000 — `app/services/ad_analytics.py:compute_hourly_rollup` — campaigns with > 1000 impressions per period are silently undercounted — Fix: add LastEvaluatedKey pagination loop to get_campaign_spending — Effort: S
- [LOW] no automated rollup trigger endpoint for E2E tests and on-demand admin use — `app/routers/ads.py` — E2E tests must seed rollup data directly in DDB; no clean trigger path — Fix: add POST /ui/admin/ads/analytics/rollup endpoint for on-demand rollup trigger — Effort: S
