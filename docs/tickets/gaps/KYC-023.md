- [HIGH] `KycPiiSection` component is never used in any page — `frontend/src/components/shared/KycPiiSection.tsx` (176 lines) — the component composes `PiiFieldDisplay` + `PiiAuditLog` for the admin case detail view as specified in the ticket (§3.12), but `frontend/src/pages/admin/KycCaseDetailPage.tsx` (685 lines) imports neither `KycPiiSection` nor `PiiFieldDisplay`; admins cannot view masked PII or click "Reveal" despite the backend endpoints being fully implemented — Fix: import `KycPiiSection` into `KycCaseDetailPage.tsx` and render it in the case detail body with `caseId`, `isAssigned`, and `isRoot` props — Effort: S
- [MED] No rate limiting on `POST /v1/kyc/cases/{case_id}/pii/decrypt` — `app/routers/kyc_cases.py:1591` — the ticket (§7.3) specifies 20 decryptions per admin per 15 minutes to prevent bulk PII extraction; the endpoint has no `rate_limit_or_429` call despite the platform-wide rate-limit infrastructure being available — Fix: add `rate_limit_or_429(user_sub=user.sub, factor="kyc_pii:decrypt")` at the start of `decrypt_kyc_pii` — Effort: S
- [MED] No rate limiting on `POST /v1/kyc/cases/{case_id}/pii` (write endpoint) — `app/routers/kyc_cases.py:1517` — unlimited PII write requests can exhaust KMS `GenerateDataKey` quota; a compromised owner account could loop this endpoint — Fix: add `rate_limit_or_429(user_sub=user.sub, factor="kyc_pii:write")` — Effort: S
- [LOW] `PiiFieldDisplay` "Reveal" timer (auto-hide after 30 seconds) is not implemented in the component — `frontend/src/components/shared/PiiFieldDisplay.tsx` — the ticket (§3.12) specifies "Shows plaintext for 30 seconds, then re-masks" and the setting `KYC_PII_REVEAL_TIMEOUT_SECONDS=30` exists in `app/core/settings.py:1168`; however the component's reveal state is a simple boolean toggle with no timeout — Fix: add a `useEffect` in `PiiFieldDisplay` that calls `setRevealed(false)` after `revealTimeoutMs` (passed as prop or read from a config endpoint) — Effort: S
