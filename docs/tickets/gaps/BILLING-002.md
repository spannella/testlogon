- [MED] No admin management UI for stuck payouts — `app/services/creator_payouts.py:55` — payouts stuck in `approved` or `processing` state can only be diagnosed via direct DDB access; no `GET /ui/admin/payouts?status=processing` endpoint exists — Fix: add admin endpoint querying `ByStatusCreatedAt` GSI to surface stuck payouts — Effort: S
- [MED] Balance recalculation scans full billing ledger on every request — `app/services/creator_payouts.py:55` — `ddb_query_pk(T.billing, pk)` loops all pages for the user; with `refetchInterval: 30_000` on the dashboard this is called every 30 seconds per active user — Fix: maintain a cached balance summary row; invalidate on new credits — Effort: M
- [LOW] 409 duplicate payout error not distinguished from generic 400 in frontend — `frontend/src/pages/payouts/PayoutDashboard.tsx:623` — `onError` handler must differentiate 409 to show "You already have a pending payout request" — Fix: check `error.response?.status === 409` in mutation `onError` — Effort: S
- [LOW] Balance query stale fallback blocks all submissions — `frontend/src/pages/payouts/PayoutDashboard.tsx:623` — Zod refine uses `balanceQ.data?.available_cents ?? 0` as fallback when query is pending, blocking submissions until balance loads — Fix: disable submit button with loading state while `balanceQ.isLoading` — Effort: S
- [LOW] Missing Playwright E2E spec for UI layer — no `frontend/e2e/payout-dashboard.spec.ts` — balance cards, form submission, cancel flow, and earnings chart are untested at the browser level — Fix: add `payout-dashboard.spec.ts` covering the scenarios in section 5.2 — Effort: S
