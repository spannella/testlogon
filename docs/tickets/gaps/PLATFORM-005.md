- [HIGH] Backend meta endpoint not yet created — `app/routers/meta.py` — file does not exist; `app/main.py` has no `meta_router` registration; crawler-detection middleware cannot function without it; social media crawlers still see `<title>Control Panel</title>` for all URLs — Fix: create `app/routers/meta.py` with `GET /api/meta?url=...`; register in `main.py`; implement `_profile_meta`, `_event_meta`, `_post_meta`, `_video_meta` helpers — Effort: M
- [HIGH] Crawler-detection middleware not implemented — `frontend/vite.config.ts` / production proxy — social bots (Facebook, Twitter/X, Discord, Slack) do not execute JS; client-side `react-helmet-async` alone produces no OG tags for them; the middleware that intercepts crawler UAs and injects server-side meta is absent — Fix: implement crawler UA detection + meta injection in production proxy and as a Vite plugin for dev — Effort: M
- [MED] XSS risk in server-side meta injection path — (proposed `frontend/vite.config.ts` crawler middleware) — user-controlled fields (display name, bio, post body) inserted into `content="..."` HTML attributes via string replacement must be HTML-escaped before injection — Fix: apply `escapeHtml()` to all user data in the crawler middleware's injection step — Effort: S
- [MED] Locked post body exposed via meta endpoint — (proposed `app/routers/meta.py`) — `_post_meta()` must check `lock_price_cents` and return generic description for locked posts; without this, locked content is readable via `GET /api/meta?url=/posts/{id}` without payment — Fix: add `lock_price_cents` check in `_post_meta()`; return `"This post is locked."` instead of body excerpt — Effort: S
- [LOW] `PublicUserProfilePage` Helmet block partially complete — `frontend/src/pages/profile/PublicUserProfilePage.tsx:264` — `og:title` is present but `og:description`, `og:image`, `og:type`, `og:url`, `twitter:card`, and `<link rel="canonical">` may be incomplete — Fix: expand `<Helmet>` block to include all required OG and Twitter Card tags plus canonical link — Effort: S
- [LOW] `PublicEventPage` lacks Helmet block — `frontend/src/pages/calendar/PublicEventPage.tsx` — no `<Helmet>` with event OG tags; event links shared on social media show generic site title — Fix: add `<Helmet>` block with event title, description, og:type="event", and canonical link — Effort: S
- [LOW] Default fallback meta tags absent from index.html — `frontend/index.html` — no `og:site_name`, `og:type`, `og:locale`, `twitter:card`, or `<meta name="description">` in the static shell; crawlers hitting the SPA root before hydration see bare title only — Fix: add fallback OG/Twitter meta tags directly to `frontend/index.html` — Effort: S
