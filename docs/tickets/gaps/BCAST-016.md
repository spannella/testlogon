# BCAST-016 Gap List

- [HIGH] Expired invite not checked in `accept_guest_invite()` — `broadcast_input_store.py:244` — only `status == "pending"` checked; expired invites in pending state can still be accepted if cleanup sweep hasn't run — Fix: add `if now_ts() > int(invite.expires_at): raise HTTPException(410, "Invite has expired")` — Effort: S
- [MED] No layout auto-fallback on input disconnect — `broadcast_multi_input.py` / `broadcast_layout.py` — if one input drops during `side_by_side`/`pip`/`grid` layout, no server-side fallback to `single`; broadcaster must manually switch — Fix: implement `handle_input_disconnected()` that calls `switch_layout(mode="single", ...)` when active layout requires the lost input — Effort: S
- [MED] Input count enforcement subject to read-then-write race — `broadcast.py:~3682` — `count_inputs(session_id) < session.max_inputs` pre-check is stale; two concurrent add-input requests can both pass and exceed `max_inputs` — Fix: use conditional DDB write or atomic counter with `ConditionExpression` — Effort: S
- [MED] SEC-025 — guest-invite and input management ownership checks not centralized — `broadcast.py:~3841,3930` — inline `ctx["user_sub"] != session.created_by` checks rather than `_require_session_owner` helper — Fix: use centralized `_require_session_owner(ctx, session)` per SEC-025 design — Effort: S
- [LOW] Mock RTMP base URL hard-coded — `broadcast_multi_input.py` — dev mock returns `rtmp://localhost:1935/live/...`; not configurable for local RTMP server testing — Fix: add `broadcast_rtmp_mock_base_url` to `settings.py` — Effort: S
- [LOW] Unauthenticated invite accept endpoint — abuse and audit gap — `broadcast.py:~3950` — no IP logging on accept; leaked token allows unauthorized guest access — Fix: log accepting IP for audit; optionally require authentication — Effort: S
