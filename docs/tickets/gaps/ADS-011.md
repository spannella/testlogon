- [HIGH] webhook system entirely absent — `app/services/` — no ad_webhooks.py, no DDB table, no CRUD endpoints, no delivery mechanism; advertisers cannot receive real-time campaign event notifications — Fix: implement app/services/ad_webhooks.py + ad_webhooks DDB table + POST/GET/DELETE/test endpoints in advertiser_api_router — Effort: L
- [HIGH] no per-API-key rate limiting — `app/routers/advertiser_api.py` — any API key can make unlimited requests; risk of DDB hot partitions from bulk analytics queries — Fix: add check_rate_limit middleware on advertiser_api_router at 1000 req/min per key — Effort: S
- [HIGH] ads:* scopes not surfaced in frontend API key creation UI — `frontend/src/pages/security/` — advertisers cannot select ads:manage/read/serve scopes via the UI; must use direct API call — Fix: add ads:manage, ads:read, ads:serve to the capability selector component — Effort: S
- [MED] resolve_account returns first account only — `app/services/advertiser_api.py:81` — advertisers with multiple ad accounts have no way to target a specific account via the API — Fix: accept optional account_id query parameter and verify ownership — Effort: S
- [MED] Authorization header format mismatch (ApiKey vs Bearer ak_...) — `app/services/api_key_auth_dependency.py:11–13` — documented spec uses Bearer ak_{id}.{secret} but implementation parses ApiKey prefix — Fix: also accept Authorization: Bearer ak_... for spec compatibility — Effort: S
- [MED] POST /campaigns/bulk-budget endpoint missing — `app/routers/advertiser_api.py` — advertisers cannot adjust budgets for multiple campaigns in a single API call — Fix: implement bulk-budget endpoint + AdsApiBulkBudget Pydantic model — Effort: S
- [MED] GET /analytics/by-campaign endpoint missing — `app/routers/advertiser_api.py` — per-campaign metrics aggregation not available; only by-date and breakdown exist — Fix: add per-campaign analytics function to advertiser_api.py and corresponding router endpoint — Effort: S
- [MED] sandbox mode not implemented — `app/routers/advertiser_api.py` — no POST /sandbox/serve or /sandbox/impression; API integration testing incurs real charges — Fix: add sandbox endpoints returning deterministic placeholder creatives without real campaign data — Effort: M
