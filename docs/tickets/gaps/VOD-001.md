- [HIGH] "queued" status bug silently swallowed — `app/routers/transcode_jobs.py:103` — `to_status="queued"` is not a valid VideoStatus; state machine returns 409 which is caught by bare `except Exception`; video stays in "created" forever, never appearing in `pending_encoding` queue — Fix: change `to_status="queued"` to `to_status="pending_encoding"` — Effort: S
- [MED] TICKET# records pollute ByOwnerCreatedAt GSI — `app/routers/vod.py:189` — upload tickets share the VideoMetadata table and GSI; FilterExpression runs after 1 MB page fetch, inflating read costs and slowing listing on busy tables — Fix: move upload tickets to a dedicated `VideoUploadTickets` table — Effort: M
- [LOW] Naming confusion between VideoUpdateIn and UpdateVideoIn — `app/models_video.py:137,183` — two update models with near-identical names but different field sets risk developers importing the wrong one — Fix: rename to `UserVideoUpdateIn` and `SystemVideoUpdateIn` to make intent explicit — Effort: S
- [LOW] VideoDetailOut defined inside router not models file — `app/routers/video_listing.py:67` — response shape defined in a router rather than `models_video.py`; diverges from the ticket spec and makes cross-router reuse harder — Fix: move `VideoDetailOut` to `app/models_video.py` — Effort: S
