- [HIGH] frontend components entirely absent — `frontend/src/pages/ads/` — no OptimizationPanel.tsx, RecommendationCards.tsx, ABTestResults.tsx, or adOptimization.ts API wrappers; advertisers cannot access optimization through the UI — Fix: create all four files per ticket spec sections 3.8-3.9 — Effort: M
- [HIGH] creative_weights written by apply_recommendation but not consumed by serving engine — `app/services/ad_serving.py:serve_ad` — applying reallocate_budget recommendation has no observable effect on ad delivery — Fix: read campaign.get("creative_weights") in serve_ad and pass to random.choices as weights — Effort: S
- [MED] no background scheduler for auto_optimize_enabled campaigns — `app/main.py` — auto_optimize_enabled field is stored but never actioned; enabling it has no effect — Fix: add startup background task running every 6 hours querying campaigns with auto_optimize_enabled=True and calling generate_recommendations — Effort: M
- [MED] recommendation idempotency broken — _store_recommendation overwrites applied/dismissed records — `app/services/ad_optimization.py:_store_recommendation` — a second /generate call within the same second un-applies or un-dismisses existing recommendations — Fix: add ConditionExpression preventing overwrite when status is applied or dismissed — Effort: S
