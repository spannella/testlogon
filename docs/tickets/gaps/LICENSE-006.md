- [HIGH] `GET /ui/licenses/compliance/content/{content_id}` blocks admin access — `app/routers/license_compliance.py:76` — The compliance detail endpoint enforces `creator_id == session["user_sub"]` with a 403 for any other user; admins cannot view compliance status for arbitrary content without a separate admin endpoint; the ticket specifies "Content owner or admin" — Fix: add a role check — if `require_admin_or_root` passes, skip the creator_id guard — Effort: S
- [MED] No per-reporter-per-content flag deduplication — `app/services/license_compliance.py:180` — Ticket section 6.2 states "1 flag per user per content item" to prevent spam; the implementation writes a new flag record unconditionally; a reporter can flood the same content with repeated flags (each incrementing the admin queue) — Fix: check existing flags by `reporter_id` for the same `content_id` (query `CONTENT#{content_id}/FLAG#...` with a filter on `reporter_id`) before inserting — Effort: S
- [MED] `run_compliance_scan` re-scan loop does not paginate GSI1 results — `app/services/license_compliance.py:495` — The compliance scan queries `GSI1PK=COMPLIANCE_STATUS#compliant` with a single DynamoDB query; on a busy platform with many compliant items, results beyond the first 1MB page are silently skipped — Fix: loop `LastEvaluatedKey` in `run_compliance_scan` until exhausted — Effort: S
- [MED] DynamoDB table name mismatch: implementation uses `license_compliance_checks` while ticket schema specifies `license_compliance` — `app/core/settings.py:2030`, `scripts/local-ddb-init.py:1734` — Any external tooling, monitoring, or documentation built against the ticket schema will use the wrong table name; the setting `license_compliance_checks_table_name` maps to `T.license_compliance` in tables.py via `_safe_table` — Fix: rename to `license_compliance` and update settings, tables, and DDB init — Effort: S
- [LOW] `content_compliance_detail` endpoint only allows creator to view; spec says "creator can trigger compliance re-check" but endpoint takes no action param — `app/routers/license_compliance.py:65` — The GET endpoint is read-only; the POST `/check` endpoint is the re-check trigger, but there is no guard ensuring only the content creator (not a random user) can trigger a re-check that writes compliance records — Fix: in `POST /content/{content_id}/check`, verify `session["user_sub"]` is the content's `creator_id` (either from the compliance record or from the content table) — Effort: S
- [LOW] `admin_update_compliance` sets `resolved_at` to `None` for `under_review` status but DynamoDB `UpdateExpression` with `SET field = :null` stores an explicit NULL which can break some query patterns — `app/services/license_compliance.py:387` — The update sets `:t` to Python `None` for non-terminal statuses; DynamoDB's `UpdateExpression = SET resolved_at = :null` with `ExpressionAttributeValues={":null": None}` stores a Null type in DynamoDB, not a missing attribute; subsequent `get_item` returns `"resolved_at": None` which may fail strict Pydantic validators — Fix: use `REMOVE resolved_at, resolved_by` clause for non-terminal statuses instead of setting to None — Effort: S
