- [MED] `ScheduledContentCalendar` frontend page never built — ticket spec `frontend/src/pages/calendar/ScheduledContentCalendar.tsx` — backend `/ui/scheduler/calendar` endpoint exists (`app/routers/scheduler.py:140`) and `scheduler.ts` API wrapper exists, but no calendar-view page; route `/calendar/scheduled` is absent from `frontend/src/App.tsx` — Fix: implement `ScheduledContentCalendar.tsx` and add route — Effort: M
- [MED] `CreatePost.tsx` uses the existing inline newsfeed scheduler (sends `publish_at` to `/ui/posts`), not the unified scheduler's `/ui/feed/posts/schedule` endpoint — `frontend/src/pages/feed/CreatePost.tsx:377,742` — unified scheduler endpoint exists and is built but is bypassed; cross-module calendar view therefore never shows scheduled posts — Fix: redirect CreatePost scheduling to `POST /ui/feed/posts/schedule` or dual-write per Phase 2 migration plan — Effort: S
- [MED] Catalog sale scheduling UI not added to `ItemEditor.tsx` — ticket spec `frontend/src/pages/shop/ItemEditor.tsx` — `POST /ui/catalog/products/{id}/sale` backend endpoint and `scheduleCatalogSale()` API wrapper (`frontend/src/api/endpoints/scheduler.ts:102`) exist, but no sale date-picker in ItemEditor; sellers cannot schedule price changes via UI — Fix: add `ScheduleDialog`-based sale start/end section to ItemEditor — Effort: S
- [LOW] `ScheduleDialog` component (`frontend/src/components/shared/ScheduleDialog.tsx`) is built but not consumed by any page other than through implicit availability; no integration test proves end-to-end scheduling from the shared component — Fix: wire into CreatePost and ItemEditor as noted above; add E2E tests from `content-scheduling.spec.ts` plan — Effort: S
