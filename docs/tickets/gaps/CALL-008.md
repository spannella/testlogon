# CALL-008 Gaps

- [HIGH] No automatic `call.end` signal sent to remote peer on `failure` phase — `frontend/src/pages/messages/ConversationView.tsx:695-699` calls `teardownCallResources` locally but does not POST `call.end` — remote peer overlay stays visible and DDB session remains non-terminal until stale-session scan — Fix: add `useEffect` on `phase === "failure"` to call `callActionMutation.mutate({action:"end", callId, reason:"reconnect_failed"})` per §4.1 — Effort: S
- [MED] Responder's `retryCount` is consumed by answering ICE restart offers — when initiator sends two consecutive restart offers, responder dispatches `CONNECTION_LOST` and increments its own `retryCount`; responder may exhaust `maxRetries:2` and enter `failure` before ICE restart succeeds — Fix: add `PEER_RESTARTING` event to `callStateMachine.ts` that transitions to `reconnecting` without incrementing `retryCount` per §4.3 — Effort: M
- [LOW] Fixed 1-second debounce with no backoff — `ConversationView.tsx:689-693` uses hardcoded 1s delay for all `RECONNECT_ATTEMPT` dispatches regardless of retry count; two attempts made at ~t=1s and ~t=5-7s; longer outages exhaust retries quickly — Fix: replace with `1000 * Math.pow(2, retryCount)` exponential backoff per §4.2 — Effort: S
- [LOW] TURN credential re-fetch may fail mid-ICE-restart consuming a retry slot — `useRtcPeerConnection.ts:421-433` if `fetchTurnCredentials` rejects (network down), function dispatches `CONNECTION_LOST` again consuming a retry; low-probability but documented — Fix: differentiate fetch failure from ICE failure in error handling; don't consume retry on fetch error — Effort: S
