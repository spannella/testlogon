- [MED] GSI5SK missing on draft post items — `app/services/delegate_feed.py:147-155` — create_post_as_creator sets GSI5PK on drafts but omits GSI5SK; draft queue has no deterministic chronological order — Fix: set GSI5SK=str(created_at) alongside GSI5PK when creating pending-approval posts — Effort: S
- [MED] scheduled-post + approval interaction broken — `app/services/delegate_feed.py:~435` (approve_draft always sets status=published) — delegate-created scheduled posts approved after scheduled_at has passed are incorrectly left as published rather than scheduled — Fix: in approve_draft check if scheduled_at > now_ts() and set status=scheduled accordingly — Effort: S
- [LOW] PostCard delegation metadata not rendered — `frontend/src/pages/feed/PostCard.tsx` (no approval_status badge or delegate_tag display) — viewers see no indication a post was written by a delegate; creators see no pending-approval badge in feed — Fix: add approval_status="pending" badge and posted_by_delegate tag rendering to PostCard — Effort: S
- [LOW] FeedPage draft queue badge missing — `frontend/src/pages/feed/FeedPage.tsx` (no pending-drafts notification) — creators must navigate to /feed/delegate/:creatorId to see draft queue; no proactive notification — Fix: query GET /ui/newsfeed/delegate/{userId}/drafts on FeedPage mount and show badge with count — Effort: S
