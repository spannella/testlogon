- [MED] Screening report scans kyc_screening_results table but SAR flagged_transactions always empty — `app/services/kyc_compliance_reports.py:414` — `generate_sar()` builds `flagged_transactions` only from the caller-supplied `transaction_ids` list; the billing ledger is never queried, so the SAR has no financial context even when `transaction_ids` is omitted — Fix: query billing ledger (`T.billing.query`) for `user_sub` inside `generate_sar()` when `transaction_ids` is None — Effort: M
- [MED] Audit trail reconstructs events from case fields only; real audit_log table is never read — `app/services/kyc_compliance_reports.py:346-399` — Events are synthesised from `submission.submitted_at` / `review.decided_at`; admin actions (requests-for-info, reassignments, document changes) recorded in the audit_log table are invisible — Fix: query the audit_log table by `USER#{user_sub}` prefix and merge with the synthesised events — Effort: M
- [LOW] Export endpoint returns CSV/PDF as `Response` body with no S3 upload or signed URL — `app/routers/kyc_compliance_reports.py:98-124` — Ticket design called for S3 upload + signed URL (reusing csv_export pattern); current implementation streams bytes directly. Large reports will timeout on slow connections — Fix: upload to S3 and return a signed URL via the existing csv_export signed-URL helper — Effort: S
- [LOW] Volume/processing-time/deadline reports all cap at Limit=500 / 100 per status without exhausting pagination — `app/services/kyc_compliance_reports.py:119-128,262-263` — `_query_cases_by_status_date_range` loops via `_query_all` (correct), but `deadline_tracker` calls `STORE.list_cases_by_status(limit=100)` which does not paginate — Fix: replace the `limit=100` call in `deadline_tracker` with a full paginated scan — Effort: S
- [LOW] `render_report_pdf` truncates records list to 200 items with no warning — `app/services/kyc_compliance_reports.py:521` — Compliance officers exporting large retention inventories silently receive a partial PDF — Fix: add a `truncated` / `total_records` note to the PDF header when `len(rows) > 200` — Effort: S
