- [HIGH] SSH terminal does not hook into session recorder — `app/routers/browser_ssh_terminal.py:452-488` — the `ParamikoSshBridge` output loop never calls `append_events()`; recordings can only be populated via explicit API calls (`POST /ui/compute/ssh-recordings/{id}/events`), so live terminal sessions produce zero-event recordings unless the browser client explicitly streams chunks — Fix: in the WebSocket output handler call `append_events(user_sub, recording_id, [[elapsed, "o", data]])` for each received SSH output chunk; requires wiring `SessionRecorder`/recording-id into the `connect` message handler — Effort: M
- [HIGH] no per-host `record_sessions` flag (INFRA-001 dependency) — `app/services/ssh_session_recording.py:1` — ticket requires a `record_sessions: bool` field on each host record to control auto-recording; `remote_hosts` service exists but has no such field; the `_should_record()` gate from the ticket design is entirely absent; recording is only started if the caller explicitly POSTs `/ui/compute/ssh-recordings` — Fix: add `record_sessions: bool` to the host schema and call `start_recording()` automatically in the WebSocket `connect` handler when the flag is set — Effort: M
- [MED] S3 storage not used — `app/services/ssh_session_recording.py:377-400` — recordings are stored as DynamoDB items (chunked event rows) rather than as `.cast` files in S3; the ticket design specified `ssh-recordings/{user_sub}/{recording_id}.cast` in S3 with a presigned stream URL; the `/stream` endpoint from the design is replaced by a `/playback` endpoint that assembles events from DDB; large recordings will hit the 400 KB DDB item limit long before the 50 000-event cap — Fix: on `stop_recording`, serialise the event stream to asciicast v2 format, upload to S3, and store `s3_key`; change the `/playback` endpoint to return a presigned URL — Effort: M
- [MED] SSRF via SEC-020 applies to recordings — `app/services/ssh_session_recording.py:1` — hostname stored in recording metadata is accepted without RFC-1918 / metadata IP validation; a user could create a recording record referencing `169.254.169.254` as the hostname; combined with the missing browser_ssh_terminal integration, the stored `hostname` field is user-controlled with no IP denylist — Fix: apply the same `_enforce_destination_policy` IP denylist (SEC-020) when validating `hostname` in `start_recording()` — Effort: S
- [MED] no retention / cleanup task registered — `app/main.py:711` — `start_recording_cleanup_task` is imported and called but the ticket specifies a daily background task deleting items where `expires_at <= now`; the DDB schema stores `expires_at` but cleanup behaviour is not tested and the DDB TTL attribute is not set on the table definition in `scripts/local-ddb-init.py` — Fix: add DDB TTL attribute `expires_at` to the `ssh_session_recordings` TableDef and verify `start_recording_cleanup_task` actually deletes S3 objects �� Effort: S
- [LOW] admin list endpoint path conflict — `app/routers/ssh_session_recording.py:205` — the admin route is `GET /ui/compute/ssh-recordings/admin/users/{target_user_sub}` but the `{recording_id}` route at line 105 will match `admin` as a recording ID and return 404 before the admin route is reached; FastAPI resolves in definition order but `{recording_id}` is registered first — Fix: register the admin route before the `/{recording_id}` route, or use a distinct prefix like `/admin-users/` — Effort: S
- [LOW] asciinema-player npm package not added — `frontend/src/pages/remote/SshRecordingsPage.tsx:1` — the ticket requires `@asciinema/player` or equivalent; `SshRecordingPlayer.tsx` exists but if it uses a custom renderer without the npm package the playback fidelity differs from the design spec — Fix: add `asciinema-player` (or `@asciinema/player`) to `package.json` and use it in `SshRecordingPlayer.tsx` — Effort: S
