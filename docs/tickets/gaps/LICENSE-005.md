- [HIGH] `syndicate_content` table described in ticket is renamed `syndicate_open_licensing` in implementation, with only GSI1 (not the ticket-specified single GSI) — `scripts/local-ddb-init.py:1699`, `app/core/settings.py:2013` — The ticket specifies a `syndicate_content` table with `CREATOR_SYND#{user_id}/CONTENT#{content_id}` pattern and GSI1; the implementation uses `syndicate_open_licensing` with a compatible but differently named table, and stores config (`CONFIG` SK) in the same table rather than in the syndicates META record as specified; no `CREATOR_SYND#` index pattern exists in the implementation — Fix: naming divergence is acceptable if consistent; verify the `CREATOR_SYND#` pattern used by `list_syndicate_content` with `creator_id` filter is implemented — Effort: S
- [MED] `register_content` does not validate that the calling member owns the content being registered — `app/services/syndicate_open_licensing.py` — The `register_content` endpoint calls `_require_is_member` but never verifies that `creator_sub` is the owner of `content_id`; a syndicate member can register another creator's content under the syndicate, forcing unwanted auto-licenses — Fix: add content ownership validation before `register_content` proceeds (call the content table for the relevant content_type) — Effort: M
- [MED] E2E spec file is named `syndicate-open-licensing.spec.ts` while ticket specifies `syndicate-licensing.spec.ts` — `frontend/e2e/syndicate-open-licensing.spec.ts` — Minor naming mismatch vs ticket section numbers 479-482; no functional impact but consistency with ticket is broken — Fix: rename spec file to match ticket, or update ticket section references in the spec file header comment — Effort: S
- [MED] `exempt_content` has a Python-level bug: two conflicting `ExpressionAttributeValues` dicts in the same `update_item` call — `app/services/syndicate_open_licensing.py` — The `update_item` call sets `ExpressionAttributeValues={":e": True}` and then immediately overwrites it with `ExpressionAttributeValues={":e": True, ":c": creator_sub}` in the same expression; this likely causes a runtime error because Python dict literal assignment shadows the first — Fix: merge both values into a single `ExpressionAttributeValues` dict — Effort: S
- [LOW] Open licensing disable does not send member notifications in all code paths — `app/services/syndicate_open_licensing.py` — The `disable_open_licensing` function's member notification depends on calling `syndicate_svc.list_members` which itself does a DynamoDB query; if the syndicate has many members and the query paginates, only the first page of members is notified — Fix: loop `LastEvaluatedKey` in the notification fan-out — Effort: S
