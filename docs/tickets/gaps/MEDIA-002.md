- [HIGH] `/internal/dev-tools/ffmpeg-health` endpoint absent — `app/routers/internal_devtools.py:1` — no HTTP health-check surface for FFmpeg; devtools UI cannot display binary status; monitoring integrations have no structured signal for FFmpeg availability — Fix: add `GET /internal/dev-tools/ffmpeg-health` to `internal_devtools.py` calling `validate_ffmpeg()` from `ffmpeg_manager` — Effort: S
- [MED] four extended settings from ticket not added — `app/core/settings.py:1430` — `ffmpeg_required_version`, `ffmpeg_auto_download`, `ffmpeg_health_check_on_startup`, `ffmpeg_strict_validation` env-var settings are missing; operators cannot pin versions, auto-provision, or hard-fail startup on missing binary — Fix: add the four settings to `Settings` dataclass per section 3.7 — Effort: S
- [MED] `shutil.which("ffprobe")` callsite not migrated — `app/services/filemanager.py:1094` — `_extract_media_duration_poster` still guards with `shutil.which("ffprobe")` directly; should use `ffmpeg_manager` to stay consistent and benefit from centralized caching/validation — Fix: replace `shutil.which("ffprobe")` with `is_ffmpeg_available()` or derive ffprobe path from `get_ffmpeg_path()` — Effort: S
- [MED] SEC-012 cross-ref: no SSRF guard on remote media inputs — `app/services/ffmpeg_abr_pipeline.py:72-75` — `get_ffmpeg_path()` is wired but FFmpeg subprocess accepts arbitrary `-i` URLs; a crafted job could pass `rtsp://internal-host/` or `http://169.254.169.254/` as the input URL triggering SSRF via the FFmpeg process — Fix: validate media input URLs in `ffmpeg_abr_pipeline.py` against an allowlist of S3 paths or local tmp paths before passing to subprocess — Effort: M
- [MED] `ffmpeg` not added to `scripts/setup_ubuntu.sh` — `scripts/setup_ubuntu.sh:1` — setup script does not install FFmpeg; fresh hosts will silently miss all video features; `just status` gives no indication — Fix: add `ffmpeg` to the `apt-get install` line and add a post-install verify block per section 4.5 — Effort: S
- [LOW] `dev.sh status` has no FFmpeg indicator — `scripts/dev.sh:1` — `just status` output shows 7 service rows but omits FFmpeg version/availability; operators have no quick-glance signal — Fix: add FFmpeg version check to `cmd_status()` per section 3.4 — Effort: S
- [LOW] Lambda layer packaging script absent — `scripts/lambda/build-ffmpeg-layer.sh` — file specified in section 4.1 does not exist; Lambda-based transcode workers (VOD-003) have no documented packaging path — Fix: create `scripts/lambda/build-ffmpeg-layer.sh` per section 3.6 — Effort: S
