- [MED] `PlanBrowser` "Subscribe" button fires API call without auth check — `frontend/src/pages/subscriptions/PlanBrowser.tsx:61-63` — `handleSubscribe()` calls `subscribeMutation.mutate(planId)` with no `isAuthenticated` guard; unauthenticated visitors on `/u/{identifier}` who click "Subscribe" receive a raw 401 API error and a generic toast ("Failed to subscribe") rather than being redirected to `/login`; the ticket acceptance criterion (US-4/US-7) explicitly requires redirect to login for unauth — Fix: add `if (!isAuthenticated) { navigate("/login") ; return; }` at the top of `handleSubscribe`, or pass an `onUnauthSubscribe` prop from `PublicUserProfilePage` — Effort: S
- [MED] Repost fan-out in profile page context uses uncapped `_get_all_follower_ids` (SOC-002 overlap note) — `app/routers/newsfeed.py:6140-6163` — the repost fan-out at line 6140 calls `_get_all_follower_ids(user_id)` which is capped at `MAX_SYNC_FOLLOWERS=500` (inherited from `SOC-002/newsfeed_fanout.py:21`); creators with >500 followers displayed via the storefront can repost a post and only 500 followers receive it; this is a pre-existing cap but the storefront increases its visibility as a creator surface — Fix: see SOC-002 gap for async queue solution; low urgency for storefront — Effort: M
- [LOW] Stats row `follower_count` / `following_count` clickable buttons have no navigation — `frontend/src/pages/profile/PublicUserProfilePage.tsx` — the Followers/Following tabs exist (`frontend/src/pages/profile/FollowersTab.tsx`, `FollowingTab.tsx`) but the stats row numbers (lines 159-161) are plain `<span>` elements; the ticket spec §3.6 AC says "N followers / N following should be shown" — this is met — but the companion SOC-005 implementation plan described stat-row buttons that navigate to the followers/following tabs; the current implementation shows them as non-interactive text — Fix: wrap the counts in `<button onClick={() => setDefaultTab("followers")}>` to jump to the corresponding tab; low UX impact since tabs are accessible directly — Effort: S
- [LOW] No E2E test for the unauthenticated Subscribe CTA redirect — `frontend/e2e/creator-storefront.spec.ts` — section 120 test 16 ("Subscribe button prompts login for unauthenticated") is included in the spec file but the actual PlanBrowser has no auth redirect (see HIGH gap above); if the E2E suite runs before the fix is applied, test 16 will fail, masking the gap — Fix: implement the auth guard (see HIGH gap), then verify test 16 passes — Effort: S (blocked on MED fix above)
