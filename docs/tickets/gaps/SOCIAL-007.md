# SOCIAL-007: Snooze Following — Gap Report

- [MED] Snooze button is absent from `PublicUserProfilePage` — `frontend/src/pages/profile/PublicUserProfilePage.tsx` (362 lines) has no import of `snoozeFollowing`/`unsnoozeFollowing`/`SnoozeDurationPicker`; snooze is only accessible via `FollowingTab` (the viewer's own following list) — ticket section 9 and 13.2 require a "Snooze" option in the profile page `MoreOptionsDropdown` alongside Follow/Unfollow; users who reach a creator's profile have no snooze surface — Fix: import `SnoozeDurationPicker` and the snooze API in `PublicUserProfilePage`; add a `DropdownMenu` with snooze option when the viewer follows the profile user — Effort: S

- [MED] Notification suppression for snoozed followings is not implemented — `app/services/social_alerts.py` and `app/services/newsfeed_fanout.py` contain no `is_snoozed`/`get_snoozed_following_ids` call (grep returns 0 matches in both files); new-post alerts and other follow-triggered notifications are dispatched to snoozed followers — ticket sections 3.3, 5.4, and 13.3 (section 325a tests) require suppression — Fix: in the notification fan-out path, load `get_snoozed_following_ids` for the poster or check `is_snoozed(follower_id, poster_id)` before writing an alert record — Effort: M

- [LOW] `SnoozeBadge` ("Snoozed until…") does not appear on `PublicUserProfilePage` for currently-snoozed users — same root cause as first gap above; no `getBlockStatus`/snooze-status query in `PublicUserProfilePage`; users cannot see their active snooze from the profile view — Fix: query `GET /ui/social/status/{user_id}` (which returns `is_snoozed` via the `FollowingOut` model) and render the badge when `is_snoozed === true` — Effort: S
