- [HIGH] Background review-checker and re-screening loops are never started — `app/main.py` — The ticket spec requires `kyc_monitoring_scheduler.py` background loops registered via `app.add_event_handler("startup", ...)`. Neither `kyc_monitoring_scheduler.py` nor any equivalent startup hook exists; overdue reviews are never auto-detected and tier downgrades never fire without manual admin API calls — Fix: create `app/services/kyc_monitoring_scheduler.py` with `_kyc_review_checker_loop` / `_kyc_rescreening_loop` asyncio tasks and register via `app.add_event_handler("startup", kyc_monitoring_startup)` in `app/main.py` — Effort: M
- [HIGH] Profile country/name change and large billing transactions never create trigger events — `app/routers/profile.py`, `app/routers/billing.py` — The ticket integration points (`create_trigger_event` calls after profile nationality/name updates and after payments exceeding `kyc_large_transaction_threshold_cents`) are absent from both routers; the monitoring system is effectively blind to these events — Fix: add `create_trigger_event(user_sub, "country_change", ...)` in the profile update handler and `create_trigger_event(user_sub, "large_transaction", ...)` in the billing payment handler — Effort: M
- [MED] `_auto_downgrade` imports `kyc_tiers.upgrade_tier` at call time; if KYC-009 is not deployed the import fails and the downgrade is silently lost — `app/services/kyc_monitoring.py:346` — A deferred `ImportError` during the background review-checker run will raise an unhandled exception; the checker logs a warning and skips remaining items, meaning other overdue cases in the same batch are also not processed — Fix: wrap the `kyc_tiers` import in try/except and log a CRIT-level error if unavailable; alternatively assert the import at startup — Effort: S
- [MED] `run_rescreening` GSI query on `ByNextReviewDate` uses `Key("gsi_status_pk").eq("active")` with no SK condition, which performs a full GSI partition scan every 24 hours — `app/services/kyc_monitoring.py:445-449` — On a large user base this will consume many RCUs; no pagination loop (`LastEvaluatedKey`) is implemented, silently truncating results to the first `Limit=500` users — Fix: add `LastEvaluatedKey` pagination loop analogous to `_query_all` in kyc_compliance_reports; also honour `needs_review` status for re-screening — Effort: M
- [LOW] `KycMonitoringPage.tsx` admin dashboard shows only the raw API JSON with no table or timeline visualisation — `frontend/src/pages/admin/KycMonitoringPage.tsx:37-68` — The ticket spec calls for upcoming-reviews table, overdue-reviews table with severity badges, and trigger-event log timeline; the current page fetches the data but renders minimal UI — Fix: build the full dashboard layout with sortable DataTables and a timeline component — Effort: L
