# PLATFORM-015 gaps — Drag-and-Drop Everywhere

- [LOW] No dedicated SortableShelf component — `ProductShelfManager.tsx` reuses the generic `SortableList` component rather than the dedicated `SortableShelf` designed in §5.4; this means no `DragOverlay` for product cards and no undo-reorder toast — Fix: extract a `SortableShelf.tsx` wrapper that adds `DragOverlay` + undo toast as per §3.6, §3.10 — Effort: S
- [LOW] UploadZone still uses sequential upload — `frontend/src/pages/files/UploadZone.tsx` uploads files one at a time in a `for` loop; parallel batch upload endpoint exists at `/v1/fs/batch-upload` but UploadZone does not use it — Fix: replace sequential loop with `handleFilesParallel` using batch-upload or `Promise.allSettled` concurrency cap — Effort: S
- [LOW] FileTable drag-to-folder only handles `onDragLeave` and `onDrop`, no `onDragEnter`/`onDragOver` — `frontend/src/pages/files/FileTable.tsx:287-300` sets `draggable`, `onDragLeave`, `onDrop` on rows but never sets `onDragEnter` or `onDragOver(e.preventDefault)`, so `dropEffect` is not set and some browsers silently discard the drop — Fix: add `onDragEnter`/`onDragOver` with `preventDefault` and `dropEffect = "move"` to the folder-row props — Effort: S
