- [HIGH] Overlap with FEED-009 (post bookmarks): same feature, separate tickets — see `docs/tickets/gaps/FEED-009.md` for all implementation gaps; SOCIAL-001 and FEED-009 describe the same bookmark/collections system and share the same codebase (`app/routers/newsfeed.py:6277-6470`, `frontend/src/pages/saved/SavedPage.tsx`); gaps already catalogued in FEED-009.md apply equally here
- [MED] Video bookmark preview returns empty object — `app/routers/newsfeed.py:6406-6422` — `list_bookmarks()` enriches posts via `_post_to_dict` inside a `if ct == "post"` block but has no `elif ct == "video"` branch; bookmarked videos produce `content_preview: {}`, so the SavedPage renders "[No content]" as snippet and shows no thumbnail or author for video items — Fix: add an `elif ct == "video"` branch that fetches the video record from the videos table and extracts `title`, `thumbnail_url`, `creator_id`, `view_count` for the preview — Effort: S
- [MED] `author_display_name` field in post bookmark preview is incorrectly set to `author_id` — `app/routers/newsfeed.py:6416` — `"author_display_name": post_dict.get("author_id", "")` copies the user_sub string into the display name field; the SavedPage shows an opaque UUID instead of the creator's display name — Fix: replace with a profile lookup `get_profile_identity(post_dict["author_id"]).get("display_name", author_id)` — Effort: S
- [MED] Video bookmark button missing from video cards throughout the UI — `frontend/src/` — `BookmarkButton` is integrated into `PostCard.tsx:236-253` for posts, but no video card component (gallery card, video detail page) shows a bookmark toggle; the SOCIAL-001 ticket US-2 requires bookmarking from the gallery and video detail page — Fix: add `BookmarkButton` with `content_type="video"` to `VideoCard.tsx` and the video detail page — Effort: S
- [LOW] See also FEED-009.md: `is_bookmarked` not in feed responses, no move-between-collections endpoint, collection sidebar missing from SavedPage, bookmark_count not decremented on delete
