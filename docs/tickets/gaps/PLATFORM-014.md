# PLATFORM-014 gaps — Keyboard Shortcuts

- [MED] No usePageShortcuts hook — page-level shortcut registration is not implemented; navigation chords (g+m etc.) work globally but no page can register scoped `n`/`j`/`k`/`r` shortcuts — Fix: implement `usePageShortcuts` hook + `ShortcutRegistryContext` as designed in §3.2–3.3 of ticket — Effort: M
- [MED] No ShortcutRegistry singleton — there is no priority-ordered registry; page shortcuts cannot override global shortcuts for conflicting keys — Fix: implement `ShortcutRegistry` class at `frontend/src/hooks/useGlobalShortcuts.ts` — Effort: M
- [MED] No KeybindingEditor component — `frontend/src/pages/settings/KeybindingEditor.tsx` does not exist; users cannot remap shortcuts — Fix: create `KeybindingEditor` page and add route to Settings — Effort: M
- [MED] Customizable keybindings not persisted — no `customKeybindings` in any store, no PATCH to `/ui/settings/preferences`, `custom_keybindings` validation in models not wired to endpoint — Fix: add `customKeybindings` to a settings store and wire to preferences PATCH — Effort: M
- [LOW] ShortcutHelpDialog does not display chord shortcuts — `formatKey()` at `frontend/src/components/shared/ShortcutHelpDialog.tsx:12` handles `+` separators but not `,` chord notation; Navigation group remains empty in the overlay — Fix: add `,` split branch to `formatKey` and populate Navigation group from chord mappings — Effort: S
- [LOW] No dedicated ChordIndicator component — chord indicator is inlined in `Header.tsx:859-876` rather than a reusable `ChordIndicator.tsx`; ChordIndicator is a shared component per design — Fix: extract to `frontend/src/components/shared/ChordIndicator.tsx` — Effort: S
- [LOW] NavigationChords missing g+b, g+d, g+h, g+a, g+p, g+k mappings — `Header.tsx:249-261` only defines 8 chords out of the 12 designed; Go to Billing, Dashboard, Shop, Alerts, Profile, Security are absent — Fix: add missing chord entries in `Header.tsx` navigationChords memo — Effort: S
