- [MED] VideoReviewQueuePage not refactored to use shared hook — `frontend/src/pages/admin/VideoReviewQueuePage.tsx:66,192-206,256-284` — Inline `selectedIds`, `toggleSelect`, `selectAll`, `deselectAll`, and floating bar duplicate logic already extracted into `useMultiSelect`/`BulkActionBar`; bug fixes to the shared primitives won't benefit the queue — Fix: Replace inline state at lines 66, 192-206, 256-284 with `useMultiSelect(items.map(v=>v.video_id))` and `<BulkActionBar>` — Effort: S
- [MED] NewsFeed has no "Manage Posts" bulk-delete UI — `frontend/src/pages/feed/NewsFeed.tsx` — `bulkDeletePosts` and `bulkArchivePosts` API wrappers exist in `newsfeed.ts:227-231` and backend endpoints exist at `app/routers/newsfeed.py:6597,6625`, but no manage-mode toggle, checkboxes, or `BulkActionBar` in `NewsFeed.tsx` — Fix: Add `manageMode` state, `useMultiSelect`, per-card checkboxes, and `BulkActionBar` with confirm dialog — Effort: M
- [LOW] Shift+Click range selection not wired into AdminCatalog checkbox onClick — `frontend/src/pages/shop/AdminCatalog.tsx:29` — `AdminCatalog` imports `useMultiSelect` and `BulkActionBar` but review needed to confirm `handleClick` (range select) is passed to checkboxes instead of plain `toggle` — Fix: Verify each catalog item checkbox uses `handleClick(id, event)` not `toggle(id)` — Effort: S
