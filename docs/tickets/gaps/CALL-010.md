# CALL-010 Gaps

- [HIGH] `RecordingsPanel.tsx` frontend component not implemented — `frontend/src/pages/messages/RecordingsPanel.tsx` does not exist — post-call recording discovery relies entirely on ephemeral upload toast; users who miss the toast have no UI path to find recordings — Fix: create `RecordingsPanel` component querying `GET /messages/recordings?conversation_id={id}` per §4.1 — Effort: M
- [MED] No `call_recording_available` timeline message (compound with CALL-009 gap 3.1) — `complete_recording_upload` does not insert a system message — no in-conversation anchor for a download button even once `RecordingsPanel` exists — Fix: handled by CALL-009 §4.1 — Effort: S
- [MED] Double concurrent recording creation race condition — `app/services/call_recording_store.py:117` `create_recording()` has no guard against two simultaneous `/recording/request` calls; both can pass the `get_active_recording_for_call()` check before either is written, creating two `pending_consent` records and potentially two simultaneous recordings — Fix: add conditional `put_item` or pre-check guard per §4.3 — Effort: S
- [LOW] Recordings list endpoint has 50-item cap with no pagination — `app/routers/call_recording.py:467` slices to `[:50]`; users with many recordings silently lose older entries — Fix: add cursor-based pagination per `app/core/cursor.py` pattern — Effort: M
- [LOW] `useCallRecording.ts` AbortController usage unverified — `useCallRecording.ts:270-302` upload path should use `AbortController` to guard state updates on unmount; if absent, React may log warnings when user navigates away during upload — Fix: audit and add `if (mounted)` guards or `AbortController` per §4.4 — Effort: S
