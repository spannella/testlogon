- [HIGH] update_creator_ad_settings is full PutItem, not partial update — `app/services/creator_ad_prefs.py:update_creator_ad_settings` — PATCH with only allow_ads=false silently clears all other stored preferences — Fix: replace PutItem with UpdateExpression SET using only non-None fields — Effort: S
- [HIGH] allowed_ad_categories enforcement missing in serving engine — `app/services/ad_serving.py` — creators setting allowed_ad_categories still receive ads from non-matching categories — Fix: read allowed_ad_categories from creator_settings in serve_ad campaign loop and skip non-matching campaigns — Effort: S
- [MED] is_advertiser_blocked called per-campaign per serve request — `app/services/ad_serving.py:serve_ad` — N DDB GetItem calls per request with many active campaigns — Fix: pre-fetch list_blocked_advertisers once per serve call, check set membership in loop — Effort: S
- [LOW] audience estimate is a mock formula with no relation to actual user counts — `app/services/ad_targeting.py:estimate_audience` — estimated reach numbers are not meaningful for advertiser planning — Fix: query DDB user-count aggregates in a future iteration (acceptable for MVP) — Effort: L
