- [LOW] `beforeunload` warning for in-progress uploads not implemented — `frontend/src/pages/videos/VideosPage.tsx` — Spec requires a `beforeunload` event handler that warns users when navigating away during an active upload; no such handler is present, risking silent upload abandonment on accidental navigation — Fix: Add `useEffect` that registers `window.addEventListener("beforeunload", handler)` when any upload is in `uploading` or `presigning` state — Effort: S
- [LOW] Progress event dispatch not throttled — `frontend/src/pages/videos/VideosPage.tsx` — The XHR `upload.onprogress` callback dispatches reducer actions on every browser progress event; on fast connections this can fire hundreds of times per second causing excessive React re-renders — Fix: Add a timestamp guard (`if (Date.now() - lastDispatch < 100) return`) before dispatching `SET_PROGRESS` — Effort: S
