# CALL-013 Gaps

- [MED] Remote video cropped during screen share — `CallSessionOverlay.tsx:101` `VideoRenderer` uses `object-cover` which crops screen content; text, slides, and code are cut off when aspect ratio differs from video element size — Fix: add `objectFit` prop to `VideoRenderer`; pass `objectFit={remoteIsScreenSharing ? "contain" : "cover"}` from overlay per §4.1 — Effort: S
- [LOW] `contentHint` not set on screen track — `frontend/src/lib/webrtc.ts:54` `acquireScreenMedia()` does not set `videoTrack.contentHint = "detail"` — encoder uses motion-optimized profile; text legibility reduced at low bitrates — Fix: add `if (videoTrack) videoTrack.contentHint = "detail"` before return per §4.2 — Effort: S
- [LOW] Group overlay screen share blocked by CALL-012 — `GroupCallOverlay.tsx:243` `toggleScreenShare` calls `mediaMut.mutate()` but without `useGroupCall` hook the `sender.replaceTrack()` call cannot execute; screen share state persists in DDB but media never actually switches — Fix: implement `useGroupCall.ts` (CALL-012) to unblock this path — Effort: M (blocked on CALL-012)
- [LOW] No unit tests for signaling validation of screen-share types — `test_screen_share_signal_allowed_in_connected_state`, `test_screen_share_signal_rejected_in_invited_state`, `_check_screen_share_conflict` direct test do not exist — Fix: add 4-5 pytest cases to existing test file per §5.1 — Effort: S
