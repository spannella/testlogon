- [MED] Expiry background tasks never wired to any execution path — `app/services/license_agreements.py:699` (`process_expiry_alerts`) and line 748 (`process_expired_agreements`) are fully implemented but are not called from `app/main.py` startup, any router endpoint, or any cron/scheduler — agreements silently expire without creator alerts or compliance flagging, undermining the core system guarantee from ticket §7 — Fix: add an admin-only `POST /ui/admin/licenses/run-expiry` endpoint in `app/routers/license_agreements.py` that calls both functions, or wire them to the platform job scheduler — Effort: S
- [LOW] No rate limiting on agreement upload — ticket §7.4 specifies max 20 uploads per user per hour but `app/routers/license_agreements.py:76` (upload endpoint) has no per-user rate-limit guard — a malicious creator could flood S3 and DDB with junk agreement files — Fix: add a request-rate check (using the existing in-memory bucket pattern from `broadcast_chat_store.py` or a DDB-backed counter) before the S3 upload call — Effort: S
- [LOW] No E2E test spec for license agreements — ticket specifies `frontend/e2e/license-agreements.spec.ts` with 16 tests across sections 463–466 but the file does not exist — Fix: create spec per ticket §6 covering upload, metadata CRUD, content linking, admin review API — Effort: M
