- [HIGH] ad_feedback records accumulate without TTL — `app/services/ad_feedback.py:record_ad_feedback` — at scale (10M users × 20 hides) billing table grows to 200M+ items with no cleanup — Fix: add expires_at = now_ts() + 90*86400 to every feedback item; enable TTL on billing table — Effort: S
- [MED] _inject_sponsored_posts is synchronous blocking in async endpoint — `app/routers/newsfeed.py:_inject_sponsored_posts` — up to 3 sequential serve_ad() DDB call chains block the event loop on GET /feed — Fix: use asyncio.gather with asyncio.to_thread to parallelize injection slots — Effort: S
- [MED] allow_ads_near=False not reliably persisted for posts created with that value — `app/routers/newsfeed.py:_post_to_dict` — if PutItem filters None values, allow_ads_near=False may not be stored; defaults to True on re-read — Fix: explicitly write allow_ads_near field in post creation regardless of value — Effort: S
- [LOW] reactions on sponsored posts silently fail with non-existent synthetic post_id — `app/routers/newsfeed.py:reactions endpoint` — sponsored_{creative_id}_{position} IDs are not in T.newsfeed; DDB write fails silently — Fix: detect sponsored_ prefix in reactions endpoint and route to ad engagement tracker — Effort: S
- [LOW] comments_enabled=False not enforced at comments endpoint — `app/routers/newsfeed.py` — client could POST comment to synthetic sponsored post_id; fails with 404 but logs noisy errors — Fix: add check for sponsored_ prefix in comments endpoint — Effort: S

## Second-pass verification (2026-06-05)

- [Confirmed] ad_feedback records accumulate without TTL — `app/services/ad_feedback.py:36–44` confirmed: no `expires_at` field written — gap still open (HIGH)
- [Corrected] "_inject_sponsored_posts is synchronous blocking in async endpoint" — WRONG; `GET /feed` handler is `def view_feed` (synchronous, not `async def`) at `app/routers/newsfeed.py:4934`. Blocking DDB calls in a sync FastAPI handler do not block the event loop; they block only the thread pool thread. The performance concern is still real but the framing (event-loop blocking) is incorrect. Downgrade to LOW.
- [Corrected] "allow_ads_near=False not reliably persisted" — FALSE; `allow_ads_near` is `bool = Field(default=True)` in `CreatePostRequest` (`newsfeed.py:1420`), always a bool, never filtered out. Written unconditionally to `post_item["allow_ads_near"]` at line 3581 and persisted via `ddb_put_item` (no None filtering). Remove this gap — it does not exist.
- [Confirmed] reactions on sponsored posts silently fail — synthetic `post_id` like `sponsored_{creative_id}_{position}` not in `T.newsfeed` — gap still open (LOW)
- [Confirmed] comments endpoint has no `sponsored_` prefix check — gap still open (LOW)
