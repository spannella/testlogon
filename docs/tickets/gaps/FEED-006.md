- [MED] no "Hide" option in PostCard overflow menu — `frontend/src/pages/feed/PostCard.tsx` — PostCard has no EyeOff/Hide dropdown item; users cannot hide posts from the feed UI (backend `hide_post` endpoint at `app/routers/newsfeed.py:4739` exists, frontend `postHide.ts` exists, HiddenPostsPage exists, but the trigger is absent from PostCard) — Fix: add `hidePost` mutation + EyeOff DropdownMenuItem for non-own posts in PostCard's overflow/action area — Effort: S
- [LOW] no undo toast after hide — `frontend/src/pages/feed/PostCard.tsx` — ticket requires 10 s undo toast with "Undo" button after hiding a post; no such toast is rendered — Fix: call `toast()` with duration=10000 and an Undo action that calls `unhidePost` + `queryClient.invalidateQueries(["feed"])` inside the hide mutation's `onSuccess` — Effort: S
- [LOW] no optimistic removal from feed cache on hide — `frontend/src/pages/feed/PostCard.tsx` — ticket requires instant optimistic removal of hidden post from feed; the hide mutation has no `onMutate` cache update — Fix: add `onMutate` handler that snapshots and filters the React Query `["feed"]` cache, restoring on error — Effort: S
