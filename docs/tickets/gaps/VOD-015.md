- [HIGH] Clip job exception never calls fail_job — `app/services/video_clipper.py:305` — on any exception the job stays in `running` state forever; the worker cannot retry or surface the error — Fix: catch exception, call `fail_job(job_id, str(e), attempt)` before re-raise — Effort: S
- [MED] Missing `video_clip_timeout_seconds` and `video_clip_max_duration_seconds` settings — `app/core/settings.py:1541-1543` — ticket specifies `VIDEO_CLIP_TIMEOUT_SECONDS` and `VIDEO_CLIP_MAX_DURATION_SECONDS` env vars but only `video_clip_max_concurrent` is present; timeout falls back to hardcoded `600` via `getattr(S, "video_clip_timeout_seconds", 600)` — Fix: add the two settings fields to `Settings` — Effort: S
- [MED] Clip endpoint returns 400 (not 409) for unpublished video — `app/routers/video_listing.py:863-864` — ticket specifies 409 for this condition; returns 400 instead — Fix: change `status_code=400` to `status_code=409` for the status-check branch — Effort: S
- [MED] No `-protocol_whitelist` on ffmpeg clip command — `app/services/video_clipper.py:331-347` — SEC-012 requires restricting ffmpeg input protocols; the `-i source_path` invocation has no `allowed_extensions`/`protocol_whitelist` flag, leaving a path open if source_s3_key is ever attacker-controlled — Fix: add `-protocol_whitelist file` before `-i` for local-file inputs per SEC-012 recommendation — Effort: S
- [LOW] `video_clip_copy_tolerance_seconds` setting absent — `app/core/settings.py:1541-1543` — tolerance is hardcoded as `2.0` in `execute_clip()` rather than read from settings — Fix: add `video_clip_copy_tolerance_seconds: float = float(os.environ.get("VIDEO_CLIP_COPY_TOLERANCE_SECONDS", "2.0"))` to Settings and use it in `execute_clip` — Effort: S
