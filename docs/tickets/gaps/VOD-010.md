- [HIGH] tenant_id absent from HKDF key derivation — `app/services/vod_drm_keys.py:57-78` — two tenants sharing the same video_id (e.g. re-upload) receive identical AES-128 keys; cross-tenant key reuse is a confidentiality violation — Fix: include tenant_id in HKDF salt or info string (salt = sha256(f"{tenant_id}|{asset_id}")) and require tenant_id query param in GET /v1/vod/drm/key/{key_id} validated against the entitlement token claim — Effort: M
- [HIGH] tenant_id not validated at key serve time — `app/routers/vod_drm.py:67-71` — endpoint validates asset_id in token vs query param but never checks tenant_id claim; attacker can supply their own tenant_id in the URL while using a valid token for a different tenant — Fix: extract and compare token tenant_id against a required tenant_id query parameter (matches spec §6.1) — Effort: S
- [HIGH] admin key revocation endpoints missing — `app/routers/vod_drm.py` — spec §4.3 and §6.5-6.7 require POST /drm/keys/revoke and POST /drm/keys/{asset_id}/revoke-all (admin-only); neither exists; compromised keys cannot be invalidated without redeploying — Fix: add admin endpoints with require_admin_session dependency; write to a ContentKeys DynamoDB table revocation record — Effort: M
- [MED] ContentKeys DynamoDB table not created — `scripts/local-ddb-init.py` — spec §4.2 defines ContentKeys table (key_id PK, ByAssetCreatedAt GSI, ByTenantCreatedAt GSI) for revocation metadata and audit trail; table is absent from local-ddb-init.py so revocation checks and audit queries have no backing store — Fix: add TableDef for ContentKeys with attr_types={"created_at": "N"} to local-ddb-init.py and app/core/settings.py — Effort: S
- [MED] key rotation slot is always 0 at serve time — `app/routers/vod_drm.py:88` — derive_content_key is called with key_slot=0 only; spec §4.5 requires time-based slot (now // drm_key_rotation_seconds) so keys rotate on a schedule; current implementation derives the same key forever, providing no rotation protection — Fix: compute current key_slot from now_ts() // S.drm_key_rotation_seconds and pass it to derive_content_key/derive_key_id; validate_key_id must accept both current and previous slot to handle player requests during rotation boundaries — Effort: M
- [MED] CORS header Access-Control-Allow-Origin: * missing on key endpoint — `app/routers/vod_drm.py:96-103` — spec §4.3 requires CORS header on GET /v1/vod/drm/key/{key_id} because HLS.js fetches keys cross-origin from the CDN domain; global CORSMiddleware in main.py may not cover this path with the required cache-control combination — Fix: add explicit Access-Control-Allow-Origin: * to the Response headers in get_decryption_key() — Effort: S
- [MED] no per-video DRM guard in key endpoint — `app/routers/vod_drm.py:33-103` — endpoint serves keys for any asset_id even if that video has drm_enabled=False; spec §7 error row 11 requires 400 DRM_NOT_ENABLED if the video record has drm_policy_id unset — Fix: fetch video record and check video.drm_enabled before deriving key; return 400 if DRM is not configured for that asset — Effort: S
- [LOW] vod_encryption.py not integrated into transcode_worker path for DRM-enabled jobs — `app/services/transcode_worker.py:276-288` — encryption is gated by rendition.get("drm_enabled") but transcode job records never populate _drm_enabled on rendition dicts; as a result no video ever gets encrypted segments regardless of the video.drm_enabled flag — Fix: read video.drm_enabled and video.drm_policy_id from the video record before building rendition list and set drm_enabled on each rendition dict — Effort: S
