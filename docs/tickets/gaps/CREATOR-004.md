- [HIGH] conversion attribution chain broken — `app/services/commerce_order_service.py:39` (create_order does not read afl_ref cookie or call record_conversion) — no conversions are ever attributed regardless of clicks; affiliate commission is never earned — Fix: extract afl_ref cookie in checkout endpoint and call record_conversion on order completion — Effort: S
- [HIGH] afl_ref cookie set with Secure=True in dev — `app/routers/affiliate_links.py:~174` (no dev_mode guard on cookie security flag) — cookie silently dropped by browser in HTTP dev environment; redirect flow untestable locally — Fix: set secure=not S.dev_mode when setting afl_ref cookie matching mock billing pattern — Effort: S
- [MED] _get_affiliate_settings helper absent — `app/services/affiliate_links.py` (function referenced in record_conversion but not implemented) — per-product and per-affiliate rate overrides cannot work; falls back to hardcoded 1000 bps — Fix: implement _get_affiliate_settings reading SETTINGS item from affiliate_links table — Effort: S
- [MED] commission settings CRUD endpoints missing — `app/routers/affiliate_links.py` (no /settings routes) — creators cannot configure default commission rate, per-product overrides, or affiliate_enabled toggle via API — Fix: add GET/PATCH /ui/affiliates/settings and PUT/DELETE /ui/affiliates/settings/products/{item_id} — Effort: M
- [MED] cross-creator affiliate permission check absent — `app/services/affiliate_links.py:63` (create_affiliate_link has no affiliate_enabled check) — any creator can generate a link to any product regardless of product owner's preferences — Fix: if product_owner_id != affiliate_user_id, call _get_affiliate_settings and check affiliate_enabled — Effort: S
- [LOW] performance dashboard endpoints missing — `app/routers/affiliate_links.py` (no /dashboard, /clicks, /daily-stats routes) — creators have no time-series analytics on click and conversion performance — Fix: add GET /ui/affiliates/dashboard and per-link clicks/conversions/daily-stats endpoints — Effort: M
- [LOW] refund clawback not implemented — `app/services/affiliate_links.py` (no refund hook) — commission is never reversed on order refund — Fix: add commission reversal call in billing refund service — Effort: M
- [LOW] affiliate earnings category missing — `app/services/creator_earnings.py:36` (no "affiliate" branch in classify_entry) — affiliate commissions fall into "other" in creator dashboard breakdown — Fix: add "if 'affiliate' in reason: return 'affiliate'" before return "other" — Effort: S
- [LOW] frontend settings panel and analytics components absent — `frontend/src/pages/affiliates/AffiliateDashboard.tsx` (no AffiliateSettingsPanel, ClickChart, ConversionLog, QRCodeDisplay, CommissionRateEditor) — commission settings and performance analytics not surfaced in UI — Fix: implement missing components after backend settings/analytics endpoints are added — Effort: L
