- [UNBUILT] feature not implemented — see writeups/BOT-004.md
- [HIGH] SSRF via creator-supplied `provider_url` — `app/services/bot_ai.py` (to be created) — `ConfigureAiIn.provider_url` accepts any URL; without validation a creator could probe `169.254.169.254` or RFC-1918 addresses via the LLM proxy — Fix: validate URL against a private-IP blocklist in `configure_ai_bot()` mirroring `app/services/webhook_ssrf.py` — Effort: S
- [HIGH] Prompt injection via user message role confusion — `app/services/bot_ai.py` (to be created) — LLM message array must strictly place user input in the `user` role; checking forbidden topics only on input misses injection that causes the LLM output to violate restrictions — Fix: run `_check_forbidden_topics()` on both user message and LLM output before delivering response — Effort: S
- [HIGH] API key ciphertext must never appear in API responses — `app/services/bot_ai.py` (to be created) — `GET /ui/bots/{bot_id}/ai-config` must return `api_key_masked` (last 3 chars), not the KMS ciphertext, per SEC-022 pattern — Fix: `AiConfigOut` excludes `ai_api_key_encrypted`; service returns `api_key_masked = "..."+key[-3:]` — Effort: S
