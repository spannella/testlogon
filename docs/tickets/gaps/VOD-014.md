- [HIGH] frontend UI for VOD bridge entirely absent — `frontend/src/pages/files/FileTable.tsx`, `frontend/src/api/endpoints/vod.ts`, `frontend/src/api/types.ts` — spec §4.1-4.3 and §4 Phase 2 require: vod_* fields on FileEntry type, importFileToVod/getVodBridgeStatus API functions in vod.ts, "Watch" and "Send to VOD" actions in FileTable.tsx, import dialog in FilesPage.tsx, and "In Files" badge in VideosPage.tsx; none of these exist — Fix: add FileEntry.vod_* fields to types.ts, add bridge API functions, add VOD context menu items to FileTable.tsx, and add "In Files" badge to video cards — Effort: L
- [MED] bridge import endpoint only accepts file path but no title/visibility validation feedback — `app/routers/vod_bridge.py:40-44` — the POST /ui/vod-bridge/import endpoint passes title and visibility to import_file_to_vod but does not validate that visibility is one of the allowed enum values at the Pydantic layer (the Literal["private", "unlisted", "public"] annotation is correct) — this is actually fine; no gap — Effort: S
- [MED] vod-file-bridge.spec.ts E2E tests verify only backend API; no UI section — `frontend/e2e/vod-file-bridge.spec.ts:558` — spec §6.2 section 3 requires E2E tests for "Watch" action navigation, "Send to VOD" dialog submission, and vod_status display in FileManager UI; implemented spec covers only sections 122-123 (API import/status/unlink); UI interactions (sections 3.1-3.3 from spec) are absent because the frontend UI is not built — Fix: once frontend UI (gap above) is built, add section 124 covering Watch navigation and Send-to-VOD dialog — Effort: M
- [LOW] unlink does not clear vod_video_id / vod_linked on the DynamoDB file node — `app/services/vod_file_bridge.py:136-153` — unlink_video_from_filemanager calls _update_video_source_file_node to clear source_file_node_id on the video record but does not update the DynamoDB file node to clear vod_video_id, vod_linked, and vod_* fields; after unlink the file node still shows as linked to VOD — Fix: add a DynamoDB UpdateItem call in unlink_video_from_filemanager to remove vod_video_id, vod_linked, vod_status, vod_hls_manifest_url from the file node — Effort: S
- [LOW] video_share_playback_token_ttl_seconds setting undeclared (same as VOD-013) — `app/core/settings.py` — getattr fallback used in messaging.py; the setting should be declared in the VOD section of settings.py alongside vod_drm_enabled and related keys — Fix: add video_share_playback_token_ttl_seconds: int field to Settings — Effort: S
