# MSG-003 gaps — Typing Indicators Real-time Push

- [MED] `message:new` event handler does not clear the sender's typing entry from the cache — `frontend/src/hooks/useMessagingStream.ts:32` — when a message arrives via SSE the typing indicator for its sender is not removed until the 5-second client-side TTL expires, so "Alice is typing" lingers for up to 5 seconds after her message appears — Fix: inside the `message:new` branch, call `queryClient.setQueryData(["typing", conversationId], (old) => (old ?? []).filter(t => t.user_id !== data.sender_id))` — Effort: S

- [LOW] no E2E test file for typing real-time — the ticket specifies `frontend/e2e/typing-realtime.spec.ts` (9 tests) but it is not present in the test suite — Fix: create the spec covering sub-1s indicator appearance, TTL expiry, multi-typer display, and message-send clearing (9 tests as designed) — Effort: M
