- [HIGH] `_next_run_from_cron()` is a stub — `app/services/bot_scheduler.py:40` — returns `now_ts() + 3600` for every cron expression regardless of configured schedule; a `0 14 * * *` daily job fires ~60 min after creation, not at 14:00 — Fix: add `croniter` to `requirements.txt` and replace stub with real cron-next calculation — Effort: S
- [HIGH] Wildcard target dispatch silently skips sends — `app/services/bot_scheduler.py:276` — `if sched["target_type"] == "conversation"` guard means `all_dms`, `all_groups`, `all_broadcasts` sends are fetched from GSI but immediately dropped; `dispatched` counter is never incremented for them — Fix: implement `_resolve_target_conversations()` to fan out wildcard targets (cap at 500) — Effort: M
- [HIGH] `start_bot_scheduler_task` may raise RuntimeError at startup — `app/main.py:687` — `asyncio.create_task()` called from a synchronous `add_event_handler` startup callable may fail with `RuntimeError: no running event loop` in some uvicorn configurations — Fix: convert to `async def` and register with `@app.on_event("startup")` — Effort: S
- [MED] `quick_replies` not populated in `_message_out_from_item()` — `app/routers/messaging.py:4162` — `quick_replies` field declared on `MessageOut` at line 2496 but never passed in the constructor; bot messages with quick-reply buttons render without buttons — Fix: add `quick_replies=merged_item.get("quick_replies") or None` to the `return MessageOut(...)` call — Effort: S
- [MED] `{subscriber_status}` and `{conversation_name}` variable stubs — `app/services/bot_template.py:283,286` — always return `"none"` and `"Conversation"` respectively; templates using these variables silently produce wrong output — Fix: resolve `{subscriber_status}` via `T.subscriptions` query; `{conversation_name}` via conversations table — Effort: S
- [LOW] A/B group lookup scans all templates in Python — `app/services/bot_template.py:347` — `list_templates(bot_id=bot_id)` fetches all templates then filters by `ab_group` in Python; bounded at 50 templates but a DDB `FilterExpression` would be cleaner — Fix: add `FilterExpression=Attr("ab_group").eq(group_name)` to the query — Effort: S
- [LOW] Missing frontend template editor and E2E tests — no `TemplateEditorPage`, `TemplateFormDialog`, `ScheduleManagerPanel`; no `frontend/e2e/bot-templates.spec.ts` — template and schedule management is inaccessible via UI and untested at browser level — Fix: implement frontend components and sections 511-514 E2E spec — Effort: L
- [LOW] `preview` and `send-test` router endpoints absent — `app/routers/bot_template.py` — template editor UI requires `POST /ui/bots/{bot_id}/templates/{template_id}/preview` and `.../send-test` but only CRUD endpoints exist — Fix: add both endpoints to `bot_template.py` router — Effort: S
