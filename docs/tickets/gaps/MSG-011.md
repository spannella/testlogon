# MSG-011: Emoji Reactions Enhancement — Gap Report

- [MED] Reaction details endpoint uses N+1 DynamoDB/profile fetches — `app/routers/messaging.py:10970-10975` — `get_profile_identity(sub)` called once per unique reacting user in a serial loop; 20 emojis × 50 users = up to 1000 sequential GetItem calls on the hot path — Fix: replace with a single `BatchGetItem` call (≤100 keys per request, paginate if needed) against the users/profiles table — Effort: S

- [LOW] ReactIn emoji max_length is 32 — `app/routers/messaging.py:2255` — custom emoji shortcodes using the `custom:shortcode` pattern can be up to ~40+ chars; current 32-char limit silently truncates or rejects valid custom emoji reactions — Fix: raise `max_length` to 64 on the `ReactIn.emoji` field — Effort: S

- [LOW] No E2E test file for MSG-011 enhancements — `frontend/e2e/` — `emoji-reactions-enhanced.spec.ts` described in ticket (sections 326–329) does not exist; reaction limit, details popover, quick-react, and animation are uncovered by automated tests — Fix: create `frontend/e2e/emoji-reactions-enhanced.spec.ts` per ticket spec — Effort: M
