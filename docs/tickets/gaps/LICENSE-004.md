- [MED] `process_expired_requests` uses GSI4 on `issued_licenses` table but GSI4 is only defined for `attr_types={"GSI4SK": "N"}` with `expires_at` stored as an integer — `app/services/license_requests.py:415` — If `expires_at` is written as a Decimal (from DynamoDB round-trip), GSI4 numeric comparison `Key("GSI4SK").lt(ts)` will fail with ValidationException; should coerce `expires_at` to `int` before write to avoid type mismatch — Fix: add explicit `int()` cast on `expires_at` before storing the GSI4SK attribute in `create_request` — Effort: S
- [MED] `_check_no_duplicate_request` does a full scan of the requester's sent requests to find duplicates — `app/services/license_requests.py:579` — Query returns all REQ_SENT# items for the user (no Limit), then filters for matching `content_id` in Python; for a requester with many sent requests this is O(n) reads; a GSI or item-based existence check would be more efficient — Fix: query GSI2 `CONTENT_STATUS#{content_id}#pending` (already defined on the table) to check for existing requests by this requester, or store a dedup key — Effort: M
- [MED] `_update_request_status` for terminal statuses uses `REMOVE GSI4PK, GSI4SK` in UpdateExpression but does not update `GSI2PK` — `app/services/license_requests.py:497` — `GSI2PK` stores `CONTENT_STATUS#{content_id}#{status}`; when status transitions to `approved`/`denied`/`withdrawn`/`expired`, the GSI2 still indexes the item under the old status — Fix: add `#gsi2pk = :gsi2pk` to the UpdateExpression and update `CONTENT_STATUS#{content_id}#{new_status}` on every status change — Effort: S
- [LOW] No background scheduler registration for `process_expired_requests` — `app/services/license_requests.py:413` — The function exists and correctly queries GSI4, but is never called from a cron or startup task; requests never expire automatically — Fix: register `process_expired_requests` in the dev scheduler (same pattern as other background tasks in `app/main.py` startup) — Effort: S
- [LOW] Counter-offer allows owner to overwrite previous counter_terms without any notification about the previous round — `app/services/license_requests.py:205` — When `status` is already `negotiating`, a second call to `counter_offer` replaces `counter_terms` silently; the requester only sees the latest counter without a history; the ticket's "multi-round negotiation" design would benefit from an append-only counter_history — Fix: append to a `counter_history` list or at minimum include the previous counter in the alert detail — Effort: S
