- [MED] availability submission has no rate limit — `app/routers/newsfeed.py:4318` (submit_find_datetime_post_availability) — the ticket spec requires 10 submissions per user per poll per hour but no rate-limiting is implemented; a user can spam updates causing repeated DDB writes — Fix: track submission count in cache or DDB and reject with 429 after threshold — Effort: M
- [LOW] get_post_find_datetime and close endpoint do not paginate the calendar table query — `app/routers/newsfeed.py:4285` and `app/routers/newsfeed.py:4399` — T.calendar.query with no Limit reads up to 1MB; for polls with many participants a second page of availability records may be silently dropped, skewing overlap computation — Fix: add LastEvaluatedKey pagination loop (same pattern used elsewhere) in both the GET detail endpoint and the close/compute endpoint — Effort: S
