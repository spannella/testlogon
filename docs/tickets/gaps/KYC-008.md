- [HIGH] Auto-escalate does not write escalation flag to case — `app/services/kyc_risk_scoring.py` (`_apply_auto_action()`, lines 528–573) — `"auto_escalated"` is returned and logged but `review.escalated=True`, `review.escalation_reason`, and `review.escalated_at` are never written to the case META; admin queue shows no escalation indicator — Fix: add DDB `update_item` writing `review.escalated`, `review.escalation_reason`, `review.escalated_at` in the critical-tier branch — Effort: S
- [HIGH] Submission hook calling `compute_score()` unverified — `app/routers/kyc_cases.py:830` (`submit_kyc_case`) — ticket spec requires `compute_score()` to fire after screening; whether the call is stitched at line 830 is not confirmed — Fix: verify hook exists; if absent, add `KycRiskScoringService().compute_score(...)` in try/except after KYC-006 screening call — Effort: S
- [MED] `attr_types={"timestamp":"N"}` must be present on `kyc_risk_scores` table — `scripts/local-ddb-init.py` — SK `timestamp` is numeric; missing `attr_types` causes DynamoDB to store it as String, breaking `ScanIndexForward` queries and `case-timestamp-index` GSI — Fix: verify `attr_types={"timestamp": "N"}` in the `TableDef`; add if missing — Effort: S
- [MED] Score collision risk in multi-worker production — `app/services/kyc_risk_scoring.py:31` (`_unique_sort_key()`) — microsecond sort key uses a process-local thread lock; two uvicorn workers scoring the same user simultaneously could collide at microsecond precision — Fix: for production readiness, use composite SK `{timestamp_us}#{score_id}` (breaking schema change; acceptable to defer until `--workers 1` constraint is lifted) — Effort: M
- [MED] `get_tier_distribution()` performs full table scan — `app/services/kyc_risk_scoring.py:201` — in production with millions of rows, scanning `kyc_risk_scores` to count `auto_approved`/`auto_escalated` is expensive and may time out — Fix: use a separate counter table or DynamoDB aggregation; document as known production-scale gap — Effort: M
- [MED] `document_quality` factor reads only `files[].verification_state` — `app/services/kyc_risk_scoring.py` (`_compute_document_quality`) — KYC-002 extraction confidence in `kyc_document_extractions` table is not consulted; quality score is imprecise — Fix: query `kyc_document_extractions` for the case and use `overall_confidence` (high/medium/low) to weight the factor — Effort: S
- [LOW] `screening_result` factor returns 40 when KYC-006 not integrated — `app/services/kyc_risk_scoring.py` (`_compute_screening_result`) — reads `case["screening"]["status"]`; if KYC-006 screening hook has not fired, field is absent and factor returns `"not_conducted"` score 40 — Fix: ensure KYC-006 submission hook fires before `compute_score()` is called (ordering dependency documented) — Effort: S
