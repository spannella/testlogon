- [HIGH] `ByGlobalStatus` GSI never added to `ec2_instances` or `k8s_pods` — `scripts/local-ddb-init.py:1773,1850` ��� `ec2_instances` has `ByStatus` (user_sub PK) and `ByCreatedAt` (user_sub PK) GSIs only; `k8s_pods` similarly; neither has a global-status GSI with `status` as PK for cross-user queries; `admin_compute.py:62-75` falls back to a full-table `scan()` for cross-user instance listing; at scale this is costly and will miss items beyond the first 1 MB page if not paginated correctly — Fix: add `ByGlobalStatus` GSI with `partition_key="status"`, `sort_key="created_at"` to both tables in `local-ddb-init.py` and update `list_all_instances()` / `list_all_pods()` to use `query()` on the GSI — Effort: M
- [HIGH] quota not enforced in `k8s_launcher.py` — `app/services/k8s_launcher.py:1` — `ec2_launcher.py` calls `enforce_ec2_quota()` (line 165); the equivalent call is absent in `k8s_launcher.py`; users can launch unlimited K8s pods regardless of `max_k8s_pods` quota — Fix: add `enforce_k8s_quota(user_sub, preset)` call in `k8s_launcher.py` matching the pattern in `ec2_launcher.py` — Effort: S
- [MED] spending aggregation uses full-table scan without pagination loop — `app/services/admin_compute.py:268-329` — `_scan_month_ticks()` calls `table.scan()` once and reads `response.get("Items", [])`; DynamoDB scan returns at most 1 MB per call; a busy compute_billing table will silently truncate results after the first page — Fix: add `LastEvaluatedKey` loop in `_scan_month_ticks()` matching the pattern used elsewhere in the codebase — Effort: S
- [MED] `force_terminate_instance` does not validate that the instance belongs to `user_sub` — `app/services/admin_compute.py:150-185` — the function calls `_user_terminate_ec2(user_sub, instance_id)` which queries by `user_sub` PK; if an admin passes an incorrect `user_sub` the call silently returns not-found rather than 404; no pre-fetch validates ownership before termination — Fix: call `get_instance(user_sub, instance_id)` first and return 404 if not found before attempting termination — Effort: S
- [MED] `AdminComputeDashboard` route missing from `App.tsx` — `frontend/src/App.tsx:1` — the ticket requires `<Route path="/admin/compute" element={<AdminComputeDashboard />} />`; `AdminComputeDashboard.tsx` exists under `frontend/src/pages/admin/` but the corresponding route may not be registered; the admin sidebar "Compute" nav item must also exist ��� Fix: verify route registration and add `<Route>` entry plus sidebar item guarded by `role >= ADMIN` — Effort: S
- [LOW] platform-wide spending query has no caching — `app/services/admin_compute.py:285-325` — the ticket design specifies a 60-second server-side cache for platform spending; currently every dashboard load triggers a full scan; for a large compute_billing table this will be slow — Fix: add an in-process TTL cache (e.g., a module-level dict with timestamp guard) around `get_platform_spending()` — Effort: S
- [LOW] admin compute feature flag always-on — `app/routers/admin_compute.py:36-42` — `_check_enabled()` checks `S.admin_compute_dashboard_enabled`; the setting exists but the rollout plan specified `ADMIN_COMPUTE_DASHBOARD_ENABLED=false` as the initial deploy state; `.env.local.example` should default to `false` until Phase 4 — Fix: set `ADMIN_COMPUTE_DASHBOARD_ENABLED=false` in `.env.local.example` and add to rollout checklist — Effort: S
