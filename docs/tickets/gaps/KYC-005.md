- [HIGH] Submission expiry background task missing — `app/main.py` (no task wired; `expires_at` stored but never acted on in `app/services/kyc_proof_of_funds.py`) — verified submissions from over a year ago remain in `verified` status indefinitely; a stale submission continues to satisfy any readiness gate check — Fix: add `expire_stale_submissions()` to the service and wire as a periodic asyncio task in `app/main.py` every 6 hours — Effort: S
- [HIGH] Admin reviewer queue page missing — `frontend/src/pages/kyc/KycProofOfFundsReviewQueue.tsx` (does not exist) — reviewers cannot list, view, or adjudicate submissions via the UI; route not registered in App.tsx — Fix: create page with status filter, DataTable, and adjudicate dialog; add `admin/kyc/proof-of-funds` route — Effort: M
- [HIGH] FinancialVerificationPanel missing from admin case detail — `frontend/src/pages/admin/KycCaseDetailPage.tsx` (depends on KYC-001) — reviewer cannot see score, declared amount, risk contribution, or adjudicate from the case view — Fix: add "Financial Verification" tab fetching `GET /ui/kyc/proof-of-funds/admin/submissions?user_sub=` — Effort: S
- [MED] Suspicious amount threshold hard-coded — `app/services/kyc_proof_of_funds.py` (`_SUSPICIOUS_AMOUNT_CENTS` module constant) — operators cannot tune the threshold without a code deploy; $10M is far above the $10K AML filing threshold — Fix: add `kyc_proof_of_funds_suspicious_amount_cents` to `app/core/settings.py` and replace the constant with `S.kyc_proof_of_funds_suspicious_amount_cents` — Effort: S
- [MED] Reviewer override not audited — `app/routers/kyc_proof_of_funds.py` (`adjudicate` endpoint) — ticket spec requires writing `reviewer_sub`, `original_decision`, `new_decision`, `reviewer_note` to the audit log; verify `adjudicate_submission()` fires an audit event — Fix: confirm or add `audit_event(...)` call inside `adjudicate_submission` — Effort: S
- [MED] Inline Pydantic models not consolidated — `app/routers/kyc_proof_of_funds.py` (`CreateProofOfFundsSubmissionIn`, `AdjudicateIn`) — models defined inline rather than in `app/models.py`; inconsistent with project conventions, harder to reuse — Fix: move models to `app/models.py` — Effort: S
- [MED] `kyc_proof_of_funds` records not covered by purge — `app/routers/kyc_cases.py` (purge endpoint) — GDPR erasure path deletes `kyc_cases` records but not financial declaration PII in `kyc_proof_of_funds` table — Fix: add companion `kyc_proof_of_funds` purge by `user_sub` inside the existing purge handler — Effort: S
- [LOW] Readiness gate not yet extended for proof-of-funds — `app/routers/kyc_cases.py:244` (`_readiness_for_case`) — enhanced/high_risk profiles can submit without a verified POF submission; gate exists for residency (KYC-004) but not for POF — Fix: add `proof_of_funds` check gated by `KYC_POF_READINESS_GATE_ENABLED` flag after data collection period — Effort: S
- [LOW] Risk flag granularity lost — `app/services/kyc_proof_of_funds.py` (`_deterministic_score`) — original spec required named risk flags; implementation uses a single numeric score; reviewers see no per-flag breakdown — Fix: document as known trade-off; revisit when KYC-008 risk scoring integration is mature — Effort: M
