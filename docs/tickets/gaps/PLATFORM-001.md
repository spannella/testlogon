- [HIGH] IP spoofing via untrusted X-Forwarded-For header — `app/core/normalize.py:9` — `client_ip_from_request()` blindly trusts `X-Forwarded-For` without validating the direct connection against `TRUSTED_PROXY_CIDRS`; attacker can send `X-Forwarded-For: 10.0.0.1` to appear as an internal/allowlisted IP; Layer 1 middleware is ineffective until this is fixed — Fix: only honour XFF when `request.client.host` falls within configured `TRUSTED_PROXY_CIDRS`; default to empty (direct IP only) in dev — Effort: S
- [MED] Layer 3 rate-limit functions missing `Retry-After` header on 429 — `app/services/rate_limit.py:17` — nine of eleven rate-limit functions raise bare `HTTPException(429)` with no `Retry-After` header; only `rate_limit_profile_lookup` (line 205) and `rate_limit_feed_query` (line 232) include it; non-conformance with RFC 6585 — Fix: add `headers={"Retry-After": str(window_end - now_ts())}` to all `HTTPException(429)` raises in `rate_limit.py` — Effort: M
- [MED] Allowlist CIDR scan has O(n) in-memory check with cold-start exposure — `app/services/rate_limit_config.py` — 60-second in-memory cache mitigates hot paths but the first request in each window performs a full DDB scan; at thousands of allowlist entries this is slow — Fix: pre-sort CIDRs into a prefix tree (e.g., `ipaddress.ip_network` sorted list) and use binary search; or limit allowlist to 100 entries — Effort: M
- [LOW] `ByStatus` GSI on rate_limits table will hot-partition at high volume — `scripts/local-ddb-init.py` — all pending rate-limit records share the same GSI partition key; at very high request volume this creates a DDB hot partition — Fix: shard the status key (e.g., `pending#0`…`pending#9`) in a future iteration — Effort: M
