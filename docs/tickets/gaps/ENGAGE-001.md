- [HIGH] Admin advance-progress endpoint open to any authenticated user — `app/routers/achievements.py:278` — `POST /ui/achievements/admin/advance` uses `require_ui_session` instead of `require_root_session`, allowing any user to freely award themselves arbitrary achievement points and unlock any achievement — Fix: change `Depends(require_ui_session)` to `Depends(require_root_session)` — Effort: S
- [HIGH] Achievement progress hooks never wired into action handlers — `app/routers/newsfeed.py`, `app/routers/messaging.py`, `app/routers/broadcast.py` (no `advance_progress` call in any) — achievements_enabled flag is false by default and none of the listed integration points (post creation, tip, broadcast start, subscribe, comment, follow, unlock, reaction, encrypted msg, calendar share) call `advance_progress` or `update_streak`, so no achievement ever auto-unlocks except via the admin endpoint — Fix: add try/except advance_progress calls at each listed integration point (see ticket section 3.7) — Effort: L
- [MED] Badge flair never injected into DM or broadcast chat output — `app/routers/messaging.py` `_message_out_from_item` (line 3766), `app/services/broadcast_chat_store.py` `_chat_msg_out` (line 344) — `sender_badges` field described in design is absent from both serializers; badge flair UI feature is completely non-functional — Fix: in each output function call `get_cached_badges(sender_id)` and add `sender_badges` field — Effort: S
- [MED] Leaderboard entries never populate `display_name` — `app/services/achievement_leaderboard.py:41` — `_update_leaderboard` in `achievement_progress.py` never writes `display_name` to the leaderboard item; all leaderboard entries return `display_name: ""` — Fix: resolve display name from profile at unlock time and store it in the leaderboard update expression — Effort: S
- [MED] `list_definitions` uses unbounded `Table.scan()` without pagination — `app/services/achievement_definitions.py:129` — a scan with no `Limit` reads the entire achievements table per call (up to DDB's 1 MB limit); in production with many definitions this silently truncates results — Fix: loop with `LastEvaluatedKey` or add a reasonable `Limit` + pagination cursor — Effort: S
- [MED] `_get_user_achievement_ids` and `get_user_achievements` lack pagination — `app/services/achievement_progress.py:204,212` — both call `T.user_achievements.query()` without looping on `LastEvaluatedKey`; a power user with >400 achievements (each ~1 KB) would hit the 1 MB DDB page limit and lose achievements from threshold checks �� Fix: loop until no `LastEvaluatedKey` — Effort: S
- [LOW] `get_my_rank` count query uses `.gt(user_points)` for rank computation — `app/services/achievement_leaderboard.py:78` — ties (multiple users with the same `total_points`) are not counted, so rank can be under-reported by the number of tied peers; also the `count_resp` query itself has no pagination loop and could miss users if DDB returns a `LastEvaluatedKey` — Fix: use `Key("GSI1SK").gte(user_points)` and subtract 1 for the user's own entry; loop on `LastEvaluatedKey` — Effort: S
- [LOW] `update_definition` filters `active=False` silently via `v is not None` — `app/services/achievement_definitions.py:82` — the filter is `{k: v for k, v in updates.items() if k in allowed_fields and v is not None}`, so `active=False` passes (False is not None), meaning the `delete_definition` soft-delete works correctly; however if someone passes `active=None` explicitly it is silently dropped — Fix: change condition to `if k in allowed_fields and v is not None` is already fine for bool False; no action needed — Effort: S
