- [HIGH] app credentials injection not implemented — `app/services/agent_stylist.py:721` — update_stylist_config has no app_auth_credentials_secret field; no mechanism to inject live-app auth for real Playwright browsing — Fix: add app_auth_credentials_secret_name to config; resolve via KMS at trigger time, expose only has_app_credentials: bool — Effort: M
- [MED] trigger_review rate limiting not enforced — `app/routers/agent_stylist.py` — no rate limit on POST /trigger-review; spec requires 5/hour/user — Fix: add in-memory or DDB-backed rate limiter checking _TRIGGER_COUNTS per user — Effort: S
- [MED] data-testid name mismatch — `frontend/src/pages/agents/StylistDesignOverviewPage.tsx` — spec E2E tests reference design-overview-page/review-detail-page/design-rules-page but implementation uses Stylist-prefixed names; E2E selectors will fail if names differ — Fix: audit all data-testid values in Stylist pages against spec and align — Effort: S
- [MED] tailwind_config_path default points to non-existent file — `app/services/agent_stylist.py` — _DEFAULT_STYLIST_CONFIG defaults tailwind_config_path to frontend/tailwind.config.js which does not exist (Tailwind v4 uses CSS config) — Fix: change default to frontend/src/globals.css — Effort: S
- [MED] get_page_scores loads all reviews into memory — `app/services/agent_stylist.py:413` — _iter_all_reviews scans full USER#{user_id} partition; 1000+ reviews expensive — Fix: add SCORE# summary item per page updated at create_review time; get_page_scores queries begins_with(sk,'SCORE#') — Effort: M
- [LOW] _page_hash uses MD5 — `app/services/agent_stylist.py:183` — hashlib.md5 used as URL-to-key mapping; triggers security scanners despite being non-security-sensitive — Fix: replace with SHA-256[:16] — Effort: S
- [LOW] no concurrent trigger prevention — `app/services/agent_stylist.py:777` — two simultaneous trigger_review requests for same user/page both execute; unlike PM Agent there is no in-flight lock — Fix: add DDB-backed RUNNING_REVIEW lock per user similar to PM Agent distributed lock fix — Effort: S
