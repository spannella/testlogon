- [HIGH] double-write claim race — `app/services/agent_orchestrator.py:255` — claim_ticket writes AGENT_CLAIM# then TICKET#META in sequence; second write failure leaves orphaned claim record that permanently blocks the ticket — Fix: wrap both writes in TransactWriteItems — Effort: M
- [CRIT] agent loop not running as background task — `app/services/agent_orchestrator.py:705` — start_agent_loop sets DDB state but spawns no asyncio coroutine; agent never autonomously picks up tickets — Fix: add _running_loops registry, create asyncio.create_task(run_agent_loop(...)) in start_agent_loop — Effort: L
- [HIGH] inject_ticket_context prompt injection — `app/services/agent_orchestrator.py:598` — ticket title/description interpolated directly into context string; adversarial [AGENT_COMPLETE] in ticket content triggers false completion signal — Fix: apply _sanitize_ticket_field to escape signal tokens before interpolation — Effort: S
- [MED] ticket filter validation gap — `app/models.py:6174` — TicketFilterConfig.types is List[str] with no enum validation; malformed filter silently matches nothing — Fix: validate against ticket type enum (bug/feature/task/chore) — Effort: S
- [MED] stale heartbeat scan covers all users — `app/services/agent_orchestrator.py:567` — check_stale_heartbeats queries GSI without per-user partition filter; full-table scan every 60s in multi-tenant prod — Fix: add ByHeartbeat GSI (PK=STATUS#{worker_status}, SK=heartbeat_at N) for efficient range queries — Effort: M
- [MED] checkpoint JSON size limit not enforced — `app/services/agent_orchestrator.py:469` — save_checkpoint stores json.dumps(checkpoint_data) without size check; DDB 400KB limit causes silent ValidationException on large diffs — Fix: add max-size check before write, raise descriptive error — Effort: S
