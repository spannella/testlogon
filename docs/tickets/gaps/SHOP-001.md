- [HIGH] Low-stock alert has no throttle/dedup — `app/routers/catalog.py:611-634` — every stock adjustment that crosses the threshold fires `write_alert` unconditionally; on high-velocity items (e.g., 1000 purchases/hour) this generates 1000 alerts; ticket specified a 1-hour TTL sentinel to prevent duplicates but it was not implemented — Fix: add a sentinel check (DDB `put_item` with `ConditionExpression=attribute_not_exists` + TTL) before calling `write_alert` — Effort: S; cross-ref SEC-024
- [HIGH] `adjust_stock` uses a full-table scan to find item by `item_id` — `app/routers/catalog.py:636-654` (`_find_item_by_id`) — `PATCH /ui/catalog/items/{item_id}/stock` resolves the DDB key via a full scan on the catalog table; O(n) cost grows with catalog size and will cause latency/RCU exhaustion in production — Fix: change route to `PATCH /ui/catalog/categories/{category_id}/items/{item_id}/stock` to accept the PK directly, or add a `ByItemId` GSI on the catalog table — Effort: S; cross-ref SEC-024
- [MED] Cart stock-decrement in `purchase_cart` does not compensate other items on partial failure — `app/services/shoppingcart.py:497-530` — if item N of a multi-item cart is out of stock, items 1..N-1 have already been decremented; the function raises 409 but does not re-increment them; the ticket specified a `_rollback_stock_decrements` helper which was not implemented — Fix: collect decremented items in a list and call a compensating `ADD stock_count :qty` for each on `ConditionalCheckFailedException` — Effort: S; cross-ref SEC-024
- [LOW] `stock_updated_at` field is set to an ISO string (`now_iso()`) in the catalog router but the `CatalogItemOut` Pydantic model declares it as `Optional[str]` with no format validation — `app/models.py:571` — inconsistent typing could cause parsing failures in strict TypeScript clients — Fix: document format or normalize to Unix timestamp — Effort: S
