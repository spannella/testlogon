# PLATFORM-009: CSV Export — Gap Report

- [HIGH] No rate limiting on `GET /ui/export/csv` (SEC-007 cross-ref) — `app/routers/csv_export.py:22-101` — Any authenticated user can hammer the endpoint to bulk-exfiltrate contacts and billing data; each call triggers full DDB pagination (up to 50,000 rows × multiple tables). Ticket section 9.5 specifies 5 exports/minute/user via the existing rate-limit middleware, but no `can_send_alert_channel` or token-bucket call was wired in — Fix: add a call to `_check_rate_limit(user_sub, "csv_export", max_n=5, window=60)` (or reuse the existing `rate_limit.py` bucket pattern) at the top of `export_csv()` and raise HTTP 429 when exceeded — Effort: S

- [MED] `billing_ledger` ExportCsvButton passes only `source` without `from_date`/`to_date` — `frontend/src/pages/billing/Ledger.tsx:187-192` — The `ExportCsvButton` is wired with `source="billing_ledger"` but the surrounding component's `startDate`/`endDate` state variables (used for client-side filter) are not forwarded as `from_date`/`to_date` params. Ticket section 4.4 explicitly specifies passing these date range params to the server-side export — Fix: pass `from_date: startDate ? Math.floor(new Date(startDate).getTime()/1000) : undefined` and `to_date` equivalently in `Ledger.tsx` `ExportCsvButton` props — Effort: S

- [LOW] No E2E test coverage for questionnaire_responses CSV export ownership check — `frontend/e2e/csv-export.spec.ts` — The spec file exists and covers billing/contacts API tests, but section for questionnaire_responses non-owner 403 path (ticket section 8.2 item 6) is absent — Fix: add test that Alice requests `?source=questionnaire_responses&questionnaire_id=<bob_q_id>` and asserts 403 — Effort: S
