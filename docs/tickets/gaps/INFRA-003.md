- [HIGH] Real EC2 launch path raises `NotImplementedError` — `app/services/ec2_launcher.py:185` — when `S.ec2_mock_enabled = False` (i.e., in production with real AWS), `launch_instance()` hits `raise NotImplementedError("Real EC2 launch not implemented yet")`; same for `stop_instance` (line ~273), `start_instance` (~302), `terminate_instance` (~332), `reboot_instance` (~370) — dev/prod parity (SECOPS-007) is broken: mock mode works; prod mode is a 500 crash — Fix: implement `_real_ec2_launch()` using `boto3.client("ec2").run_instances(...)`, add EC2 client to `app/core/aws.py` following the existing `sns_client()` pattern — Effort: L

- [HIGH] Auto-registration in host inventory never happens — `app/services/ec2_launcher.py:196-202` — `launch_instance()` stores `host_id: ""` and never calls `create_host()` from `app/services/host_inventory`; the `host_id` field is always empty string; acceptance criterion #3 ("Launched instances auto-register in host inventory with `source: ec2_auto`") is unmet — Fix: after storing the instance, call `host_inventory.create_host(user_sub, label=f"{label} (EC2)", hostname=result["public_ip"], port=22, protocol="ssh", source="ec2_auto", group="EC2 Instances")` and write the returned `host_id` back to the DDB record — Effort: S

- [HIGH] Host inventory not cleaned up on instance termination — `app/services/ec2_launcher.py:320-357` — `terminate_instance()` updates DDB status to `"terminated"` but does not delete or soft-delete the linked host record from `host_inventory`; acceptance criterion #10 ("Terminated instances are cleaned up from host inventory") is unmet — Fix: retrieve `item["host_id"]`, call `host_inventory.delete_host(user_sub, host_id)` inside `terminate_instance()` — Effort: S

- [MED] No audit events emitted — `app/services/ec2_launcher.py` (0 `audit_event` calls throughout) — ticket spec requires `ec2.launch`, `ec2.stop`, `ec2.start`, `ec2.terminate`, `ec2.auto_terminate` audit events; only `logger.info` structured logs are present; no entries reach the audit trail or SECOPS-001 telemetry — Fix: import `audit_event` from `app.services.alerts` and call in `launch_instance`, `stop_instance`, `start_instance`, `terminate_instance`, `check_idle_instances` — Effort: S

- [MED] Idle instance background scan does not paginate — `app/services/ec2_launcher.py:394-418` — `check_idle_instances()` calls `T.ec2_instances.scan()` with no pagination loop; DynamoDB returns at most 1MB per call and sets `LastEvaluatedKey` when more items exist; instances beyond the first 1MB page are silently skipped and never auto-terminated (per CLAUDE.md "DDB FilterExpression doesn't reduce page size" gotcha) — Fix: add `while True: resp = ...; items.extend(...); if not resp.get("LastEvaluatedKey"): break; kwargs["ExclusiveStartKey"] = resp["LastEvaluatedKey"]` — Effort: S

- [MED] SSH key not injected into EC2 user-data — `app/services/ec2_launcher.py:174-182` — `launch_instance()` stores `ssh_key_id` in DDB but never fetches the public key via `ssh_key_manager.get_key_metadata()` or injects it into `user_data`; acceptance criterion #4 ("SSH keys from INFRA-002 can be injected at launch time") is unmet; launched instances cannot be accessed with a stored key — Fix: when `ssh_key_id` is set, call `get_key_metadata(user_sub, ssh_key_id)` and prepend the `_inject_ssh_key_userdata()` cloud-init stanza to `user_data` — SEC-022 cross-ref: only the public key is included in user-data, never the encrypted blob — Effort: S

- [LOW] `check_idle_instances` uses full table scan rather than GSI — `app/services/ec2_launcher.py:394` — the `ByStatus` GSI on the `ec2_instances` table allows efficient queries for `status=running`; the current scan reads all items; at scale this creates unnecessary DDB read costs and latency — Fix: switch to `T.ec2_instances.query(IndexName="ByStatus", KeyConditionExpression=...)` using a "platform" partition (requires a GSI design adjustment) or query per-user with a composite approach — Effort: M
