- [HIGH] live GitHub API not wired — `app/services/agent_pr_integration.py:380` — _create_pr_via_api raises NotImplementedError when method="api" and a real token is configured; production PR creation via API is blocked — Fix: implement httpx POST to GitHub REST API /repos/{owner}/{repo}/pulls with parsed owner/repo from repo_url — Effort: S
- [MED] _find_pr_by_url uses full table scan — `app/services/agent_pr_integration.py:664` — webhook handler finds PR by URL via T.agent_workers.scan(FilterExpression="pr_url=:url"); expensive at scale — Fix: add GSI on pr_url (PR_URL#{pr_url} PK) and replace scan with GSI query — Effort: M
- [LOW] complete_agent_work silently swallows memory add failure — `app/services/agent_pr_integration.py:569` — broad except Exception suppresses agent_memory.add_memory errors; learnings silently lost — Fix: log warning with exc_info on memory failure so ops can detect patterns — Effort: S
- [LOW] branch name from terminal output not sanitized before gh command — `app/services/agent_pr_integration.py:350` — detect_git_state extracts branch from regex but does not sanitize before interpolation into _build_gh_pr_command; null bytes or control chars passed through — Fix: strip non-printable characters from extracted branch name before shell escaping — Effort: S
