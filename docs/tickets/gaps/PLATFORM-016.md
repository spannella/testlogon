# PLATFORM-016 gaps — Web Push Delivery

- [HIGH] Auto-revoke fires on ALL web_push_send failures, not only 410 Gone — `app/services/push.py:300-312`: `send_push_for_alert` revokes the device whenever `success=False`; `web_push_send` returns `False` on 500-server-error, network timeout, and misconfiguration too — legitimate devices are silently deleted on transient errors — Fix: have `web_push_send` return a typed result distinguishing `(True, None)` / `(False, "410")` / `(False, "other")`; only revoke on `"410"` — Effort: S
- [MED] Production password verification for push registration is a `pass` placeholder in `app/routers/privacy.py:148-151` — the GDPR deletion endpoint also lives in that router and skips Cognito verification in prod; the newer `/ui/privacy/account-deletion/request` in `app/routers/account_deletion.py` correctly calls `verify_user_password`, but the old `POST /ui/delete-account` endpoint remains reachable with a no-op verification — Fix: route the old `DELETE /ui/delete-account` to the new router or add real Cognito verification — Effort: S
- [MED] No admin push delivery stats endpoint — email/SMS have `GET /admin/email/stats` and `GET /admin/sms/stats` backed by `get_delivery_stats`/`get_sms_delivery_stats`; there is no `GET /admin/push/stats` endpoint for monitoring push sent/failed/expired counts — Fix: add `get_push_delivery_stats()` service function and admin endpoint analogous to `admin_email.py:41` — Effort: M
- [LOW] `push_event_types` in `send_push_for_alert` uses exact string equality against user preferences — `app/services/push.py:271`; broadcast-specific alert type strings like `broadcast.started` are not enumerated in `ALERT_EVENT_TYPES` so they are silently stripped by `set_alert_prefs`'s allowlist filter — Fix: add broadcast and tip alert types to `ALERT_EVENT_TYPES` in alerts.py — Effort: S
