- [MED] `GET /ui/videos/{video_id}` returns 403 for private videos, leaking existence — `app/routers/video_listing.py:678` — non-owners receive 403 (not 404) for private/unlisted videos; callers can distinguish valid-but-private IDs from invalid IDs — Fix: return 404 for non-public videos accessed by non-owners, consistent with standard privacy patterns — Effort: S
- [MED] No `GET /ui/videos` owner library endpoint — `app/routers/video_listing.py` — ticket spec includes a clean `GET /ui/videos` returning the authenticated user's own videos across all statuses; no such route exists; creator library view relies on other routes — Fix: add `@router.get("")` endpoint before the `/{video_id}` catch-all, delegating to `list_videos_by_owner` — Effort: S
- [MED] Geo-access check triggers a second DynamoDB read on every non-owner detail request — `app/routers/video_listing.py:678` — when `video.geo_mode` is None, a second `get_item` fetches the raw DDB item to check for geo fields; doubles read cost for all non-owner video detail calls — Fix: add `geo_mode` and `geo_countries` fields to `VideoMetadataModel` to eliminate the extra read — Effort: M
- [LOW] Gallery feature-flag returns 404 instead of 403 — `app/routers/video_listing.py:466` — disabled gallery endpoints return 404 rather than 403; misleading to frontend developers and monitoring tools — Fix: return 403 with `"gallery not enabled"` detail when feature flag is off — Effort: S
- [LOW] `VideoUpdateIn` vs `UpdateVideoIn` naming is counter-intuitive — `app/models_video.py:137,183` — user-facing model is named `VideoUpdateIn`; system-facing is `UpdateVideoIn`; the more specific-sounding name is actually the user-facing one, risking wrong import — Fix: rename per VOD-001 recommendation (`UserVideoUpdateIn` / `SystemVideoUpdateIn`) — Effort: S
