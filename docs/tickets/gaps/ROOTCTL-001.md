- [CRIT] No break-glass secret or KMS gate on CLI mutations — `app/cli/rootctl.py:2006-2018` — anyone with DDB credentials + `--actor-sub` flag can run any mutation; ticket says "only a string-equality requires_root check" — Fix: add `_require_break_glass_auth()` gate reading `ROOTCTL_BREAK_GLASS_SECRET` env var (or KMS) before any write in `_dispatch_mutation_gate()` — Effort: M
- [HIGH] `rotate-secrets` is still a placeholder — `app/cli/rootctl.py:2134-2144` — command parses and audits but calls `_placeholder_mutation_command` with no-op return; signing secrets never rotated — Fix: implement real key rotation (re-generate `UI_ACCESS_TOKEN_SECRET`, `API_KEY_PEPPER`, KMS break-glass key) with audit trail — Effort: L
- [HIGH] No `user reactivate` or `user undelete` commands — `app/cli/rootctl.py:2218-2263` — `deactivate` and `delete` exist but their inverse operations require manual DDB repair; deactivated/soft-deleted users cannot be restored via CLI — Fix: add `reactivate` and `undelete` sub-parsers mirroring `deactivate`/`delete` guard patterns (`--ticket`, `--confirm`) — Effort: S
- [MED] `admin_capabilities` stored by rootctl but never enforced by backend API — `app/cli/rootctl.py:51-58` vs `app/auth/policy.py:116-164` — policy enforces `AdminScope` scopes but `capabilities` field is decorative; rootctl and API use two distinct permission systems (see ADMIN-PERMS-001) — Fix: converge capabilities onto `AdminScope` enum or enforce capabilities in `require_admin_scope` — Effort: M
- [MED] `_find_item_by_id` (used by adjust_stock) does a full-table scan — `app/routers/catalog.py:636-653` — O(n) scan on the catalog table for every stock adjustment; at scale causes latency and RCU exhaustion — Fix: store item PK/SK in a separate GSI by `item_id` (already done for catalog items), or use the route path `/categories/{cat_id}/items/{item_id}/stock` to avoid the scan — Effort: S
- [LOW] Root network gate enforced on `/auth/root/login` (`app/routers/root_auth.py:23`) but not on shared `/ui/session/finalize` (`app/routers/ui_session.py:170-189`) — a root challenge minted from an allowed IP could be finalized from another IP (tracked separately as ROOT-AUTH-001 `B3`) — Fix: call `enforce_root_network_gate` inside `maybe_finalize` when the challenge belongs to a root-role principal — Effort: S
