- [MED] Startup script content not sanitised — `app/models.py:12256`, `app/services/instance_templates.py:29-55` — `startup_script` is capped at 16 KB but only length-validated; a user could embed NUL bytes or non-UTF-8 sequences that break cloud-init parsing on the real launcher; content is returned verbatim in API responses — Fix: enforce UTF-8 codec at model layer; strip NUL bytes — Effort: S
- [LOW] `UpdateTemplateIn` accepts `category` PATCH but `target` (ec2/k8s) cannot be updated — `app/models.py:12263-12275`, `app/services/instance_templates.py:42-55` — changing the target type post-creation would invalidate `instance_type`/`ami_id` vs `k8s_image`/`k8s_preset` fields; no validation prevents a user from creating an EC2 template then patching `k8s_image` onto it — Fix: add cross-field validation in `update_template`: if `k8s_image` is set, verify stored `target="k8s"`; likewise for `instance_type`/`ami_id` — Effort: S
- [LOW] No rate limit on template creation — `app/services/instance_templates.py:306-344` — no per-user template count cap; a user can create arbitrarily many templates (DDB storage + list-templates scan cost); ticket spec implies "user templates" are bounded — Fix: add a configurable `S.instance_templates_max_per_user` limit and raise 409 when exceeded, similar to connection_profiles — Effort: S
