- [CRIT] Webhook `secret` is stored in plaintext in DynamoDB — `app/services/kyc_partner_api.py:543` — `register_webhook` writes `"secret": str(secret)` directly into the `kyc_cases` table item; anyone with DynamoDB read access (IAM, internal services, leaked credentials) can exfiltrate all partner webhook secrets; secrets are used for HMAC-SHA256 signing of outbound webhooks; SEC-022 / API key handling policy requires secrets to be encrypted at rest — Fix: encrypt the secret with KMS (reuse `app/core/crypto.py`'s `kms_encrypt` / `kms_decrypt`) before storing and decrypt on read in `_emit_callback`; alternatively store a PBKDF2-derived verification hash and require partners to rotate — Effort: M
- [HIGH] Per-API-key rate limiting is completely absent from all partner API endpoints — `app/routers/kyc_partner_api.py` (no `rate_limit_or_429` calls) — the ticket (§4.7) specifies 100/hour for `POST /applications`, 200/hour for `POST /documents`, 1000/hour for GET endpoints, and 10/hour for webhook test; the existing `rate_limit_or_429` infrastructure (`app/services/rate_limit.py`) supports `user_sub` + `factor` keys and is used throughout the platform; without limits a single partner can exhaust KYC case creation capacity or DynamoDB write throughput — Fix: add `rate_limit_or_429(user_sub=f"apikey:{api_key_id}", factor="kyc_api:applications")` etc. at the top of each endpoint handler — Effort: S
- [HIGH] Document file bytes are read from the multipart upload but never stored in S3 — `app/routers/kyc_partner_api.py:196-204` — `contents = await file.read()` reads the file bytes but `svc.upload_document()` receives only `file_type`, `filename`, and `size_bytes`; actual file content is discarded; the partner-facing document record has a `document_id` but no S3 key or retrievable content; this means document processing (OCR, selfie check) cannot be performed and the document attachment is effectively a no-op — Fix: upload `contents` to S3 under `kyc-api-docs/{partner_id}/{application_id}/{document_id}` and pass the resulting S3 key to `upload_document()`; store the key on the document dict and expose a download endpoint — Effort: M
- [MED] `register_webhook` error code `kyc_api_webhook_url_invalid` is reused for invalid event names — `app/services/kyc_partner_api.py:523-530` — when the `events` list contains unrecognized event names the service raises HTTP 400 with `code="kyc_api_webhook_url_invalid"`, which is misleading (the URL may be fine); partners relying on error codes for programmatic handling will be confused — Fix: use a distinct error code `kyc_api_invalid_webhook_events` for the invalid-events error path — Effort: S
- [MED] `sandbox_create_application` does not check for duplicate `external_id` within the sandbox partition — `app/services/kyc_partner_api.py:630-663` — the real `create_application` checks for a prior `EXT#{external_id}` pointer item and raises 409 on duplicate; the sandbox path skips this check; a partner calling sandbox create twice with the same `external_id` silently overwrites the existing sandbox record — Fix: apply the same `EXT#{external_id}` pointer check and 409 response in `sandbox_create_application` — Effort: S
- [LOW] `kyc:*` scopes are not registered in `app/services/api_key_route_scope_registry.py` — `app/services/api_key_route_scope_registry.py` (not verified as updated) ��� the ticket (§13) specifies modifying `api_key_route_scope_registry.py` to register `kyc:submit`, `kyc:read`, `kyc:upload`, `kyc:webhook`, `kyc:admin`; if these are missing from `CANONICAL_API_KEY_CAPABILITIES` then `expand_api_key_capabilities` will never return them and all scope checks in `require_scope()` will always fail with 403 — Fix: verify that `kyc:*` capability entries are present in `CANONICAL_API_KEY_CAPABILITIES` and `CAPABILITY_IMPLICATIONS` in `api_key_capabilities.py` — Effort: S
