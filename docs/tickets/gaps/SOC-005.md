- [MED] No rate limiting on `GET /profile/public/{identifier}` and `GET /profile/public/{identifier}/posts` — `app/routers/profile.py:298-390,391-510` — the original profile lookup endpoint at line ~80 calls `rate_limit_profile_lookup(requester_user_sub, client_ip_from_request(req))` but the two new public profile endpoints introduced by SOC-005 skip this call entirely; unauthenticated callers can enumerate all public profiles and scrape their posts without rate throttling — Fix: add `rate_limit_profile_lookup(viewer_sub, client_ip_from_request(req))` (or equivalent sliding-window check) at the top of both handlers after auth extraction — Effort: S
- [MED] Dual-query waterfall on every profile page load — `frontend/src/pages/profile/PublicUserProfilePage.tsx:42-63` — the page makes two sequential queries: `pubQ` (`GET /profile/public/{identifier}`) and a detail query (`GET /ui/profiles/{identifier}`); the detail query fires only after `pubQ` resolves (React Query `enabled: !!identifier` both fire concurrently, but the detail query adds a second round-trip for data already available in the public profile response); `is_followed_by` and `is_mutual` are returned by the public profile endpoint but never used in the component — Fix: unify into a single request; remove the secondary `getProfileByIdentifier` query or parallelize with `Promise.all` — Effort: S
- [LOW] `GET /profile/public/{identifier}/posts` over-fetches with `limit * 2` then truncates — `app/routers/profile.py:444` — the posts endpoint queries `Limit=limit * 2` to account for filtered posts, but with sparse visibility filters (e.g., `filter=locked`) this may still return fewer than `limit` items while a non-null `LastEvaluatedKey` causes the frontend to show a "Load more" button that returns nothing — Fix: loop via `LastEvaluatedKey` until `len(items) >= limit` or pages exhausted, capped at 4 pages per the CLAUDE.md filter-expression guidance — Effort: S
- [LOW] `profile_meta_tags` endpoint never rate-limited or cached — `app/routers/profile.py:511-545` — the lightweight SEO meta tags endpoint performs 2-3 DDB reads (resolve identifier + check discoverability + fetch profile) per request and has no rate limit; social crawlers may call it repeatedly — Fix: add a short-TTL in-process LRU cache (5 minutes) on the response keyed by identifier, and add `Cache-Control: public, max-age=300` response header — Effort: S
