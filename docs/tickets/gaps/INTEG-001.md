- [HIGH] "Connect Google Drive" UI section absent from `FilesPage.tsx` — `frontend/src/pages/files/FilesPage.tsx:1571` — `FilesPage.tsx` has zero references to Google Drive; the `GoogleDrivePickerDialog` component exists at `frontend/src/components/shared/GoogleDrivePickerDialog.tsx` and `googleDrive.ts` API client exists, but neither is imported or rendered; users have no way to connect, browse, or import from Google Drive in the UI — Fix: add the Google Drive integration section (connection badge, Connect/Disconnect/Browse buttons) to `FilesPage.tsx` as described in ticket section 4.6 and render `<GoogleDrivePickerDialog>` — Effort: M
- [HIGH] `GoogleDrivePickerDialog` not used anywhere in the frontend — `frontend/src/components/shared/GoogleDrivePickerDialog.tsx:84` — the component is defined and renders correctly but is never imported by any page; the E2E test at `frontend/e2e/drive-picker.spec.ts:201` confirms this with a conditional `if (visible)` guard and notes "may not be wired into the Files page toolbar yet" — Fix: import and render the dialog from `FilesPage.tsx` with `drivePickerOpen` state controlled by the Browse button — Effort: S
- [HIGH] OAuth state parameter not HMAC-signed — `app/routers/google_drive_integration.py:122-131` — the `initiate_google_drive_connect` endpoint returns a stub `auth_url` without generating or signing a `state` parameter; the ticket's acceptance criterion 12 requires HMAC-signed, time-limited state; in non-mock mode the `callback` endpoint does not validate `state` at all (line 155 raises 501); without signed state the OAuth flow is vulnerable to CSRF — Fix: implement `_sign_oauth_state(user_sub)` using `S.google_oauth_state_signing_secret` + timestamp, verify in `complete_google_drive_connect`, and reject stale states (> `google_oauth_state_ttl_seconds`) — Effort: M
- [HIGH] real OAuth token exchange not implemented — `app/routers/google_drive_integration.py:155` — `complete_google_drive_connect` raises `HTTP 501 "Real OAuth not implemented"` for any non-mock code; production OAuth exchange (`_exchange_code()`) is entirely absent; the integration only works with `GOOGLE_DRIVE_MOCK_ENABLED=true` — Fix: implement token exchange POST to `S.google_oauth_token_url` with code, redirect_uri, client_id, client_secret; store access_token, refresh_token, expires_at — Effort: M
- [MED] import endpoint has no file-size limit — `app/routers/google_drive_integration.py:248-292` — the download at line 250 uses `requests.get()` which buffers the entire file into memory as `download_resp.content`; a user can import a multi-GB file and exhaust server RAM; the ticket design mentioned a 500 MB limit and streaming for large files — Fix: stream the response with `stream=True` and enforce a `MAX_IMPORT_BYTES` cap (e.g., 500 MB) that raises 413 — Effort: M
- [MED] mount creation for Google Drive not exposed in UI — `app/routers/filemanager.py:3530` — the `POST /mounts` endpoint supports `provider="google_drive"` and `filemgr_google_drive_mounts_enabled=true`; the picker dialog has a "Mount" button stub, but `FilesPage.tsx` never renders the picker so mount creation is inaccessible; ticket US-4 acceptance criteria are unmet — Fix: after wiring the picker into FilesPage, wire the "Mount" button to call `createMount()` from the files API — Effort: S
- [LOW] E2E UI test is effectively a no-op — `frontend/e2e/drive-picker.spec.ts:199-214` — the UI section (test 80.9) uses `if (visible)` to skip all picker dialog assertions when the button is not found; it always passes regardless of whether the UI is wired — Fix: once the Connect button is added to FilesPage, remove the conditional guard and assert unconditionally — Effort: S
- [LOW] no E2E test for mount creation via picker — `frontend/e2e/drive-picker.spec.ts:1` ��� the spec tests status/connect/browse/import/disconnect API flows but has no section for mount creation; ticket acceptance criterion 10 requires mount functionality — Fix: add a section 80.10 that calls `POST /mounts` with `provider="google_drive"` and verifies the mount appears in `GET /mounts` — Effort: S
