- [HIGH] `concat -safe 0` allows arbitrary file:// entries in filelist.txt — `app/services/video_concatenator.py:338` — if the filelist path is writable or the scratch_dir is attacker-influenced, ffmpeg will read any local file via the concat demuxer; SEC-012 explicitly flags this pattern — Fix: use `-safe 1` and validate every entry is inside the owned scratch_dir before writing it to filelist.txt (i.e., assert `p.is_relative_to(scratch_dir)` per SEC-012 recommendation) — Effort: S
- [HIGH] Concat job exception never calls fail_job — `app/services/video_concatenator.py:308` — on any exception the job stays in `running` state forever; worker cannot retry or surface the error — Fix: catch exception, call `fail_job(job_id, str(e), attempt)` before re-raise — Effort: S
- [MED] `_run_concat_filter` hardcodes 1920x1080 target resolution instead of using `_determine_target_resolution` — `app/services/video_concatenator.py:381` — ticket design calls for picking the highest input resolution capped at 1080p; the implementation sets `target_w, target_h = 1920, 1080` unconditionally, ignoring smaller inputs — Fix: implement `_determine_target_resolution(input_paths)` and call it before building the filter graph — Effort: M
- [MED] No `-protocol_whitelist` on ffmpeg concat-filter input files — `app/services/video_concatenator.py:371-406` — SEC-012 requires restricting input protocols; `-i` flags have no whitelist on the re-encode path — Fix: add `-protocol_whitelist file` before each `-i str(p)` in `_run_concat_filter` — Effort: S
- [LOW] `video_concat_timeout_seconds` setting exists but `_run_concat_demuxer` and `_run_concat_filter` still use default parameter values (600 / 1800) rather than reading from `S` — `app/services/video_concatenator.py:321,360` and `execute_concat:259,262` — Fix: pass `timeout_seconds=S.video_concat_timeout_seconds` from `execute_concat` to both helpers — Effort: S
