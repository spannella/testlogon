- [HIGH] "queued" status transition silently fails on job submission — `app/routers/transcode_jobs.py:103` — same bug as VOD-001 §3.1; video stays in "created" after job submit; `ByStatusCreatedAt` GSI for "pending_encoding" always empty, breaking admin dashboards — Fix: change `to_status="queued"` to `to_status="pending_encoding"` — Effort: S
- [HIGH] Worker completes job then tries illegal "published" transition — `app/services/transcode_worker.py:202` — `transition_video_status(..., to_status="published")` is illegal from "pending_review"; 409 silently swallowed; video stays in "pending_review" with a valid playback URL but never publicly published — Fix: change to `to_status="pending_review"` and add explicit "encoding" transition after claim — Effort: S
- [MED] No "encoding" status transition when worker claims a job — `app/services/transcode_worker.py:95` — claim_job sets DDB running but never calls `transition_video_status("encoding")`; "encoding" GSI partition is always empty — Fix: call `transition_video_status(video_id, to_status="encoding")` after successful `claim_job()` — Effort: S
- [MED] Orphaned "running" jobs on process restart — `app/services/transcode_worker.py:38` — in-process asyncio.Semaphore resets on restart; up to 2× `max_concurrent` jobs may run simultaneously after restart — Fix: add startup recovery scan that re-queues running jobs owned by this worker_id older than `transcode_rendition_timeout_seconds` — Effort: M
- [LOW] Scratch directory defaults to CWD-relative path — `app/core/settings.py` — `TRANSCODE_SCRATCH_DIR` defaults to `tmp/transcode-scratch`; on prod this creates files on the root volume — Fix: default to `/tmp/transcode-scratch` or require explicit env var — Effort: S
