# BCAST-012 Gap List

- [HIGH] Private chat messages exposed in public chat history — `broadcast_chat_store.py:296` / `broadcast.py` — `get_chat_history_route` returns `kind="private_chat"` items mixed with public messages; any viewer reading `GET /sessions/{id}/chat` sees plaintext of all private chats — Fix: add `FilterExpression=Attr("kind").ne("private_chat")` to `get_chat_history()` by default — Effort: S
- [HIGH] Billing atomicity gap in `_write_private_chat_billing()` — `broadcast_private_chat.py:~500` — debit and credit `put_item` calls in separate `try/except`; credit failure after debit = viewer charged, creator not paid — Fix: use `TransactWriteItems` for atomic debit+credit — Effort: S
- [MED] `check_and_expire_chats()` is a stub — `broadcast_private_chat.py:~430` — returns 0; no background task registered in `app/main.py`; active chats run past `expires_at` indefinitely — Fix: implement expiry loop and register startup task — Effort: M
- [MED] `get_private_chat_history()` FilterExpression pagination gap — `broadcast_private_chat.py:~150` — `Limit=limit*2` workaround fails on busy session partitions; private history returns 0 results if 1MB pre-filter exhausted by public messages — Fix: add `PrivateChatIndex` GSI to `BroadcastChatMessages` on `private_chat_id` — Effort: M
- [MED] SEC-025 — creator ownership not centralized on chat management endpoints — `broadcast.py` — inline `created_by` checks rather than `_require_session_owner` helper — Fix: use centralized `_require_session_owner(ctx, session)` per SEC-025 design — Effort: S
- [LOW] Voyeur rate validation race — `broadcast_private_chat.py` — two sequential PATCH calls can temporarily produce `voyeur_rate > participant_rate` between updates — Fix: add a single-call validator that enforces `voyeur_rate < participant_rate` atomically — Effort: S
