- [HIGH] No admin-global dispute arbitration endpoint — the ticket spec requires `GET /ui/admin/collaboration-disputes` and `POST /ui/admin/collaboration-disputes/{dispute_id}/arbitrate` for admins to view all open disputes cross-collaboration, but `app/routers/collaborations.py` only exposes `GET /{collab_id}/disputes` (scoped to participants); admins cannot discover disputes without knowing the `collab_id` — Fix: add `require_admin_session`-gated list-all-disputes and arbitrate endpoints using `cr.list_disputes(collaboration_id=None)` from `app/services/collaboration_revenue.py:553` — Effort: S
- [HIGH] Content ownership not validated on assign — `app/services/collaboration_revenue.py:115` (`assign_content`) validates that the assigner is a collaboration participant but does not verify that the assigner owns the content item being assigned; any participant can assign another creator's content to the collaboration — Fix: add content-metadata ownership check before `put_item` (cross-ref SEC-004) — Effort: S
- [MED] `assign_content` `put_item` has no `ConditionExpression` — `app/services/collaboration_revenue.py:173` writes the content-assignment item without `attribute_not_exists(pk)`, so a race between two concurrent assign requests for the same `content_id` to the same collaboration can both succeed (idempotent by the preceding `get_content_assignment` check, but that check is not atomic) — Fix: add `ConditionExpression="attribute_not_exists(collaboration_id)"` or use the pre-check result more defensively — Effort: S
- [MED] No E2E test spec matching the ticket test plan — the ticket specifies `frontend/e2e/fin-collab-splits.spec.ts` (21 tests across sections 579–583) but only `frontend/e2e/collaboration-revenue.spec.ts` (481 lines) exists; the existing file should be verified to cover the content-assignment, auto-split, split-history, dispute, and edge-case sections from the ticket — Fix: audit `collaboration-revenue.spec.ts` against the 21-test plan and fill any gaps — Effort: M
- [LOW] Split execution record lacks immutability guard — `app/services/collaboration_revenue.py` writes split-execution records via plain `put_item` with no `ConditionExpression`; a replay of the billing hook with the same `content_id` and `amount_cents` would write a second split record and double-credit all collaborators — Fix: derive a deterministic `split_id` from `source_tx_id` and add `attribute_not_exists` guard (cross-ref SEC-004) — Effort: S
