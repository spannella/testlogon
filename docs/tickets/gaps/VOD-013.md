- [MED] playback entitlement auto-issued for deleted/archived videos in message history — `app/routers/messaging.py:4039-4052` — _message_out_from_item issues a fresh playback token for any video_share message regardless of current video status; if the video is later deleted, archived, or made private, all past conversation recipients continue to receive valid playback tokens on every message fetch — Fix: in the video_share projection block, check vr.status in ("approved", "published") and vr.visibility != "private" before issuing the entitlement; return playback_token=None otherwise — Effort: S
- [MED] playback entitlement TTL uses getattr fallback (video_share_playback_token_ttl_seconds undefined in settings) — `app/routers/messaging.py:4040` — getattr(S, "video_share_playback_token_ttl_seconds", 300) silently defaults to 300s because the setting is not declared in app/core/settings.py; a missing or misconfigured env var goes undetected — Fix: add video_share_playback_token_ttl_seconds: int = int(os.environ.get("VIDEO_SHARE_PLAYBACK_TOKEN_TTL_SECONDS", "300")) to Settings in app/core/settings.py — Effort: S
- [LOW] non-owner cannot share videos in "approved" status — `app/routers/messaging.py:9035-9036` — spec §2.3 states non-owners can share published+public/unlisted videos; the endpoint correctly requires status=="published" for non-owners; however this means a video in "approved" (awaiting final publish step) cannot be shared by non-owners even if visibility is public — Fix: if the platform uses "approved" as a final user-visible state, allow non-owners to share approved+public videos as well — Effort: S
- [LOW] no E2E test coverage for inline playback within VideoShareCard — `frontend/e2e/messaging-video-share.spec.ts` — tests 124-126 verify API responses and conversation previews but do not click the play button on the VideoShareCard or assert that MediaPlayer renders; the inline player flow (click thumbnail → MediaPlayer mounts) is untested in E2E — Fix: add a UI section that navigates to /messages, finds the video-share-card, clicks play, and asserts the video element is present — Effort: M
