# CALL-007 Gaps

- [HIGH] Missing SSE fanout for `call.missed` — `app/routers/messaging.py:14028-14059` timeout endpoint and `_expire_stale_invites()` backstop do not call `fanout_event_to_conversation` — callee learns of timeout only via slow SSE poll (up to 1-3s delay); callee ringing UI rings past intended timeout — Fix: add `fanout_event_to_conversation(...)` call after `timeout_call()` in both the endpoint and the backstop per §4.1 — Effort: S
- [HIGH] No callee-side local timer to auto-dismiss ringing overlay — `frontend/src/pages/messages/ConversationView.tsx` has no `setTimeout` for the incoming-ringing phase — if `call.missed` SSE event is lost or delayed, callee sees ringing UI indefinitely — Fix: add `useEffect` firing `REMOTE_DECLINE` at `ringing_timeout + 2000ms` on `incoming_ringing` phase per §4.2 — Effort: S
- [MED] Race condition: 409 on late timeout not handled correctly — `ConversationView.tsx:776-778` swallows 409 from `timeoutCall()` when callee accepted at last moment; caller UI shows "Call timed out" while call is actually accepted — Fix: handle 409 by calling `queryClient.invalidateQueries({queryKey:["call", callId]})` per §4.3 — Effort: S
- [MED] Backstop uses full table scan — `app/routers/messaging.py:13318-13347` `_expire_stale_invites()` scans entire `MessageCallSessions` table; no `ByStateStartedAt` GSI in `scripts/local-ddb-init.py:629-639` — high RCU cost at scale — Fix: add GSI and `list_call_sessions_by_state()` query per §4.4 — Effort: M
- [LOW] No kill-switch feature flag for the backstop — `app/core/settings.py:1149` has timeout seconds but no enable/disable flag for `_expire_stale_invites()` — Fix: add `messaging_webrtc_call_timeout_backstop_enabled` setting per §4.5 — Effort: S
