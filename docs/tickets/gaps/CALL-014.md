# CALL-014 Gaps

- [MED] `conversationId` prop may not be passed to `CallSessionOverlay` — `frontend/src/pages/messages/ConversationView.tsx:1345` passes `voicemailEligible={callsEnabled}` but `CallSessionOverlay.tsx:659` requires `conversationId` to also be non-null before rendering the recorder; if `conversationId` prop is absent the voicemail prompt never renders — Fix: confirm `conversationId={conversationId}` is present alongside `voicemailEligible` at line 1345; add if missing per §4.1 — Effort: S
- [MED] `VoicemailRecorder.tsx` may not enforce 60-second duration cap — `CreateVoicemailRequest.duration_seconds` has `le=60` backend validation but `VoicemailRecorder.tsx` may embed `VoiceRecorder` with default `maxDuration=300`; users record up to 5 min but get 422 on submit — Fix: ensure `<VoiceRecorder maxDuration={60} ...>` is used in `VoicemailRecorder.tsx` per §4.2 — Effort: S
- [LOW] Waveform computation for video voicemail unverified — `VoicemailRecorder.tsx` video mode records `video/webm` blob; `waveform_data` (min_length=10) is required for video voicemails but the `AnalyserNode` audio analysis may not be wired for the video recording code path — Fix: verify and add `AudioContext`+`AnalyserNode` branch for `mode==="video"` per §4.3 — Effort: S
- [LOW] Alert renderer has no dedicated case for `voicemail_received` — `frontend/src/pages/alerts/AlertsPage.tsx` likely renders voicemail alerts with the generic layout lacking "Listen" action link and voicemail-specific messaging — Fix: add `event === "voicemail_received"` case with "Missed call — voicemail left" title and conversation deep link per §4.4 — Effort: S
- [LOW] Group call voicemail not designed or implemented — `app/routers/messaging.py:8349` voicemail presign uses `callee_user_id` from `CallSessionRecord` which is meaningless for group calls (CALL-012); `write_alert` would only notify one user — Fix: deferred to CALL-014 v2; design `missed_by_user_ids` list on group call META item and fan-out alerts per §4.6 — Effort: L
