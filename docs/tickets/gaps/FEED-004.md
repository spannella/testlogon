- [HIGH] gif_url accepts arbitrary URL schemes with no domain allowlist — `app/routers/newsfeed.py:1585` (CreateCommentRequest gif_url field) — any URL including data: or javascript: URIs can be stored and rendered as img src; malicious data: URIs can embed arbitrary content, and future CSP relaxations or renderer bugs could enable XSS — Fix: validate gif_url is http/https and restrict to an allowlist of known GIF CDN domains (e.g. media.giphy.com, media.tenor.com) — Effort: S
- [HIGH] sticker_url accepts arbitrary external URLs with no platform-only enforcement — `app/routers/newsfeed.py:1591` (CreateCommentRequest sticker_url field) — the ticket spec requires sticker URLs to reference platform CDN only; any external URL is currently accepted and stored, enabling hotlinking of off-platform resources or SSRF-adjacent abuse — Fix: validate sticker_url matches platform S3/CDN origin (e.g. begins with /mock/s3/ in dev, or configured CDN_BASE_URL) — Effort: S
- [MED] no rate limiting on comment creation for media kinds — `app/routers/newsfeed.py:5324` (create_comment) — ticket spec mentions comment rate limiting applies uniformly to all kinds but no rate-limit check exists for gif/sticker comments, allowing a burst of media comment spam — Fix: apply existing or new per-user-per-post comment rate limit before the DDB write — Effort: S
- [LOW] rich_comments_enabled feature flag only gates gif/sticker at creation but not at read — `app/routers/newsfeed.py:5337` (create_comment flag check) — if the flag is toggled off after existing gif/sticker comments are stored, they are still returned verbatim in GET /posts/{id}/comments with kind=gif/sticker; clients may render media even when the feature is disabled — Fix: filter or mask kind/media fields in _comment_to_dict when rich_comments_enabled is False — Effort: S
