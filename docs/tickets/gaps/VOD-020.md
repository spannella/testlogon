- [HIGH] watermarked-download endpoint does not enforce VOD-019 download-tier entitlement — `app/routers/watermark.py:106-110` — the endpoint checks `video.allow_download` (global flag) and `video.download_mp4_status == "ready"` but never calls `check_entitlement_purchase_only` to verify `ent.download_allowed`; a viewer who purchased "permanent" tier (download_allowed=False) can call `POST /ui/videos/{id}/download/watermarked` and receive a watermarked MP4, circumventing the download-tier paywall enforced by the plain download endpoint at `app/routers/video_listing.py:800-804` — Fix: before the cache lookup in `request_watermarked_download`, add the same entitlement check used in `video_listing.py:800-804` — Effort: S
- [MED] SEC-012 cross-ref: watermark asset preparation fetches arbitrary HTTP(S) URLs and copies arbitrary file paths — `app/services/ffmpeg_watermark_lifecycle.py:49-58` — `patch_watermark_input` branches on `file://` (copies the path to temp dir, any local path allowed) and `http(s)://` (calls `urllib.request.urlretrieve` with no timeout, no redirect limit, no domain allowlist), enabling SSRF to cloud metadata endpoints or local-file-read of any file the uvicorn process can access — Fix: restrict HTTP fetches to an allowlist of S3 hostnames or configured CDN origins; reject `file://` schemes entirely; add a connection timeout — Effort: M
- [LOW] `_unset_all_defaults` in `watermark_generator.py` read-modify-write path has no concurrent-safety guard — `app/services/watermark_generator.py` — does not apply to this module (watermark generator does not manage subtitle defaults); this note applies to `vod_subtitle_manager.py` (see VOD-021) — n/a for VOD-020
- [LOW] watermark toggle endpoint (`PATCH /ui/videos/{id}/watermark`) does not check that the video has `allow_download=true` before accepting `watermark_downloads=true` — `app/routers/watermark.py:210-231` — a creator can set `watermark_downloads=true` on a video with `allow_download=false`; the setting is stored but has no effect (the download endpoint gating on `allow_download` prevents generation); this creates confusing creator UX — Fix: add a validation check in `toggle_watermark_setting`: if `body.watermark_downloads is True and not video.allow_download`, return 409 with detail "enable downloads first" — Effort: S
