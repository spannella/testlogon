- [HIGH] ExtractionResultsPanel not integrated into admin case detail — `frontend/src/pages/admin/KycCaseDetailPage.tsx` (depends on KYC-001) — reviewer cannot see extracted OCR fields, match status, or overall confidence without switching to a separate queue page — Fix: add "Document Extraction" tab with per-document ExtractionResultsPanel calling `GET /ui/kyc/documents/admin/by-status?case_id=` — Effort: S
- [HIGH] No `case_id` filter on admin by-status endpoint — `app/routers/kyc_documents.py` — listing all documents across statuses for a specific case requires multiple calls or a full scan; no `case_id` query param exists — Fix: add optional `case_id: str | None = Query(default=None)` with client-side `FilterExpression` on GSI result — Effort: S
- [MED] `kyc_documents` purge not covered — `app/routers/kyc_cases.py` (purge endpoint) — POST /admin/purge/run covers only `kyc_cases`; extracted name/DOB PII in `kyc_documents` table is never deleted; violates GDPR erasure requirement — Fix: add companion `kyc_documents` purge keyed by `case_id` inside the existing purge handler — Effort: S
- [MED] Missing profile data shown as mismatch — `app/services/kyc_document_verification.py:159` (`_load_profile_identity`) — if user has no `display_name` or `birthday`, match returns `NOT_AVAILABLE`; reviewer UI must distinguish this from a real mismatch — Fix: ensure ExtractionResultsPanel renders `NOT_AVAILABLE` fields with a gray "N/A" badge, not a red "Mismatch" badge — Effort: S
- [LOW] Extraction observability absent — `app/services/kyc_document_verification.py:40` — only `KycDocumentValidationError` is logged; confidence outcomes and name-mismatch events are silent — Fix: add `logger.info("kyc.extraction.completed", ...)` and `logger.warning("kyc.extraction.name_mismatch", ...)` structured log events — Effort: S
