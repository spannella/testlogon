# CALL-011 Gaps

- [CRIT] `useCallBillingHeartbeat.ts` hook does not exist — `frontend/src/hooks/useCallBillingHeartbeat.ts` is missing — no periodic `PATCH /messaging/messages/calls/{call_id}/heartbeat` is sent during connected paid calls; billing cycles never fire mid-call; `finalize_call_billing` at end attempts to charge entire duration in one shot which may exceed wallet balance — Fix: implement hook with `setInterval` at `heartbeat_interval_seconds`, dispatch `end_call` action on `action==="end_call"` response per §4.1 — Effort: M
- [HIGH] No billing props wired into `ConversationView.tsx` or `CallSessionOverlay.tsx` — `callBillingHeartbeat` hook not instantiated in `ConversationView`; `CallBillingOverlay` component exists (`frontend/src/components/calls/CallBillingOverlay.tsx`) but is imported nowhere; callers see no cost ticker or balance warning — Fix: wire hook in ConversationView and pass billing props to overlay per §4.2/4.3 — Effort: M
- [HIGH] No caller rate display before initiating call — `frontend/src/pages/messages/PaidCallRateBadge.tsx` does not exist — callers have no visual indication of per-minute cost before pressing the call button; risk of unexpected charges — Fix: implement `PaidCallRateBadge` using `useQuery` on `GET /ui/calls/rates/{partner_user_id}` per §4.4 — Effort: S
- [HIGH] No creator rate settings UI — `frontend/src/pages/settings/CallRateSettings.tsx` does not exist — creators cannot configure their rate through the platform UI — Fix: implement settings form at `/settings/call-rate` per §4.4 — Effort: M
- [MED] `call.balance_low` and `call.balance_depleted` SSE events not handled in UI — `useMessagingStream.ts:200-202` registers the events but `ConversationView` `onCallEvent` handler has no dispatch cases; callee is disconnected without advance warning — Fix: add `BILLING_LOW_BALANCE` and `BILLING_BALANCE_DEPLETED` events to `callStateMachine.ts` and handle in `onCallEvent` per §4.5 — Effort: S
- [MED] Concurrent paid calls can pass balance pre-check simultaneously — `create_call_invite` pre-check uses caller's current wallet balance without accounting for other active paid calls; 5 concurrent paid calls each passing a $25 check could expose $125 total — Fix: query `ByCallerStartedAt` GSI for active paid sessions and reject if any exist per §4.6 — Effort: S
- [LOW] `rate_cents_per_min` vs `rate_cents_per_minute` naming inconsistency — `CallSessionRecord` at `app/services/messaging_call_sessions.py:38` uses `_per_min`; `CallRateSettings`, `CallRateIn`, `CallRateOut`, `CallInviteOut` use `_per_minute`; latent bug if wrong field name passed to `start_call_billing` sets rate to 0 — Fix: normalize to one name across all models and services — Effort: S
