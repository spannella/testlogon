- [MED] "Not for me" / not_interesting signal missing — `app/routers/newsfeed.py:4763` — only "interesting" (POST `/feed/interesting`) and unmark (POST `/feed/uninteresting`) are implemented; the ticket requires a distinct `not_interesting` signal that also hides the post via FEED-006; no `not_interesting_count` counter is incremented — Fix: add `POST /feed/not-interesting` endpoint that (a) stores `POST_SIGNAL#{post_id}` with `signal="not_interesting"` in billing table, (b) calls `hide_post`, and (c) does ADD on `not_interesting_count`; add a toggle-off endpoint — Effort: M
- [LOW] "Not for me" / "Not interesting" UI missing from PostCard — `frontend/src/pages/feed/PostCard.tsx:719-730` — PostCard renders an "Interesting" button but has no "Not for me" button; the ticket requires a ThumbsDown/EyeOff option in the overflow menu that triggers not_interesting signal + hide + undo toast — Fix: add `notInterestingMutation` and a corresponding button for non-own posts, reusing the FEED-006 undo toast pattern — Effort: S
- [LOW] signal toggle between interesting/not_interesting not implemented — `app/routers/newsfeed.py:4763` / `app/services/post_interesting.py` — the backend has no swap logic; marking "interesting" on a post already marked "not_interesting" (or vice versa) does not decrement the prior counter or un-hide the post — Fix: in the service layer, get the existing signal before writing; if different, decrement old counter and conditionally call unhide_post — Effort: S
