- [CRIT] internal charge endpoints lack access control — `app/routers/ads.py:447,460,473` — any authenticated user can post arbitrary charges against any advertiser account, draining balances to zero — Fix: add account ownership verification before each charge handler, or move to require_admin_or_root — Effort: S
- [HIGH] no daily spent_today_cents reset background task — `app/services/ad_billing.py:_process_charge` — daily-budget campaigns accumulate spend indefinitely; budget exhaustion is permanent after first day — Fix: add startup background task resetting spent_today_cents=0 at midnight UTC with last_reset_date guard — Effort: M
- [HIGH] platform revenue (30% share) not written to any ledger — `app/services/ad_billing.py:_split_revenue` — no audit trail for platform-side revenue; reconciliation is impossible — Fix: write platform_revenue entry to ad_billing table with PK=PLATFORM#revenue after each split — Effort: S
- [MED] generate_invoice uses FilterExpression on GSI scan without pagination — `app/services/ad_billing.py:generate_invoice` — at scale, invoices silently truncate at first DDB page (~1MB) of monthly entries — Fix: add LastEvaluatedKey loop to exhaust all pages before aggregating — Effort: S
- [MED] _check_budget_and_alert re-reads campaign via get_campaign on every impression — `app/services/ad_billing.py:_check_budget_and_alert` — unbounded DDB reads at high impression volume for active campaigns — Fix: pass campaign dict through from the caller to avoid per-impression re-fetch — Effort: M
- [LOW] AD_BILLING_ENABLED feature flag not implemented — `app/core/settings.py` — billing code cannot be deployed dormant; no rollback path without code deploy — Fix: add ad_billing_enabled bool setting and gate _process_charge — Effort: S
