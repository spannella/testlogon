- [HIGH] `add_catalog_item` does not store `creator_user_id` on the cart item record — `app/services/shoppingcart.py:366-392` — the `add_catalog_item` helper fetches the catalog item but only copies `sku`, `name`, `quantity`, `unit_price_cents` into the cart item; `creator_id` from the catalog item is not included; consequently `_resolve_cart_creator` (`shoppingcart.py:457-465`) always returns `None` at purchase time, causing the code to fall back to `user_sub` (the buyer) as `creator_user_id` for promo validation, meaning creator-scoped promo codes will always fail with "not valid for this creator" for all catalog-backed cart items — Fix: add `"creator_user_id": item.get("creator_id")` to the payload dict in `add_catalog_item` — Effort: S; cross-ref SEC-024
- [MED] No rate limiting on `POST /ui/promo-codes/validate` — `app/routers/promo_codes.py:141` — the validate endpoint is publicly callable and has no per-user rate limit; a buyer can enumerate valid codes via brute force (ticket flagged this at §7.3 but it was not implemented) — Fix: add `@rate_limit(event="promo_validate", per_user_per_minute=10)` decorator — Effort: S
- [LOW] `purchaseCart` frontend wrapper sends `body ?? {}` even when no promo applied — `frontend/src/api/endpoints/cart.ts:43-47` — sends empty JSON body `{}` on non-promo purchases where previously no body was sent; existing backend `CartPurchaseIn` defaults handle this correctly (all fields `Optional`), but old API clients that do not send a body at all must still be supported and are — no functional breakage, but the empty-body case is untested — Fix: document backward compatibility explicitly in tests — Effort: S
