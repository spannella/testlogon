- [LOW] Sidebar "Videos" entry uses `Video` icon instead of spec-required `Film` icon — `frontend/src/components/layout/Sidebar.tsx:160` and `frontend/src/components/layout/AppShell.tsx:219` and `frontend/src/components/layout/MobileNav.tsx:90` — Ticket specifies `Film` icon for the Videos nav entry; all three nav surfaces use the `Video` (camera) icon instead; minor visual inconsistency — Fix: Replace `Video` with `Film` in all three nav definitions — Effort: S
