- [MED] growth_series always returns empty list — `app/services/admin_subscription_tiers.py:389` — tier analytics chart renders empty dataset; no time-series subscriber growth data available — Fix: add GSI on T.subscriptions keyed by creator_id+created_at and bucket new subscriptions by week in get_tier_analytics — Effort: M
- [MED] subscriber_count denormalised but never auto-updated — `app/services/admin_subscription_tiers.py:340` — tiers without plan_id linkage show stale analytics; count initialised to 0 and never incremented/decremented on subscription lifecycle events — Fix: call update_tier with new subscriber_count from subscription_cycle_orders.py on activate/cancel events — Effort: M
- [MED] no feature flag gates the endpoint — `app/routers/admin_subscription_tiers.py:1` — feature is live for all admin/root users immediately with no phased rollout control — Fix: add ADMIN_SUBSCRIPTION_TIER_MANAGER_ENABLED to app/core/settings.py and guard in router — Effort: S
- [MED] scan-based analytics costly at scale — `app/services/admin_subscription_tiers.py:310` — _active_subscriptions_for_creator performs full-table Scan on T.subscriptions filtered by creator_id; O(total_subscriptions) read cost — Fix: add GSI on T.subscriptions with creator_id as partition key — Effort: M
- [LOW] stale tier-manager.spec.ts targets wrong URL prefix — `frontend/e2e/tier-manager.spec.ts:1` — spec uses /v1/subscriptions/tiers (ticket-proposed) not /ui/admin/subscription-tiers (implemented); tests would fail against current backend — Fix: delete or reassign tier-manager.spec.ts sections to cover user-facing TierManager.tsx at /subscriptions/manage — Effort: S
- [LOW] archive_tier / unarchive_tier no-op guard is Python-side only — `app/services/admin_subscription_tiers.py:211` — concurrent archive by two admins silently succeeds for both; no DynamoDB ConditionExpression guard unlike delete_tier — Fix: add ConditionExpression attribute_exists + status check on the PutItem call — Effort: S
- [LOW] root user managing tiers keyed to root sub — `app/routers/admin_subscription_tiers.py:1` — creator_id = actor.sub; root users' tiers are keyed to root sub; no way for root to manage arbitrary creators' tiers — Fix: add optional creator_id query param for root-role callers — Effort: S
