- [HIGH] `list_templates` uses a full-table Scan instead of the `status-updated-index` GSI — `app/services/kyc_document_templates.py:333-346` — at scale, scanning the entire `kyc_document_templates` table on every admin list request is unbounded and expensive; the `status-updated-index` GSI (`PK=status, SK=updated_at`) was designed for exactly this query but is bypassed; only the `get_required_templates_for_tier` method (line 371) uses the GSI correctly — Fix: replace the `scan()` loop in `list_templates` with a `query()` on `status-updated-index` when `status` is supplied, and a parallel query over all statuses otherwise — Effort: S
- [HIGH] `_readiness_for_case` in `app/routers/kyc_cases.py` (line 244) does not include template-signing completeness check — `app/routers/kyc_cases.py:244-307` — the ticket specifies (§3.6) that `_readiness_for_case` must check whether all tier-required templates have been signed and add `"unsigned_templates:..."` to `missing_requirements`; the current implementation checks only questionnaire, files, and signature packet without consulting `KycDocumentTemplateService.get_required_templates_for_tier()`; cases with un-signed template documents will appear ready when they are not — Fix: add a `KYC_TEMPLATE_READINESS_GATE`-gated call to `SERVICE.get_required_templates_for_tier(target_tier)` in `_readiness_for_case` and append any missing slugs to `missing_requirements` — Effort: M
- [MED] Template PDF is not stored in S3 when created — `app/services/kyc_document_templates.py:172-209` — `create_template` creates only a metadata record (VERSION#0) with an empty `s3_key`; the PDF must be uploaded separately via `upload_template_version`; however, there is no guard in `preview_template` or `render_template` preventing a call on a version-0 template that has never had a PDF uploaded, so both methods fall through to the `_placeholder_pdf()` fallback silently instead of returning a clear "no PDF uploaded" error to admins — Fix: `preview_template` and `render_template` already raise `KycTemplateValidationError("kyc_no_pdf_uploaded")` when `s3_key` is empty on a version item, but `create_template` does not block preview on VERSION#0; document this in the router's 400 response and add an E2E test that calling preview on a freshly-created (no version uploaded) template returns 400 — Effort: S
- [LOW] `upload_template_version` does not deactivate prior active versions on upload — `app/services/kyc_document_templates.py:211-263` — a newly uploaded version is immediately stored with `status="active"` (line 248) without deactivating the previously active version for the same slug; the ticket intends that only one version per slug is active at a time, but multiple active versions can coexist until admin explicitly calls `activate_template`; `get_active_template_by_slug` handles this gracefully by returning the highest version, but `get_required_templates_for_tier` will still return a per-slug deduplicated list, so behavior is correct albeit surprising — Fix: document the intentional behavior (upload does NOT auto-deactivate; admin must call activate) or auto-deactivate prior versions in `upload_template_version` — Effort: S
