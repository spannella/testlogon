- [HIGH] Title length mismatch causes unhandled 500 — `app/routers/vod.py:72` — `VideoUploadPresignIn.title` allows 500 chars but `VideoMetadataModel.title` enforces `max_length=256`; passing a 300-char title reaches `create_video()` and raises an unhandled `ValidationError` (500) — Fix: change `max_length=500` to `max_length=256` in `VideoUploadPresignIn.title` — Effort: S
- [LOW] No file extension validation — `app/routers/vod.py:134` — `filename` accepts any string; a user can submit `filename="malicious.sh"` with a valid video content-type, storing a misleadingly named S3 key — Fix: add extension allowlist check against `ALLOWED_EXTENSIONS` set — Effort: S
- [LOW] Dev presigned URL uses public_base_url; E2E tests via page.request bypass Vite proxy — `app/routers/vod.py:167` — tests using `page.request.put()` must target `http://localhost:8000/mock/s3/` directly; tests using the browser context work correctly — Fix: document in E2E test helpers; no code change required — Effort: S
