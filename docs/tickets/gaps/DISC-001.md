- [HIGH] background refresh job not registered — `app/main.py` (no startup handler for recommendations) — compute_for_you records never written; every user stays in cold-start/trending-fallback permanently despite algorithm being implemented — Fix: add refresh_all_users() function and register asyncio periodic loop via app.add_event_handler("startup", ...) at S.reco_refresh_interval_hours cadence — Effort: S
- [HIGH] recordEngagement never called from frontend — `frontend/src/pages/videos/VideoPlayerPage.tsx` (no recordEngagement call) — POST /ui/recommendations/engagement endpoint is live but zero signals are recorded; compute_affinity_scores always receives empty input — Fix: call recordEngagement on video onEnded and at 30-second mark in VideoPlayerPage — Effort: S
- [LOW] recommendation_reason discarded in ForYouTab — `frontend/src/pages/videos/ForYouTab.tsx:8-23` (toGalleryItem drops recommendation_reason field) — "Why this?" tooltip user story not met; recommendation_reason populated by backend at recommendations.py:186 is never shown — Fix: pass recommendation_reason as tooltipText to GalleryVideoCard and render Info tooltip — Effort: S
- [LOW] CreatorSuggestions subscribe button missing — `frontend/src/pages/videos/CreatorSuggestions.tsx` — creator name renders but no subscribe button; ticket user story "avatar, name, and subscribe button" not fully met — Fix: add subscribe button calling POST /api/creators/{id}/plans — Effort: S
