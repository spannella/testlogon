- [MED] template references stale LLM key not surfaced — `app/services/agent_fleet.py:140` — when referenced llm_key_id is deleted create_worker_from_template fails with 400 but list_templates shows no warning — Fix: cross-reference key_ids in list_templates, add key_valid: bool flag per template — Effort: S
- [MED] bulk action max-worker safeguard missing — `app/services/agent_fleet.py:98` — bulk_start/bulk_stop has no cap; user with many workers causes long-running event-loop-blocking sequential operations — Fix: add workers[:50] slice and ValueError when more workers would be affected — Effort: S
- [HIGH] eligible ticket count pagination incomplete — `app/services/agent_fleet.py:288` — _count_eligible_tickets uses FilterExpression without LastEvaluatedKey loop; sparse tickets table silently returns incorrect (low) count — Fix: add pagination loop over LastEvaluatedKey collecting COUNT — Effort: S
- [MED] SSE event hook may be missing — `app/services/agent_fleet.py` — SSE bus requires transition_agent_state (AGENT-003) to call emit_fleet_event; if hooks are unwired SSE stream is permanently silent — Fix: add emit_fleet_event call after each state transition in agent_orchestrator.py — Effort: S
- [LOW] branch_convention regex rejects dot — `app/models.py` — WorkerTemplateIn.validate_branch_convention pattern ^[a-zA-Z0-9_/{}\-]+$ rejects dot, blocking e.g. hotfix/v1.2.3 — Fix: expand allowlist to include . and # — Effort: S
