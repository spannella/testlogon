# BCAST-011 Gap List

- [HIGH] Billing atomicity gap in `_write_private_billing()` — `broadcast_private.py:300` — debit and credit `put_item` calls wrapped in separate `try/except`; failed credit after successful debit = viewer charged, creator not paid — Fix: use `TransactWriteItems` for atomic debit+credit; raise 500 on failure instead of silently swallowing — Effort: S
- [HIGH] `behavior="end"` not fully wired in accept handler — `broadcast.py:~1097` — when `behavior="end"`, the accept route transitions session to `"private"` status but never calls `stop_session_with_provider()`; MediaLive channel continues running — Fix: call `stop_session_with_provider(session_id, actor, reason="go_private_end")` when `body.behavior == "end"` — Effort: S
- [MED] No auto-timeout background task for max-duration enforcement — `broadcast_private.py` / `app/main.py` — private sessions can run indefinitely past `max_duration_minutes`; no background loop registered — Fix: register `start_private_session_timeout_loop` in `app/main.py`; scan active sessions and call `end_private_session` for expired ones — Effort: S
- [MED] SEC-025 — `_require_session_owner` helper not reused on accept/decline — `broadcast.py:1062,1147` — inline `session.created_by` ownership check rather than centralized helper; defense-in-depth gap — Fix: use `_require_session_owner(ctx, session)` helper per SEC-025 design — Effort: S
- [MED] No `BROADCAST_PRIVATE_ENABLED` feature flag — `app/core/settings.py` — no platform-level kill switch to disable go-private globally — Fix: add `broadcast_private_enabled: bool` to settings; gate all private endpoints with `_require_broadcast_private_enabled()` — Effort: S
- [LOW] Frontend go-private components not split into standalone files — `frontend/src/pages/broadcast/` — `GoPrivateButton.tsx`, `PrivateSessionView.tsx`, `PrivateHoldingScreen.tsx`, `PrivateRequestNotification.tsx` not present as separate components — Fix: extract inline implementations into named component files — Effort: M
