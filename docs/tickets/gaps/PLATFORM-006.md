- [HIGH] send_alert_email() still uses silent bare except — `app/services/alerts.py:458` — the old `except Exception: pass` implementation remains; SES rejections, quota exhaustion, and domain verification errors are silently swallowed with no log, no metric, and no retry notification; callers always receive None and cannot distinguish success from failure — Fix: replace with logged version that calls `logger.exception()`, increments `EMAIL_FAILED`, calls `record_email_failure()`, and returns None explicitly — Effort: S
- [HIGH] Double silent-failure envelope around email fanout — `app/services/alerts.py:670` — the email section of the alert fanout is wrapped in its own `except Exception: pass` in addition to the one inside `send_alert_email()`; security alert emails (login from new device, MFA changes) may vanish with no trace — Fix: remove the outer `except Exception: pass` from the email fanout section; let exceptions surface to the caller — Effort: S
- [MED] Per-call boto3.client("ses") — no connection reuse — `app/services/alerts.py:461` — a new HTTP connection is established on every email send (~50 ms overhead); high-frequency alert bursts create connection churn — Fix: use a module-level `_get_ses_client()` singleton that initialises once and reuses the connection — Effort: S
- [MED] Prometheus email metrics not yet added to app/metrics.py — `app/metrics.py` — `EMAIL_SENT`, `EMAIL_FAILED`, `EMAIL_BOUNCED`, `EMAIL_COMPLAINED`, `EMAIL_SUPPRESSED` counters described in the design are absent; no alerting on bounce or complaint rates is possible — Fix: add the five counters to `app/metrics.py` following the existing noop pattern for non-Prometheus environments — Effort: S
- [MED] SNS notification endpoint reachable without network restriction — `app/routers/ses_notifications.py` — `POST /internal/ses/notifications` is unauthenticated (required for SNS); without a security group restricting to AWS SNS IP ranges, an attacker can POST crafted bounce/complaint payloads to suppress arbitrary email addresses — Fix: restrict endpoint to VPC-only traffic; document in ops runbook — Effort: M
- [LOW] Admin unsuppress action has no audit log — `app/routers/admin_email.py` — `DELETE /ui/admin/email/suppressed/{email}` removes a suppression without writing an audit record; removing a spam-complaint address re-enables delivery and can degrade SES reputation — Fix: write an audit DDB item (or log at WARN level with admin sub) before removing the suppression record — Effort: S
