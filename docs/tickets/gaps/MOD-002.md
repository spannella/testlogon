- [CRIT] DMCA waiting-period background timer not wired to startup — `app/main.py:482-483` — `process_expired_dmca_waiting_periods()` exists in `app/services/dmca_claims.py:617` but no `app.add_event_handler("startup", ...)` loop is registered in `main.py`; counter-notices whose 14-day window expires will never auto-restore content, violating 17 U.S.C. §512(g)(2)(C) — Fix: add `_dmca_timer_loop` async background task registered on startup per section 3.7 — Effort: S
- [HIGH] SEC-012 cross-ref: content_url not validated against SSRF or internal network targets — `app/services/dmca_content_operations.py:35-62` — `resolve_content_from_url` only does path-segment splitting; an attacker supplying `http://169.254.169.254/latest/meta-data/` or `http://localhost:8001/` as `content_url` passes the validator; if the URL is later fetched by any downstream service (CDN pre-warm, screenshot service, etc.) it enables SSRF — Fix: reject non-relative URLs or validate against platform hostname allowlist in `DmcaClaimIn._validate_content_url`; the existing `javascript:`/`data:` check in the ticket design is absent from the implementation — Effort: S
- [HIGH] claimant rate-limit not enforced — `app/services/dmca_claims.py:443` — `ByClaimantCreatedAt` GSI exists but `file_dmca_claim` never queries it to cap submissions per claimant; a bad actor can file unlimited claims against a single creator, automating takedown harassment at scale — Fix: query `ByClaimantCreatedAt` before inserting; reject with 429 if claimant has N claims in the last 24h (configurable) — Effort: S
- [MED] counter-notice form page absent — `frontend/src/pages/dmca/` — `DmcaCounterNoticeForm.tsx` specified in section 4.6 is not present; `DmcaClaimForm.tsx` exists but creators have no browser-accessible page to file a counter-notice after receiving a takedown alert — Fix: create `frontend/src/pages/dmca/DmcaCounterNoticeForm.tsx` and add route `/dmca/counter-notice/:claimId` to `App.tsx` — Effort: M
- [MED] DMCA counter-notice route absent from `App.tsx` — `frontend/src/App.tsx:421-424` — `/dmca/counter-notice/:claimId` route is not registered; alert deep-links from `write_alert` pointing to this path would 404 — Fix: add `<Route path="dmca/counter-notice/:claimId" element={<DmcaCounterNoticeForm />} />` — Effort: S
- [MED] `content_url` `javascript:`/`data:` scheme check absent — `app/services/dmca_claims.py:96-121` — the model-level `_validate_content_url` validator from section 3.2 that rejects `javascript:` and `data:` prefixes was not implemented; stored XSS via crafted content_url is possible if the URL is rendered as a link in the admin dashboard — Fix: add field_validator on `DmcaClaimIn.content_url` matching the ticket design — Effort: S
- [LOW] repeat-infringer strike lookback window not enforced — `app/services/dmca_claims.py:400-450` — `count_strikes` queries all time history but `DMCA_STRIKE_LOOKBACK_DAYS` is defined in the ticket design (default 365 days); all historical claims count regardless of age, making the strike policy more punitive than intended — Fix: add `KeyConditionExpression` with `created_at >= now - lookback_days * 86400` in the GSI query — Effort: S
