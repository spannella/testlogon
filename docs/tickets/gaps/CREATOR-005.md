- [MED] broadcast aggregation missing FilterExpression pagination — `app/routers/content_calendar.py:26` — list_sessions_by_creator with FilterExpression silently misses scheduled broadcasts beyond first DDB page for prolific creators — Fix: add LastEvaluatedKey pagination loop capped at 100 broadcasts per view — Effort: S
- [MED] VOD cancel proxy may be absent — `app/routers/content_calendar.py:87` — POST /ui/content-calendar/cancel dispatches to posts and broadcasts but VOD cancel path (clearing scheduled_publish_at) may not be implemented — Fix: add item_type="vod" branch calling PATCH on video with scheduled_publish_at=null — Effort: S
- [LOW] timezone context lost in ContentItemDetail — `frontend/src/pages/content-calendar/ContentItemDetail.tsx` — schedule_timezone and scheduled_at_local from post items not displayed; creators cannot verify intended timezone — Fix: surface schedule_timezone and scheduled_at_local in ContentCalendarItem.metadata from aggregation endpoint — Effort: S
- [LOW] overdue badge not confirmed implemented — `frontend/src/pages/content-calendar/ContentCalendarWeek.tsx` and ContentCalendarMonth.tsx — items past scheduled_at with status=scheduled may not show amber overdue badge as required by acceptance criteria — Fix: add isOverdue check and render Badge variant="warning" on overdue items — Effort: S
- [LOW] QuickScheduleDialog publish_at pre-fill dependency — `frontend/src/pages/feed/CreatePost.tsx` — QuickScheduleDialog navigates to /feed?compose=true&publish_at=... but CreatePost must read publish_at from URL query param — Fix: verify CreatePost reads compose and publish_at query params; add if missing — Effort: S
