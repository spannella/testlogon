# MON-001 gaps

- [MED] `purchase_video` checks idempotency with a plain `get_item` then `put_item` — no conditional write guards concurrent duplicate purchases — `app/services/vod_purchase.py:428-452` — two simultaneous buyers could each pass the `existing` check before either write lands, producing double billing ledger debits and overwriting the entitlement record — Fix: replace the two-step check with a single `put_item(ConditionExpression="attribute_not_exists(pk)")` and catch `ConditionalCheckFailedException` to return the idempotency response — Effort: S

- [MED] `purchase_video` writes viewer debit and creator credit as separate best-effort `put_item` calls with no link between them — `app/services/vod_purchase.py:498-535` — if the debit succeeds and the credit fails, the creator never sees the revenue; no reconciliation mechanism exists — Fix: log both `entry_id` values on the entitlement record so an offline reconciliation scan can detect orphaned debits; alternatively, delegate to `write_tip_ledger`-style paired write — Effort: S

- [LOW] `VideoPlayerPage.tsx` uses `video.is_entitled` (line 221) but the backend response field is named `entitled` per `VideoDetailOut`; TypeScript type in `frontend/src/api/types.ts` must map the field correctly — `frontend/src/pages/videos/VideoPlayerPage.tsx:221` — if the field name mapping is wrong the access gate never closes — Fix: audit `VideoDetailResponse` in `frontend/src/api/types.ts` and ensure `is_entitled` is either an alias or the backend renames the field — Effort: S

- [LOW] `vod_entitlements` DDB table has no TTL attribute configured in `scripts/local-ddb-init.py:754` — rental entitlements with `expires_at` set rely on application-level TTL checks in `check_entitlement_purchase_only()` rather than DDB native TTL expiry — `scripts/local-ddb-init.py:754` — stale rental records accumulate in the table indefinitely — Fix: add `ttl_attribute="expires_at"` to the `TableDef` and set DDB TTL on the table; `expires_at=0` means no expiry (safe) — Effort: S
