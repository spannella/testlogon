- [HIGH] admin_capabilities system is dead code — never enforced — `app/cli/rootctl.py:51` — rootctl admin permissions set writes admin_capabilities DDB attribute; no FastAPI endpoint reads or checks it; operators believe they are restricting admin access when no enforcement exists; wrongly-privileged admin retains full access to billing, impersonation, content moderation, and DMCA endpoints — Fix: repoint _admin_capabilities_set_command to write admin_profile.scopes (Option A in §4.1) — Effort: M
- [HIGH] CLI admin grant produces implicit full admin — `app/cli/rootctl.py:1303` — _admin_grant_command UpdateExpression sets only role=admin with no admin_profile; normalize_admin_profile falls back to GENERAL type; GENERAL admins pass all require_admin_scope checks including permanent bans and impersonation — Fix: add --scope and --profile-type args to grant command; write admin_profile in UpdateExpression — Effort: M
- [MED] content_moderation_senior scope absent from frontend grant UI — `frontend/src/pages/admin/RootRoleManagementPage.tsx:26` — ADMIN_SCOPE_OPTIONS array has only 3 entries missing content_moderation_senior; backend enforces it at admin_moderation.py:47 for permanent bans; operators cannot grant this scope via UI and must use raw API calls — Fix: add content_moderation_senior entry to ADMIN_SCOPE_OPTIONS and to AdminScope TypeScript type in adminRoles.ts:4 — Effort: S
- [MED] content_moderation_senior missing from frontend TypeScript type — `frontend/src/api/endpoints/adminRoles.ts:4` — AdminScope type union is missing "content_moderation_senior"; TypeScript consumers cannot reference or validate this scope — Fix: add "content_moderation_senior" to the AdminScope union type — Effort: S
- [LOW] admin_capabilities DDB records orphaned — `app/cli/rootctl.py:1703` — existing user records may have admin_capabilities attribute that is misleading and never cleaned up — Fix: run one-off migration scan to convert capability names to AdminScope equivalents and REMOVE admin_capabilities attribute — Effort: M
