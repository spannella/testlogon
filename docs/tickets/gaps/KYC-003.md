- [HIGH] `expire_stale_calls` not wired as background task — `app/main.py` (absent hook; function exists at `app/services/kyc_liveness_call.py`) — missed calls remain `scheduled` indefinitely; conflict check blocks re-scheduling; `ByStatus` GSI `scheduled` partition grows without bound — Fix: wire `expire_stale_calls` as a periodic asyncio task at startup, matching `start_kyc_sla_checker_task` pattern at `app/main.py:528` — Effort: S
- [HIGH] VerificationCallPanel missing from admin case detail — `frontend/src/pages/admin/KycCaseDetailPage.tsx` (depends on KYC-001) — reviewers cannot see call status, outcome, or recording reference from the case view — Fix: add collapsible `VerificationCallPanel` section with schedule/conduct/result states and a "Schedule Call" dialog — Effort: M
- [HIGH] No `GET /admin/case/{case_id}` endpoint — `app/routers/kyc_liveness_call.py` — admin must filter the by-status GSI across all statuses to find a call for a specific case; no direct case-level lookup exists — Fix: add `GET /admin/case/{case_id}` backed by `STORE.get_call_for_case(case_id)` querying ByStatus GSI and filtering by `case_id` — Effort: S
- [MED] ScheduledCallBanner missing from applicant KYC status page — `frontend/src/pages/kyc/KycStatusPage.tsx` — applicant has no countdown, no join link, and no awareness of a scheduled call — Fix: add `ScheduledCallBanner` with `refetchInterval: 60_000` and client-side join-window enable/disable logic (5 min before) — Effort: S
- [MED] KYC_VERIFIER scope not implemented — `app/auth/roles.py:14` — all ADMIN users can record call outcomes, not just trained verifiers; any admin can mark a liveness call passed/failed for a case they are unrelated to — Fix: add `KYC_VERIFIER` scope to `AdminScope` enum and filter `require_admin_or_root` on verifier endpoints — Effort: M
- [MED] Join window enforced client-side only — `app/services/kyc_liveness_call.py` (`conduct_call`) — backend allows `conduct_call` at any time; the 5-minute join window is a UI-only constraint — Fix: add server-side check that `|now - scheduled_at| <= join_window_seconds` before transitioning to `in_progress` — Effort: S
- [LOW] Double-scheduling race under concurrent requests — `app/services/kyc_liveness_call.py` (`schedule_call`) — conflict check is a read then write; two concurrent requests can both pass the read before either writes — Fix: document as known limitation; for strict idempotency, add a conditional write on a case-level scheduling lock attribute — Effort: M
