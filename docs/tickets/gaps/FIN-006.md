- [MED] No billing-hook integration for rollup upserts — `app/services/per_content_revenue.py` (no `upsert_content_revenue_rollup`), `app/services/tip_ledger.py:88` — Revenue is aggregated on-demand by scanning the billing ledger; the ticket design calls for proactive rollup writes from billing hooks (tips, unlocks, VOD) but this layer was not implemented. Under high concurrency the per-request ledger scan grows unboundedly with credit volume. Fix: add `upsert_content_revenue_rollup()` and call it from `tip_ledger.write_tip_ledger` and the unlock/VOD paths — Effort: M
- [LOW] No internal rebuild-from-ledger endpoint — `app/routers/per_content_revenue.py` (no `/internal/analytics/content-revenue/rebuild` endpoint) — Backfill and repair of rollup data requires direct DDB access; ticket specifies a `POST /internal/analytics/content-revenue/rebuild` admin path for on-demand reconciliation. Fix: add rebuild endpoint in router calling a `rebuild_content_revenue_from_ledger(user_id)` service function — Effort: S
- [LOW] Subscription revenue not attributed to content — `app/services/per_content_revenue.py:72` (`classify_source`) — Subscription credits are lump-sum; no `content_id` is stored in their ledger `meta`, so `subscriptions_cents` is always 0 in the response. Ticket acknowledges this gap but does not resolve it. Fix: requires subscription-to-content attribution design (out of scope for this ticket but should be tracked) — Effort: L
