- [CRIT] Revenue split hooks absent from all qualifying billing flows — `app/routers/messaging.py`, `app/routers/newsfeed.py`, `app/services/vod_purchase.py` — `process_revenue_split` is only callable via the manual `/ui/licenses/revenue/process-split` endpoint; the tip, unlock, post-tip, post-unlock, and VOD-sale flows never trigger automatic revenue splits; licensors receive no credit for qualifying transactions — Fix: add `process_revenue_split(...)` call after the primary ledger entry in each qualifying billing handler (messaging tip/unlock, newsfeed post tip/unlock, vod_purchase sale) — Effort: M
- [HIGH] `/ui/licenses/revenue/register-license` endpoint allows spoofing `licensor_id` — `app/routers/license_revenue.py:117` — The body-supplied `licensor_id` field overrides the authenticated user's sub; any authenticated user can register an arbitrary license mapping claiming any licensor, enabling fraudulent revenue credits — Fix: remove `licensor_id` from the request body; always use `ctx["user_sub"]` as the licensor, or restrict this endpoint to admin/internal use only — Effort: S
- [HIGH] `/ui/licenses/revenue/process-split` endpoint allows any user to trigger arbitrary splits — `app/routers/license_revenue.py:140` — Any authenticated user can POST to this endpoint to manually fire revenue splits with any `content_id`, `licensee_id`, and `source_amount_cents`; this is a test-only endpoint inadvertently exposed in production code — Fix: gate behind `require_admin_or_root` or `S.dev_mode` flag; remove from production router — Effort: S
- [MED] `list_licenses_for_content` in `license_revenue.py` queries a separate `CONTENT_LIC#` partition instead of the `issued_licenses` table — `app/services/license_revenue.py:393` — The revenue split engine does not read from `T.issued_licenses` (the canonical LICENSE-002 store); it uses a shadow `CONTENT_LIC#{content_id}/LIC#{id}` pattern in `T.license_revenue`; licenses issued via LICENSE-002 never produce revenue splits unless also registered via the manual endpoint — Fix: make `process_revenue_split` call `issued_licenses.list_licenses_for_content` directly, or remove the shadow store entirely — Effort: M
- [MED] Revenue split `counterparty_display_name` not populated in `RevenueTransactionOut` — `app/services/license_revenue.py:415` — `_txn_from_item` does not resolve display names; the revenue dashboard shows raw user IDs instead of names — Fix: call `get_profile(counterparty_id)` in `list_revenue_transactions` and enrich the output — Effort: S
- [LOW] Admin revenue audit (`GET /ui/admin/licenses/revenue`) returns only LICENSOR-partition records via GSI1 — `app/services/license_revenue.py:322` — Both licensor and licensee records are written to `T.license_revenue` with `GSI1PK=PLATFORM_REV`; the admin query returns duplicate rows (one per party) for each transaction — Fix: write `GSI1PK` only on the LICENSOR record or add a `record_type` discriminator and filter in the query — Effort: S
