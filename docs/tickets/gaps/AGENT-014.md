- [HIGH] backend computes source hashes directly from filesystem — `app/services/agent_docs.py:198` — _compute_source_hash opens files via open(path,"rb") on the backend server; in prod where backend and agent terminal are separate machines this returns wrong/missing hashes — Fix: gate local hash computation on S.dev_mode; accept externally-provided source_hashes dict from agent via API payload — Effort: M
- [MED] check_freshness does not re-validate stored paths — `app/services/agent_docs.py:453` — check_freshness calls compute_source_hashes without re-running validate_paths; a stored source_ref containing ../ would open files outside the repo — Fix: add validate_paths call in check_freshness and skip/log invalid entries — Effort: S
- [MED] PR impact assessment O(N×M) with no enforcement — `app/services/agent_docs.py:566` — assess_pr_impact loads all doc records and Python-intersects; 500 docs × 100 changed files = 50K membership tests with no cap — Fix: document practical limit and add early exit or cap on changed_files length — Effort: S
- [LOW] coverage score is fully self-reported — `app/services/agent_docs.py:285` — register_doc accepts coverage_score as user/agent-supplied float with no automated verification; overall_coverage metric reflects reported values only — Fix: none material in current architecture — Effort: S
- [LOW] doc_path URL-encoding round-trip untested — `app/routers/agent_docs.py` — {doc_path:path} FastAPI parameter captures slashes; verify encode_cursor/decode_cursor and DDB SK prefixes handle slash-containing paths correctly — Fix: add explicit test coverage for doc paths containing / — Effort: S
