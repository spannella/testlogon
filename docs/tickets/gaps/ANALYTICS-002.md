- [MED] post view time series returns zero-series — `app/services/creator_analytics.py:526` — _get_content_view_time_series only queries T.video_views ByVideoViewedAt GSI for videos; posts generate an empty zero-series; ContentDetailPage shows "No view data for this period" for all post content — Fix: add post view write path and query path, or store per_content_views map in daily rollup rows — Effort: M
- [MED] per-content revenue scan capped at 4 pages (2000 entries) — `app/services/creator_analytics.py:605` — _get_content_revenue_breakdown uses FilterExpression scan of billing LEDGER; for creators with >2000 LEDGER entries revenue is silently under-counted — Fix: pre-aggregate content_revenue as DDB map in daily rollup rows during rollup job — Effort: M
- [MED] post ownership check field names unverified — `app/services/creator_analytics.py:453` — get_content_detail checks item.get("user_id") and item.get("author_id"); if actual newsfeed table uses a different field name (e.g., poster_sub) all posts return 403 "Not your content" — Fix: verify ownership field name against scripts/local-ddb-init.py newsfeed table schema — Effort: S
- [LOW] post metadata lookup is serial not batched — `app/services/creator_analytics.py:403` — _resolve_content_details does one get_item per post ID; for 20 items this is ~100ms worst case — Fix: replace with BatchGetItem to app_single_table using pk=POST#{pid}, sk=META — Effort: S
- [LOW] analytics-depth.spec.ts E2E test file not yet created — `frontend/e2e/analytics-depth.spec.ts:1` — ticket §4.10 specifies 14 tests; test file does not exist; coverage for engagement rate, title resolution, and content detail page is absent — Fix: create the spec file per ticket §5.2 test plan — Effort: S
- [LOW] app_single_table accessed via env var workaround — `app/services/creator_analytics.py:399` — same issue as ANALYTICS-001 §3.2; ddb.Table(os.environ.get("APP_TABLE", "app_single_table")) bypasses T.* handle pattern — Fix: wire T.app_single in app/core/tables.py — Effort: S
