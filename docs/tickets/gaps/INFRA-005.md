- [HIGH] Background billing timer not wired — `app/main.py:740-741` — auto-deduction never runs; resources accrue charges only via manual POST `/ui/remote/billing/tick`; zero-balance auto-terminate never fires — Fix: register `asyncio.create_task(run_compute_billing_timer())` in the `startup` lifespan event and add `run_compute_billing_timer` to `app/services/compute_billing.py` — Effort: M
- [MED] Budget endpoint not admin-gated — `app/routers/compute_billing.py:127-139` — any authenticated user can set their own budget to any value (ticket spec: "Admin-only"); blocks admin-imposed spending caps — Fix: add `require_admin_session` dependency or check `session["role"] >= ADMIN`; expose a separate user-facing alerts-only PATCH — Effort: S
- [MED] Zero-balance auto-terminate not implemented — `app/services/compute_billing.py:45,140-151` — `InsufficientBalanceError` is raised and propagated to callers but no code terminates the instance; dependent on billing timer (see above) — Fix: in the timer loop catch `InsufficientBalanceError` per resource, call `terminate_instance`/`terminate_pod`, write an alert — Effort: S
- [LOW] `_query_ticks_for_month` scans entire ByMonth GSI then filters in Python — `app/services/compute_billing.py:214-232` — high-traffic months with many users produce large scans; user_sub filter should be applied as a DDB `FilterExpression` instead of Python loop — Fix: add `FilterExpression=Attr("user_sub").eq(user_sub)` to the query kwargs — Effort: S
