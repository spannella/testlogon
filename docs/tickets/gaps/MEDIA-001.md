- [HIGH] DRM support reduced to AES-128 flag only — `frontend/src/components/shared/MediaPlayer.tsx:79,321-323` — `drmKeyUrl` prop only enables `emeEnabled=true` with no `drmSystems` config; full Widevine/FairPlay key-system negotiation specified by the ticket is not implemented; DRM-protected VOD/live streams will fail EME handshake — Fix: add `DrmConfig` interface with `licenseUrl`/`token`/`fairplayCertificateUrl`; set `drmSystems` on Hls config and add `licenseXhrSetup` callback; add Safari native FairPlay path — Effort: M
- [HIGH] token refresh callback absent — `frontend/src/components/shared/MediaPlayer.tsx:1` — `onTokenExpiring` prop is not in the interface; playback entitlement JWTs (120s TTL from `app/services/playback_entitlements.py`) expire mid-stream with no re-fetch path, cutting off long VOD sessions — Fix: add `onTokenExpiring?: () => Promise<string>` prop; schedule refresh 30s before JWT `exp` claim; update `hls.config.xhrSetup` with new token — Effort: S
- [MED] `xhrSetup` bearer-token injection not implemented — `frontend/src/components/shared/MediaPlayer.tsx:310-400` — no `xhrSetup` callback on the Hls instance; CDN-gated segment requests requiring `Authorization: Bearer` header will 401; only query-param tokens (broadcast `?md5=` style) work today — Fix: add `xhrSetup` option wiring `drm.token` into the request header — Effort: S
- [MED] unit test file absent — `frontend/src/components/shared/MediaPlayer.test.tsx` — no unit tests for HLS lifecycle, quality switching, error recovery, or DRM config; regressions in core player logic will be silent — Fix: create `MediaPlayer.test.tsx` with vi.mock for hls.js covering the ~12 test cases described in section 5.1 — Effort: M
- [MED] sub-directory hook decomposition not done — `frontend/src/components/shared/media-player/` — all logic lives in a single 884-line file; `useHlsPlayer`, `usePlayerControls`, `useQualityLevels`, `useFullscreen`, `usePictureInPicture` sub-hooks from the ticket design are absent; file will grow unmanageable as VOD-008/BCAST-002 add features — Fix: extract hooks to `media-player/` sub-directory per section 4.2 — Effort: M
- [LOW] auto-hide controls timer not implemented — `frontend/src/components/shared/MediaPlayer.tsx:1` — ticket section 3.10 specifies controls auto-hide after 3s of mouse inactivity; controls are always visible, degrading full-screen experience — Fix: add 3s idle timer with pointer-events-none toggle on the controls overlay — Effort: S
- [LOW] E2E test fixture video segment absent — `frontend/e2e/media-player.spec.ts:1` — spec file exists but requires `frontend/e2e/fixtures/silent-2s.ts` MPEG-TS segment; fixture is not present so any test that loads a real manifest will fail or stall — Fix: generate fixture with `ffmpeg -f lavfi -i color=c=black:s=640x360:d=2 ... -f mpegts` and commit — Effort: S
