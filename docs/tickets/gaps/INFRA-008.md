- [HIGH] Auto-restart policy not implemented — ticket design: `_check_restart_policy` + `record_timeline_event` + `PATCH /restart-policy` — `app/routers/instance_monitoring.py` (174 lines, no restart endpoint), `app/services/instance_monitoring.py` (414 lines, no restart logic) — instances cannot auto-recover on crash; no `/restart-policy` endpoint exists — Fix: add `auto_restart_enabled` + `max_restarts` + `restart_count` fields to EC2/K8s instance items; implement `_check_restart_policy()`; add `PATCH /ui/compute/monitoring/instances/{id}/restart-policy` endpoint — Effort: L
- [HIGH] Event timeline not implemented — ticket design: `TIMELINE#{instance_id}#{ts}#{event_id}` SK pattern in ec2_instances/k8s_pods tables — no timeline events are written on launch/stop/terminate; no `GET /timeline` endpoint exists in `app/routers/instance_monitoring.py` — operators cannot audit lifecycle events — Fix: implement `record_timeline_event()` service function; wire it into ec2/k8s launch, stop, terminate paths; add `GET /ui/compute/monitoring/instances/{id}/timeline` endpoint — Effort: L
- [MED] Background health checker not wired — ticket design: `run_health_checker()` asyncio loop every 30 s — `app/main.py` has no registration of a health-checker background task; health status (`health_status`, `consecutive_failures`) fields are not set on instance items; the monitoring service derives health only from ingested datapoints, not from proactive TCP probes — Fix: add `run_health_checker()` async function; register with `asyncio.create_task` in startup; update instance health fields on each probe cycle — Effort: M
- [LOW] `seed` endpoint (POST `/instances/{id}/seed`) is not dev-mode gated — `app/routers/instance_monitoring.py:90-113` — any authenticated user can overwrite metric history with arbitrary deterministic data in production — Fix: guard with `if not S.dev_mode: raise HTTPException(403, "dev only")` — Effort: S
