- [HIGH] command injection in build_coder_workflow coding_cmd — `app/services/agent_coder.py:559` — prompt[:200] f-stringed inside double-quoted shell argument to claude/codex; ticket subject with " or $(...) breaks or executes when AGENT_CODER_EXECUTE_COMMANDS=1 — Fix: store as structured argv field; exec via subprocess(argv, shell=False) — Effort: M
- [HIGH] build_pr_command double-quote injection — `app/services/agent_coder.py:482` — title/body_escaped use replace('"',"'") which is not shell-safe; title with ' breaks escaping — Fix: apply single-quote escaping _sq(v) = "'" + v.replace("'","'\\''"+"'") + "'" — Effort: S
- [MED] repo_url ext:: transport not blocked — `app/services/agent_coder.py` — validate_coder_config checks startswith https:// or git@ but not ext:: or git+ transport attacks — Fix: reject ext::, file://, git+ prefixes explicitly — Effort: S
- [MED] claim atomicity: ConditionExpression with None value — `app/services/agent_coder.py:420` — ConditionExpression uses :none=None; DynamoDB may not handle NULL in ExpressionAttributeValues the same as attribute_not_exists for empty-string stored values — Fix: use attribute_not_exists(assigned_to_sub) only, or store "" sentinel and check = :empty — Effort: S
- [LOW] metrics aggregation full scan at scale — `app/services/agent_coder.py:820` — get_coder_metrics scans all agent_runs filtered in-memory; >10K runs will be slow — Fix: implement daily snapshot pre-computation per ticket design — Effort: M
