- [HIGH] frontend entirely absent — `frontend/src/` — `appeals.ts` API client, `AppealFormPage.tsx`, `MyAppealsPage.tsx`, `AppealReviewQueuePage.tsx`, routes in `App.tsx`, and sidebar nav links are all missing; the backend is fully wired but users have no browser surface to submit or track appeals, and admins have no queue UI — Fix: create the four files listed in section 4.6-4.7 and add routes per section 4.8 — Effort: L
- [HIGH] `enforcement_id` absent from warning and ban alert details — `app/services/moderation_policy_engine.py:20-34,59-110` — `issue_warning_notification()` and `apply_ban()` do not include `enforcement_id` in their `details` dict; the frontend "Appeal this decision" deep-link requires `details.enforcement_id` to pre-populate `/appeals/new?enforcement_id=`; without it, users cannot reach the appeal form from the notification — Fix: pass `enforcement_id` kwarg through `_persist_enforcement_if_needed()` in `admin_moderation.py:482` to both notification functions per section 3.3 — Effort: S
- [MED] `appeals_enforcement.py` not created — `app/services/appeals_enforcement.py` — `reverse_enforcement()` and `modify_enforcement()` are inlined in `appeals.py` as `_reverse_enforcement`/`_modify_enforcement` rather than in the dedicated module specified by the ticket; the file-level separation aids testability and is referenced by the test plan — Fix: extract to `app/services/appeals_enforcement.py` (or document the deviation and update test imports) — Effort: S
- [MED] `AppealReviewQueuePage` admin route absent from `App.tsx` — `frontend/src/App.tsx:1` — even if the frontend files were created, no route for `/admin/appeals` exists in `App.tsx` and no sidebar link is configured; admin queue would be inaccessible — Fix: add route and sidebar entry per section 4.8 (blocked by the frontend files gap above) — Effort: S
- [MED] no `Retry-After` header on 429 response — `app/services/appeals.py:1` — `_check_no_pending_appeal()` raises `HTTPException(status_code=429)` but the ticket specifies `headers={"Retry-After": "3600"}`; clients have no machine-readable signal for when to retry — Fix: add `headers={"Retry-After": "3600"}` to the 429 HTTPException — Effort: S
- [LOW] appeal queue stats `oldest_submitted_age_minutes` not computed — `app/services/appeals.py:1` — `get_appeal_queue_stats()` likely returns a placeholder or zero for `oldest_submitted_age_minutes`; without the oldest-appeal age, admins cannot monitor SLA compliance — Fix: query `ByStatusCreatedAt` with `ScanIndexForward=True, Limit=1` to get the oldest submitted appeal, then compute `(now_ts() - created_at) // 60` — Effort: S
