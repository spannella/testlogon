- [CRIT] Commission replay — no idempotency guard on `put_item` — `app/services/referrals.py:289` — same `transaction_id` called at different seconds produces distinct SK `COMMISSION#{ts}#{tx_id}` so both writes succeed, paying referrer twice — Fix: write dedup sentinel `COMMISSION_DEDUP#{transaction_id}` with `attribute_not_exists(pk)` before commission put, or drop `ts` from SK and add `ConditionExpression="attribute_not_exists(pk)"` (cross-ref SEC-013, SEC-004) — Effort: S
- [HIGH] Attribution race — `put_item` at `app/services/referrals.py:203` has no `ConditionExpression="attribute_not_exists(pk)"` — concurrent signups with same `referred_user_id` both pass the `existing` get-check and both write attributions, creating duplicate referrer credits — Fix: add `ConditionExpression="attribute_not_exists(pk)"` and catch `ConditionalCheckFailedException` (cross-ref SEC-013) — Effort: S
- [HIGH] Withdrawal endpoint absent — `POST /ui/referrals/withdraw` is not implemented in `app/routers/referrals.py` and no `withdraw()` function exists in `app/services/referrals.py` — users can view earned commissions but cannot redeem them; MON-004 payout integration is entirely missing — Fix: implement `withdraw(user_id, amount_cents)` in service and add router endpoint per ticket §8 spec — Effort: M
- [MED] Dashboard over-reports available balance — `get_referral_dashboard` at `app/services/referrals.py:384` includes `pending` commissions (before 7-day holdback) in `confirmed_commission`, inflating `available_for_withdrawal_cents` — Fix: filter to `status == "confirmed"` only in the `confirmed_commission` sum — Effort: S
- [MED] No `confirm_referral` job — the 7-day holdback status transition (`pending` → `confirmed`) has no scheduled background task or on-demand API; referrals remain `pending` indefinitely — Fix: add a daily job or a `confirm_pending_referrals()` function that checks `attributed_at + referral_holdback_days <= now()` and updates `status` to `confirmed` — Effort: S
- [MED] No `revoke_attribution` function or ban-integration hook — `app/services/referrals.py` has no `revoke_attribution()` and no admin endpoint to trigger it; the fraud spec requires auto-revocation when a referred account is banned within 30 days — Fix: add `revoke_attribution(referred_user_id, reason)` service function and call it from the account-ban flow — Effort: S
- [LOW] `_tbl()` bypasses `app/core/tables.py` wiring — `app/services/referrals.py:36–47` constructs its own `boto3.resource("dynamodb")` client rather than using the `T.*` table handles, duplicating DDB endpoint selection logic and making dev/prod parity (SECOPS-007) depend on a second code path — Fix: replace `_tbl()` with `T.app_single_table` from `app/core/tables.py` — Effort: S
