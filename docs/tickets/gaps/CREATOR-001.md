- [MED] discover endpoint missing — `app/routers/collaborations.py` (no /discover route) — creators have no in-platform way to find collaborators; must know user ID in advance — Fix: add GET /ui/collaborations/discover scanning app table for accepting_requests=true settings items — Effort: S
- [MED] broadcast tip split not wired — `app/routers/broadcast.py:~1643` — broadcast tips always go to single recipient; collaboration split never triggered — Fix: check session.collaboration_id at tip time and call write_collaboration_split_ledger — Effort: M
- [MED] newsfeed co-author integration absent — `app/routers/newsfeed.py` (PostResponse has no co_authors field) — posts with collaboration_id do not write dual feed entries or expose co_author_ids — Fix: add _write_collab_feed_entries and co_authors to PostResponse — Effort: M
- [LOW] earnings category missing for collaborations — `app/services/creator_earnings.py:22` — collaboration split credits fall into "other" in dashboard breakdown — Fix: add "collaboration" branch to classify_entry before return "other" — Effort: S
- [LOW] expiry background worker not registered — `app/services/collaboration_expiry.py` (no call site in app/main.py startup) — stale pending/accepted collaborations never auto-expire — Fix: register process_expired_collaborations in app/main.py startup tasks — Effort: S
- [LOW] counter-proposal DoS — `app/services/collaborations.py:219` — no limit on revision rounds; bad actor can flood creator with notifications indefinitely — Fix: add max_revision_depth=10 check and return 409 after limit — Effort: S
- [LOW] admin collaboration endpoints missing — `app/routers/collaborations.py` (no /admin/collaborations routes) — admins cannot list or audit collaborations via API — Fix: add GET /ui/admin/collaborations, /{id}, /{id}/ledger with require_admin_session — Effort: M
- [LOW] CollaboratorBadge absent from post/broadcast renders — `frontend/src/pages/feed/PostCard.tsx` (no badge) — co-author attribution benefit is invisible to viewers — Fix: implement CollaboratorBadge component and render in PostCard and broadcast header — Effort: S
- [LOW] no pytest unit tests — `tests/test_collaborations.py` does not exist — edge cases (rounding, concurrent accept+reject, 10-request limit) are untested — Fix: create test file with moto mock_aws covering all service edge cases — Effort: S
