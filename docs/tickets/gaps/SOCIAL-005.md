# SOCIAL-005: Tip Leaderboards / Top Supporters — Gap Report

- [MED] No background hourly refresh loop registered at startup — `app/main.py:150-151,614-615` registers routers but does NOT register a `startup` event handler for leaderboard pre-computation; `app/services/tip_leaderboard.py:186` provides `refresh_all_leaderboards()` but it is never called automatically — ticket acceptance criterion 5 ("Background job pre-computes leaderboards hourly") and section 7.3 explicitly specify a startup-registered `asyncio` loop; without it the `/internal/tip-leaderboards/refresh` endpoint must be called manually to warm up data — Fix: add `start_leaderboard_refresh_task` in `app/main.py` analogous to `start_billing_dunning_task` (line 521) — Effort: S

- [MED] Viewer's own rank is not returned in the response — `app/models.py:2541-2547` `TopSupportersOut` has no `viewer_rank` field; `app/services/tip_leaderboard.py:121` accepts `viewer_id` but the response dict returned and the Pydantic model do not expose the viewer's position when they appear outside the `limit` window — ticket US-5 ("supporter wants to see their position") and the API design (section 6.1) specify `viewer_rank` in the response — Fix: add `viewer_rank: Optional[int] = None` to `TopSupportersOut`; compute and populate in `get_leaderboard` — Effort: S

- [LOW] `TopSupportersWidget` does not appear on public creator profile pages — `frontend/src/pages/profile/TopSupportersWidget.tsx` does not exist (only `TopSupportersCard.tsx` in `analytics/`); `PublicUserProfilePage.tsx` has no import of any leaderboard widget — ticket sections 7.8 and US-4 require the widget on the creator profile; currently it is analytics-page only — Fix: create `TopSupportersWidget.tsx` in `frontend/src/pages/profile/` (or reuse `TopSupportersCard`) and add it to `PublicUserProfilePage` — Effort: S
