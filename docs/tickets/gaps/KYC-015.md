- [MED] `kyc_business_cases` DDB table GSIs have no `attr_types` declared for the sort keys, which are alphanumeric strings — `scripts/local-ddb-init.py:126-133` — The owner/status GSI sort keys (`gsi_owner_sk`, `gsi_status_sk`) embed a zero-padded timestamp (`UPDATED#{ts:013d}#BIZ#...`). If a future query uses a numeric comparator on these keys DynamoDB will reject with `ValidationException`; at minimum the org-index GSI sort key (`gsi_org_sk = BIZ#{case_id}`) is purely string and the status-index sort key should follow the numeric declaration pattern used by `kyc_cases` — Fix: add `attr_types={"gsi_status_sk": "S", "gsi_owner_sk": "S"}` (or confirm string sort is intentional) to the `kyc_business_cases` TableDef — Effort: S
- [MED] Admin `approve` endpoint calls `kyc_tiers.upgrade_tier()` inside `kyc_business.py` but `kyc_tiers.py` raises `ImportError` if KYC-009 is not deployed — `app/services/kyc_business.py:355` — The import is deferred (`from app.services.kyc_tiers import ...`) so it only fails at approval time; in a deployment where KYC-009 is not yet merged, approving a business case will 500 — Fix: wrap the tier upgrade in a try/except `ImportError` and log a warning so approval still succeeds without granting Tier 4 — Effort: S
- [MED] UBO `ownership_percentage` per-UBO maximum is validated only at submit time, not at add time — `app/services/kyc_business.py:338-341` — A single UBO with 200% ownership passes the individual add (`> 0`, `<= 100` is checked by Pydantic model), but an existing UBO with no Pydantic cap means the submit validator catches `> 100` total. Pydantic model allows `le=100` per-UBO which is correct, but the STORE.add_ubo service layer does not re-validate the field range — Fix: add `if pct > 100: raise KybValidationError(...)` in `add_ubo` for defense-in-depth — Effort: S
- [LOW] `list_directors` is not exposed as a dedicated endpoint; directors are only returned inside the full case detail (`GET /{case_id}`) — `app/routers/kyc_business.py` — Ticket spec listed `GET /{case_id}/directors` as an endpoint; absence forces UI to re-fetch the full case to refresh the directors list — Fix: add `@router.get("/{case_id}/directors")` returning `STORE.list_directors(case_id)` — Effort: S
- [LOW] `BusinessKycPage.tsx` renders a minimal skeleton with no document upload step or address form — `frontend/src/pages/kyc/BusinessKycPage.tsx` — The ticket specifies a multi-step KYB wizard (company info, UBOs, directors, documents, addresses); the current page only renders the company-info form and a list of UBOs/directors without document or address management — Fix: add Document and Address steps to `BusinessKycPage.tsx` (or create `KybWizard.tsx` as spec requires) — Effort: L
