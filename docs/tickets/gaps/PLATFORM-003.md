- [HIGH] Frontend i18n packages not installed; no t() calls exist — `frontend/package.json` — `react-i18next`, `i18next`, `i18next-browser-languagedetector`, and `i18next-http-backend` are absent; no `useTranslation()` call exists anywhere; ~200 component files still use hardcoded English strings; feature is not functional in the browser — Fix: install packages; create `frontend/src/i18n/config.ts`; wrap App in `I18nextProvider`; migrate component strings to `t()` calls — Effort: L
- [HIGH] `locale` field absent from user profile model — `app/services/profile.py:189` — `empty_profile()` has no `locale` field; `normalize_profile_payload()` and `apply_profile_update()` do not accept it; users cannot persist a locale choice — Fix: add `locale: Optional[str]` to profile, validated against `I18N_SUPPORTED_LOCALES` before storage — Effort: S
- [MED] Locale parameter not validated against whitelist; path traversal risk — `app/i18n/__init__.py` — `translate()` function constructs `app/i18n/translations/{locale}.json`; an unsanitized locale value could traverse directories; must validate against `I18N_SUPPORTED_LOCALES` before file access — Fix: add allowlist check in `translate()` and in `PATCH /ui/profile` locale update handler — Effort: S
- [MED] Admin-managed translation values not HTML-stripped before DDB write — `app/routers/i18n.py` — a malicious admin can inject `<script>alert(1)</script>` into a translation value; served to frontend as JSON and rendered via `t()` output — Fix: strip HTML tags from translation values at write time in the admin endpoint; document that `dangerouslySetInnerHTML` with `t()` output is forbidden — Effort: S
- [MED] ~100 Tailwind directional classes not converted to logical equivalents — `frontend/src/components/layout/` — `pl-*`, `pr-*`, `ml-auto`, `text-left` etc. remain throughout the codebase; RTL layout will be broken for Arabic/Hebrew users — Fix: global replace directional classes with logical equivalents (`ps-*`, `pe-*`, `ms-auto`, `text-start`) — Effort: M
- [LOW] Backend error messages still use hardcoded English strings — `app/routers/` — `HTTPException` detail strings and `write_alert()` titles are English-only; non-English locale users see mixed-language UI — Fix: replace English `HTTPException` strings with `translate(key, get_user_locale_from_request(request))` calls — Effort: L
