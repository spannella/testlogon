# GROUP-004 gaps

- [CRIT] `dissolve_group()` never calls `dissolve_treasury()` — `app/services/user_groups.py:474-501` sets group status to dissolved, deletes all member records and user-group indexes, but does NOT invoke `group_treasury.dissolve_treasury(group_id)` — result: when a group is dissolved all contributed and donated funds are permanently orphaned in the `billing` table under `GROUP#{group_id}` with no wallet to debit them from (group is dissolved but treasury balance remains positive); contributors get no refund and donations never reach escrow — Fix: add `from app.services import group_treasury as treasury_svc` to `user_groups.py` and call `treasury_svc.dissolve_treasury(group_id)` before member cleanup in `dissolve_group()` — Effort: S — (Cross-ref: **SEC-004** billing authz and integrity)

- [HIGH] Two-phase treasury dissolution is not atomic — `app/services/group_treasury.py:487-635` issues multiple independent DDB writes (one per contributor wallet credit + one per ledger entry + escrow write + balance zero); if any write fails mid-loop, some contributors are refunded and others are not, with no compensation/retry mechanism — balance is zeroed last (`line:616`) so a failure leaves the treasury in a partially-distributed state with a non-zero balance — Fix: use DDB `TransactWriteItems` for batches of up to 25; for larger contributor counts use a saga pattern with a `dissolution_state` marker to enable idempotent retry — Effort: L ��� (Cross-ref: **SEC-004**)

- [MED] `contribute()` and `spend_treasury()` are not atomic across the two DDB tables — `app/services/group_treasury.py:85-146` first debits the personal wallet (`T.billing` write 1), then credits the group treasury (`write 2`), then writes two ledger entries (`writes 3-4`); if the process dies between writes 1 and 2, money is debited from the user but never credited to the treasury — Fix: use `TransactWriteItems` to make the personal debit and treasury credit atomic (both in the same `billing` table, so one transact call covers both) — Effort: M — (Cross-ref: **SEC-004**)

- [MED] `display_name` in the treasury contribute flow is passed as `user_sub` string — `app/routers/group_treasury.py:69` passes `display_name=user_sub` instead of resolving the user's real display name from `T.profile` — treasury ledger entries show raw user IDs instead of human-readable names, degrading financial transparency for group members — Fix: resolve display_name from profile at the router layer before calling `contribute()` — Effort: S

- [LOW] `list_treasury_ledger` may miss ledger entries due to DDB `FilterExpression` page-size reduction — `app/services/group_treasury.py:344` queries `LEDGER#` prefix with `Limit=limit` but if the PK partition contains non-LEDGER records (e.g. WALLET, CONTRIB), the `begins_with("LEDGER#")` condition is a `KeyConditionExpression` (not FilterExpression) so this is actually correct — no gap here. (Verified clean.)

- [LOW] No rate limiting on treasury `contribute` and `spend` endpoints as specified in ticket §16 (10/hour contributions, 10/hour spends) — `app/routers/group_treasury.py` has no rate-limit decorators — Fix: add `RateLimiter` dependency per ticket spec — Effort: S
