- [CRIT] serve_broadcast_ad stub not wired to ADS-004 engine — `app/services/broadcast_ads.py:55–83` — all broadcast viewers see same hardcoded house creative; no targeting, frequency caps, or budget enforcement — Fix: replace stub body with delegation to app.services.ad_serving.serve_ad with graceful degradation — Effort: S
- [HIGH] role-type comparison bug in ad-break authorization — `app/routers/broadcast_ads.py:127` — ctx["role"] is a Role enum but compared against {"admin","root"} strings; admins incorrectly fail the check — Fix: change comparison to {Role.ADMIN, Role.ROOT} using imported Role enum — Effort: S
- [HIGH] BROADCAST_PREROLL_ENABLED and BROADCAST_MIDROLL_ENABLED flags absent — `app/core/settings.py` — broadcast ad features cannot be independently gated for rollout — Fix: add both flags to settings.py and gate in build_pre_roll and trigger_ad_break_route — Effort: S
- [MED] schedule_ad_break_end asyncio.sleep task lost on worker restart — `app/services/broadcast_ads.py:schedule_ad_break_end` — if initiating worker dies during break, ad_break_active stays True in DDB indefinitely — Fix: add recovery scan loop checking ad_break_started_at + duration < now_ts() every 60s — Effort: M
- [MED] mid-roll ad-free check is client-side only — `frontend/src/pages/broadcast/LivePlayer.tsx:~233` — client adFree state set on join; if incorrectly stored, subscriber may see mid-roll ads — Fix: ensure adFree from /ad-join response is authoritative and persisted in client session state — Effort: S
