- [HIGH] no server-side MIME magic-byte validation on asset upload — `app/routers/ads.py:_validate_asset` — attacker uploads executable disguised as JPEG; content_type header is browser-controlled — Fix: add imghdr/magic-bytes check on raw file data before S3 upload — Effort: S
- [MED] no max-creatives-per-campaign limit — `app/services/ad_creatives.py:create_creative` — unbounded creatives per campaign; list_approved_creatives fetch becomes expensive — Fix: count list_creatives before put_item, raise ValueError if >= 50 — Effort: S
- [MED] list_approved_creatives fetches all statuses then filters client-side — `app/services/ad_creatives.py:list_approved_creatives` — unnecessary DDB read cost as campaign creative count grows — Fix: add FilterExpression=Attr("status").eq("approved") or dedicated GSI — Effort: M
- [MED] S3 objects stored without explicit ACL; no pre-signed URLs for prod — `app/services/ad_creatives.py:upload_creative_asset` — public-read S3 ACLs allow hotlinking before review approval and bypass impression tracking — Fix: store with ACL=private; generate pre-signed URLs at read time in prod — Effort: M
- [LOW] promo_code_id and affiliate_link_id stored without referential validation — `app/services/ad_creatives.py:create_creative` — stale/mistyped references silently have no effect at serve time — Fix: validate referenced records exist via DDB GetItem on create — Effort: S
- [LOW] ByFormat GSI defined but never queried — `scripts/local-ddb-init.py:~1095` — unused GSI adds write cost on every creative upsert — Fix: remove or defer GSI until a query is actually needed — Effort: S
