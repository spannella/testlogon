- [HIGH] Platform benchmarks endpoint absent — the ticket spec requires `GET /ui/analytics/engagement/benchmarks` (returning `EngagementBenchmarksOut` with `average_rate`, `median_rate`, `p25_rate`, `p75_rate`, `sample_size`, `my_percentile`) and `POST /internal/analytics/engagement/compute-benchmarks`, but neither endpoint nor any `compute_platform_benchmarks` / `get_platform_benchmarks` function exists in `app/routers/creator_analytics.py` or `app/services/engagement_rate.py` — Fix: implement `compute_platform_benchmarks(date_str)` in `engagement_rate.py` and add the router endpoint; add daily job call from main startup — Effort: M
- [MED] Benchmarks UI absent from `EngagementRateSection.tsx` — `frontend/src/pages/analytics/EngagementRateSection.tsx` (245 lines) has no `Platform Benchmarks` card, no percentile bar, and no `my_percentile` display as specified in the ticket frontend design; creators cannot compare their rate to the platform average — Fix: add `BenchmarkComparison` sub-component once the benchmarks endpoint is implemented — Effort: M
- [MED] `EngagementBadge` public profile component absent — `frontend/src/components/shared/EngagementBadge.tsx` does not exist; the ticket requires a badge component for public creator profiles showing the 30-day rate when `show_engagement_public=true` — Fix: create `EngagementBadge.tsx` (40-line component) and integrate into the creator public profile page — Effort: S
- [LOW] E2E test file is renamed — the ticket specifies `frontend/e2e/fin-engagement.spec.ts` but the actual file is `frontend/e2e/engagement-rate.spec.ts` (594 lines); benchmarks sections (585.1–585.4) cannot exist until the benchmarks endpoint is built — Fix: audit test file for benchmark coverage gaps once endpoint is added — Effort: S
- [LOW] `upsert_daily_rollup` engagement fields populated only on explicit refresh — `app/services/creator_analytics.py:523` calls `build_engagement_rollup_fields` and writes `engagement_rate_bps` to the rollup, but this only runs when `analytics_refresh` (`POST /ui/analytics/refresh`) is called; there is no scheduled daily job that calls `upsert_daily_rollup` for all active creators — Fix: wire a daily background task that iterates active creators and refreshes rollups (prerequisite for benchmark computation) — Effort: M
