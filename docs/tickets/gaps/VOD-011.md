- [MED] video fixture file missing — `frontend/fixtures/test-video-5s-720p.mp4` — spec §3.4 requires a 5-second 720p MP4 fixture generated by setup_ubuntu.sh and scripts/generate-test-fixtures.sh; neither the fixture nor the generation script exists; vod-pipeline.spec.ts sections 110.2 / 217-230 use a Buffer.alloc stub instead of a real video — Fix: create scripts/generate-test-fixtures.sh (FFmpeg or minimal MP4 stub fallback) and call it from scripts/setup_ubuntu.sh — Effort: S
- [MED] generate-test-fixtures.sh script not created — `scripts/generate-test-fixtures.sh` — spec §4.5 and Appendix A require this script to ensure fixture availability in CI; script is absent so fresh CI environments will have no video file for upload tests — Fix: create the script per spec §4.5 with FFmpeg path and minimal-stub fallback — Effort: S
- [MED] justfile missing `fixtures` and `e2e-vod` targets — `justfile` — spec §4.6 requires these two make targets; neither exists; CI and developers have no standard way to generate fixtures or run the VOD-specific subset of E2E tests — Fix: add `fixtures` (runs generate-test-fixtures.sh) and `e2e-vod` (runs npx playwright test e2e/vod-pipeline.spec.ts) to justfile — Effort: S
- [LOW] spec section numbers 108-115 diverged from as-built section numbers — `frontend/e2e/video-upload.spec.ts` and `frontend/e2e/vod-pipeline.spec.ts` — spec Appendix C assigns sections 108-115 to a single video-upload.spec.ts; actual implementation uses sections 105-107 in video-upload.spec.ts and 110-115 in vod-pipeline.spec.ts; section 108 is used by video-player.spec.ts and other unrelated specs — Fix: update CLAUDE.md or spec section allocation table to reflect as-built numbering to avoid future collision — Effort: S
- [LOW] total test count is 49 across split files instead of spec's 35 in one file — `frontend/e2e/vod-pipeline.spec.ts:703`, `frontend/e2e/video-upload.spec.ts:638` — implementation distributes tests across more files with broader coverage; the divergence from the spec plan is benign but makes cross-referencing the ticket harder — Fix: no action needed; document as intentional expansion — Effort: S
