- [MED] stakeholder_subs not validated at config save — `app/services/agent_project.py:230` — validate_pm_config accepts non-existent user subs in stakeholder_subs; report delivery fails silently later — Fix: add _user_exists(sub) DDB lookup for each sub in validate_pm_config, reject unknown subs with 422 — Effort: S
- [MED] sprint overlap not prevented — `app/services/agent_project.py:1013` — create_sprint writes without checking date overlap with existing sprints; overlapping sprints corrupt velocity metrics — Fix: call list_sprints(space_id) before write, return 409 on overlap — Effort: S
- [HIGH] backlog scan misses sparse tickets — `app/services/agent_project.py:685` — _scan_backlog_tickets uses FilterExpression with Limit=500 without LastEvaluatedKey loop; sparse status values silently miss items beyond first page — Fix: add pagination loop collecting up to limit matching results — Effort: S
- [MED] coding_tool_model field unsanitized before worker framework injection — `app/services/agent_project.py:571` — pm_config.coding_tool_model has only max_length=100 Pydantic constraint; if framework interpolates into shell string SEC-021 applies — Fix: validate coding_tool_model against allowlist of known model identifiers — Effort: S
