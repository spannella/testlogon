- [MED] story bar entries missing display_name and avatar_url — `app/services/stories.py:195` (get_story_bar) — bar dict only includes user_id; frontend falls back to `user_id.split("@")[0]` (StoryBar.tsx:184) and shows a single initial letter instead of avatar image — Fix: enrich each bar entry with user profile fields (display_name, avatar_url) via a batch profile lookup before returning — Effort: M
- [MED] story viewers list missing display_name — `app/services/stories.py:175` (get_story_viewers) — viewers list returns only user_id and viewed_at; ticket spec requires display_name in the viewer entry — Fix: batch-fetch display names for returned viewer user_ids and include in response — Effort: S
- [LOW] unhighlight_story re-applies original expires_at as TTL without checking whether that time has already passed — `app/services/stories.py:276` (unhighlight_story) — if expires_at < now the re-applied ttl_epoch is in the past; DynamoDB TTL will immediately (eventually) delete a story that was just un-highlighted — Fix: guard with `if expires_at > _now_ts()` before re-applying TTL; otherwise leave item without TTL or delete immediately — Effort: S
- [LOW] story bar N+1 view-check per creator is not batched — `app/services/stories.py:230` (get_story_bar loop) — for each of up to 200 followed creators, one GSI1 query + one GetItem view-check = up to 400 DDB reads per bar request — Fix: collect all latest story_ids then issue a single BatchGetItem for all view records — Effort: M
