- [HIGH] Watch party SSE reuses broadcast session namespace — `app/routers/watch_party.py:266` and `app/services/broadcast_sse.py:11` — `broadcast_sse_subscribe(party_id)` uses the same `_BROADCAST_SUBSCRIBERS` dict as broadcast sessions; a `party_id` of `wp_<hex>` could theoretically collide with a broadcast `session_id` sharing the same hex suffix, or — more practically — events published by `broadcast_sse_publish(session_id, ...)` from the broadcast router would be delivered to watch party SSE subscribers if a naming collision occurred; the namespace is flat — Fix: prefix party SSE keys with a watch-party namespace (`wp_sse:{party_id}`) or create a separate `_WATCH_PARTY_SUBSCRIBERS` dict in a dedicated `watch_party_sse.py` module — Effort: S
- [HIGH] `participant_count` can go negative and has no floor — `app/services/watch_party.py:428` — `_update_participant_count` does `participant_count + :d` with no `ConditionExpression`; calling `leave_party` twice (race condition or duplicate request), `kick_participant` on an already-left participant followed by a `leave_party`, or any double-decrement path drives the count below zero — Fix: use `ConditionExpression="participant_count > :zero"` with `:zero=0` on decrement calls, or use `SET participant_count = :max(participant_count + :d, :zero)` via a conditional — Effort: S
- [MED] No group-chat integration — `app/services/watch_party.py` — the ticket design spec (section 1.2) calls for "integrated group chat sidebar"; the service creates no messaging conversation and the router exposes no `/chat` or `/messages` sub-resource; chat between watch party participants is entirely absent — Fix: on `create_party`, create a group messaging conversation and return `conversation_id`; expose `GET /ui/watch-parties/{party_id}/conversation` that returns the conversation_id for participants to use with the existing messaging UI — Effort: M
- [MED] No frontend page for watch parties — `frontend/src/pages/` — no `WatchParty*` page or component exists; the feature is backend-complete but completely invisible to users — Fix: implement `WatchPartyPage.tsx` with HLS player, sync logic consuming the SSE stream, participant list, and chat sidebar — Effort: L
- [MED] `create_party` does not validate that the requesting user owns or has access to the video — `app/routers/watch_party.py:110` — the check `video.status != "published"` ensures the video is published, but any user can host a watch party for any other user's published video; there is no check that `video.owner_user_id == user_sub` or that the creator has allowed parties for their content — Fix: add a check `if video.owner_user_id != user_sub` (or check a creator-level `allow_watch_parties` flag on the video or profile) — Effort: S
- [LOW] `list_participants` returns all status rows (including `kicked` and `left`) without filtering — `app/services/watch_party.py:380` — the endpoint `GET /ui/watch-parties/{party_id}/participants` returns every participant row; the viewer list will include kicked and departed users — Fix: add `FilterExpression=Attr("status").eq("active")` or filter in Python after the query — Effort: S
- [LOW] `heartbeat` silently swallows all exceptions including ConditionalCheckFailedException — `app/services/watch_party.py:370` — the `except Exception: pass` suppresses all errors; if the participant row has been deleted (kicked and hard-deleted), the heartbeat creates no new row and no feedback is given — Fix: at minimum log the exception; consider returning an error when the participant no longer exists — Effort: S
