# BCAST-004 Gap List

- [HIGH] SEC-010 IDOR — SSE event stream lacks viewer access check — `broadcast.py:717` — any authenticated user can subscribe to private session SSE and receive real-time viewer count, health, and ad-break data; `ctx` is ignored (`_ = ctx`) — Fix: call `check_viewer_access(session_id, ctx["user_sub"], ...)` before queue subscription — Effort: S
- [MED] Heartbeat IDOR — `viewer_id` is caller-controlled with no ownership check — `broadcast.py:596` — Alice can heartbeat on Bob's `viewer_id` (extending Bob's TTL) or evict Bob via leave; `ctx` is ignored — Fix: validate `viewer_id.startswith(ctx["user_sub"] + "#")` before calling `touch_viewer` / `unregister_viewer` — Effort: S
- [MED] Health report endpoint — no broadcaster ownership check — `broadcast.py:658` — any authenticated user can POST fabricated health metrics to any live session, poisoning the health history and triggering false SSE events — Fix: require `ctx["user_sub"] == session.created_by` or admin role — Effort: S
- [MED] CloudWatch health collector for prod not implemented — `broadcast_health_collector.py` (missing) — in prod mode health metrics are only client-reported; no automatic CloudWatch polling — Fix: create `broadcast_health_collector.py` background task gated by `S.broadcast_provider == "aws"` — Effort: M
- [LOW] SSE in-memory pub/sub breaks with multiple uvicorn workers — `broadcast_sse.py` — `_BROADCAST_SUBSCRIBERS` is per-process; events from worker A not delivered to SSE clients on worker B — Fix: document `--workers 1` requirement; long-term replace with Redis pub/sub — Effort: L
