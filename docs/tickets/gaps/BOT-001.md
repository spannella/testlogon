- [CRIT] `_message_out_from_item` never populates bot fields — `app/routers/messaging.py:4162` — `sender_type`, `bot_id`, `bot_name`, `bot_avatar_url`, `quick_replies` are declared on `MessageOut` but not passed in the constructor; every bot message loses its identity fields on read-back, "Bot" badge never renders — Fix: add `sender_type=merged_item.get("sender_type")`, `bot_id=...`, `bot_name=...`, `bot_avatar_url=...`, `quick_replies=...` to the `return MessageOut(...)` call — Effort: S
- [CRIT] `send_bot_message()` returns a dict but never writes to DynamoDB — `app/services/chat_bot.py:283` — the function increments `message_count` and returns a dict but makes no DDB write and does not call into the messaging pipeline; callers relying on it to produce a visible message will get nothing — Fix: implement `create_internal_bot_message()` helper in `messaging.py` and call it from `send_bot_message()` — Effort: M
- [HIGH] No post-send trigger hook in `send_text_message()` — `app/routers/messaging.py:7852` — bot auto-reply evaluation is never invoked after a message is persisted; bots are effectively inert for all conversation traffic — Fix: spawn `asyncio.create_task(_run_bot_trigger_evaluation(...))` after the message write at line ~8053 — Effort: M
- [HIGH] Wildcard scope assignments never resolved in `get_bots_for_conversation()` — `app/services/chat_bot.py:251` — only direct `CONV#{conversation_id}` GSI assignments are queried; `SCOPE#ALL_DMS`, `SCOPE#ALL_GROUPS` records have no `GSI1PK` and are never found — Fix: add a secondary lookup for wildcard-scoped bot assignments per creator — Effort: M
- [MED] Assignment scope not validated against creator participation — `app/services/chat_bot.py:187` — `assign_bot()` writes an assignment without verifying the creator is a participant in the target conversation, allowing bots to be assigned to unrelated conversations — Fix: check creator membership in the target conversation before writing the assignment — Effort: S
- [MED] `sender_type` Literal missing `"system"` value — `app/routers/messaging.py:2492` — the ticket design requires `"system"` as a third sender type but the Literal only covers `"user"` and `"bot"` — Fix: extend to `Literal["user", "bot", "system"]` — Effort: S
- [MED] Missing E2E test spec — `frontend/e2e/chat-bots.spec.ts` does not exist — CRUD, status lifecycle, assignment API, and Bot Manager UI are entirely untested at browser level — Fix: implement sections 507-510 as described in section 5.2 — Effort: M
