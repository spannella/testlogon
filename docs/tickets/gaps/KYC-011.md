- [HIGH] `kyc.case.approved` and `kyc.case.rejected` events never dispatched — `app/services/kyc_cases.py:586-657` — `apply_admin_decision()` updates the case status to `approved`/`rejected` but does not call `_emit_kyc_event_safe()`; `_admin_decide_case()` in the router also has no emit call (line 1245 only emits a metric); users receive no in-app alert, email, or webhook event when their case is decided — Fix: add `_emit_kyc_event_safe(event="kyc.case.approved", user_sub=..., case_id=...)` / `kyc.case.rejected` at the end of `apply_admin_decision()` after a successful DDB update — Effort: S
- [HIGH] `kyc.case.needs_info` event never dispatched — `app/routers/kyc_cases.py:1087-1163` — `admin_request_more_info()` transitions the case to `needs_more_info` but does not call `emit_kyc_event` or `_emit_kyc_event_safe()`; no in-app alert, email, or SMS is sent when the admin requests additional documents — Fix: add `_emit_kyc_event_safe(event="kyc.case.needs_info", user_sub=case["user_sub"], case_id=case_id, requested_items=..., note=...)` after the successful DDB update — Effort: S
- [HIGH] `kyc.case.created` event not dispatched from the router — `app/routers/kyc_cases.py:376,548` — the router logs an audit event `"kyc_case_created"` at line 548 but never calls `emit_kyc_event`; `kyc.case.created` does not appear in any service-layer call path that originates from the user-facing create endpoint; the kyc_partner_api fires it only for partner-originated flows — Fix: add `_emit_kyc_event_safe(event="kyc.case.created", ...)` in `create_kyc_case()` in the router or in `STORE.create_case()` in the service — Effort: S
- [MED] `KycNotificationPrefs` UI not wired into KYC status/settings flow — `frontend/src/pages/kyc/KycWebhookSettingsPage.tsx:1` — a `KycWebhookSettingsPage` exists but the ticket requires a `KycNotificationPrefs.tsx` page with channel toggles (email/SMS/push) and per-event checkboxes; the implemented page focuses on webhook endpoint subscription listing, not per-channel preference toggles; the in-app alert channel toggle and SMS toggle are absent — Fix: add email/SMS/push toggle switches to `KycWebhookSettingsPage.tsx` backed by `PATCH /ui/kyc/webhooks/preferences` — Effort: M
- [MED] webhook signature not validated for admin webhook subscriptions — `app/routers/kyc_webhooks.py:1` ��� the ticket section 3.1/12.1 defines `AdminWebhookSubscribeRequest.secret` for per-subscription HMAC; the current `/preferences` endpoint stores channel flags but not a per-subscription secret; the existing `webhook_service.py` supports secrets per endpoint but `kyc_webhooks.py` does not expose a subscribe-with-secret endpoint for admin webhooks — SEC-002 cross-reference: KYC webhook deliveries rely on the global webhook dispatcher secret, not a KYC-specific one — Fix: expose an admin-only `POST /ui/kyc/webhooks/admin/subscribe` endpoint with `secret` field and store encrypted secret per subscription — Effort: M
- [MED] notification history endpoint has unbounded DDB scan — `app/routers/kyc_webhooks.py:1` — `list_kyc_notifications()` queries the `alerts` table with a `FilterExpression` on `event begins_with "kyc."`; DDB filter is applied after page fetch; on a user with many non-KYC alerts, a single `query()` page will return very few KYC items; results will be silently incomplete — Fix: add a `LastEvaluatedKey` loop to `list_kyc_notifications()` in `kyc_webhooks.py` that iterates until `Limit` KYC events are collected or the table is exhausted — Effort: S
- [LOW] `kyc.tier.upgraded` / `kyc.tier.downgraded` events not emitted on tier changes — `app/services/kyc_tiers.py:1` — the KYC-011 event registry includes these events but `kyc_tiers.py` never calls `emit_kyc_event`; tier upgrades triggered by case approval are silent — Fix: call `_emit_kyc_event_safe(event="kyc.tier.upgraded", user_sub=..., from_tier=..., to_tier=...)` from `kyc_tiers.py` when the tier changes — Effort: S
