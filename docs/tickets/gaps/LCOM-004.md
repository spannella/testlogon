- [HIGH] `ShelfItem` TypeScript interface missing broadcast pricing fields — `frontend/src/api/endpoints/broadcast-shelf.ts:5-17` — `ShelfItem` has no `broadcast_price_cents`, `broadcast_price_expires_at`, `effective_price_cents`, `is_broadcast_price`, `discount_pct` fields; the backend `list_shelf_products_with_pricing` (`app/services/broadcast_product_shelf.py:296`) already returns them — `ProductShelf.tsx` and `ProductShelfManager.tsx` silently lose all pricing data — Fix: extend `ShelfItem` with optional pricing fields and add `setBroadcastPrice`/`clearBroadcastPrice` API functions — Effort: S
- [HIGH] `BroadcastPrice.tsx` component does not exist — no viewer-facing UI shows struck-through original price, effective broadcast price, LIVE DEAL badge, or expiry countdown — core visual selling signal of the feature absent — Fix: create `BroadcastPrice.tsx` per ticket spec §3.8 and integrate into `ProductShelf.tsx` `ProductShelfCard` — Effort: M
- [HIGH] `BroadcastPriceEditor.tsx` component does not exist — `ProductShelfManager.tsx` has no price-setting controls (`frontend/src/pages/broadcast/ProductShelfManager.tsx` has zero references to `broadcast_price`) — broadcaster cannot set or clear broadcast-exclusive prices from the UI despite the backend endpoints at `app/routers/broadcast.py:2227` being fully implemented — Fix: create `BroadcastPriceEditor.tsx` per ticket spec §3.9 and add it to each shelf item row in `ProductShelfManager.tsx` — Effort: M
- [MED] `shelf:price_update` SSE event not handled in frontend — `ProductShelf.tsx:152-154` only listens for `shelf:add`, `shelf:remove`, `shelf:reorder` window events; no `shelf:price_update` listener — effective price displayed to viewers goes stale when broadcaster changes broadcast price during a live session — Fix: add `shelf:price_update` window event listener in `ProductShelf.tsx` and a dispatch in `LivePlayer.tsx` (blocked on LCOM-001 SSE dispatcher gap) — Effort: S
- [LOW] `broadcast-shelf.ts` missing `setBroadcastPrice` and `clearBroadcastPrice` API functions — backend endpoints `PATCH .../price` and `DELETE .../price` are implemented at `app/routers/broadcast.py:2227` and `2274` but have no corresponding frontend API wrappers — Fix: add `setBroadcastPrice(sessionId, itemId, body)` and `clearBroadcastPrice(sessionId, itemId)` to `broadcast-shelf.ts` — Effort: S
