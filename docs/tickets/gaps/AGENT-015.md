- [HIGH] concurrent audit trigger race condition — `app/services/agent_compliance.py:788` — trigger_audit reads get_running_audit then start_audit in sequence; two concurrent requests both pass the read and create duplicate running audits — Fix: add conditional put_item on RUNNING_LOCK item in start_audit with attribute_not_exists ConditionExpression — Effort: S
- [MED] get_running_audit uses PK scan not indexed lookup — `app/services/agent_compliance.py:788` — queries compliance_audits with begins_with(sk,'AUDIT#') and Python-filters for status=running; O(all audits) on busy table — Fix: use RUNNING_LOCK sentinel item for O(1) running-audit check — Effort: S
- [HIGH] SEC-021 latent: no repo_url validation before real execution — `app/services/agent_compliance.py:1041` — review_pr_mock has no repo_url allowlist; when compliance_agent_execute_commands=True PR source URLs go to Worker Agent Framework unvalidated — Fix: validate repo_url against allowed_repo_hosts allowlist before dispatching — Effort: S
- [HIGH] SEC-022: GitHub token must not be stored in DDB config — `app/services/agent_compliance.py` — ticket spec §7 requires GitHub review service account token in secrets manager; if future config extension adds plaintext github_token it must be stripped from GET responses — Fix: add github_token_secret_name (reference only) to config; resolve via KMS at execution time, expose only has_github_token: bool — Effort: M
- [MED] finding trend aggregation scans all findings — `app/services/agent_compliance.py:942` — _all_findings does full PK scan with FilterExpression on created_at; DDB fetches all pages before applying filter — Fix: use GSI1/GSI2 with ScanIndexForward and timestamp range if available — Effort: M
- [LOW] frontend component consolidation deviates from spec — `frontend/src/pages/agents/ComplianceAgentConfigPage.tsx` — spec called for four separate component files; all consolidated into one; data-testid values from E2E specs must be present inside consolidated component — Fix: verify all required data-testid attributes are present in ComplianceAgentConfigPage — Effort: S
