- [HIGH] Affiliate summary, time-series, earnings, and top-products backend endpoints absent — `app/services/affiliate_links.py` (no `get_affiliate_summary`, `get_link_click_timeseries`, `get_earnings_breakdown`, `get_top_products`), `app/routers/affiliate_links.py` (no `/ui/affiliates/summary`, `/clicks`, `/earnings`, `/top-products` routes) — Creators cannot see aggregate stats, click trends, or earnings breakdown; four out of five new dashboard endpoints specified in the ticket are missing — Fix: add four service functions + router endpoints + `AffiliateSummaryOut`/`AffiliateClickTimeSeriesOut`/`AffiliateEarningsBreakdownOut`/`AffiliateTopProductsOut` models — Effort: M
- [HIGH] AffiliateDashboard.tsx is still the 204-line stub — `frontend/src/pages/affiliates/AffiliateDashboard.tsx` — All analytics tabs (Analytics, Earnings, Top Products), summary cards, and chart components specified in the ticket are absent; the frontend API wrappers for the new endpoints do not exist in `affiliates.ts` — Fix: rewrite dashboard with tabs, add `getAffiliateSummary`/`getAffiliateEarnings`/`getLinkClickTimeSeries`/`getTopProducts` wrappers, add TypeScript types — Effort: M
- [MED] Platform-wide admin affiliate stats endpoint missing — `app/services/affiliate_links.py` (no `get_platform_affiliate_stats`), `app/routers/affiliate_links.py` (no `GET /ui/admin/affiliates/stats`) — Admins cannot view cross-creator click/conversion/commission totals — Fix: add `get_platform_affiliate_stats()` (table scan with aggregation) behind `require_admin_session` — Effort: S
