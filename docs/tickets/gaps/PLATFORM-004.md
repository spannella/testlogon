- [MED] `_post_to_dict` variant return not confirmed wired — `app/routers/newsfeed.py:1792` — writeup flags this as needing verification: `_post_to_dict()` must return `image_variants` alongside `image_urls`; if absent, `PostCard.tsx` receives no variants and falls back to unoptimized `<img src>` for all feed images — Fix: verify `image_variants` key is populated in `_post_to_dict()`; if missing, read `image_variants` from DDB item and include in returned dict — Effort: S
- [MED] Upload latency regression ~500 ms synchronous on the request path — `app/routers/newsfeed.py:3051` — `generate_variants()` runs synchronously in the uvicorn request handler; 5 MB JPEG raises total upload time from ~100 ms to ~600 ms; blocks the event loop — Fix: run variant generation in a thread pool executor (`asyncio.run_in_executor`) or move to async background task — Effort: M
- [LOW] `libwebp` dependency not explicitly installed in setup script — `scripts/setup_ubuntu.sh` — Pillow requires `libwebp-dev` for WebP encoding; if absent, `Pillow` installs without WebP support and `generate_variants()` silently produces zero variants — Fix: add `libwebp-dev` to the `apt-get install` list in `setup_ubuntu.sh` — Effort: S
- [LOW] Posts created before PLATFORM-004 return null image_variants — `app/routers/newsfeed.py:1792` — pre-existing posts lack the `image_variants` DDB field; `_post_to_dict()` must return `image_variants: []` (not `null`) to avoid frontend null-dereference in `ResponsiveImage` — Fix: default to empty list when key absent from DDB item — Effort: S
