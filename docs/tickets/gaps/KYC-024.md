- [MED] Drop-off analysis endpoint exists but is never rendered in the analytics dashboard — `frontend/src/pages/admin/KycAnalyticsDashboard.tsx` (imports at line 28-36 do not include `getDropOff`) — the ticket (§3.8 section "DropOffSection") and the backend endpoint `GET /v1/kyc/analytics/drop-off` (`app/routers/kyc_analytics.py:206`) are both implemented; the `getDropOff` function exists in `frontend/src/api/endpoints/kyc-analytics.ts:55` but the dashboard never calls it, so operators get no drop-off analysis view — Fix: import `getDropOff`, add a `useQuery` for it, and render a `DropOffChart` section in the dashboard alongside the funnel chart — Effort: S
- [MED] Screening hits trend endpoint exists but is never rendered in the dashboard — `frontend/src/pages/admin/KycAnalyticsDashboard.tsx` (imports at line 28-36 do not include `getScreeningHits`) — `GET /v1/kyc/analytics/screening-hits` is implemented in `app/routers/kyc_analytics.py:179` and `getScreeningHits` is in `frontend/src/api/endpoints/kyc-analytics.ts:51` but the dashboard never calls or renders it — Fix: add a `useQuery` for screening hits and render a simple line chart showing sanctions/PEP hit rate over time — Effort: S
- [LOW] Geographic section has a hardcoded country list (`["", "SE", "DE", "US", "GB", "FR", "NO", "DK"]`) in the filter dropdown — `frontend/src/pages/admin/KycAnalyticsDashboard.tsx:40` — the geographic API returns whatever countries appear in the data; the hardcoded filter list may not include countries with actual applications, silently showing no data when filtered — Fix: derive the country filter options from `geographicQ.data?.countries` so only countries with data appear — Effort: S
