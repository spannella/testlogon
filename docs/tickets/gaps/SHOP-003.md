- [MED] `scan_abandoned_carts` caps result at 200 items total across all pages — `app/services/shoppingcart.py:735,756` — the pagination loop exits when `len(items) >= 200`; on a busy platform more than 200 eligible abandoned carts will silently be skipped in each scan cycle, causing reminder delivery delays for those users — Fix: remove the 200-item hard cap and process results in batches (send reminders per batch, then continue paginating), or track cursor across loop invocations — Effort: S
- [MED] `send_cart_reminder` reads item count with `Select="COUNT"` but DynamoDB Local's `begins_with` KeyCondition on SK only works when the GSI PK matches; this pattern may return incorrect COUNT on large tables because the prefix query scans within a single PK partition only and does not paginate — `app/services/shoppingcart.py:803-810` — in production a cart with >1MB of item records would only count the first page — Fix: add `LastEvaluatedKey` loop or switch to paginated item query — Effort: S
- [LOW] Purchased carts have the `ttl` attribute removed but `_cart_from_item` still exposes `last_activity_at` and `abandoned_at` in the API response — `app/services/shoppingcart.py:61-63` — purchased carts should omit abandonment fields from the response model to avoid confusing clients — Fix: filter abandonment fields out for `status=PURCHASED` records in `_cart_from_item` — Effort: S
- [LOW] `cart.abandoned` is registered in `ALERT_EVENT_TYPES` (`app/services/alerts.py:144`) and user preference opt-out is supported, but the frontend `AlertPrefs.tsx` page does not visibly group or label the `cart.abandoned` event type for users to find and disable it — Fix: add a "Commerce" group in AlertPrefs UI that surfaces `cart.abandoned` — Effort: S
