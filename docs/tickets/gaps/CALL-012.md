# CALL-012 Gaps

- [CRIT] `useGroupCall.ts` hook does not exist — `frontend/src/hooks/useGroupCall.ts` is missing — `GroupCallOverlay.tsx` imports from it but cannot resolve it; the entire WebRTC media layer (getUserMedia, RTCPeerConnection per participant, SDP offer/answer, ICE candidates, track cleanup) is absent for group calls — Fix: implement hook with mesh/SFU mode selection driven by join response per §4.1 — Effort: M
- [HIGH] No pytest unit tests for group call service — `tests/test_group_calls.py` does not exist — `create_group_call`, `join_call`, `leave_call`, `end_call`, `relay_signal`, `update_media_state` are entirely untested at unit level — Fix: create test file using moto + `monkeypatch` for `_get_conversation_participant_ids` per §4.3 — Effort: S
- [MED] `ParticipantTile.tsx` not extracted — tile rendering is inline in `GroupCallOverlay.tsx:382-428` — reduces testability and makes isolated unit tests of the tile impossible — Fix: extract to `frontend/src/pages/messages/ParticipantTile.tsx` per §4.2 — Effort: S
- [MED] Group call timeline events fall through to generic fallback — `app/services/messaging_call_timeline.py:21-36` `_preview_for_event` has no cases for `group_call.start`, `group_call.join`, `group_call.end` — timeline entries show `"Call event: {call_state}"` instead of human-readable text — Fix: add three preview string cases per §4.4 — Effort: S
- [MED] Router does not call `_check_enabled()` before endpoints — feature flag check is only in the service (`group_call_service.py:84`) raising `GroupCallError(403)`; HTTP clients receive non-standard error shape instead of clean 404 — Fix: add `_check_enabled()` at top of each endpoint handler per §4.5 — Effort: S
- [LOW] Auto-expiry background check not confirmed registered — `_end_call_internal` handles `S.group_call_max_duration_seconds` expiry but its registration in `app/main.py` background scheduler is unconfirmed — Fix: verify or add to `app/main.py` lifespan startup — Effort: S
