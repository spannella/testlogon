# CALL-009 Gaps

- [HIGH] Missing timeline system message on recording completion — `app/routers/call_recording.py:322` transitions recording to `"ready"` but does not insert a `call_recording_available` system message — users cannot discover recordings from the conversation view without a separate panel — Fix: call `emit_call_timeline_event` with `event_type="call.recording_available"` after status update per §4.1 — Effort: S
- [MED] `complete_recording_upload` skips `head_object` size validation in dev — `app/routers/call_recording.py:363` skips `head_object` in dev; prod validation gap allows client to lie about `file_size_bytes` at presign time and upload a larger file bypassing quota — Fix: remove the dev short-circuit; moto supports `head_object` natively per §4.4 — Effort: S
- [MED] Consent endpoint allows initiator to consent to own request — `app/routers/call_recording.py:183` validates participant membership but not that `consenting user != initiated_by`; attacker who knows `call_id`+`recording_id` (participant-only) could self-consent — Fix: add `if recording.initiated_by == user_id: raise HTTPException(400, ...)` per §4.3 — Effort: S
- [MED] No cleanup job for stale `"uploading"` recordings — page refresh during upload leaves DDB record in `"uploading"` status indefinitely; no background task clears these orphaned records — Fix: add `_cleanup_stale_recordings()` step to `_messaging_background_loop()` per §4.2 — Effort: M
- [MED] Global recordings list uses full table scan — `app/routers/call_recording.py:455-463` `list_user_recordings` without `conversation_id` does `Limit=200` scan; no per-user GSI exists — Fix: add `ByParticipantCreatedAt` GSI and per-participant DDB items per §4.5 — Effort: M
- [LOW] No IndexedDB fallback for interrupted uploads — in-memory `MediaRecorder` Blob is lost on page refresh; no recovery path — Fix: buffer recording chunks in IndexedDB during recording and clear on successful upload per §3.2 — Effort: L
