- [MED] playback-token TTL not reduced for view-once purchases — `app/routers/video_listing.py:283-305` — `_try_issue_playback_token` always uses the global `video_playback_token_ttl_seconds` (default 300 s) regardless of purchase type; for view-once the ticket design (§3.9) requires `ttl = video.duration_seconds + 1800` so a leaked token expires when the video does, not arbitrarily sooner or later; the call site at line 692 passes no entitlement_status — Fix: add an optional `entitlement_status` param to `_try_issue_playback_token`, compute `ttl = int(video.duration_seconds or 3600) + 1800` when `purchase_type == "view_once"`, update the call site at line 692 to pass the access result — Effort: S
- [MED] watermarked-download endpoint bypasses VOD-019 download-tier entitlement check — `app/routers/watermark.py:106` — `request_watermarked_download` only checks `video.allow_download` (the global flag) but never calls `check_entitlement_purchase_only` to verify `ent.download_allowed`; a viewer who purchased "permanent" (download_allowed=False) can trigger watermark generation even though the plain `/download` endpoint at `video_listing.py:800-804` would reject them — Fix: mirror the entitlement check from `video_listing.py:800-804` in `watermark.py` before the cache lookup — Effort: S
- [LOW] re-purchase idempotency window: concurrent calls can both pass the allow_repurchase guard and double-charge — `app/services/vod_purchase.py:428-452` — the read-then-write pattern (get_item → check allow_repurchase → put_item) is not atomic; two simultaneous requests for a consumed view-once entitlement both read `views_remaining=0`, both set `allow_repurchase=True`, and both proceed to create new purchase records — Fix: use a DynamoDB conditional expression on the put_item (`ConditionExpression="attribute_not_exists(pk) OR (purchase_type = :vo AND views_remaining = :zero) OR (purchase_type = :rental AND expires_at < :now)"`) to make re-purchase atomic — Effort: M
