- [HIGH] `ProductLinkCard.tsx` component does not exist — `frontend/src/pages/broadcast/BroadcastChat.tsx:288` handles `msg.kind === "lottery"` but has no branch for `kind === "product_link"` — product link messages sent by the broadcaster render as plain text or nothing, blocking the core viewer UX — Fix: create `ProductLinkCard.tsx` (per ticket spec §3.5) and add `kind === "product_link"` branch in `BroadcastChat.tsx` render loop — Effort: M
- [HIGH] `ShelfProductPicker.tsx` dialog does not exist — broadcaster has no UI to select a shelf product to share in chat; the `POST .../chat/product` API endpoint at `app/routers/broadcast.py:1698` is fully built but unreachable from the UI — Fix: create `ShelfProductPicker.tsx` (per ticket spec §3.7) and wire "Share Product" button in `BroadcastChat.tsx` — Effort: M
- [MED] `chat:product_link` SSE event not handled in `BroadcastChat.tsx` — `frontend/src/pages/broadcast/BroadcastChat.tsx:81-116` has listeners for `chat:message`, `chat:delete`, `chat:reaction`, `chat:unlock`, `chat:lottery` but no `chat:product_link` — new product link messages pushed via SSE are silently dropped until page reload — Fix: add `es.addEventListener("chat:product_link", ...)` alongside existing handlers — Effort: S
- [MED] `ProductLinkData` TypeScript type and `shareProductInChat` API function missing from `broadcast-chat.ts` — `frontend/src/api/endpoints/broadcast-chat.ts` `ChatMessage` has `kind?: string` (line 11) but no `product_link` field or `ProductLinkData` interface — Fix: add `ProductLinkData` interface and `product_link?: ProductLinkData` to `ChatMessage`, add `shareProductInChat` function — Effort: S
- [LOW] `ChatOverlay.tsx` has no compact product link format — ticket spec §3.6 requires compact `"{name} - ${price}"` rendering for product links in the overlay but no branch for `kind === "product_link"` exists — Fix: add compact product link rendering in `ChatOverlay.tsx` — Effort: S
