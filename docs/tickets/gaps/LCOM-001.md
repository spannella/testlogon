- [MED] SSE shelf events never dispatched in frontend — `frontend/src/pages/broadcast/ProductShelf.tsx:152-154` registers `window.addEventListener("shelf:add"|"shelf:remove"|"shelf:reorder")` but no code in `LivePlayer.tsx` connects the backend broadcast SSE stream and dispatches these `window.CustomEvent`s — product shelf never updates in real time for viewers — Fix: add `shelf:add`, `shelf:remove`, `shelf:reorder` listeners on the session broadcast EventSource in `LivePlayer.tsx` and `dispatchEvent(new CustomEvent("shelf:*", {detail: data}))` — Effort: S
- [LOW] `ShelfItem` TypeScript interface missing LCOM-004 pricing fields — `frontend/src/api/endpoints/broadcast-shelf.ts:5-17` — `ShelfItem` lacks `broadcast_price_cents`, `broadcast_price_expires_at`, `effective_price_cents`, `is_broadcast_price`, `discount_pct` that the backend `list_shelf_products_with_pricing` now returns — causes silent type drop when pricing data arrives — Fix: extend `ShelfItem` with optional pricing fields — Effort: S
- [LOW] No E2E test spec for broadcast product shelf API or UI — ticket specifies E2E coverage but no `broadcast-product-shelf.spec.ts` file exists — Fix: create spec covering add/remove/reorder/list and viewer ProductShelf render — Effort: M
