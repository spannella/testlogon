- [HIGH] SEC-012 cross-ref: VTT sanitizer uses a denylist instead of an allowlist — `app/services/vod_subtitle_manager.py:123-138` — `_DISALLOWED_TAGS_RE` strips only a hard-coded set of tags (`script`, `style`, `link`, `meta`, `object`, `embed`, `iframe`, `applet`, `form`, `input`, `textarea`, `select`, `button`) but leaves through `<img>`, `<svg>`, `<a href>`, `<c class="...">`, `<v>` with arbitrary attributes, and HTML entities/CDATA that bypass the regex; Chromium's VTT parser and HLS.js players can render `<c.malicious>` as styled HTML, enabling CSS injection; `<img src=data:...>` passes through if `data:` appears without whitespace before the colon — Fix: replace the denylist with an allowlist parser that strips all tags except `<b>`, `<i>`, `<u>`, `<v NAME>`, `<ruby>`, `<rt>` and removes all attributes from permitted tags (SEC-012 §4.4 design already written at `docs/tickets/writeups/SEC-012.md:181-206`) — Effort: M
- [MED] dedicated `GET /{track_id}/vtt` endpoint missing from subtitle router — `app/routers/video_subtitles.py` — the ticket spec (§3 API Contract, §4.1 files-to-create) requires a `GET /ui/videos/{video_id}/subtitles/{track_id}/vtt` endpoint that returns a fresh presigned S3 URL so the frontend can fetch VTT independently without re-listing all tracks; the router implements upload, list, delete, and patch but omits this endpoint; the `vtt_url` field in list responses uses a 3600 s presigned URL baked at list time, which expires before a long session reuses it — Fix: add a `GET /{video_id}/subtitles/{track_id}/vtt` route that calls `mint_subtitle_url(track.s3_key)` and returns `{"vtt_url": url}`; update `MediaPlayer.tsx` to re-fetch this URL when the presigned URL expires — Effort: S
- [MED] `_unset_all_defaults` read-modify-write is not concurrency-safe — `app/services/vod_subtitle_manager.py:284-298` — the ticket design (§5.9) specifies a `ConditionExpression="updated_at = :old_ts"` guard to prevent lost updates when two concurrent uploads both set `is_default=True`; the implementation does a plain `update_item` with `SET subtitle_tracks = :tracks` with no condition, so the second write silently overwrites the first — Fix: add `ConditionExpression="updated_at = :old_ts"` and retry on `ConditionalCheckFailedException` (max 3 retries) as described in the ticket design — Effort: S
- [MED] `SubtitleManager` panel is inlined in `VideoPlayerPage.tsx` rather than extracted to a standalone component — `frontend/src/pages/videos/VideoPlayerPage.tsx:651` — the ticket spec (§4.1) calls for a separate `frontend/src/pages/videos/SubtitleManager.tsx` file to keep the player page manageable; the inline implementation is ~150 lines of form and list logic embedded inside a 700+ line component, making unit testing and maintenance harder — Fix: extract the `data-testid="subtitle-manager"` Card block into `SubtitleManager.tsx` with props `{ videoId, ownerId }` and import it in `VideoPlayerPage.tsx` — Effort: S
- [LOW] `list_subtitles` endpoint accessible to any authenticated user regardless of video visibility — `app/routers/video_subtitles.py:118-128` — `list_subtitles_endpoint` calls `get_video` and returns all subtitle tracks with no check on video status, visibility, or whether the requester is entitled to view the video; a user can enumerate subtitle track IDs (and get short-lived presigned VTT URLs) for a private video they have no access to — Fix: add the same visibility/entitlement gate used in `video_listing.py` before returning the track list — Effort: S
