- [HIGH] Marketplace refund missing seller-side debit — `app/routers/billing.py:1164-1179` — approve path only adjusts buyer balance; seller retains the credit for tipped/unlocked amount, enabling double-enrichment on refund — Fix: write paired seller-DEBIT ledger entry via `new_ledger_entry()` when `reason` indicates tip/unlock — Effort: M
- [MED] Refund window not enforced in service layer — `app/services/refund_requests.py:50` — `create_refund_request()` design calls for 30-day window check against ledger entry `created_at` but no window enforcement is noted as confirmed implemented — Fix: query ledger entry and reject if `created_at < now_ts() - REFUND_WINDOW_DAYS * 86400` — Effort: S
- [MED] Partial refund amount not validated against original — `app/services/refund_requests.py:50` — Pydantic only enforces `ge=1`; `amount_cents` could exceed original charge, causing over-refund — Fix: load original ledger entry and assert `amount_cents <= original_amount_cents` in service layer — Effort: S
- [MED] Dedup check is a read-before-write with no condition expression — `app/services/refund_requests.py:50` — concurrent duplicate submissions can both pass the GSI3 `ByTransactionId` check before either is written — Fix: add DDB `ConditionExpression=attribute_not_exists(transaction_entry_id)` or use a distributed lock — Effort: S
- [LOW] Rate limit query scans entire ByRequesterCreatedAt GSI — `app/services/refund_requests.py:50` — rolling 30-day count requires full GSI scan per submission; acceptable at low volume but not at scale — Fix: add a monthly counter row as a DDB atomic counter — Effort: S
