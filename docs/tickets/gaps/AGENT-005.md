- [HIGH] memory content prompt injection — `app/services/agent_memory.py:400` — assemble_full_context concatenates memory entry content directly; [AGENT_COMPLETE] written into memory re-triggers false completion on future tickets — Fix: apply _sanitize_ticket_field signal-token escaping to all memory content and title fields before assembly — Effort: S
- [HIGH] summarization LLM call not implemented for prod — `app/services/agent_memory.py:535` — _maybe_trigger_summarization truncates to 200 chars in all environments; prod loses semantic information silently — Fix: implement SummarizationClient interface with MockSummarizationClient (dev) and AnthropicSummarizationClient (prod) gated by S.dev_mode — Effort: M
- [MED] token count estimation inaccurate — `app/services/agent_memory.py:273` — len(content)//4 systematically underestimates for code-heavy content (2x actual); context window may overflow in prod — Fix: use provider-specific tokenizer or calibrated per-provider estimate gated by S.dev_mode — Effort: S
- [MED] set_project_context dynamic UpdateExpression field names — `app/services/agent_memory.py:218` — field names k interpolated directly into UpdateExpression string; if called internally with pk or sk key names would overwrite table key — Fix: add allowlist check k in ProjectContextUpdateIn.model_fields before building expression — Effort: S
- [MED] memory import does not validate content size — `app/services/agent_memory.py:481` — import_memory calls add_memory for each entry without cap; malicious import of 1000×10KB entries floods DDB and triggers summarization loop — Fix: enforce max(len(entries), agent_memory_max_entries) cap and validate len(content) <= 10000 per entry — Effort: S
- [LOW] memory not deleted when worker is terminated — `app/services/agent_worker_provisioner.py:422` — terminate_worker leaves all agent_memory records for that worker in DDB indefinitely — Fix: call delete_all_memory(worker_id) from terminate_worker via batch_write_item — Effort: S
