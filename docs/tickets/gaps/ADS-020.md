- [CRIT] serve_broadcast_ad not wired to real ad serving engine — `app/services/broadcast_ads.py:55–83` — all broadcast ads serve hardcoded house stub; no advertiser spend, no targeting, no frequency caps — Fix: replace stub with serve_ad() delegation; fall back to house creative on engine error — Effort: S
- [HIGH] record_ad_event does not write to ad_billing, ad_fraud_prevention, or ad_analytics — `app/services/broadcast_ads.py:record_ad_event:185–216` — broadcast impressions generate zero revenue, zero fraud signals, zero analytics — Fix: wire charge_ad_event, check_impression_fraud, and record_impression_event into record_ad_event gated by BROADCAST_ADS_BILLING_ENABLED flag — Effort: M
- [HIGH] BROADCAST_ADS_BILLING_ENABLED feature flag missing — `app/core/settings.py` — billing integration cannot be deployed and rolled back independently — Fix: add broadcast_ads_billing_enabled bool to settings.py; add to .env.local.example — Effort: S
- [HIGH] LivePlayer.tsx adJoinMutation triggered by session state instead of sessionId — `frontend/src/pages/broadcast/LivePlayer.tsx:138–142` — if sessionMutation fails (auth/playback error), adJoinMutation never fires; pre-roll overlay never mounts; causes E2E tests 369.1/369.2 to fail — Fix: change useEffect dependency from [session] to [sessionId, isAuthenticated] to decouple ad join from playback resolution — Effort: S
- [MED] billing failures in record_ad_event silently swallowed — `app/services/broadcast_ads.py:~214` — try/except Exception: pass hides billing call failures with no audit trail — Fix: add logger.warning and increment broadcast_ad_billing_failure_total counter on exception — Effort: S
- [LOW] unit tests absent — `tests/test_broadcast_ads.py` — no offline pytest coverage for is_ad_free, serve_broadcast_ad, or record_ad_event paths — Fix: create test_broadcast_ads.py with 12 tests covering ad-free, stub/real serve, billing, and fraud paths — Effort: M
