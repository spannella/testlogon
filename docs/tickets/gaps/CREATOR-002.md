- [HIGH] feature flag not enforced at router level — `app/routers/fan_club.py` (S.fan_clubs_enabled never checked) — FAN_CLUBS_ENABLED=0 has no effect; all endpoints remain live — Fix: add _check_enabled() call in each handler matching collaborations.py:89 pattern — Effort: S
- [MED] TierMemberOut model missing — `app/routers/fan_club.py:112` — api_tier_members returns raw DDB dicts; OpenAPI schema shows {} for response — Fix: create TierMemberOut in app/models.py and use as response_model on the endpoint — Effort: S
- [MED] broadcast chat badge not wired — `app/services/broadcast_chat_store.py:136` — send_chat_message does not call resolve_member_badge; badges absent from broadcast chat — Fix: add creator_id param to send_chat_message, call resolve_member_badge, add sender_badge fields to BroadcastChatMessageOut — Effort: M
- [MED] welcome message not sent on subscribe — `app/routers/subscription_server.py` (no post-subscribe welcome DM call) — welcome_message field stored on tier record is never acted upon — Fix: after subscription activation call _get_tier_by_plan then send DM if welcome_message set — Effort: S
- [MED] ExclusiveChatView has no real-time SSE — `frontend/src/pages/fan-club/FanClubPage.tsx:~226` — channel chat uses refetchInterval only; new messages from others not shown in real-time — Fix: publish fan_club:message SSE events from send_channel_message; subscribe on frontend — Effort: M
- [LOW] TierAnalyticsOut model missing and analytics endpoint absent — `app/routers/fan_club.py` (no GET /analytics route) — creators have no visibility into tier distribution or churn — Fix: add TierAnalyticsOut model and GET /ui/fan-club/analytics endpoint — Effort: S
- [LOW] channel reaction deletion missing — `app/routers/fan_club.py` (no DELETE react endpoint) — users can add reactions but not remove them — Fix: add DELETE /channels/{channel_id}/messages/{message_id}/react/{emoji} endpoint — Effort: S
- [LOW] badge cache per-process not shared — `app/services/fan_club_badges.py` (_BADGE_CACHE dict) — in multi-worker/multi-instance prod, invalidate_badge_cache only clears one worker — Fix: document limitation; for prod scale use Redis or accept 60-second stale window — Effort: M
- [LOW] badge cache invalidation not wired on plan change — `app/routers/subscription_server.py` (no invalidate_badge_cache call) — badge can show stale tier for up to 60 seconds after plan upgrade/downgrade — Fix: call invalidate_badge_cache(subscriber_id, creator_id) in subscription upgrade/downgrade path — Effort: S
- [LOW] no pytest unit tests — `tests/test_fan_club.py` does not exist — max-tier, access-enforcement, slowmode, and early-access edge cases untested — Fix: create with moto mock_aws covering all six planned test cases — Effort: S
