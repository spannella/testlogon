- [HIGH] PR review command injection — `app/services/agent_qa.py:890` — build_pr_review_command interpolates report Markdown into double-quoted shell argument; report containing " or $() executes when QA_AGENT_EXECUTE_COMMANDS=1 — Fix: apply single-quote escaping _sq(v) to report body — Effort: S
- [MED] regression scope "affected" heuristic incomplete — `app/services/agent_qa.py:560` — app/routers/*.py changes not mapped to E2E specs; app/core/*.py changes don't fall back to full scope — Fix: add ROUTER_TO_SPEC static map and core/* → full-scope fallback — Effort: S
- [MED] bug ticket deduplication absent — `app/services/agent_qa.py:780` — file_bug_tickets creates duplicate bug tickets on re-test of the same PR; no idempotency check on source_pr_url + test_name — Fix: compute fingerprint sha256(pr_url+test_name)[:16], query label index before create_ticket — Effort: M
- [LOW] flaky verdict soft-pass risk — `app/services/agent_qa.py` — determine_verdict returns "flaky" → PR approved even if consistently flaky failures mask real regressions — Fix: document the limitation and consider requiring 3+ retry passes before classifying as flaky — Effort: S
