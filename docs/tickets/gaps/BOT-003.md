- [UNBUILT] feature not implemented — see writeups/BOT-003.md
- [HIGH] Orphaned auto-reply rules on bot deletion — `app/services/chat_bot.py:167` — `delete_bot()` cascades assignments in `T.bot_assignments` but does not delete `SK=RULE#*` items stored in `T.chat_bots`; orphaned records accumulate indefinitely — Fix: extend cascade to query and delete `SK begins_with RULE#` items in `T.chat_bots` — Effort: S
