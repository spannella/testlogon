- [HIGH] banned delegate bypasses account ban via API key path — `app/services/delegation_api.py:239` — authenticate_key checks delegation status but not delegate account ban; a banned user retains full delegation API key access — Fix: add is_user_currently_banned(item["owner_sub"]) check after delegation relationship check at line 241 — Effort: S
- [MED] creator-approval flow absent — `app/services/delegation_api.py:127` — keys created with status=active immediately; no delegation_status=pending state or creator approval endpoints; creators not notified before external tools gain access — Fix: set delegation_status=pending at creation; add /pending GET/approve/reject endpoints; block authenticate_key until approved — Effort: M
- [MED] v1/* surface limited to chat only — `app/routers/delegation_api.py` (no v1/posts, v1/broadcast routes) — external tools cannot create posts, moderate comments, or control broadcasts via the delegation API; only 4 of 17 specified endpoints implemented — Fix: add v1/posts/*, v1/analytics, v1/broadcast/* wrapping delegate_feed and delegate_broadcast service functions — Effort: M
- [LOW] per-key rate_limit_rpm not configurable at creation — `app/models.py:9923` (DelegationApiKeyCreateIn has no rate_limit_rpm field) — all keys default to 60 RPM; developers cannot request higher or lower limits per key — Fix: add rate_limit_rpm: int = Field(default=60, ge=1, le=300) to DelegationApiKeyCreateIn and persist on key item — Effort: S
- [LOW] webhook system entirely absent — `app/services/delegation_api.py` (no webhook_url, webhook_events, deliver_webhook, or delivery log) — external tools must poll; no push notification on delegation events — Fix: implement webhook_delivery.py background task, HMAC signing, WEBHOOK# DDB items (separate ticket, large effort) — Effort: L
- [LOW] revoke_delegate in DELEGATE-001 does not revoke associated API keys — `app/services/delegates.py:196` — after delegate revocation, authenticate_key correctly returns 403 via live delegation check, but creator has no per-key revocation path independent of full delegation revoke — Fix: add cascade revoke or document that delegation revoke is the correct revocation path — Effort: S
