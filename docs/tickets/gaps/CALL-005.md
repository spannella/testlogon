# CALL-005 Gaps

- [MED] No unit tests for `useConnectionQuality` — `frontend/src/hooks/useConnectionQuality.ts` (106 lines) has no test file — quality threshold logic, polling interval cleanup, `getStats()` rejection handling, and `isConnected=false` reset path are untested — Fix: create `useConnectionQuality.test.ts` (~120 lines) using `vi.useFakeTimers()` per §4.1 — Effort: S
- [LOW] Keyboard shortcuts not implemented — `CallSessionOverlay.tsx` has no `keydown` listener; `M` (mute) and `V` (camera) shortcuts described in ticket design are absent — Fix: add `useEffect` with `document.addEventListener("keydown", ...)` and `aria-keyshortcuts` attributes per §4.3 — Effort: S
- [LOW] `CallTimer` uses `setInterval` counter instead of `Date.now()` delta — `CallSessionOverlay.tsx:131` increments `elapsed` by 1 per tick; can drift ±1s/minute under CPU load or tab throttling — Fix: replace with `Date.now() - startRef.current` delta per §4.2 — Effort: S
- [LOW] `isMuted`/`isCameraOff` can drift from track state on stream replacement — `ConversationView.tsx:98-99` React state flips (`!m`) independently of `track.enabled`; if stream is replaced during reconnect the states may desync — Fix: add sync `useEffect` reading `stream.getAudioTracks()[0]?.enabled` when `localStream` changes per §4.4 — Effort: S
- [LOW] `packetsLost` not guarded against negative values — `useConnectionQuality.ts` uses cumulative `packetsLost` from `inbound-rtp` without `Math.max(0, ...)` guard; negative values from out-of-order RTCP can produce nonsensical loss rates — Fix: add `Math.max(0, report.packetsLost ?? 0)` per §5.1 — Effort: S
