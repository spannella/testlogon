- [MED] Fan-out uses individual `put_item` per follower — `app/services/newsfeed_fanout.py:71` makes one DynamoDB write per follower in a serial loop; no `batch_writer` used; for 500 followers this is ~500 sequential round-trips vs ~20 batch calls; adds 1-2s to post creation latency — Fix: replace loop body with `tbl.batch_writer()` context manager (25 items per batch call) — Effort: S
- [MED] Async background dispatch for >500-follower fan-out not implemented — `fan_out_post_to_followers()` hard-caps at 500 followers (`MAX_SYNC_FOLLOWERS=500`) and discards the rest silently; the ticket's asyncio queue + `fanout_worker` background coroutine are not implemented and `fanout_worker` is not registered in `app/main.py` — Fix: implement queue + worker per ticket §3.3 and register at startup — Effort: M
- [LOW] Fan-out on visibility change not implemented — `app/services/newsfeed_fanout.py` has no `fan_out_visibility_change()` function; changing a post's visibility from `private` to `public`/`followers` or vice versa does not add/remove FEEDREF items from followers' feeds — Fix: implement per ticket §3.1 `fan_out_visibility_change()` and call from the edit-post handler — Effort: M
- [LOW] FEEDREF stale items accumulate after unfollow with no TTL cleanup — ticket §5.3 notes this is by-design but no TTL field is set on FEEDREF items; they grow unboundedly for active users; items older than 90 days should be garbage-collected — Fix: set `ttl` attribute on FEEDREF items at write time and enable DynamoDB TTL on the table — Effort: S
