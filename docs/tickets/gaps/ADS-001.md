- [HIGH] no max-5-accounts-per-user rate limit — `app/services/ad_accounts.py:create_ad_account` — unlimited account creation floods admin review queue — Fix: count `list_accounts_by_owner` before put_item, raise ValueError if >= 5 — Effort: S
- [MED] no max-50-campaigns-per-account rate limit — `app/services/ad_campaigns.py:create_campaign` — unlimited campaign creation per account — Fix: count `list_campaigns` before put_item, raise ValueError if >= 50 — Effort: S
- [MED] raw dict returned from create_ad_account (no response_model) — `app/routers/ads.py:107` — internal DDB fields (pk, sk) leak in API response — Fix: add response_model=AdAccountOut to POST /ui/ads/accounts and GET endpoints — Effort: S
- [MED] bid_cpm_cents missing from CampaignCreateIn — `app/models.py:CampaignCreateIn` — all campaigns default to 500 CPM; auction cannot differentiate bids — Fix: add bid_cpm_cents: int = Field(default=500, ge=100, le=100_000) to model — Effort: S
- [LOW] no audit log for admin review actions — `app/services/ad_campaigns.py:review_campaign` — admin moderation decisions have no immutable secondary history — Fix: write AUDIT#{ts} SK item on review; or integrate with SECOPS-001 event stream — Effort: M

## Second-pass verification (2026-06-05)

- [Confirmed] no max-5-accounts-per-user rate limit — `app/services/ad_accounts.py:create_ad_account` (lines 14–31) — no count check before put_item, gap still open
- [Confirmed] no max-50-campaigns-per-account rate limit — `app/services/ad_campaigns.py:create_campaign` (lines 35–61) — no count check before put_item, gap still open
- [Confirmed] raw dict returned from create_ad_account — `app/routers/ads.py:107–109` — no response_model on POST /ui/ads/accounts, pk/sk fields exposed
- [Confirmed] bid_cpm_cents absent from CampaignCreateIn — verified via `app/models.py`; `ad_serving.py` uses `campaign.get("bid_cpm_cents", 500)` fallback
- [Confirmed] no audit log for admin review actions — `app/services/ad_accounts.py:review_ad_account` and `app/services/ad_campaigns.py:review_campaign` write no secondary AUDIT# items
