- [HIGH] KycQueuePage missing — `frontend/src/pages/admin/KycQueuePage.tsx` (does not exist) — admins have no way to see pending submissions, apply filters, or paginate the review queue — Fix: create page using `useQuery(["kyc","admin","queue",filters])` with cursor-based pagination and shadcn/ui filter bar — Effort: M
- [HIGH] KycCaseDetailPage missing — `frontend/src/pages/admin/KycCaseDetailPage.tsx` (does not exist) — no document viewer, no approve/reject/request-info dialogs, no timeline; App.tsx lazy-import fails at runtime — Fix: create two-column page with CaseInfoPanel, CaseActionPanel (all three dialogs), CaseTimeline — Effort: M
- [HIGH] KycMetricsDashboard missing — `frontend/src/pages/admin/KycMetricsDashboard.tsx` (does not exist) — compliance leads cannot monitor queue depth, approval rate, or latency SLAs — Fix: create page with FunnelChart, ApprovalRateCard, LatencyCards, StaleQueueAlert — Effort: S
- [HIGH] DocumentViewer component missing — `frontend/src/components/shared/DocumentViewer.tsx` (does not exist) — no zoom/rotate/fullscreen for identity document review — Fix: create component with tab bar, zoom/rotation controls, VerificationStateBadge overlay, presigned-URL caching — Effort: S
- [MED] OCC 409 not surfaced to user — `frontend/src/pages/admin/KycCaseDetailPage.tsx` (to be created) — concurrent approve from two admins silently fails on second reviewer; `expected_version` enforcement at `kyc_cases.py:1165` is backend-only — Fix: catch 409 in dialog onError and show toast "Case updated by another reviewer — please reload" — Effort: S
- [MED] KYC Review nav entry absent — `frontend/src/components/layout/Sidebar.tsx` — admins have no nav link to the KYC dashboard; routes exist in App.tsx:430–432 but are unreachable via UI — Fix: add "KYC Review" nav entry under Admin group — Effort: S
- [LOW] Sidebar navigation entry missing for KYC metrics — `frontend/src/App.tsx:430` — /admin/kyc/metrics route exists but no direct link from sidebar — Fix: include metrics link as sub-item under KYC Review nav section — Effort: S
