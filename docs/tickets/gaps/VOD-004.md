- [CRIT] FFmpeg receives s3:// URI directly; most builds lack S3 protocol support — `app/services/transcode_worker.py:234` — `source_uri = job["source_uri"]` passed as `-i s3://...`; stock FFmpeg cannot fetch from S3 or moto; all transcoding fails in both dev and prod unless an S3 FUSE mount is present — Fix: create `app/services/vod_s3_downloader.py`; download source to `scratch_dir/source.<ext>` before rendition loop and pass local path as input — Effort: M
- [MED] `expected_duration_us` defaults to 600 s regardless of actual video length — `app/services/transcode_worker.py:295` — a 2-hour video shows 100% progress at the 10-minute mark; progress UI is misleading for all videos not exactly 10 minutes long — Fix: probe source with ffprobe after download; set `expected_duration_us = duration_seconds * 1_000_000` per rendition — Effort: M
- [MED] No ffprobe step before transcoding; corrupted uploads waste queue capacity — `app/services/transcode_worker.py:86` — `probe_failed` status is never reached; first sign of a corrupt file is `NonRetryableError` after claiming disk and thread resources — Fix: add `app/services/ffprobe_inspector.py`; run before rendition loop; set video metadata fields and fail fast on corrupt uploads — Effort: M
- [LOW] Subprocess environment omits TMPDIR — `app/services/ffmpeg_executor.py:307` — `_build_restricted_env` sets `HOME=/tmp` but not `TMPDIR`; some FFmpeg builds write temp files to `$TMPDIR`; omitting it causes fallback to `/tmp` which may be on a separate mount — Fix: add `TMPDIR` pointing to the job's scratch directory in the restricted env dict — Effort: S
