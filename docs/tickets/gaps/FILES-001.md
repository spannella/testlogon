- [HIGH] no IP-based rate limiting on public download endpoint — `app/routers/file_share_links.py:96` — the ticket design specifies 10 req/min per IP and 5 password attempts per link+IP per minute; neither limit is enforced; the public endpoint has no rate-limiter dependency and no `X-Forwarded-For` inspection — Fix: add rate-limit middleware or a per-request in-memory / DDB counter keyed on `(link_id, client_ip)` for password attempts; apply general IP rate limit via existing rate-limit infrastructure or a new decorator — Effort: M
- [HIGH] TOCTOU window between status check and atomic increment — `app/services/file_share_links.py:297-317` (cross-ref SEC-007) — `consume_share_link` reads the item, checks `is_revoked`/`_is_expired`/`_is_used`, then issues a ConditionExpression update; the three `if` guards are NOT inside the conditional expression, so a concurrent revocation or expiry between line 297 (get_link) and line 318 (update_item) can allow one extra successful download if the condition races — Fix: fold the `is_revoked` and `expires_at` checks into the `ConditionExpression` (add `& Attr("is_revoked").ne(True) & Attr("expires_at").gt(now_ts())` — both are already partially present at line 321; confirm `is_revoked` is included in the expression and not just the pre-check) — Effort: S
- [MED] no password-attempt rate limiting per link — `app/services/file_share_links.py:309-313` — the ticket design requires 5 password attempts per link+IP per minute; wrong passwords are rejected (403) but there is no attempt counter and no lockout; brute-force of short passwords is unrestricted — Fix: maintain a short-TTL DDB or in-memory counter keyed on `(link_id, ip_hash)`; raise 429 after 5 failures in 60 s — Effort: M
- [LOW] S3 encrypted object not deleted on revocation — `app/services/file_share_links.py` — `revoke_share_link` sets `is_revoked=True` but the ticket specifies that revocation should also delete the encrypted S3 object immediately; the encrypted bytes remain in S3 until TTL expiry — Fix: after marking revoked, call `_s3.delete_object(Bucket=_bucket(), Key=item["encrypted_s3_key"])` — Effort: S
- [LOW] no DDB TTL field on share link records — `app/services/file_share_links.py` — the ticket design specifies a `ttl` attribute (set to `expires_at + 86400`) for DDB auto-expiry; the current implementation stores `expires_at` but does not set a separate `ttl` field, so DDB Time-To-Live never auto-deletes stale link records — Fix: when creating a link, set `"ttl": expires_at + 86400` and ensure the DDB table has TTL enabled on the `ttl` attribute — Effort: S
