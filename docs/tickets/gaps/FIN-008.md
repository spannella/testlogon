- [CRIT] TIN/SSN collection and KMS-encrypted storage absent — `app/services/tax_form_1099.py:55` (`_PAYER_TIN_LAST4 = "0000"` placeholder) — 1099 PDFs contain a hardcoded placeholder TIN, making generated forms non-compliant with IRS 1099-NEC requirements; no W-9 form submission path exists — Fix: implement `submit_tax_info()` with `kms_encrypt(tin)` before storage, replace placeholder with real recipient TIN in `_render_1099_pdf` — Effort: L
- [HIGH] No W-9/TIN collection endpoints or frontend form — `app/routers/tax_form_1099.py` (no `POST /ui/tax/w9`, `GET /ui/tax/info`), no `TaxInfoPage.tsx` — Creators cannot submit tax information; the tax info page specified in the ticket does not exist — Fix: add W-9 submission router endpoints, `W9SubmissionIn`/`TaxInfoOut` models, `TaxInfoPage.tsx` — Effort: L
- [HIGH] Admin TIN reveal endpoint missing — `app/routers/tax_form_1099.py` (no `GET /ui/admin/tax/info/{user_id}`) — Admins cannot retrieve full decrypted TIN for compliance review; no IP-logged audit trail for TIN access — Fix: add admin TIN reveal endpoint with `kms_decrypt` + `_write_tax_audit(action="tin_viewed", actor, target, ip)` — Effort: M
- [MED] Admin tax submissions list endpoint absent — `app/routers/tax_form_1099.py` (no `GET /ui/admin/tax/submissions`) — Admins cannot see which creators have submitted tax info or identify creators with missing W-9s before annual 1099 generation — Fix: add endpoint querying billing table `ByTaxStatus` GSI; add GSI to `scripts/local-ddb-init.py` — Effort: M
- [MED] TIN access audit log uses generic alerts, not dedicated tax audit table — `app/routers/tax_form_1099.py:149,176,197` (calls `audit_event` from `app/services/alerts`) — Audit entries lack dedicated `TAX_AUDIT` table schema with `tin_viewed`/`1099_generated` action codes and IP address fields required for SEC-004 compliance — Fix: implement `_write_tax_audit()` writing to `TAX_AUDIT` PK/SK in billing table with IP address capture — Effort: S
