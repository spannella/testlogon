# OFB-018 — Accounting/GL tests

**Type:** Chore | **Priority:** P1 | **Estimate:** 3 days

---

## 1. Summary & Goal

OFB-018 is the dedicated test layer for the four preceding accounting tickets
(OFB-013 through OFB-016). Its goal is to prove, in hermetic offline pytest
suites, that the GL system built by those tickets satisfies its stated
invariants — and to provide an E2E smoke test that confirms the ROOT-only
statements page renders a balancing trial balance in the real browser stack.

The deliverables are:

| File | Coverage |
|---|---|
| `tests/test_ofb_gl_accounts.py` | OFB-013: chart seed, account CRUD, disable-with-entries guard |
| `tests/test_ofb_gl_posting.py` | OFB-014: balanced entries, idempotent re-post, ledger-type mapping |
| `tests/test_ofb_gl_ar_ap.py` | OFB-015: AR/AP aging buckets, GL control-account tie-out |
| `tests/test_ofb_gl_statements.py` | OFB-016: trial balance equation, P&L, balance-sheet equation, dashboard reconciliation |
| `frontend/e2e/accounting.spec.ts` | E2E smoke: ROOT views the trial balance page and it visually balances |

Nothing in this ticket writes production code. Every assertion targets
interfaces delivered by OFB-013–016 (`app/services/gl_accounts.py`,
`app/services/gl_posting.py`, `app/services/gl_ar_ap.py`,
`app/services/gl_statements.py`) and their associated router endpoints.

---

## 2. Context & Current State

### Billing ledger (the GL input)

The single-entry billing ledger already exists. `new_ledger_entry` in
`app/services/billing_shared.py:224` writes rows with keys
`pk=USER#{user_id}`, `sk=LEDGER#{ts}#{entry_id}`, carrying fields `type`,
`amount_cents`, `state`, `reason`, `ledger_date` (YYYY-MM-DD denorm), and an
optional `provider` pass-through from `extra` (`billing_shared.py:255`).

The `billing` DynamoDB table has a `GSI_LEDGER_DATE` secondary index
(partition key `ledger_date`, range key `sk`) declared in
`scripts/local-ddb-init.py:64-77` (comment at line 64, `TableDef` at 67).
The platform financial dashboard
(`app/services/platform_financial_dashboard.py:161`) already queries this
index day-by-day to avoid full scans — the GL poster (OFB-014) will use the
same pattern.

`BAL_FIELDS` (`billing_shared.py:15`) and `compute_due` (`billing_shared.py:158`)
expose per-user receivable state that feeds the AR subledger (OFB-015).

`app/services/invoices.py` tracks invoice records; `list_invoices` returns
`status` and due amounts that OFB-015 builds aging buckets from.

### Platform financial dashboard (the reconciliation anchor)

`app/services/platform_financial_dashboard.py` defines the canonical entry-type
classifiers — `GMV_ENTRY_TYPES`, `REVENUE_ENTRY_TYPES`, `REFUND_ENTRY_TYPES`
(lines 37-58) — and a `_aggregate` function (line 210) that buckets each ledger
row. OFB-016's income-statement revenue must reconcile to this dashboard within
a declared tolerance; OFB-018's statement tests will verify that parity.

### Accounting CSV mapper (existing)

`app/services/audit_export_accounting.py` provides `QUICKBOOKS_COLUMNS`,
`XERO_COLUMNS`, and pure row-mapper functions. It has its own unit test in
`tests/test_gap_0211_accounting_column_mapping.py`. OFB-018 does not re-test
those functions but does import column constants to verify that the GL export
endpoint (OFB-017) maps journal lines correctly.

### Existing OFB test pattern

`tests/test_ofb_returns_rma.py` and `tests/test_ofb_inventory.py` establish the
canonical offline isolation pattern used in this repo:

- `mock_aws()` context manager wraps `setUp`.
- All `T.<name>` handles on the frozen `app.core.tables.T` singleton are
  swapped to moto-backed tables via `object.__setattr__` and restored in
  `addCleanup`.
- Feature flags on the frozen `S` singleton are enabled via
  `object.__setattr__`.
- Service functions are called directly; no `TestClient` (it is broken in this
  environment).
- Route handler coroutines are `asyncio.run()`-ed directly when HTTP behavior
  must be tested.

### What does not yet exist

No `app/services/gl_accounts.py`, `gl_posting.py`, `gl_ar_ap.py`, or
`gl_statements.py` exist yet (verified with `ls app/services/ | grep gl_`).
No corresponding tables exist in `app/core/tables.py` or
`scripts/local-ddb-init.py`. The tests written under this ticket are written
against the interfaces that OFB-013–016 must deliver; they drive the
implementation design for those tickets.

---

## 3. Data Model

OFB-018 does not introduce new tables. It exercises tables introduced by
OFB-013–016. This section documents the expected DDB schema so the test
fixtures can create matching in-memory tables.

### `gl_accounts` table (introduced by OFB-013)

| Attribute | Type | Role |
|---|---|---|
| `account_code` | S | HASH key — e.g. `"1000"` |
| `sk` | S | RANGE key — always `"META"` for the account record |
| `name` | S | Human name (e.g. `"Cash/Bank"`) |
| `account_class` | S | `asset \| liability \| equity \| revenue \| expense` |
| `normal_balance` | S | `debit \| credit` |
| `enabled` | BOOL | Soft-disable guard |
| `created_at` | N | Unix timestamp |

GSI **GSI_BY_CLASS**: `account_class` (S, HASH) / `account_code` (S, RANGE).
No numeric sort key on this GSI, so no `attr_types` override needed.

### `gl_journal` table (introduced by OFB-014)

| Attribute | Type | Role |
|---|---|---|
| `journal_id` | S | HASH key — deterministic `sha256(source_entry_id)[:16]` |
| `sk` | S | RANGE key — `"META"` for the header, `"LINE#{n}"` for each debit/credit line |
| `source_entry_id` | S | Correlates back to the billing ledger `entry_id` |
| `ledger_date` | S | Copied from the source ledger row |
| `posted_at` | N | Unix timestamp of posting |
| `status` | S | `posted \| reversed` |
| `total_debit_cents` | N | Sum of debit lines (for balance assertion) |
| `total_credit_cents` | N | Sum of credit lines |

Line row additional fields: `account_code` (S), `direction` (`debit \| credit`), `amount_cents` (N), `description` (S).

GSI **GSI_BY_DATE**: `ledger_date` (S, HASH) / `posted_at` (N, RANGE).
`attr_types={"posted_at": "N"}` is **required** in `scripts/local-ddb-init.py`
(per the CLAUDE.md DynamoDB numeric-GSI-sort-key rule); omitting it causes
`ValidationException` at query time.

GSI **GSI_BY_SOURCE**: `source_entry_id` (S, HASH) — enables the
idempotency check (`attribute_not_exists(source_entry_id)`) and drill-through
from journal entry to billing ledger row.

### `gl_ar_subledger` table (introduced by OFB-015)

Derived open-item rows; queryable by party and aging bucket.

| Attribute | Type | Role |
|---|---|---|
| `pk` | S | HASH — `AR#{user_sub}` or `AP#{counterparty_id}` |
| `sk` | S | RANGE — `ITEM#{invoice_number_or_entry_id}` |
| `due_date_ts` | N | Unix timestamp of payment due date |
| `amount_cents` | N | Open amount in cents |
| `currency` | S | `"usd"` |
| `status` | S | `open \| partially_paid \| overdue` |
| `source_type` | S | `invoice \| ledger_entry` |

GSI **GSI_AGING**: `pk` (S, HASH) / `due_date_ts` (N, RANGE).
`attr_types={"due_date_ts": "N"}` required.

This table is populated by OFB-015's derivation pass. The test fixtures seed
it directly (bypassing derivation) to test aging and tie-out in isolation.

### Existing tables used (no changes)

- `billing` (PK `pk`, SK `sk`, GSI `GSI_LEDGER_DATE`) — source ledger rows.
- `gl_accounts`, `gl_journal`, `gl_ar_subledger` — created above.

---

## 4. API / Service Design

OFB-018 is a test-only ticket. The service and router interfaces below are what
the tests call; they are defined by OFB-013–016. This section documents them
so the test suite can be written without ambiguity.

### `app/services/gl_accounts.py` (OFB-013)

```python
def seed_chart_of_accounts() -> None: ...
# Idempotent: puts default accounts only when account_code not present.
# Default accounts: 1000 Cash/Bank (asset/debit), 1100 AR (asset/debit),
# 2000 AP (liability/credit), 4000 Sales Revenue (revenue/credit),
# 4900 Refunds/Returns contra (revenue/debit), 2100 Sales Tax Payable
# (liability/credit), 5000 Processor Fees (expense/debit).

def get_account(account_code: str) -> Optional[Dict[str, Any]]: ...
def list_accounts(enabled_only: bool = True) -> List[Dict[str, Any]]: ...
def create_account(account_code, name, account_class, normal_balance) -> Dict: ...
def disable_account(account_code: str) -> None: ...
# Raises HTTPException(409) when journal lines reference account_code.
```

### `app/services/gl_posting.py` (OFB-014)

```python
def post_journal_entry(
    source_entry: Dict[str, Any],
    *,
    dry_run: bool = False,
) -> Optional[Dict[str, Any]]: ...
# Returns the journal header dict, or None if already posted (idempotent).
# Raises ValueError if Σdebits != Σcredits (never persisted).

def post_date_range(start_date: str, end_date: str) -> Dict[str, int]: ...
# Returns {"posted": N, "skipped": N, "errors": N}.

LEDGER_TYPE_ACCOUNT_MAP: Dict[str, List[Tuple[str, str, str]]] = ...
# Maps entry type -> [(account_code, direction, "amount"|"tax"|"fee"), ...]
# e.g. "catalog_purchase" -> [("1000","debit","amount"), ("4000","credit","amount")]
```

### `app/services/gl_ar_ap.py` (OFB-015)

```python
AR_BUCKETS = ["current", "1_30", "31_60", "61_90", "90_plus"]

def compute_ar_aging(as_of_ts: int) -> Dict[str, int]: ...
# Returns {bucket: total_open_cents} across all open AR items.

def compute_ap_aging(as_of_ts: int) -> Dict[str, int]: ...
# Same shape for AP.

def get_ar_control_account_balance() -> int: ...
# Sum of posted GL debits to account 1100 minus credits, in cents.

def get_ap_control_account_balance() -> int: ...
# Sum of posted GL credits to account 2000 minus debits.
```

### `app/services/gl_statements.py` (OFB-016)

```python
@dataclass
class TrialBalanceRow:
    account_code: str
    name: str
    debit_cents: int
    credit_cents: int

@dataclass
class TrialBalance:
    rows: List[TrialBalanceRow]
    total_debits: int
    total_credits: int
    balanced: bool   # total_debits == total_credits

def compute_trial_balance(
    start_date: str,
    end_date: str,
) -> TrialBalance: ...
# Raises ValueError("trial balance out of balance") if not balanced.

def compute_income_statement(start_date: str, end_date: str) -> Dict[str, Any]: ...
# Returns {revenue_cents, expense_cents, net_income_cents,
#          dashboard_net_revenue_cents, variance_cents, within_tolerance: bool}

def compute_balance_sheet(as_of_date: str) -> Dict[str, Any]: ...
# Returns {total_assets, total_liabilities, total_equity,
#          equation_holds: bool}  (assets == liabilities + equity)
```

### Router endpoints (OFB-016/017)

All are ROOT-only (`Depends(require_root)` from `app.auth.policy`,
matching the existing `platform_financial_dashboard.py:167` pattern —
**not** `require_root_session`) under the prefix `/ui/admin/accounting`.
<!-- CORRECTED: spec previously listed both; require_root (policy) is the
     standard for admin-dashboard routers in this codebase. -->

| Method | Path | Handler | Notes |
|---|---|---|---|
| `GET` | `/ui/admin/accounting/trial-balance` | `get_trial_balance` | `?start_date=&end_date=` |
| `GET` | `/ui/admin/accounting/income-statement` | `get_income_statement` | `?start_date=&end_date=` |
| `GET` | `/ui/admin/accounting/balance-sheet` | `get_balance_sheet` | `?as_of_date=` |
| `GET` | `/ui/admin/accounting/chart-of-accounts` | `get_chart_of_accounts` | |
| `GET` | `/ui/admin/accounting/journals` | `list_journal_entries` | `?start_date=&end_date=&cursor=` |
| `GET` | `/ui/admin/accounting/ar-aging` | `get_ar_aging` | `?as_of_date=` |
| `GET` | `/ui/admin/accounting/ap-aging` | `get_ap_aging` | `?as_of_date=` |

The E2E smoke test targets `GET /ui/admin/accounting/trial-balance` via the
frontend page at `/admin/accounting` (route added by OFB-017).

---

## 5. Detailed Behavior & Edge Cases

### 5.1 Balanced-entry invariant (OFB-014 contract)

Every call to `post_journal_entry` must produce journal lines where
`Σ(amount_cents for line if direction=="debit") == Σ(amount_cents for line if direction=="credit")`. This is enforced inside `gl_posting.py` **before** writing to DynamoDB. A ledger row with an unmapped `type` must raise `KeyError` or a domain error, not silently produce a single-leg entry.

Tests in `test_ofb_gl_posting.py` must cover:
- A standard `catalog_purchase` → Dr Cash / Cr Sales Revenue, balanced.
- A `subscription_charge` with tax split → Dr Cash full / Cr Sales Revenue (net) / Cr Sales Tax Payable (tax), balanced.
- A `refund` → Dr Refunds/Returns (contra) / Cr Cash, balanced.
- A `platform_commission` → Dr Cash / Cr Platform Revenue, balanced.
- An **unknown** type → raises, no DDB write.
- A **dry_run=True** call → returns the would-be journal, writes nothing.

### 5.2 Idempotent re-posting (OFB-014 contract)

The `journal_id` is `sha256(source_entry_id)[:16]`. Writing the header uses a
DynamoDB conditional put: `attribute_not_exists(journal_id)`. A second call
with the same `source_entry_id` returns `None` (skipped), never writes a
duplicate journal entry, and increments the `skipped` counter in
`post_date_range` results.

Tests must call `post_journal_entry` twice on the same source entry and assert:
- Second call returns `None`.
- The `gl_journal` table contains exactly one header row for that `journal_id`.
- The debit/credit line count does not double.

### 5.3 AR aging correctness (OFB-015 contract)

Aging buckets based on `as_of_ts - due_date_ts`:
- **current**: due in the future (diff < 0).
- **1_30**: 0–30 days overdue.
- **31_60**: 31–60 days.
- **61_90**: 61–90 days.
- **90_plus**: > 90 days.

Tests in `test_ofb_gl_ar_ap.py` seed the `gl_ar_subledger` table directly with
open items straddling bucket boundaries (e.g. `due_date_ts = as_of_ts - 25*86400`
for the 1_30 bucket) and assert bucket totals match exactly.

The tie-out test calls `get_ar_control_account_balance()` and asserts it equals
`sum(all AR subledger open amounts)` within zero variance.

### 5.4 Trial balance equation

`compute_trial_balance(start_date, end_date)` must:
1. Query all journal lines in the date range via `GSI_BY_DATE`.
2. Aggregate debits and credits per account code.
3. Return `balanced = (total_debits == total_credits)`.
4. Raise `ValueError("trial balance out of balance")` if not balanced — this
   is a data-integrity failure and must never be silently swallowed.

Test fixture seeds a complete set of balanced journal entries (manually placed
in `gl_journal`) and asserts `balanced == True` and `total_debits == total_credits`.
A second fixture seeds a deliberately unbalanced entry (bypassing
`post_journal_entry` via raw DDB put) and asserts the `ValueError` is raised.

### 5.5 Balance-sheet equation

Assets = Liabilities + Equity. The test fixture seeds account balances (via
journal lines) such that the equation holds exactly, then asserts
`equation_holds == True`. A separate fixture seeds a 1-cent imbalance and
asserts `equation_holds == False`.

### 5.6 Income-statement / dashboard reconciliation

The test seeds ledger entries of types from `GMV_ENTRY_TYPES` and
`REVENUE_ENTRY_TYPES` (defined in
`app/services/platform_financial_dashboard.py:37-50`) into both the `billing`
<!-- CORRECTED: REVENUE_ENTRY_TYPES closes at line 50; line 51 is blank. -->
table and the corresponding posted GL journal lines. It then:
1. Calls `compute_income_statement(start_date, end_date)`.
2. Calls `_aggregate(_scan_ledger_entries(date_set={...}))` from
   `platform_financial_dashboard.py` to get the dashboard figure.
3. Asserts `variance_cents == 0` (zero tolerance for hermetic test data).

### 5.7 `disable_account` guard

Calling `disable_account("1000")` when `gl_journal` contains a `LINE#` row
with `account_code = "1000"` must raise `HTTPException(409, "account has posted entries")`.
Calling it on an account with no posted entries sets `enabled = False` in DDB.

### 5.8 Flag-off guard

When `S.gl_double_entry_enabled == False`, all endpoints in the accounting
router must return `404` (or `503` per convention). Tests must call the route
handler directly with the flag off and assert the 404/503 response.

---

## 6. Feature Flag & Config

OFB-018 does not introduce new feature flags; it verifies the flags from
OFB-013–016.

| Setting name | Env var | Default | Ticket |
|---|---|---|---|
| `S.gl_double_entry_enabled` | `GL_DOUBLE_ENTRY_ENABLED` | `false` | OFB-014 |
| `S.gl_accounts_table_name` | `GL_ACCOUNTS_TABLE_NAME` | `"gl_accounts"` | OFB-013 |
| `S.gl_journal_table_name` | `GL_JOURNAL_TABLE_NAME` | `"gl_journal"` | OFB-014 |
| `S.gl_ar_subledger_table_name` | `GL_AR_SUBLEDGER_TABLE_NAME` | `"gl_ar_subledger"` | OFB-015 |

The test fixtures always flip `S.gl_double_entry_enabled = True` via
`object.__setattr__` and restore it in `addCleanup`.

The E2E smoke test sets `GL_DOUBLE_ENTRY_ENABLED=true` in the dev stack's
`.env.local` before running (or seeds DDB directly to bypass the background
poster).

---

## 7. Dev/Prod Parity (SECOPS-007), Idempotency, Security

**No AWS divergence**: The GL services introduced by OFB-013–016 must operate
identically against moto (dev) and real DynamoDB (prod). No `S.dev_mode`
branch is permitted inside `gl_accounts.py`, `gl_posting.py`, `gl_ar_ap.py`,
or `gl_statements.py`. Tests verify behavior using moto-backed tables; the
same code runs in prod.

**Idempotency by deterministic ID**: The journal `journal_id` is
`sha256(source_entry_id)[:16]`, a pure function of the source ledger row.
The conditional-put `attribute_not_exists(journal_id)` means replay of
`post_journal_entry` for the same row is a safe no-op. Tests assert this
explicitly (section 5.2 above).

**No forking of `new_ledger_entry`**: GL posting reads existing ledger rows
(written by the unmodified `billing_shared.new_ledger_entry`). It never
re-implements or duplicates ledger writes. The billing path is byte-for-byte
unchanged when `GL_DOUBLE_ENTRY_ENABLED=false`.

**ROOT-only access**: All accounting router endpoints use
`Depends(require_root_session)`. The E2E smoke confirms a non-ROOT identity
(Alice) receives `403` when hitting `/ui/admin/accounting/trial-balance`.

**Audit trail**: `gl_posting.py` calls `audit_event("gl_journal.posted", ...)` for each successful
journal post (same fire-and-forget pattern used by `billing_disputes.py` and
`ssh_key_manager.py`). Tests do not assert on audit events (they are
best-effort), but the call is mocked via a `patch` context rather than making
a real DDB write.

---

## 8. Backward Compatibility & Migration

**Flag-off behavior**: With `GL_DOUBLE_ENTRY_ENABLED=false` (the default),
all GL service functions are dormant. The billing, cart, orders, and catalog
paths are byte-for-byte unchanged. No new DDB tables are required at startup
if the flag is off (the init script creates them but they remain empty).

**Additive-only table additions**: `gl_accounts`, `gl_journal`, and
`gl_ar_subledger` are new tables. They never modify existing table schemas.
The `billing` table gains no new attributes; the GSI `GSI_LEDGER_DATE` is
already present in production (introduced by GAP-0202 / FIN-013, documented
in `scripts/local-ddb-init.py:64-77`).

**Backfill**: `post_date_range(start_date, end_date)` provides idempotent
historical backfill. Because journal IDs are deterministic, running backfill
multiple times is safe. The backfill is not automatically triggered at startup;
it is run on-demand by a ROOT-initiated API call or CLI command.

**DDB table creation order**: The three new tables must be added to
`scripts/local-ddb-init.py` before any `just up` run that enables the flag.
In production, `UpdateTable` is not required (new tables, not schema changes).
Running `local-ddb-init.py` is idempotent (existing tables are skipped via
`ResourceInUseException` catch).

---

## 9. Test Plan

### 9.1 `tests/test_ofb_gl_accounts.py`

**Class**: `TestGlAccounts(unittest.TestCase)` — inherits the standard OFB
isolation setup (moto `mock_aws`, `T.gl_accounts` swapped via
`object.__setattr__`).

| Test method | Assertion |
|---|---|
| `test_seed_idempotent` | Call `seed_chart_of_accounts()` twice; assert exactly 7 rows in `gl_accounts` |
| `test_seed_default_accounts` | After seed, each of the 7 default codes exists with correct `account_class` and `normal_balance` |
| `test_get_account_exists` | `get_account("1000")` returns a dict with `name="Cash/Bank"` |
| `test_get_account_missing` | `get_account("9999")` returns `None` |
| `test_create_account` | Creates a new account code; `get_account` retrieves it |
| `test_create_duplicate_raises` | Creating the same code twice raises `HTTPException(409)` |
| `test_disable_account_no_entries` | Disables account; `list_accounts(enabled_only=True)` excludes it |
| `test_disable_account_with_entries_raises` | Seeds a `LINE#` row in `gl_journal` for the account, then asserts `HTTPException(409)` |
| `test_list_accounts_enabled_only` | `list_accounts(enabled_only=True)` excludes disabled; `enabled_only=False` includes all |

### 9.2 `tests/test_ofb_gl_posting.py`

**Class**: `TestGlPosting(unittest.TestCase)` — swaps `T.gl_accounts`,
`T.gl_journal`, `T.billing`; flips `S.gl_double_entry_enabled=True`.

| Test method | Assertion |
|---|---|
| `test_catalog_purchase_balanced` | Σdebits == Σcredits; Dr 1000 / Cr 4000 |
| `test_subscription_with_tax_balanced` | Three lines; Σdebits == Σcredits; Cr 2100 for tax |
| `test_refund_balanced` | Dr 4900 (contra) / Cr 1000; Σdebits == Σcredits |
| `test_platform_commission_balanced` | Dr 1000 / Cr platform revenue account |
| `test_unknown_type_raises` | `ValueError` or `KeyError`; zero rows written to `gl_journal` |
| `test_dry_run_no_write` | `dry_run=True` returns journal dict; `gl_journal` scan returns 0 items |
| `test_idempotent_second_call_returns_none` | Second `post_journal_entry` with same entry returns `None` |
| `test_idempotent_no_duplicate_rows` | After two calls, `gl_journal` has exactly 1 header + N line rows |
| `test_post_date_range_counts` | Seeds 3 entries for a date; asserts `posted==3, skipped==0` |
| `test_post_date_range_replay` | Run `post_date_range` twice; second run returns `skipped==3` |
| `test_flag_off_skips_posting` | `S.gl_double_entry_enabled=False`; `post_journal_entry` returns `None` without writing |

### 9.3 `tests/test_ofb_gl_ar_ap.py`

**Class**: `TestGlArAp(unittest.TestCase)` — swaps `T.gl_ar_subledger`,
`T.gl_journal`, `T.billing`; flips flag.

| Test method | Assertion |
|---|---|
| `test_ar_aging_current` | Item with `due_date_ts = as_of_ts + 86400` lands in `current` bucket |
| `test_ar_aging_1_30` | Item 15 days past due → `1_30` bucket |
| `test_ar_aging_31_60` | Item 45 days past due → `31_60` bucket |
| `test_ar_aging_61_90` | Item 75 days past due → `61_90` bucket |
| `test_ar_aging_90_plus` | Item 95 days past due → `90_plus` bucket |
| `test_ar_aging_multiple` | Mixed items; verify all bucket totals correct |
| `test_ap_aging_mirrors_ar` | Same bucket logic; AP items aggregate independently |
| `test_ar_gl_tieout_zero_variance` | `get_ar_control_account_balance()` == sum of all open AR items |
| `test_ap_gl_tieout_zero_variance` | `get_ap_control_account_balance()` == sum of all open AP items |
| `test_tieout_after_partial_payment` | Reduce an AR item; control account balance decreases by same amount |

### 9.4 `tests/test_ofb_gl_statements.py`

**Class**: `TestGlStatements(unittest.TestCase)` — swaps `T.gl_accounts`,
`T.gl_journal`, `T.billing`; flips flag.

| Test method | Assertion |
|---|---|
| `test_trial_balance_balanced` | Fixture seeds balanced journal lines; `balanced == True`, `total_debits == total_credits` |
| `test_trial_balance_unbalanced_raises` | Seeds raw unbalanced line via DDB put; `ValueError` raised |
| `test_trial_balance_empty_range` | No entries in range; `balanced == True`, both totals zero |
| `test_trial_balance_row_per_account` | Returned rows contain one entry per used account code |
| `test_balance_sheet_equation_holds` | Seeded asset/liability/equity balances satisfy A = L + E |
| `test_balance_sheet_equation_fails_flag` | Tweak one account by 1 cent; `equation_holds == False` |
| `test_income_statement_revenue_expense` | Seeded revenue 10000¢, expense 3000¢; `net_income_cents == 7000` |
| `test_income_statement_reconcile_zero_variance` | Same ledger seeded in billing table and journal; `variance_cents == 0` |
| `test_income_statement_reconcile_tolerance` | Inject 1-cent float rounding; verify `within_tolerance` based on declared tolerance constant |
| `test_trial_balance_date_filter` | Entries outside requested range excluded; balance still holds |

### 9.5 E2E smoke — `frontend/e2e/accounting.spec.ts`

**Section 801**: ROOT accounting page (1 `describe` block, ~8 tests).
<!-- CORRECTED: Section 70 is already taken by `billing-wallet.spec.ts`
     (`frontend/e2e/billing-wallet.spec.ts:354`). Section numbers 800+
     are in use by kyc-metrics.spec.ts; 801 is the next free slot. -->

Auth: `injectAuth(page, "root")` using cookies from `e2e_admin_session_setup.py`.

Pre-condition: Dev stack must have `GL_DOUBLE_ENTRY_ENABLED=true` in `.env.local`
and at least one billing ledger entry posted via `post_date_range` covering
the current month. A `beforeAll` fixture calls:

```ts
await request.post("http://localhost:8000/ui/admin/accounting/backfill", {
  headers: { "x-csrf-token": sessions.root.csrf_token },
  data: { start_date: "2026-01-01", end_date: todayIso() },
});
```

| Test | Assertion |
|---|---|
| ROOT navigates to `/admin/accounting`, page loads | `<h1>` containing `"Accounting"` is visible |
| Trial balance tab shows debit and credit columns | `getByRole("columnheader", { name: "Debit" })` visible |
| Debit total equals credit total | Parse text content of debit-total cell and credit-total cell; assert equal strings |
| Out-of-balance shows error badge | (Negative fixture via API injection) error badge with text "Out of balance" visible |
| Non-ROOT (Alice) gets 403 on trial-balance API | `page.request.get("/ui/admin/accounting/trial-balance")` → status 403 |
| Non-ROOT cannot see `/admin/accounting` route | Navigate as Alice; redirected or gets 403 banner |
| Income statement tab shows revenue and net income | `getByTestId("income-revenue")` and `getByTestId("income-net")` visible |
| Export button produces downloadable response | Click "Export CSV (QuickBooks)"; `waitForResponse` with `content-type: text/csv` |

**Strict-mode note**: The "Debit" and "Credit" column headers appear in both
the trial balance table and the income statement table. Scope locators to the
trial balance section container (`page.locator("[data-testid='trial-balance-table']")`)
before using `getByRole("columnheader")`.

---

## 10. Open Questions / Assumptions

1. **Journal ID collision probability**: Assuming `sha256(entry_id)[:16]` (8 bytes, 64-bit space) is sufficient for this platform's transaction volume (~millions, not billions). If collision resistance is a concern, extending to 32 chars should be discussed before OFB-014 implementation.

2. **Tax and fee split amounts**: The test suite assumes that a `catalog_purchase` ledger entry carries `meta.tax_cents` and `meta.fee_cents` fields. If OFB-014 decides not to persist sub-amounts on the ledger row and instead uses a fixed fee rate from `S`, the test fixtures must be updated accordingly.

3. **Dashboard reconciliation tolerance**: The income-statement reconciliation test uses `variance_cents == 0` for hermetic data. The production tolerance constant (for float/Decimal rounding) is assumed to be defined in `gl_statements.py` as `DASHBOARD_RECONCILE_TOLERANCE_CENTS = 0` (strict) or a small value like `1`. OFB-016 must declare this constant explicitly.

4. **AR subledger derivation timing**: OFB-015 may derive AR open items lazily (on-demand from invoices + balance fields) rather than persisting a physical `gl_ar_subledger` table. If so, `test_ofb_gl_ar_ap.py` must call the derivation function instead of seeding the table directly. This spec assumes a physical table; the test structure must adapt if OFB-015 chooses a virtual approach.

5. **`post_date_range` driving mechanism**: The background GL poster (OFB-014) drives from `GSI_LEDGER_DATE`. This spec assumes `post_date_range` is also callable as a synchronous function (not only via background task) so the E2E `beforeAll` can prime the journal via a `POST /ui/admin/accounting/backfill` endpoint. If the router does not expose a synchronous backfill endpoint, the E2E fixture must seed the journal table via DDB directly (as `e2e_session_setup.py` does for sessions).

6. **`require_root_session` vs `require_root`** — **RESOLVED**: Use `Depends(require_root)` imported from `app.auth.policy`. This is the pattern used by `platform_financial_dashboard.py:167` and other admin-dashboard routers (e.g., `fraud_detection.py`, `billing_config.py`). `require_root_session` (`app.auth.deps:308`) is a separate, older dependency used primarily by tenant-admin endpoints. Do not mix them.

7. **Balance-sheet equity derivation**: Equity = retained earnings = cumulative net income less distributions. The test fixture seeds equity directly. If OFB-016 computes equity dynamically (P&L accumulation), the balance-sheet test must provide a longer history fixture. This spec assumes a simple seeded value for the unit test.

8. **`gl_ar_subledger` vs on-the-fly compute**: An alternative OFB-015 design computes AR/AP aging from `compute_due(balance_item)` and `invoices.list_invoices` without a physical subledger table. In that case the `gl_ar_subledger` table definition in section 3 is unused; the aging test must instead seed `billing` BALANCE rows and invoice records. A decision must be made before OFB-015 is implemented.

---

## 11. Dependencies

| Ticket | What this test file needs from it |
|---|---|
| **OFB-013** (Chart of accounts & GL account model) | `app/services/gl_accounts.py` with `seed_chart_of_accounts`, `get_account`, `list_accounts`, `create_account`, `disable_account`; `gl_accounts` DynamoDB table definition in `scripts/local-ddb-init.py`; `S.gl_accounts_table_name` setting; `T.gl_accounts` handle in `app/core/tables.py` |
| **OFB-014** (Double-entry journal entries) | `app/services/gl_posting.py` with `post_journal_entry`, `post_date_range`, `LEDGER_TYPE_ACCOUNT_MAP`; `gl_journal` table with `GSI_BY_DATE` (numeric `posted_at` sort key) and `GSI_BY_SOURCE`; `S.gl_double_entry_enabled` flag; `T.gl_journal` handle |
| **OFB-015** (AR / AP subledgers) | `app/services/gl_ar_ap.py` with `compute_ar_aging`, `compute_ap_aging`, `get_ar_control_account_balance`, `get_ap_control_account_balance`; `gl_ar_subledger` table (or equivalent virtual derivation); `T.gl_ar_subledger` handle (if physical) |
| **OFB-016** (Financial statements) | `app/services/gl_statements.py` with `compute_trial_balance`, `compute_income_statement`, `compute_balance_sheet`; accounting router at `/ui/admin/accounting/*` registered in `app/main.py` |
| **OFB-017** (Accounting admin UI) | `frontend/src/pages/admin/AccountingPage.tsx` (or equivalent), route at `/admin/accounting` in `App.tsx`, `data-testid` attributes on trial-balance table, column totals, income statement figures for E2E assertions |
| **GAP-0202 / FIN-013** (existing) | `GSI_LEDGER_DATE` already present on `billing` table; `_scan_ledger_entries` in `platform_financial_dashboard.py` used by income-statement reconciliation test |

---

## 12. Verification Log

Every factual claim about the current codebase was checked against the repo.
Status: **VERIFIED** = claim is accurate, **CORRECTED** = spec was wrong and has been fixed, **UNCONFIRMED / RISK** = could not be fully confirmed.

| # | Claim | Status | Evidence |
|---|---|---|---|
| 1 | `new_ledger_entry` at `billing_shared.py:224` | VERIFIED | `app/services/billing_shared.py:224` |
| 2 | Ledger row uses `pk=USER#{user_id}` | VERIFIED | `billing_shared.py:24` (`pk_for_user` helper); callers pass `key_name="pk"` |
| 3 | Ledger SK = `LEDGER#{ts}#{entry_id}` | VERIFIED | `billing_shared.py:220-221` (`ledger_sk` helper) |
| 4 | `provider` pass-through via `extra` at `billing_shared.py:255` | VERIFIED | `billing_shared.py:255-256` (`if extra: item.update(extra)`) |
| 5 | `GSI_LEDGER_DATE` on `billing` table, partition key `ledger_date` (S), range key `sk` | VERIFIED | `scripts/local-ddb-init.py:64-77` |
| 6 | GSI declared at `scripts/local-ddb-init.py:67-77` (spec §2 said `:67-77`) | CORRECTED | Comment block starts at line 64; spec §2 updated to `64-77`. `TableDef` itself is 67-77. |
| 7 | `_scan_ledger_entries` at `platform_financial_dashboard.py:161` | VERIFIED | `app/services/platform_financial_dashboard.py:161` |
| 8 | `_aggregate` at `platform_financial_dashboard.py:210` | VERIFIED | `app/services/platform_financial_dashboard.py:210` |
| 9 | `GMV_ENTRY_TYPES`, `REVENUE_ENTRY_TYPES`, `REFUND_ENTRY_TYPES` at lines 37-58 | VERIFIED | `platform_financial_dashboard.py:37`, `46`, `53-58` |
| 10 | §5.6 cited these classifiers at `lines 37-51` | CORRECTED | `REVENUE_ENTRY_TYPES` closes at line 50; corrected to `37-50` |
| 11 | `BAL_FIELDS` at `billing_shared.py:15` | VERIFIED | `billing_shared.py:15` |
| 12 | `compute_due` at `billing_shared.py:158` | VERIFIED | `billing_shared.py:158` |
| 13 | `QUICKBOOKS_COLUMNS`, `XERO_COLUMNS` in `audit_export_accounting.py` | VERIFIED | `app/services/audit_export_accounting.py:26`, `32` |
| 14 | `tests/test_gap_0211_accounting_column_mapping.py` exists | VERIFIED | `tests/test_gap_0211_accounting_column_mapping.py` |
| 15 | `tests/test_ofb_returns_rma.py` and `tests/test_ofb_inventory.py` exist | VERIFIED | Both files confirmed present |
| 16 | OFB test pattern: `mock_aws()` via `ExitStack` in `setUp`, `object.__setattr__` for `T` + `S` | VERIFIED | `tests/test_ofb_returns_rma.py:67-126` |
| 17 | No `gl_accounts.py`, `gl_posting.py`, `gl_ar_ap.py`, `gl_statements.py` yet | VERIFIED | `ls app/services/gl_*.py` returns empty |
| 18 | No GL table handles in `app/core/tables.py` | VERIFIED | No `gl_` matches in `app/core/tables.py` |
| 19 | No GL table defs in `scripts/local-ddb-init.py` | VERIFIED | No `gl_` matches in `scripts/local-ddb-init.py` |
| 20 | `app/services/invoices.py` exists and has `list_invoices` | VERIFIED | `app/services/invoices.py:395` |
| 21 | `app/services/billing_disputes.py` uses `audit_event` (fire-and-forget pattern) | VERIFIED | `billing_disputes.py:28` (import), `143`, `228`, `289` (calls) |
| 22 | `ssh_key_manager.py` uses fire-and-forget `_audit()` wrapper | VERIFIED | `app/services/ssh_key_manager.py:33` (docstring), `38-42` (wrapper) |
| 23 | Router auth: spec §4 said `Depends(require_root_session)` or `Depends(require_root)` | CORRECTED | Spec was ambiguous. `platform_financial_dashboard.py:167` uses `require_root` from `app.auth.policy`. Spec §4 updated to specify `require_root` only. Section 7 and Open Question 6 already noted the correct answer; §4 header now matches. |
| 24 | `platform_financial_dashboard.py:167` uses `require_root` from `app.auth.policy` | VERIFIED | `app/routers/platform_financial_dashboard.py:20`, `167` |
| 25 | `require_root` is in `app.auth.policy`, `require_root_session` is in `app.auth.deps` | VERIFIED | `app/auth/policy.py:63`; `app/auth/deps.py:308` |
| 26 | E2E smoke test assigned **Section 70** | CORRECTED | Section 70 is already used by `billing-wallet.spec.ts` (`frontend/e2e/billing-wallet.spec.ts:354`). Changed to **Section 801** (next free slot after 800 used by `kyc-metrics.spec.ts`). |
| 27 | `injectAuth(page, "root")` is the E2E cookie-injection pattern | VERIFIED | Pattern confirmed in multiple spec files, e.g. `frontend/e2e/catalog-subscriptions.spec.ts:81` |
| 28 | `sessions[identity].csrf_token` pattern for E2E CSRF | VERIFIED | `frontend/e2e/vod-ad-supported.spec.ts:74` and others |
| 29 | GL service files drive from `GSI_LEDGER_DATE` (same as dashboard) | VERIFIED (design intent) | `_scan_ledger_entries` pattern at `platform_financial_dashboard.py:161-176` |
| 30 | `billing_shared.py:255` provider line number | VERIFIED (minor) | Line 255 is `if extra:`, line 256 is `item.update(extra)` — guard starts at 255, update at 256; citation is acceptable. |

### Design changes made
1. **§4 router auth**: Corrected from ambiguous `require_root_session` or `require_root` to definitive `require_root` (from `app.auth.policy`) with inline comment.
2. **§5.6 line citation**: `37-51` → `37-50` (REVENUE_ENTRY_TYPES closes at line 50).
3. **§8 / §2 ddb-init line range**: `67-77` → `64-77` to include the comment block that starts at 64.
4. **§9.5 section number**: `Section 70` → `Section 801` (Section 70 was already claimed by `billing-wallet.spec.ts`).
5. **Open Question 6**: Upgraded from open question to RESOLVED, citing exact evidence.

### Unconfirmed assumptions (RISK flags)
- **RISK-A**: The spec assumes `post_journal_entry` accepts a `dry_run=True` kwarg. This interface does not yet exist and must be implemented exactly this way by OFB-014 for the tests to work.
- **RISK-B**: The spec assumes `disable_account` raises `HTTPException(409)` specifically (not `ValueError`). OFB-013 must implement this precise exception type for tests using `assertRaises(HTTPException)` to pass.
- **RISK-C**: The `gl_ar_subledger` physical-table vs on-the-fly-compute decision (Open Question 4/8) is unresolved. If OFB-015 chooses virtual derivation, `test_ofb_gl_ar_ap.py` fixture setup changes substantially.
- **RISK-D**: `S.gl_accounts_table_name`, `S.gl_journal_table_name`, `S.gl_ar_subledger_table_name`, `S.gl_double_entry_enabled` settings do not yet exist in `app/core/settings.py`. They must be added by OFB-013/014/015 exactly as named or the test fixture `object.__setattr__` calls will target non-existent attributes.
