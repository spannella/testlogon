# /etc/livekit/livekit.yaml on prod EC2 i-08f937fc705ebea75 (mode 0600, root).
# SECRET REDACTED for the committed copy — real value lives ONLY in the on-box file
# and in the standing secrets-rotation list (see README.md).
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50200
  use_external_ip: true
keys:
  API03cbfc10f882: <REDACTED-56-char-secret — see /etc/livekit/livekit.yaml on the box>
logging:
  level: info
