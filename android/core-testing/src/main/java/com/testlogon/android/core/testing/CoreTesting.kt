package com.testlogon.android.core.testing

/**
 * Placeholder marker for the core-testing module.
 *
 * Shared test infrastructure (MockWebServer dispatchers, fixtures, MainDispatcherRule) lands
 * in AND-046. This object only keeps the module non-empty and compilable.
 */
object CoreTesting
