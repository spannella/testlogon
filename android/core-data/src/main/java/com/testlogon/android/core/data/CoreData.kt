package com.testlogon.android.core.data

/**
 * Placeholder marker for the core-data module.
 *
 * Repository implementations, Room cache, and DataStore prefs land in AND-115. This object
 * only keeps the module non-empty and compilable.
 */
object CoreData
