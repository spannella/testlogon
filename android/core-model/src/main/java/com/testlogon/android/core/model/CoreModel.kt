package com.testlogon.android.core.model

/**
 * Placeholder marker for the core-model module.
 *
 * Domain entities, DTOs, and shared result/error types (e.g. ApiResult / DomainError) land in
 * AND-018 / AND-007. This object only keeps the module non-empty and compilable.
 */
object CoreModel
