package com.testlogon.android.core.network

/**
 * Placeholder marker for the core-network module.
 *
 * Retrofit/OkHttp/Moshi setup, auth interceptors, and the 401 -> refresh -> retry policy land
 * in AND-009. This object only keeps the module non-empty and compilable.
 */
object CoreNetwork
