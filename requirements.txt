fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
boto3==1.34.20
pydantic==2.6.4
pyotp==2.9.0
twilio==9.0.5
requests==2.31.0
snowballstemmer==2.2.0
stripe==8.5.0
pytest==8.2.2
prometheus-client==0.20.0
PyJWT==2.8.0
zipstream-new==1.1.8
webauthn==1.11.1
qrcode==7.4.2
paramiko==3.4.1
pywebpush>=2.0.0
cryptography>=41.0.0
Pillow>=10.0.0
# Optional / production-only: real Kubernetes pod launcher (INFRA-004 / GAP-0225).
# Imported lazily in app/services/k8s_launcher.py only when K8S_MOCK_ENABLED=0,
# so dev/CI does not require it. Install in the production image.
# kubernetes>=28.1.0
